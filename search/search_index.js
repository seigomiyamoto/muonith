var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Home","text":""},{"location":"index.html#muonith","title":"MUONITH","text":"<p>MUOgraphic Numerical Inversion &amp; Tomography Harness</p> <p>A fast C++ simulation tool for multi-directional muography observation design and 3D density reconstruction.</p>"},{"location":"index.html#what-is-muography","title":"What is muography?","text":"<p>Muography uses naturally occurring cosmic-ray muons to image the internal bulk-density structure of large objects such as volcanoes (Tanaka et al., 2007; Nishiyama et al., 2014; Nishiyama et al., 2022), pyramids (Morishima et al., 2017), underground cavities, and scoria cones such as Mount \u014cmuro (Miyamoto et al., 2022; Nagahara et al., 2022). Just as medical X-rays reveal bones inside a body, muons penetrate geological structures and their attenuation reveals density variations within.</p> <p>While conventional muography with 2--3 viewing directions provides projected density images, portable nuclear-emulsion detectors now enable observations from many directions, making detailed 3D density reconstruction feasible. MUONITH is designed to rapidly evaluate and compare such multi-directional observation configurations. It is fast enough to run on a commodity laptop, so researchers can evaluate observation designs at their own desk \u2014 without a dedicated compute cluster.</p>"},{"location":"index.html#key-features","title":"Key features","text":""},{"location":"index.html#single-view-analysis","title":"Single-view analysis","text":"<p>For each detector viewing direction, MUONITH computes:</p> <ul> <li>Path-length distributions through a 3D voxel model</li> <li>Expected muon counts based on flux models</li> <li>Projected average density and its statistical uncertainty</li> </ul>"},{"location":"index.html#spatial-resolution-vs-depth","title":"Spatial resolution vs. depth","text":"<p>Exclusion-plot analysis answers the question: \"What size and magnitude of density anomaly is detectable at a given depth, for a given detector area and exposure time?\"</p> <p>This enables systematic comparison of candidate detector sites and observation parameters.</p>"},{"location":"index.html#3d-density-reconstruction","title":"3D density reconstruction","text":"<p>Given multi-directional muon-count data, MUONITH performs linear inversion (based on Nagahara et al., 2022) to reconstruct 3D bulk-density distributions. The tool also exports the system matrix A, prior density vector, and observed counts, allowing users to apply their own reconstruction methods.</p>"},{"location":"index.html#why-muonith","title":"Why MUONITH?","text":"<p>Existing muography simulation tools fall into two categories:</p> Approach Strengths Limitations Script-based tools Flexible, user-friendly Too slow for large parameter scans Monte Carlo tools Detailed physics and noise modeling Computationally heavy <p>MUONITH fills the gap as a fast approximate engine for broad exploration of observation designs:</p> <ul> <li>~100x faster (/thread) and ~1/4 peak memory compared to a previous   GNU Octave implementation for the same 3D inversion problem   (Nagahara et al., 2022)</li> <li>Fast enough to explore many observation-design variations on a single   machine \u2014 down to a commodity laptop \u2014 rather than a dedicated cluster</li> <li>Best candidates can then be validated with full Monte Carlo codes</li> </ul>"},{"location":"index.html#technical-highlights","title":"Technical highlights","text":"<ul> <li>C++20 with a simple ray-tracing algorithm</li> <li>OpenMP parallelization for independent rays and detector elements</li> <li>Platform-native BLAS/LAPACK (Apple Accelerate on macOS, OpenBLAS on Linux)</li> <li>Sparse matrix storage to keep memory usage low with large 3D grids</li> <li>Reproducible builds via Nix development environment</li> </ul>"},{"location":"index.html#platform-support","title":"Platform support","text":"Platform Status Linux (Ubuntu 22.04 / 24.04, including WSL2) Fully supported Linux (Docker) Fully supported macOS (Apple Clang + Accelerate) Fully supported macOS (Nix environment) Fully supported"},{"location":"index.html#getting-started","title":"Getting started","text":"<ul> <li>Installation -- Build dependencies and   compilation instructions</li> <li>Nix Setup -- Optional: isolated reproducible   development environment</li> <li>Quick Start -- Run your first simulation</li> </ul>"},{"location":"auxiliary-tools/gsi-dem.html","title":"GSI DEM","text":""},{"location":"auxiliary-tools/gsi-dem.html#muonith-gsi-dem","title":"muonith-gsi-dem","text":"<p>External repository</p> <p>muonith-gsi-dem is maintained in a separate repository. This page documents its stable interface for use with MUONITH. To obtain the tool, contact the maintainers.</p>"},{"location":"auxiliary-tools/gsi-dem.html#overview","title":"Overview","text":"<p>muonith-gsi-dem converts DEM (Digital Elevation Model) data from the Geospatial Information Authority of Japan (GSI) into formats usable by MUONITH. It processes ZIP archives of GSI's Fundamental Geospatial Data (\u57fa\u76e4\u5730\u56f3\u60c5\u5831 \u6570\u5024\u6a19\u9ad8\u30e2\u30c7\u30eb) and produces:</p> <ul> <li><code>.g2zbin</code> \u2014 Compact uniform-grid binary (direct input to MUONITH)</li> <li>GeoTIFF \u2014 Georeferenced raster (for GIS tools and muonith-path-view)</li> <li>PNG \u2014 Colored elevation maps with contour lines</li> </ul> <pre><code>GSI ZIP archives \u2500\u2500\u2192 convertZipDEM.py \u2500\u2500\u2192 g2zbin (\u2192 MUONITH)\n                                   \u2500\u2500\u2192 GeoTIFF (\u2192 GIS / muonith-path-view)\n                                   \u2500\u2500\u2192 PNG (\u2192 visual inspection)\n</code></pre>"},{"location":"auxiliary-tools/gsi-dem.html#dependencies","title":"Dependencies","text":"Package Purpose Python 3.x Runtime numpy Array computation GDAL (<code>osgeo.gdal</code>, <code>osgeo.osr</code>) GeoTIFF I/O and CRS transformation pyproj Coordinate reference system conversion matplotlib PNG rendering (colormap, contour lines) json5 (optional) JSON5 config parser (falls back to standard <code>json</code>) <p>The recommended way to install these is the setup script shipped with the tool, which creates <code>.venv/</code> and pins the GDAL Python bindings to the system GDAL:</p> <pre><code>bash setup-venv.sh\n</code></pre> <p>It requires <code>uv</code> and the system libgdal development headers, i.e. <code>gdal-config</code> on <code>PATH</code> (Debian/Ubuntu: <code>sudo apt install libgdal-dev</code>). The script stops with an error message if <code>gdal-config</code> is missing.</p> <p>Note</p> <p>If you instead manage the environment with <code>uv</code>, note that numpy, pyproj, matplotlib and json5 come from <code>uv sync</code> (<code>pyproject.toml</code>), while GDAL does not: its Python package must match the system libgdal, so install it on top of the synced <code>.venv</code> with <code>uv pip install GDAL</code>. A later plain <code>uv sync</code> removes GDAL again; re-run that command, or use <code>uv sync --inexact</code> to keep manually added packages.</p>"},{"location":"auxiliary-tools/gsi-dem.html#obtaining-gsi-dem-data","title":"Obtaining GSI DEM Data","text":"<ol> <li>Visit the GSI download service: https://service.gsi.go.jp/kiban/app/map/?search=dem</li> <li>Select \u6570\u5024\u6a19\u9ad8\u30e2\u30c7\u30eb (Digital Elevation Model)</li> <li>Choose mesh resolution:<ul> <li>DEM5A \u2014 5 m mesh (high resolution, limited coverage)</li> <li>DEM10 \u2014 10 m mesh (nationwide coverage)</li> </ul> </li> <li>Download every ZIP whose mesh tile overlaps your area of interest. GSI    distributes DEM data per secondary mesh tile (about 10 km x 10 km), and the    tile code is embedded in the ZIP file name (<code>FG-GML-&lt;mesh&gt;-DEM5A-&lt;date&gt;.zip</code>).    Zoom to your target area on the map and the service shows which tiles cover    it. Examples:<ul> <li>Satsuma-Iwojima: a single tile, mesh 4630-12 \u2014   <code>FG-GML-463012-DEM5A-20250620.zip</code> (the trailing date differs per release)</li> <li>Mount Asama: nine tiles, mesh 5438-43 ... 5438-65</li> </ul> </li> </ol> <p>List the downloaded files in <code>zip_files</code> of the JSON5 configuration; the pipeline merges all tiles into one grid.</p> <p>Before running, open the JSON5 file and check that each name in <code>zip_files</code> matches the ZIP you actually downloaded: GSI appends a release date to the file name (<code>FG-GML-&lt;mesh&gt;-DEM5A-&lt;date&gt;.zip</code>), so a fresh download differs from the bundled example and must be rewritten to match, otherwise the conversion stops at the first step.</p> <p>For regions outside Japan, obtain DEM data from other sources (e.g., SRTM, ALOS, national surveys) and convert it to GeoTIFF. The muonith-gsi-dem README documents how to convert that GeoTIFF to <code>.g2zbin</code> and how to inspect a cropped area.</p>"},{"location":"auxiliary-tools/gsi-dem.html#configuration-json5","title":"Configuration (JSON5)","text":"<p>Configuration files are stored in the <code>params/</code> directory. Below is the bundled example <code>params/showa.json5</code> for the Showa-shinzan area:</p> params/showa.json5 (click to expand) <pre><code>{\n  \"project_name\": \"showa\",                 // Output goes to projects/showa/\n  \"zip_files\": [                           // All GSI DEM ZIPs covering the area (two tiles)\n    \"../../dem/showa/FG-GML-634066-DEM5A-20250620.zip\",\n    \"../../dem/showa/FG-GML-634067-DEM5A-20250620.zip\"\n  ],\n  \"xy_units\": [ \"meters\", \"DMS\" ],         // Axis units; one PNG is rendered per unit\n  \"cmap\" : \"terrain\",                      // Matplotlib colormap\n  \"XY_EPSG\": 6679,                         // JGD2011 zone XI (southern Hokkaido)\n\n  \"tf_save_png\" : true,                    // Save colored PNG maps\n  \"png_basename\" : \"showa\",                // fig/fig_showa_&lt;unit&gt;.png\n  \"tf_save_g2zbin\" : true,                 // Save g2zbin (MUONITH input)\n  \"g2zbin_basename\" : \"showa\",             // g2zbin/showa.g2zbin\n  \"tf_save_geotiff\": true,                 // Save GeoTIFF raster\n  \"geotiff_basename\": \"showa\",             // geotiff/showa.tif\n\n  \"cont_pitch\" : 100,                      // Contour interval [m]\n\n  \"cut_params\": {                          // Sub-region extraction (2x2 km around Showa-shinzan)\n    \"zmin\" : 0,                            // Colorbar minimum [m]\n    \"zmax\" : 400,                          // Colorbar maximum [m]\n    \"cmap\" : \"terrain\",                    // Matplotlib colormap for the cut PNGs\n    \"xy_units\": [\"km\"],                    // Axis units; one PNG is rendered per unit\n    \"XY_EPSG\": 6679,                       // CRS of the cut outputs\n\n    \"tf_save_png\": true,                   // Save colored PNG maps of the cut\n    \"png_basename\" : \"showa_2x2km\",        // fig/fig_showa_2x2km_km.png\n    \"tf_save_g2zbin\" : true,               // Save g2zbin of the cut (MUONITH input)\n    \"g2zbin_basename\" : \"showa_2x2km\",     // g2zbin/showa_2x2km.g2zbin\n    \"tf_save_geotiff\": true,               // Save GeoTIFF of the cut\n    \"geotiff_basename\": \"showa_2x2km\",     // geotiff/showa_2x2km_cut.tif\n\n    \"xy_center_unit\" : \"degrees\",          // Unit of xcnt/ycnt: \"degrees\" or \"meters\"\n    \"xcnt\": 140.866702,                    // Center longitude\n    \"ycnt\": 42.542692,                     // Center latitude\n    \"xy_unit\" : \"meters\",                  // Unit of xsize/ysize\n    // xcnt - xsize &lt;= x &lt;= xcnt + xsize, same for y\n    \"xsize\" : 1000,                        // Half-width [m] -&gt; 2x2 km area\n    \"ysize\" : 1000,                        // Half-height [m]\n\n    \"cont_pitch\": 10,                      // Contour interval [m]\n    \"contour_text\": 20,                    // Contour label interval [m]\n\n    \"font_scale_min\": 1.5,                 // Label font auto-scaling lower bound\n    \"font_scale_max\": 5.0,                 // Label font auto-scaling upper bound\n    \"min_side_px\": 2000                    // Minimum pixel count of the shorter PNG side\n  }\n}\n</code></pre>"},{"location":"auxiliary-tools/gsi-dem.html#other-parameters","title":"Other Parameters","text":"<p>Every key is explained by the inline comments in the examples above (and in the Walkthrough's <code>params/iwojima.json5</code>). A few optional keys do not appear in the examples:</p> <ul> <li><code>zmin</code> / <code>zmax</code> \u2014 colorbar range; when omitted, the 2nd\u201398th percentile of   the elevation data is used.</li> <li><code>shade</code> (default <code>true</code>) / <code>shade_alpha</code> (default <code>0.2</code>) \u2014 hillshade overlay   on the elevation colormap and its opacity (0.0\u20131.0).</li> <li><code>XY_EPSG</code> defaults to 4612 when omitted.</li> <li><code>g2zbin_float64</code> (default <code>false</code>) \u2014 store g2zbin as float64 instead of   float32.</li> <li><code>tf_gapfill</code> (default <code>false</code>) \u2014 fill NoData gaps using the GSI elevation   tile API (see Gap Filling).</li> </ul>"},{"location":"auxiliary-tools/gsi-dem.html#choosing-xy_epsg","title":"Choosing <code>XY_EPSG</code>","text":"<p>For metric output, use the JGD2011 Japan Plane Rectangular Coordinate System zone that covers your area: zones I\u2013XIX correspond to EPSG codes 6669\u20136687. Look up the zone for your prefecture on the GSI reference page https://www.gsi.go.jp/sokuchikijun/jpc.html. Examples used in this project:</p> Area Zone <code>XY_EPSG</code> Satsuma-Iwojima (Kagoshima) II 6670 Showa-shinzan (southern Hokkaido) XI 6679"},{"location":"auxiliary-tools/gsi-dem.html#cut_params-sub-region-extraction","title":"<code>cut_params</code> \u2014 Sub-Region Extraction","text":"Parameter Description <code>xcnt</code>, <code>ycnt</code> Center coordinates of the cut region <code>xy_center_unit</code> Unit of center coordinates: <code>\"degrees\"</code>, <code>\"meters\"</code>, or <code>\"DMS\"</code> <code>xsize</code>, <code>ysize</code> Half-width of the cut region <code>xy_unit</code> Unit of size values: <code>\"degrees\"</code> or <code>\"meters\"</code> <p>The cut region extends from <code>(xcnt - xsize)</code> to <code>(xcnt + xsize)</code> in both axes.</p>"},{"location":"auxiliary-tools/gsi-dem.html#usage","title":"Usage","text":"<pre><code>python3 scripts/convertZipDEM.py --prm_json params/showa.json5\n</code></pre> Option Required Description <code>--prm_json</code> Yes Path to JSON5 configuration file <code>--zmin</code> No Override colorbar minimum (overrides config and auto) <code>--zmax</code> No Override colorbar maximum (overrides config and auto) <p>A shell script is often used for convenience:</p> <pre><code>#!/bin/bash\nproject_dir_name=\"showa\"\njsonfile=\"showa.json5\"\nrepo_dir=$(pwd)\ndir_name=\"projects/${project_dir_name}\"\nmkdir -p ${dir_name}\ncd ${dir_name} || exit\npython3 ../../scripts/convertZipDEM.py --prm_json ../../params/${jsonfile}\n</code></pre>"},{"location":"auxiliary-tools/gsi-dem.html#output-files","title":"Output Files","text":"<p>Output is written to <code>projects/&lt;project_name&gt;/</code>:</p> <pre><code>projects/showa/\n\u251c\u2500\u2500 geotiff/\n\u2502   \u251c\u2500\u2500 showa.tif                    # Full-area GeoTIFF\n\u2502   \u2514\u2500\u2500 showa_2x2km_cut.tif          # Cut-region GeoTIFF\n\u251c\u2500\u2500 fig/\n\u2502   \u251c\u2500\u2500 fig_showa_meters.png         # Full-area map (meters)\n\u2502   \u251c\u2500\u2500 fig_showa_DMS.png            # Full-area map (DMS)\n\u2502   \u2514\u2500\u2500 fig_showa_2x2km_km.png       # Cut-region map (km)\n\u2514\u2500\u2500 g2zbin/\n    \u251c\u2500\u2500 showa.g2zbin                 # Full-area binary\n    \u2514\u2500\u2500 showa_2x2km.g2zbin           # Cut-region binary (\u2192 MUONITH)\n</code></pre> <p>The <code>.g2zbin</code> format is a little-endian binary file. See Input Data \u2014 DEM for the format specification.</p>"},{"location":"auxiliary-tools/gsi-dem.html#walkthrough-satsuma-iwojima","title":"Walkthrough: Satsuma-Iwojima","text":"<p>This walkthrough converts a GSI ZIP archive into <code>.g2zbin</code> and GeoTIFF files for Satsuma-Iwojima (a volcanic island in Kagoshima Prefecture; identifier <code>iwojima</code> in the repository \u2014 not the Ogasawara Iwo Jima). The whole island fits in a single mesh tile, so one ZIP is enough. The configuration <code>params/iwojima.json5</code> and the runner <code>iwojima.sh</code> are bundled with muonith-gsi-dem.</p> <ol> <li>Download the ZIP. From the GSI download service (see    Obtaining GSI DEM Data), download the DEM5A    archive for mesh 4630-12, which covers the whole island:    <code>FG-GML-463012-DEM5A-20250620.zip</code> (the trailing date differs per release).    Place it under <code>dem/iwojima/</code>.</li> <li> <p>Check the configuration. <code>params/iwojima.json5</code> lists the ZIP in    <code>zip_files</code>, sets <code>XY_EPSG: 6670</code> (Plane Rectangular CS zone II), and cuts a    3 km x 3 km region around the summit (<code>cut_params</code>, center    130.3087735 E / 30.792022 N).</p> params/iwojima.json5 (click to expand) <pre><code>{\n  \"project_name\": \"iwojima\",               // Output goes to projects/iwojima/\n  \"zip_files\": [                           // All GSI DEM ZIPs covering the area (one tile)\n    \"../../dem/iwojima/FG-GML-463012-DEM5A-20250620.zip\"\n  ],\n  \"xy_units\": [ \"meters\", \"km\", \"DMS\" ],   // Axis units; one PNG is rendered per unit\n  \"cmap\": \"terrain\",                       // Matplotlib colormap\n  \"XY_EPSG\": 6670,                         // JGD2011 zone II (Kagoshima)\n\n  \"tf_save_png\": true,                     // Save colored PNG maps\n  \"png_basename\": \"iwojima\",               // fig/fig_iwojima_&lt;unit&gt;.png\n  \"tf_save_g2zbin\": true,                  // Save g2zbin (MUONITH input)\n  \"g2zbin_basename\": \"iwojima\",            // g2zbin/iwojima.g2zbin\n  \"tf_save_geotiff\": true,                 // Save GeoTIFF raster\n  \"geotiff_basename\": \"iwojima\",           // geotiff/iwojima.tif\n\n  \"cont_pitch\": 50,                        // Contour interval [m]\n  \"dpi\": 300,                              // Output PNG resolution\n  \"min_side_px\": 2000,                     // Minimum pixel count of the shorter PNG side\n  \"font_scale_min\": 1.5,                   // Label font auto-scaling lower bound\n  \"font_scale_max\": 4.0,                   // Label font auto-scaling upper bound\n\n  \"cut_params\": {                          // Sub-region extraction (3x3 km around the summit)\n    \"cmap\": \"terrain\",                     // Matplotlib colormap for the cut PNGs\n    \"xy_units\": [ \"meters\", \"km\", \"DMS\" ], // Axis units; one PNG is rendered per unit\n    \"XY_EPSG\": 6670,                       // CRS of the cut outputs\n\n    \"tf_save_png\": true,                   // Save colored PNG maps of the cut\n    \"png_basename\": \"iwojima_3x3km\",       // fig/fig_iwojima_3x3km_&lt;unit&gt;.png\n    \"tf_save_g2zbin\": true,                // Save g2zbin of the cut (MUONITH input)\n    \"g2zbin_basename\": \"iwojima_3x3km\",    // g2zbin/iwojima_3x3km.g2zbin\n    \"tf_save_geotiff\": true,               // Save GeoTIFF of the cut\n    \"geotiff_basename\": \"iwojima_3x3km\",   // geotiff/iwojima_3x3km_cut.tif\n\n    \"xy_center_unit\": \"degrees\",           // Unit of xcnt/ycnt: \"degrees\" or \"meters\"\n    \"xcnt\": 130.3087735,                   // Center longitude (summit)\n    \"ycnt\": 30.792022,                     // Center latitude (summit)\n    \"xy_unit\": \"meters\",                   // Unit of xsize/ysize\n    // xcnt - xsize &lt;= x &lt;= xcnt + xsize, same for y\n    \"xsize\": 1500,                         // Half-width [m] \u2192 3x3 km area\n    \"ysize\": 1500,                         // Half-height [m]\n\n    \"cont_pitch\": 10,                      // Contour interval [m]\n    \"dpi\": 300,                            // Output PNG resolution\n    \"min_side_px\": 2000,                   // Minimum pixel count of the shorter PNG side\n    \"font_scale_min\": 1.5,                 // Label font auto-scaling lower bound\n    \"font_scale_max\": 4.0                  // Label font auto-scaling upper bound\n  }\n}\n</code></pre> </li> <li> <p>Run the conversion at the repository root:</p> <pre><code>bash iwojima.sh\n</code></pre> </li> <li> <p>Check the outputs under <code>projects/iwojima/</code>:</p> <pre><code>projects/iwojima/\n\u251c\u2500\u2500 geotiff/  iwojima.tif, iwojima_3x3km_cut.tif\n\u251c\u2500\u2500 g2zbin/   iwojima.g2zbin, iwojima_3x3km.g2zbin   (\u2192 MUONITH)\n\u2514\u2500\u2500 fig/      fig_iwojima_{meters,km,DMS}.png, fig_iwojima_3x3km_{...}.png\n</code></pre> </li> </ol> <p></p> <p>Full-area map with latitude/longitude axes (<code>fig_iwojima_DMS.png</code>, from <code>\"DMS\"</code> in <code>xy_units</code>; the raster is warped to EPSG:4326 for this view). Color: elevation in meters; black lines: 50 m contours; the gray frame is the sea. The sea has no LiDAR return and is stored as NoData \u2014 about 90% of mesh tile 4630-12 \u2014 which is expected for an island target.</p> <p></p> <p>Cut-region map with axes in kilometers (<code>fig_iwojima_3x3km_km.png</code>, from <code>\"km\"</code> in <code>xy_units</code>, 10 m contours), with the default hillshade overlay (<code>shade: true</code>, <code>shade_alpha: 0.2</code>).</p>"},{"location":"auxiliary-tools/gsi-dem.html#utility-scripts","title":"Utility Scripts","text":"Script Description Usage <code>geotif2PNG_terrain.py</code> Render GeoTIFF as a colored PNG with contours <code>python3 scripts/geotif2PNG_terrain.py --input_tif input.tif --output_png out.png</code> <code>g2zbin2PNG_terrain.py</code> Render g2zbin as colored PNG with contours <code>python3 scripts/g2zbin2PNG_terrain.py --input_g2zbin input.g2zbin --output_png out.png --contour 50</code> <code>gap_fill.py</code> Fill DEM NoData gaps using the GSI elevation tile API <code>python3 scripts/gap_fill.py --input_tif input.tif --output_tif filled.tif</code> <code>fetch_dem_tiles.py</code> Fetch DEM tiles from the GSI tile API <code>python3 scripts/fetch_dem_tiles.py --input_tif input.tif --output_tif api.tif</code>"},{"location":"auxiliary-tools/gsi-dem.html#gap-filling-gsi-tile-api","title":"Gap Filling (GSI Tile API)","text":"<p>GSI DEM ZIP archives occasionally contain NoData gaps. muonith-gsi-dem can fill these gaps automatically using the GSI elevation tile API (<code>cyberjapandata.gsi.go.jp</code>).</p>"},{"location":"auxiliary-tools/gsi-dem.html#pipeline-integration","title":"Pipeline integration","text":"<p>Set <code>\"tf_gapfill\": true</code> in the JSON5 configuration. The pipeline calls <code>gap_fill.py</code> internally after merging DEM tiles, filling NoData pixels via bilinear interpolation from the nearest available GSI tile data.</p>"},{"location":"auxiliary-tools/gsi-dem.html#standalone-usage","title":"Standalone usage","text":"<p><code>gap_fill.py</code> and <code>fetch_dem_tiles.py</code> can also be used independently:</p> Script Input Description <code>gap_fill.py</code> GeoTIFF or g2zbin Fill NoData gaps in an existing DEM raster <code>fetch_dem_tiles.py</code> GeoTIFF, g2zbin, or JSON5 Build a DEM raster entirely from the GSI tile API <p>Both scripts support tile caching (<code>--cache_dir</code>) to avoid redundant HTTP requests and a configurable request delay (<code>--request_delay</code>) to respect API rate limits.</p>"},{"location":"auxiliary-tools/path-view.html","title":"Path Viewer","text":""},{"location":"auxiliary-tools/path-view.html#muonith-path-view","title":"muonith-path-view","text":"<p>External repository</p> <p>muonith-path-view is maintained in a separate repository. This page documents its stable interface for use with MUONITH.</p>"},{"location":"auxiliary-tools/path-view.html#overview","title":"Overview","text":"<p>muonith-path-view visualizes muon paths through terrain using DEM (Digital Elevation Model) data. For each elevation angle, it draws ray paths from a detector position, color-coding air (above ground) and rock (below ground) segments on a topographic map. The output is a set of PNG images assembled into a GIF animation and a multi-page PDF.</p> <p>This tool helps verify detector placement and angular coverage before running the MUONITH pipeline.</p> Showa-shinzan DEM with 13 detector positions. Example output for Showa-shinzan (det_00): black lines show muon paths through air, white lines show paths through rock. The animation sweeps through elevation angles. Example output for Showa-shinzan (det_08): black lines show muon paths through air, white lines show paths through rock. The animation sweeps through elevation angles. <p>All beams in one frame share the same elevation angle</p> <p>Each frame fixes one elevation angle \\(\\theta_{\\mathrm{elev}}\\) and sweeps the azimuth \\(\\phi\\), so the drawn beams lie on a cone of constant physical elevation \u2014 they are not contained in a single plane. Each beam direction is the unit vector \\((v_x, v_y, v_z) = (\\cos\\theta_{\\mathrm{elev}}\\cos\\phi,\\ \\cos\\theta_{\\mathrm{elev}}\\sin\\phi,\\ \\sin\\theta_{\\mathrm{elev}})\\). This holds for every <code>angle_unit</code> mode (<code>deg</code>, <code>rad</code>, <code>tangent</code>); see <code>direction_setup</code> \u2014 Angle Configuration for the exact convention. This differs from MUONITH's <code>DetectorPanel</code> in Tangent mode, where the bin coordinates are slopes about the panel's forward axis (\\(t_x = v_x/v_y\\), \\(t_y = v_z/v_y\\)): a row of constant \\(t_y\\) is a tilted plane of directions, not a cone of constant elevation; see From (tx, ty) to a Ray Direction.</p>"},{"location":"auxiliary-tools/path-view.html#dependencies","title":"Dependencies","text":"Package Purpose Python \u2265 3.10 Runtime numpy Array computation matplotlib Map rendering (pcolormesh, contour, hillshade) pyproj CRS transformation json5 JSON5 config parser rasterio GeoTIFF reading scipy <code>RegularGridInterpolator</code> for DEM queries (a slow fallback is used if absent) ImageMagick <code>convert</code> command for PDF/GIF generation <p>The Python packages are declared in <code>pyproject.toml</code> and pinned in <code>uv.lock</code>. ImageMagick is a system package and is not covered by either.</p>"},{"location":"auxiliary-tools/path-view.html#installation","title":"Installation","text":"<pre><code>cd muonith-path-view\n\n# Create .venv and install the pinned dependencies\nuv sync\n\n# ImageMagick\nsudo apt install imagemagick    # Ubuntu/Debian\nbrew install imagemagick         # macOS (Homebrew)\n</code></pre> <p><code>uv sync</code> creates <code>.venv/</code> in the repository root. The bundled <code>.envrc</code> puts <code>.venv/bin</code> on the PATH, so with direnv installed (<code>direnv allow</code> on first entry) every <code>python3</code> call in the repository uses that environment. Without direnv, either activate it (<code>source .venv/bin/activate</code>) or prefix commands with <code>uv run</code>:</p> <pre><code>uv run python3 scripts/mp_pypathviewer_pdf.py params/tutorial.json5\n</code></pre>"},{"location":"auxiliary-tools/path-view.html#quick-start","title":"Quick Start","text":""},{"location":"auxiliary-tools/path-view.html#run-the-bundled-tutorial-no-geotiff-to-prepare","title":"Run the bundled tutorial (no GeoTIFF to prepare)","text":"<p>muonith-path-view ships a self-contained example under <code>examples/tutorial/</code>: a synthetic DEM (<code>tutorial-5m.g2zbin</code>, and the identical grid as <code>tutorial-5m.tif</code>) together with 11 detector parameter files under <code>examples/tutorial/detparams/</code>. Nothing has to be downloaded or converted, so a fresh clone runs as-is:</p> <pre><code>cd muonith-path-view\npython3 scripts/mp_pypathviewer_pdf.py params/tutorial.json5\n</code></pre> <p>Eleven detectors \u00d7 16 elevation angles are rendered (about one minute with <code>num_threads: 8</code>). The PNGs land in <code>projects/tutorial/&lt;det_name&gt;/</code> as <code>fig_elev00.0.png</code> \u2026 <code>fig_elev45.0.png</code>, and the per-detector animations and documents are collected in <code>projects/tutorial/gif/</code> and <code>projects/tutorial/pdf/</code>.</p> <p>Hillshade (relief shading) can be toggled on the command line. These flags override <code>rendering.shade</code> and <code>rendering.shade_alpha</code> in the config file:</p> <pre><code>python3 scripts/mp_pypathviewer_pdf.py params/tutorial.json5 --shade --shade-alpha 0.4\npython3 scripts/mp_pypathviewer_pdf.py params/tutorial.json5 --no-shade\n</code></pre> <p>The tutorial DEM is synthetic</p> <p>The grid is a synthetic primitive shape, not derived from any third-party map data \u2014 which is why it can be redistributed. It has no real-world location. EPSG:6677 (JGD2011 Zone 9) is simply borrowed because muonith-path-view only needs path lengths in meters, so any real projected metric CRS will do. A made-up code (e.g. 9999) is rejected.</p> <p>To exercise the GeoTIFF path with the same terrain, point <code>input_dem</code> at <code>examples/tutorial/tutorial-5m.tif</code> and delete <code>input_dem_epsg</code> \u2014 a GeoTIFF embeds its own CRS. Both files read back as the same grid.</p>"},{"location":"auxiliary-tools/path-view.html#use-your-own-dem","title":"Use your own DEM","text":"<p>Set <code>file_path.input_dem</code> to your own file. A GeoTIFF carries its own CRS; a <code>.g2zbin</code> grid does not, so <code>file_path.input_dem_epsg</code> must be declared explicitly:</p> <pre><code>// GeoTIFF \u2014 CRS is read from the file\n\"input_dem\": \"path/to/my_dem.tif\",\n\n// g2zbin \u2014 CRS must be given (real projected metric CRS)\n\"input_dem\": \"path/to/my_dem.g2zbin\",\n\"input_dem_epsg\": 6677,\n</code></pre> <p>The DEM CRS must match <code>detector_setup.epsg_system.epsg_code_output</code>. Then run:</p> <pre><code>python3 scripts/mp_pypathviewer_pdf.py params/my_config.json5\n</code></pre>"},{"location":"auxiliary-tools/path-view.html#configuration","title":"Configuration","text":"<p>Configuration files are stored in <code>params/</code> as JSON5 (<code>.json5</code>) or JSON (<code>.json</code>). JSON5 supports comments (<code>//</code>, <code>/* */</code>) and trailing commas.</p>"},{"location":"auxiliary-tools/path-view.html#top-level-parameters","title":"Top-Level Parameters","text":"Parameter Default Description <code>num_threads</code> \u2014 Number of parallel subprocesses for PNG generation (capped to logical cores \u2212 1) <code>skip_making_png</code> <code>false</code> If <code>true</code>, reuse existing PNGs (skip generation)"},{"location":"auxiliary-tools/path-view.html#file_path-inputoutput-paths","title":"<code>file_path</code> \u2014 Input/Output Paths","text":"Parameter Description <code>input_dem</code> Path to the input DEM file (GeoTIFF, <code>.g2zbin</code>, or text) <code>input_dem_epsg</code> EPSG code of the DEM grid. Required for <code>.g2zbin</code> (which stores no CRS); omit for GeoTIFF <code>input_dem_nodata</code> Value marking missing elevation in the input grid (e.g. <code>-9999.0</code>) <code>output_dir</code> Output directory for PNG, PDF, GIF <code>sub_process_python_path</code> Path to <code>sub_pathviewer.py</code> <code>out_pdf_header_name</code> Filename prefix for PDF output <code>out_gif_header_name</code> Filename prefix for GIF output <code>outlog</code> Path to the general log file <code>error_log</code> Path to the error log file <code>log_mode</code> Log write mode: <code>\"overwrite\"</code>, <code>\"append\"</code>, or <code>\"archive\"</code>"},{"location":"auxiliary-tools/path-view.html#rendering-display-settings","title":"<code>rendering</code> \u2014 Display Settings","text":"Parameter Description <code>dpi</code> Output resolution (dots per inch) <code>gif_delay_msec</code> Frame interval for GIF animation [ms] <code>colormap</code> Matplotlib colormap name (e.g., <code>\"terrain\"</code>) <code>color_over</code> / <code>color_under</code> / <code>color_nodata</code> Colors for values above max / below min / NaN <code>n_grad</code> Number of discrete color levels <code>vmin</code>, <code>vmax</code> Elevation range for colormap [m] <code>shade</code> Overlay hillshade (relief shading) on the elevation colors. Default <code>true</code>. Overridden by <code>--shade</code> / <code>--no-shade</code> <code>shade_alpha</code> Hillshade opacity (0.0 = invisible, 1.0 = opaque). Default <code>0.2</code>. Overridden by <code>--shade-alpha</code> <code>shade_azimuth_deg</code> Light direction, degrees clockwise from north. Default <code>315</code> <code>shade_altitude_deg</code> Light height above the horizon [deg]. Default <code>45</code> <code>shade_vert_exag</code> Vertical exaggeration applied to the elevation before shading. Default <code>1.0</code> <code>x_wid</code>, <code>y_wid</code> Half-width of the display window [m] <code>xy_unit</code> Axis label unit: <code>\"m\"</code> or <code>\"km\"</code> <code>fit_xy_size</code> If <code>true</code>, clip axis range to actual data extent <code>xy_offset_center</code> If <code>true</code>, shift coordinates so the center becomes (0, 0) <code>grid</code> Show grid lines (<code>true</code> / <code>false</code>) <code>grid_color</code>, <code>grid_linewidth</code>, <code>grid_linestyle</code> Grid line appearance"},{"location":"auxiliary-tools/path-view.html#detector_setup-detector-and-path-settings","title":"<code>detector_setup</code> \u2014 Detector and Path Settings","text":""},{"location":"auxiliary-tools/path-view.html#epsg_system-coordinate-reference-system","title":"<code>epsg_system</code> \u2014 Coordinate Reference System","text":"Parameter Description <code>epsg_code_input</code> EPSG code for input detector coordinates <code>epsg_code_output</code> EPSG code for internal processing (must be a projected/metric CRS) <p>All coordinates are transformed to <code>epsg_code_output</code> for processing:</p> <pre><code>GeoTIFF        \u2192 CRS from file metadata     \u2192 must match epsg_code_output\ndet_info_files \u2192 no CRS                     \u2192 read as epsg_code_input\nx_cnt / y_cnt  \u2192 epsg_code_center           \u2192 transformed to epsg_code_output\n</code></pre> <p>Common EPSG codes:</p> Code CRS 4326 WGS84 (latitude/longitude) 6676 JGD2011 Zone 8 6677 JGD2011 Zone 9 6679 JGD2011 Zone 11"},{"location":"auxiliary-tools/path-view.html#det_info_files-external-detector-files-optional","title":"<code>det_info_files</code> \u2014 External Detector Files (optional)","text":"<p>When specified, detector coordinates are loaded from external JSON5 files. This allows reusing the same detector parameter files used by MUONITH:</p> <pre><code>\"det_info_files\": [\n    \"/path/to/det_00.json5\",\n    \"/path/to/det_01.json5\"\n]\n</code></pre> <p>Each external file must contain a <code>DETECTOR_PARAMETERS</code> key:</p> <pre><code>{\n  \"DETECTOR_PARAMETERS\": {\n    \"name\": \"det_00\",\n    \"x\": 50168.1,      // \u2192 x_det\n    \"y\": -161872.5,    // \u2192 y_det\n    \"z\": 208.4,        // \u2192 z_det\n    \"yaw_deg\": -22.031 // \u2192 azimuth_center\n  }\n}\n</code></pre> <p>When <code>det_info_files</code> is used, the merge behavior is:</p> Parameter Source <code>x_det</code>, <code>y_det</code>, <code>z_det</code> External file (<code>det_info_files</code>) <code>epsg_code_input</code>, <code>epsg_code_output</code> Config file (<code>epsg_system</code>) <code>direction_setup</code>, <code>line_path</code> Config file <p>When multiple detector files are specified, each detector is processed sequentially with output in <code>&lt;output_dir&gt;/&lt;det_name&gt;/</code>.</p>"},{"location":"auxiliary-tools/path-view.html#direction_setup-angle-configuration","title":"<code>direction_setup</code> \u2014 Angle Configuration","text":"Parameter Description <code>angle_unit</code> Unit for angle values: <code>\"deg\"</code>, <code>\"rad\"</code>, or <code>\"tangent\"</code> <code>azimuth_zero</code> Reference direction for azimuth = 0: <code>\"north\"</code> or <code>\"east\"</code> <code>azimuth_center</code> Azimuth offset (overridden by <code>yaw_deg</code> from <code>det_info_files</code>) <code>azimuth_mode</code> <code>\"interval\"</code> for <code>[start, end, step]</code> or <code>\"array\"</code> for explicit list <code>azimuth_range</code> Azimuth sweep range, e.g., <code>[-45, 45, 5]</code> <code>elevation_mode</code> <code>\"interval\"</code> or <code>\"array\"</code> <code>elevation_range</code> Elevation sweep range, e.g., <code>[45, 0, -3]</code> (high \u2192 low) <p>Each beam direction is a unit vector built from one elevation angle \\(\\theta_{\\mathrm{elev}}\\) and one azimuth angle \\(\\phi\\):</p> \\[ \\begin{aligned} v_z &amp;= \\sin\\theta_{\\mathrm{elev}} \\\\ v_x &amp;= \\cos\\theta_{\\mathrm{elev}} \\cos\\phi \\\\ v_y &amp;= \\cos\\theta_{\\mathrm{elev}} \\sin\\phi \\end{aligned} \\] <p>where \\(\\phi\\) is the internal azimuth after the <code>azimuth_zero</code> / <code>azimuth_center</code> conversions. The elevation \\(\\theta_{\\mathrm{elev}}\\) is fixed while the azimuth is swept, so the beams for one elevation value lie on a cone of constant physical elevation in every <code>angle_unit</code> mode.</p> <p><code>angle_unit</code> selects how the numbers in <code>azimuth_range</code> / <code>elevation_range</code> are read, for both azimuth and elevation. Below, \\(u\\) denotes each raw number written in those ranges:</p> <code>angle_unit</code> Interpretation of each value \\(u\\) <code>deg</code> angle in degrees <code>rad</code> angle in radians <code>tangent</code> slope: the angle is \\(\\arctan u\\). Equal steps in \\(u\\) give unequal angular steps. <p>Note: when <code>angle_unit</code> is not <code>deg</code>, the <code>azimuth_center</code> offset is added as radians.</p> <p>Comparison with MUONITH's <code>DetectorPanel</code> (see From (tx, ty) to a Ray Direction); \\(u\\) denotes each raw number written in <code>azimuth_range</code> / <code>elevation_range</code> (see the table above):</p> Tool / mode Direction from bin values Row of constant vertical value muonith-path-view, every <code>angle_unit</code> spherical, \\(v_z = \\sin\\theta_{\\mathrm{elev}}\\) (<code>tangent</code> only converts the input, \\(\\theta_{\\mathrm{elev}} = \\arctan u\\)) cone of constant elevation MUONITH <code>DetectorPanel</code>, Degree / Radian spherical, \\(v_z = \\sin t_y\\) cone of constant elevation MUONITH <code>DetectorPanel</code>, Tangent slopes, \\(t_y = v_z / v_y\\) tilted plane"},{"location":"auxiliary-tools/path-view.html#line_path-ray-path-rendering","title":"<code>line_path</code> \u2014 Ray Path Rendering","text":"Parameter Description <code>min</code> Minimum path distance from detector [m] <code>max</code> Maximum path distance from detector [m] <code>pitch</code> Distance step along the path [m] <code>air_path_width</code>, <code>air_path_color</code>, <code>air_path_alpha</code> Style for above-ground segments <code>rock_path_width</code>, <code>rock_path_color</code>, <code>rock_path_alpha</code> Style for below-ground segments"},{"location":"auxiliary-tools/path-view.html#charactor_setting-typography","title":"<code>charactor_setting</code> \u2014 Typography","text":"Parameter Description <code>title_fontsize</code> Font size for plot title [pt] <code>azimuth_fontsize</code>, <code>azimuth_fontcolor</code> Font size and color for azimuth annotations <code>xaxis_tick_fontsize</code>, <code>yaxis_tick_fontsize</code> Tick label font sizes <code>xaxis_title_fontsize</code>, <code>yaxis_title_fontsize</code> Axis title font sizes <code>caxis_tick_fontsize</code>, <code>caxis_title_fontsize</code> Colorbar font sizes <code>caxis_title</code> Colorbar title text (e.g., <code>\"Elevation (m)\"</code>) Full example: Showa-shinzan (<code>params/showa.json5</code>) <p>Every key is annotated inline. Paths are placeholders \u2014 replace them with your own.</p> <pre><code>{\n  // ==== Top-level ====\n  // Number of parallel subprocesses for PNG generation (capped to logical_cores - 1)\n  \"num_threads\": 25,\n  // If true, skip PNG generation and reuse existing PNGs (only if all PNGs already exist)\n  \"skip_making_png\": false,\n\n  \"file_path\": {\n    // Path to the input DEM. GeoTIFF carries its own CRS, so input_dem_epsg is omitted here\n    \"input_dem\": \"path/to/showa_2x2km_cut.tif\",\n    // Output directory for generated PNG, PDF, and GIF files\n    \"output_dir\": \"projects/showa\",\n    // Path to the subprocess script that renders each elevation angle as a PNG\n    \"sub_process_python_path\": \"scripts/sub_pathviewer.py\",\n    // Filename prefix for the output PDF (used in single-detector mode)\n    \"out_pdf_header_name\": \"showa\",\n    // Filename prefix for the output GIF\n    \"out_gif_header_name\": \"showa\",\n    // Path to the general output log file\n    \"outlog\": \"projects/showa/tmp.log\",\n    // Path to the error log file\n    \"error_log\": \"projects/showa/ERROR.log\",\n    // Log file write mode: \"overwrite\" replaces, \"append\" adds, \"archive\" rotates\n    \"log_mode\": \"overwrite\",\n  },\n\n  \"rendering\": {\n    // If true, shift all coordinates so that the center point becomes the origin (0,0)\n    \"xy_offset_center\": false,\n    // Resolution in dots per inch for PNG/PDF output\n    \"dpi\": 300,\n    // Frame delay in milliseconds for GIF animation\n    \"gif_delay_msec\": 1000,\n\n    // ==== Colormap ====\n    // Matplotlib colormap name for elevation visualization\n    \"colormap\": \"terrain\",\n    // Color for values exceeding vmax (matplotlib set_over)\n    \"color_over\": \"white\",\n    // Color for values below vmin (matplotlib set_under)\n    \"color_under\": \"black\",\n    // Color for NoData/NaN pixels (matplotlib set_bad)\n    \"color_nodata\": \"silver\",\n    // Number of discrete color gradient levels between vmin and vmax\n    \"n_grad\": 40,\n    // Minimum elevation value (meters) for colormap range\n    \"vmin\": 0,\n    // Maximum elevation value (meters) for colormap range. Showa-shinzan spans 0-400 m\n    \"vmax\": 400,\n\n    // ==== Hillshade ====\n    // If true, overlay a hillshade (relief shading) on the elevation colors.\n    // The command line options --shade / --no-shade override this.\n    \"shade\": true,\n    // Opacity of the hillshade overlay (0.0 = invisible, 1.0 = opaque).\n    // The command line option --shade-alpha overrides this.\n    \"shade_alpha\": 0.2,\n    // Direction the light comes from, in degrees clockwise from north\n    \"shade_azimuth_deg\": 315,\n    // Height of the light above the horizon, in degrees\n    \"shade_altitude_deg\": 45,\n    // Vertical exaggeration applied to the elevation before shading\n    \"shade_vert_exag\": 1.0,\n\n    // ==== Contour labels ====\n    // Contour label interval (meters)\n    \"contour_label_interval\": 10,\n    // Contour label font size (pt)\n    \"contour_label_fontsize\": 4,\n    // Contour label color\n    \"contour_label_color\": \"black\",\n\n    // ==== Grid ====\n    // Whether to display grid lines on the plot\n    \"grid\": true,\n    // Color of grid lines\n    \"grid_color\": \"black\",\n    // Width of grid lines in points\n    \"grid_linewidth\": 0.5,\n    // Line style for grid: \"solid\", \"dashed\", \"dashdot\", or \"dotted\"\n    \"grid_linestyle\": \"dotted\",\n\n    // ==== Axis ====\n    // Unit for axis labels: \"m\" for meters, \"km\" for kilometers\n    \"xy_unit\": \"km\",\n    // Font size (pt) for axis tick labels\n    \"xy_tick_font_size\": 8,\n    // Font size (pt) for axis title labels (e.g. \"X (km)\")\n    \"xy_axis_title_font_size\": 10,\n\n    // ==== Window ====\n    // Half-width of the plot window in meters (xmin = xcnt - x_wid, xmax = xcnt + x_wid)\n    \"x_wid\": 2000,\n    // Half-height of the plot window in meters (ymin = ycnt - y_wid, ymax = ycnt + y_wid)\n    \"y_wid\": 2000,\n    // If true, clip the axis range to the actual data extent (remove empty margins)\n    \"fit_xy_size\": true,\n  },\n\n  // Detector and path calculation settings\n  \"detector_setup\": {\n\n    // Coordinate reference system (EPSG codes) for coordinate transformation\n    \"epsg_system\": {\n      // EPSG code for the input detector coordinates\n      \"epsg_code_input\": \"6679\",      // JGD2011 Zone 11\n      // EPSG code for the output plot coordinates (must be a metric/projected CRS)\n      \"epsg_code_output\": \"6679\",\n    },\n\n    // External detector files: each supplies x, y, z and yaw_deg (-&gt; azimuth_center).\n    // 13 detectors around Showa-shinzan; these are the same files MUONITH uses.\n    \"det_info_files\": [\n      \"path/to/detparams/det_00.json5\",\n      \"path/to/detparams/det_01.json5\",\n      \"path/to/detparams/det_02.json5\",\n      \"path/to/detparams/det_03.json5\",\n      \"path/to/detparams/det_04.json5\",\n      \"path/to/detparams/det_05.json5\",\n      \"path/to/detparams/det_06.json5\",\n      \"path/to/detparams/det_07.json5\",\n      \"path/to/detparams/det_08.json5\",\n      \"path/to/detparams/det_09.json5\",\n      \"path/to/detparams/det_10.json5\",\n      \"path/to/detparams/det_11.json5\",\n      \"path/to/detparams/det_12.json5\",\n    ],\n\n    // Direction/angle configuration for path calculation\n    \"direction_setup\": {\n      // Unit for angle values: \"deg\", \"rad\", or \"tangent\"\n      \"angle_unit\": \"deg\",\n      // Reference direction for azimuth=0: \"north\" or \"east\"\n      \"azimuth_zero\": \"north\",\n      // azimuth_center is loaded from each det_info_file (yaw_deg)\n      // Azimuth generation mode: \"array\" for explicit list, \"interval\" for [start, end, step]\n      \"azimuth_mode\": \"interval\",\n      // Azimuth sweep range as [start, end, step]: +-45 deg in 5 deg steps\n      \"azimuth_range\": [-45, 45, 5],\n      // Elevation generation mode: \"array\" for explicit list, \"interval\" for [start, end, step]\n      \"elevation_mode\": \"interval\",\n      // Elevation sweep range as [start, end, step]: 45 deg down to 0 deg in 3 deg steps\n      \"elevation_range\": [45, 0, -3],\n    },\n\n    // Path line rendering parameters for the ray-tracing visualization\n    \"line_path\": {\n      // Minimum path distance (meters) from the detector\n      \"min\": 0.0,\n      // Maximum path distance (meters) from the detector\n      \"max\": 1500.0,\n      // Distance step/interval (meters) along the path\n      \"pitch\": 5.0,\n      // Line width (points) for the air (above-ground) segment of the path\n      \"air_path_width\": 0.8,\n      // Color for the air (above-ground) segment of the path\n      \"air_path_color\": \"black\",\n      // Opacity for the air segment (0.0 = transparent, 1.0 = opaque)\n      \"air_path_alpha\": 1.0,\n      // Line width (points) for the rock (below-ground) segment of the path\n      \"rock_path_width\": 1.5,\n      // Color for the rock (below-ground) segment of the path\n      \"rock_path_color\": \"white\",\n      // Opacity for the rock segment (0.0 = transparent, 1.0 = opaque)\n      \"rock_path_alpha\": 0.5,\n    },\n  },\n\n  // Typography and text formatting settings for plot elements\n  \"charactor_setting\": {\n    // Scaling factor for beam path visualization relative to character size\n    \"beam_path_factor\": 1.03,\n    // Font size (pt) for the plot main title\n    \"title_fontsize\": 10,\n    // Font size (pt) for azimuth angle text annotations on the plot\n    \"azimuth_fontsize\": 5,\n    // Color for azimuth angle text annotations\n    \"azimuth_fontcolor\": \"black\",\n    // Font size (pt) for x-axis tick labels\n    \"xaxis_tick_fontsize\": 8,\n    // Font size (pt) for y-axis tick labels\n    \"yaxis_tick_fontsize\": 8,\n    // Font size (pt) for colorbar tick labels\n    \"caxis_tick_fontsize\": 8,\n    // Font size (pt) for x-axis title\n    \"xaxis_title_fontsize\": 8,\n    // Font size (pt) for y-axis title\n    \"yaxis_title_fontsize\": 8,\n    // Font size (pt) for colorbar title\n    \"caxis_title_fontsize\": 8,\n    // Text displayed as the colorbar title\n    \"caxis_title\": \"Elevation (m)\",\n  },\n}\n</code></pre>"},{"location":"auxiliary-tools/path-view.html#processing-flow","title":"Processing Flow","text":"<pre><code>           Config file (JSON5)                       DEM (GeoTIFF / .g2zbin / text)\n                    \u2502                                               \u2502\n                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                            \u2502\n                                            \u25bc\n                             mp_pypathviewer_pdf.py\n                                            \u2502\n                  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                  \u25bc                         \u25bc                         \u25bc\n         sub_pathviewer.py         sub_pathviewer.py         sub_pathviewer.py\n         (elev 45\u00b0)                (elev 42\u00b0)                (elev 39\u00b0) ...\n                  \u2502                         \u2502                         \u2502\n                  \u25bc                         \u25bc                         \u25bc\n               PNG                       PNG                       PNG\n                  \u2502                         \u2502                         \u2502\n                  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                            \u2502\n                            \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                            \u25bc                               \u25bc\n                         PDF                              GIF\n                      (ImageMagick)                   (ImageMagick)\n</code></pre> <p>Each <code>sub_pathviewer.py</code> invocation:</p> <ol> <li>Loads the DEM and builds a 2D elevation grid</li> <li>For each azimuth angle, traces a ray path from the detector</li> <li>Classifies each path segment as air (above terrain) or rock (below terrain)</li> <li>Draws the segments with distinct colors and line widths</li> <li>Saves the result as a PNG</li> </ol>"},{"location":"auxiliary-tools/path-view.html#output-files","title":"Output Files","text":"Format Filename pattern Description PNG <code>fig_elev45.0.png</code> Topographic map with ray paths for one elevation angle GIF <code>&lt;det_name&gt;.gif</code> Animated sweep through elevation angles PDF <code>&lt;det_name&gt;.pdf</code> Multi-page document (one page per elevation angle) <p>In multi-detector mode, the PNGs are grouped per detector and the animations and documents are collected together:</p> <pre><code>&lt;output_dir&gt;/\n\u251c\u2500\u2500 det_00/\n\u2502   \u251c\u2500\u2500 fig_elev00.0.png\n\u2502   \u251c\u2500\u2500 ...\n\u2502   \u2514\u2500\u2500 fig_elev45.0.png\n\u251c\u2500\u2500 det_01/\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 gif/\n\u2502   \u251c\u2500\u2500 det_00.gif ...\n\u2514\u2500\u2500 pdf/\n    \u251c\u2500\u2500 det_00.pdf ...\n</code></pre>"},{"location":"auxiliary-tools/path-view.html#dem-input-formats","title":"DEM Input Formats","text":"<p>muonith-path-view automatically detects the input format based on the file extension:</p> Format Extension Description GeoTIFF <code>.tif</code>, <code>.tiff</code> Georeferenced raster with embedded CRS. CRS must match <code>epsg_code_output</code>. Binary <code>.g2zbin</code> Compact uniform-grid binary: magic + <code>Grid2d</code> header (x/y axes) + row-major <code>z</code> array. Stores no CRS, so <code>input_dem_epsg</code> must be set in the config. See Input Data. Text <code>.xyz</code>, <code>.dem</code>, etc. Space-separated <code>x y z</code> values, one point per line <p>To convert a <code>.g2zbin</code> grid into a GeoTIFF, run the bundled converter with the EPSG code of the grid as the third argument. This is how <code>examples/tutorial/tutorial-5m.tif</code> was produced:</p> <pre><code>python3 scripts/g2zbin_to_geotiff.py examples/tutorial/tutorial-5m.g2zbin examples/tutorial/tutorial-5m.tif 6677\n</code></pre> <p>The EPSG code must be a real projected metre CRS; a made-up code (e.g. 9999) is rejected because it cannot be resolved.</p>"},{"location":"auxiliary-tools/path-view.html#supplementary-tool-azimuthpy","title":"Supplementary Tool: azimuth.py","text":"<p>Calculates the azimuth angle between two points:</p> <pre><code>python3 scripts/azimuth.py\n</code></pre>"},{"location":"auxiliary-tools/path-view.html#troubleshooting","title":"Troubleshooting","text":""},{"location":"auxiliary-tools/path-view.html#imagemagick-policy-error","title":"ImageMagick Policy Error","text":"<p>If PDF or GIF generation fails with a permission error, check the ImageMagick security policy:</p> <pre><code># Policy file location\n/etc/ImageMagick-6/policy.xml\n# or\n/etc/ImageMagick-7/policy.xml\n</code></pre> <p>Look for <code>&lt;policy domain=\"coder\" rights=\"none\" pattern=\"PDF\" /&gt;</code> and change <code>rights</code> to <code>\"read|write\"</code>.</p>"},{"location":"auxiliary-tools/path-view.html#out-of-memory","title":"Out of Memory","text":"<p>When processing large DEMs, reduce <code>num_threads</code> or crop the DEM to a smaller region. Each subprocess loads the full DEM into memory, so total memory usage scales with the number of threads.</p>"},{"location":"concepts/bin-grouping.html","title":"Bin Grouping","text":""},{"location":"concepts/bin-grouping.html#bin-grouping","title":"Bin Grouping","text":"<p>A DetectorPanel discretizes its angular field of view with a Grid2dBinGroup \u2014 a 2D grid of angular bins, one DetectorElement per bin. Where the muon signal is sparse, neighboring bins are merged into groups to recover statistical significance. This page explains the grouping algorithm and the index map that binds grouped and individual bins.</p> <p>The panel structure holding this grid is described in Detector Model \u2014 DetectorPanel.</p> <p></p>"},{"location":"concepts/bin-grouping.html#automatic-bin-grouping","title":"Automatic Bin Grouping","text":"<p>When <code>tf_run_auto_grouping</code> is enabled, the angular bins are recursively subdivided based on signal distribution:</p> <p></p> <p>The example below shows the effect on forward-modeled signal data (synthetic 11-detector example, detector 01). The left panel is the per-element signal field in angular (tangent) coordinates with a logarithmic color scale; the right panel is the same signal after grouping, where each cell is one Grid2dBinGroup group \u2014 coarse where the signal is sparse, fine where the signal is dense.</p> <p></p> <p>Geographic setting of the synthetic example: the Omuro DEM (5 m resolution, elevation in meters above sea level) with the 11 detectors placed around the dome. Numbers along the lines indicate each detector's horizontal distance to the summit. The two panels below show the per-element and grouped signal for detector 01 (<code>det_2018_B</code>), located on the western foot of the dome.</p> Per-element signal (before grouping) Grouped bins (after grouping)"},{"location":"concepts/bin-grouping.html#manual-bin-grouping","title":"Manual Bin Grouping","text":"<p>Instead of the automatic subdivision, groups can be specified explicitly with rectangle-list files. This route is active only when both <code>tf_run_1st_grouping</code> and <code>tf_run_auto_grouping</code> are <code>false</code> and <code>n_detector_grouping_manual &gt; 0</code> in <code>BIN_GROUP_PARAMETERS</code> (see Parameter Reference \u2014 BIN_GROUP_PARAMETERS).</p> <p>Each detector reads a plain-text file listing one rectangle per line:</p> <pre><code>xlow xup ylow yup\n</code></pre> <ul> <li>Values are angular coordinates in the panel's <code>angle_unit</code> (default: tangent, dimensionless).</li> <li>Each rectangle becomes one group; membership uses half-open intervals (<code>min &lt;= t &lt; max</code>).</li> <li>The rectangles must tile the field of view: any overlap, any gap between   rectangles, or any bin center left uncovered raises an error at load time.</li> </ul>"},{"location":"concepts/bin-grouping.html#onetomanyuobimap-bin-index-mapping","title":"OneToManyUOBimap \u2014 Bin Index Mapping","text":"<p>Grid2dBinGroup uses OneToManyUOBimap to maintain bidirectional mappings between grouped bins and individual bins. This data structure provides:</p> Direction Lookup Complexity One \u2192 Many <code>get_vec_Many(One)</code> \u2014 retrieve all individual bins belonging to a group O(k) where k = group size Many \u2192 One <code>getOne(Many)</code> \u2014 find which group an individual bin belongs to O(1) <p>Key constraints:</p> <ul> <li>Each <code>Many</code> value maps to exactly one <code>One</code> key (uniqueness enforced on insert)</li> <li>Each <code>One</code> key can map to multiple <code>Many</code> values</li> <li><code>insertOrOverwrite()</code> allows reassignment of a <code>Many</code> value to a different <code>One</code> key</li> </ul>"},{"location":"concepts/coordinate-system.html","title":"Coordinate System","text":""},{"location":"concepts/coordinate-system.html#coordinate-system","title":"Coordinate System","text":"<p>This page defines the spatial conventions, units, and angle representations used throughout MUONITH.</p>"},{"location":"concepts/coordinate-system.html#coordinate-axes","title":"Coordinate Axes","text":"<p>MUONITH uses a right-handed, z-up Cartesian coordinate system:</p> Axis Direction Typical usage x East Horizontal (easting) y North Horizontal (northing) z Up Vertical (elevation) <p>All spatial coordinates are expressed in meters (m).</p> <pre><code>        z (elevation)\n        |\n        |\n        |_______ y (northing)\n       /\n      /\n     x (easting)\n</code></pre> <p>This convention is consistent across all geometric classes: Grid1d/2d/3d, Ray3d, Pillar, DetectorPanel, and shape primitives.</p>"},{"location":"concepts/coordinate-system.html#units","title":"Units","text":"Quantity Unit Symbol Spatial position meters m Path length (PL) meters m Density length (DL) kilograms per square meter kg/m\u00b2 Density kilograms per cubic meter kg/m\u00b3 Penetrating muon flux per (m\u00b2 sr s) 1/(m\u00b2 sr s) Effective area square meters m\u00b2 Solid angle steradians sr Exposure time seconds s <p>Note</p> <p>Some visualization outputs may display density in g/cm\u00b3 for convenience. Internally, all computations use SI units (kg/m\u00b3).</p>"},{"location":"concepts/coordinate-system.html#angle-representation","title":"Angle Representation","text":"<p>MUONITH supports three angular representations, selectable per detector panel:</p> Mode Description Example Tangent Slope (rise/run) tan(\u03b8) Degree Angular degrees 0\u00b0\u2013360\u00b0 Radian SI angular unit 0\u20132\u03c0 <p>The angular unit is configured through the detector parameter file and applies consistently to all detector elements within a panel.</p> <p>The detector's angular bins are addressed by two coordinates, <code>tx</code> (horizontal) and <code>ty</code> (vertical), interpreted in this unit. How a <code>(tx, ty)</code> bin is turned into a look direction is described in From (tx, ty) to a Ray Direction.</p>"},{"location":"concepts/coordinate-system.html#yaw-convention","title":"Yaw Convention","text":"<ul> <li>Yaw angle = 0\u00b0 points North (+y direction)</li> <li>Yaw angle = 90\u00b0 points East (+x direction)</li> <li>Yaw is measured clockwise when viewed from above (compass bearing): 0\u00b0 = N, 90\u00b0 = E, 180\u00b0 = S, 270\u00b0 (\u221290\u00b0) = W</li> </ul>"},{"location":"concepts/coordinate-system.html#bin-intervals","title":"Bin Intervals","text":"<p>Grid bins use half-open intervals:</p> \\[[x_{\\min},\\; x_{\\max})\\] <p>The lower bound is inclusive and the upper bound is exclusive. This ensures that every point maps to exactly one bin.</p>"},{"location":"concepts/coordinate-system.html#key-types","title":"Key Types","text":"<p>MUONITH defines several index types for grid operations:</p> Type Definition Description <code>Ixiy</code> <code>std::array&lt;int, 2&gt;</code> 2D grid index <code>[ix, iy]</code> <code>Ixiyiz</code> <code>std::array&lt;int, 3&gt;</code> 3D grid index <code>[ix, iy, iz]</code> <code>Uqiv</code> <code>int</code> Unique voxel index (linearized) <code>Uqid</code> <code>int</code> Unique detector element index"},{"location":"concepts/coordinate-system.html#spatial-objects","title":"Spatial Objects","text":""},{"location":"concepts/coordinate-system.html#ray3d","title":"Ray3d","text":"<p>A ray is defined by an origin (position) and a direction (unit vector):</p> \\[\\mathbf{r}(t) = \\mathbf{p} + t\\,\\hat{\\mathbf{d}}, \\quad t \\geq 0\\] <p>The direction vector is automatically normalized to unit length upon construction.</p> <p>For a detector element, the direction is built from its angular bin <code>(tx, ty)</code>; see From (tx, ty) to a Ray Direction.</p>"},{"location":"concepts/coordinate-system.html#aabb-axis-aligned-bounding-box","title":"AABB (Axis-Aligned Bounding Box)","text":"<p>Bounding boxes are used to accelerate ray-geometry intersection tests:</p> <ul> <li>AABB2d: 2D bounding box in the xy-plane</li> <li>AABB3d: 3D bounding box</li> </ul> <p>Each grid caches its AABB to avoid per-ray reconstruction overhead.</p> <p></p> <p></p>"},{"location":"concepts/depth-resolution.html","title":"Depth vs. Spatial Resolution","text":""},{"location":"concepts/depth-resolution.html#depth-vs-spatial-resolution","title":"Depth vs. Spatial Resolution","text":"<p>This page explains how MUONITH evaluates the minimum detectable anomaly size as a function of depth \u2014 a key capability for planning muon observation campaigns.</p>"},{"location":"concepts/depth-resolution.html#motivation-resource-constraints-in-muography","title":"Motivation: Resource Constraints in Muography","text":"<p>Muographic observation operates under finite resources. Even with an ideal detector, the cosmic-ray muon flux at a given zenith angle is fixed by nature. The total number of muons collected by a detector element is:</p> \\[N_\\mu = F(\\theta, X) \\times S_{\\text{eff}} \\times \\Delta\\Omega \\times T\\] <p>where \\(F\\) is the penetrating muon flux (a function of zenith angle \\(\\theta\\) and density length \\(X\\)), \\(S_{\\text{eff}}\\) is the effective detector area, \\(\\Delta\\Omega\\) is the solid angle of the angular bin, and \\(T\\) is the observation time.</p> <p>This leads to a fundamental tradeoff:</p> <ul> <li>Widening \\(\\Delta\\Omega\\) (coarser angular bins) increases \\(N_\\mu\\) per bin, improving density precision \u2014 but at the cost of spatial resolution.</li> <li>Narrowing \\(\\Delta\\Omega\\) (finer angular bins) improves spatial resolution \u2014 but reduces \\(N_\\mu\\) per bin, degrading statistical power.</li> </ul> <p>In addition:</p> <ul> <li>Deeper targets require muons to traverse more rock (\\(X\\) increases), so \\(F\\) drops exponentially, further reducing \\(N_\\mu\\).</li> <li>Detector area and observation time are practically limited by cost, logistics, and site access.</li> </ul> <p>Before committing to an expensive field campaign, one must therefore answer:</p> <p>\"Given the available observation resources (detector area, exposure time, number of detectors), what is the smallest density anomaly detectable at a given depth?\"</p> <p>This is exactly what MUONITH's exclusion plot tool (<code>depth_reso</code>) computes.</p>"},{"location":"concepts/depth-resolution.html#physical-concept","title":"Physical Concept","text":""},{"location":"concepts/depth-resolution.html#simplified-model","title":"Simplified Model","text":"<p>Consider a mountain with uniform background density \\(\\rho_0\\) and a single anomaly of diameter \\(l\\) embedded at depth \\(D\\) from the surface along the muon path. The anomaly has density \\(\\rho_0 + \\Delta\\rho\\) (where \\(\\Delta\\rho &lt; 0\\) for a low-density structure such as a cavity or magma conduit).</p> <p>The density length for a ray passing through the anomaly differs from the background by:</p> \\[\\Delta X \\approx \\Delta\\rho \\times l\\] <p>This changes the expected muon count. The question is whether this change is statistically significant given Poisson counting noise.</p>"},{"location":"concepts/depth-resolution.html#statistical-test","title":"Statistical Test","text":"<p>MUONITH compares two muon counts:</p> <ul> <li>\\(N_{\\text{base}}\\): expected count with uniform density \\(\\rho_0\\) (no anomaly)</li> <li>\\(N_{\\text{modi}}\\): expected count with the anomaly (\\(\\rho_0 + \\Delta\\rho\\), size \\(l\\), at depth \\(D\\))</li> </ul> <p>Both are computed from the flux table, given the density length along the ray. A z-test quantifies the significance:</p> \\[z =\\frac{N_{\\text{modi}} - N_{\\text{base}}}{\\sqrt{(\\sqrt{N_{\\text{modi}}})^2 + (\\sqrt{N_{\\text{base}}})^2}}= \\frac{N_{\\text{modi}} - N_{\\text{base}}}{\\sqrt{N_{\\text{modi}} + N_{\\text{base}}}}\\] <p>If the p-value from this test is below a chosen significance level \\(\\alpha\\) (e.g., \\(\\alpha = 0.05\\)), the anomaly is considered statistically detectable under the given observation conditions.</p>"},{"location":"concepts/depth-resolution.html#depth-dependence","title":"Depth Dependence","text":"<p>The relationship between depth and detectability follows directly from Poisson statistics:</p> <ul> <li>Shallow anomaly: short path through rock \u2192 high muon count \\(N_{\\text{base}}\\) \u2192 strong statistical power \u2192 small anomalies detectable</li> <li>Deep anomaly: long path through rock \u2192 low muon count \\(N_{\\text{base}}\\) \u2192 weak statistical power \u2192 only large anomalies detectable</li> </ul>"},{"location":"concepts/depth-resolution.html#exclusion-plots","title":"Exclusion Plots","text":"<p>An exclusion plot (also called a detection limit diagram) maps the boundary between detectable and undetectable anomalies on the (depth, anomaly size) plane.</p>"},{"location":"concepts/depth-resolution.html#how-to-read-an-exclusion-plot","title":"How to Read an Exclusion Plot","text":"<p>Each curve on the plot corresponds to a fixed density contrast \\(\\Delta\\rho\\) and significance level \\(\\alpha\\). Points above (or to the right of) the curve represent (depth, size) combinations that are detectable; points below (or to the left) are undetectable.</p> <p>Key observations:</p> <ul> <li>Curves slope upward to the right: deeper anomalies require larger sizes to be detected.</li> <li>Stronger density contrast (larger \\(|\\Delta\\rho|\\)) shifts curves to the left, meaning smaller anomalies become detectable at the same depth.</li> <li>Better observation resources (larger detector area, longer exposure) also shift curves to the left.</li> </ul>"},{"location":"concepts/depth-resolution.html#heatmap-variant","title":"Heatmap Variant","text":"<p>MUONITH can also produce heatmaps showing \\(\\log_{10}(p)\\) on the (depth, anomaly size) plane, with contour lines at specific significance thresholds (\\(p = 0.3, 0.1, 0.01\\), etc.). The region below a contour represents detectable combinations for that threshold.</p>"},{"location":"concepts/depth-resolution.html#geometry-angular-resolution-and-anomaly-size","title":"Geometry: Angular Resolution and Anomaly Size","text":"<p>The angular size of an anomaly as seen from the detector determines the relevant angular resolution. MUONITH computes this using the geometric relationship between the detector position, the anomaly center, and the anomaly radius:</p> <p></p> <p>Given a detector at position \\(\\mathbf{p}_{\\text{det}}\\) and a cylindrical anomaly centered at \\(\\mathbf{p}_{\\text{obj}}\\), the radius \\(r\\) at which the anomaly subtends a horizontal angle \\(\\phi\\) is:</p> \\[r = |\\vec{L}| \\sin(\\phi)\\] <p>where \\(\\vec{L} = \\mathbf{p}_{\\text{obj}} - \\mathbf{p}_{\\text{det}}\\) is the vector from detector to anomaly center.</p> <p>This calculation (<code>geom_util::calc_cylinder_radius_from_horizontal_angle_size</code>) maps angular bin sizes to physical anomaly sizes at each depth.</p>"},{"location":"concepts/depth-resolution.html#computation-modes","title":"Computation Modes","text":""},{"location":"concepts/depth-resolution.html#ddl-mode-fast-approximation","title":"DDL Mode (Fast Approximation)","text":"<p>The Delta Density-Length Approximation (DDL, also called \"Baumkuchen\") approximates the density length perturbation as:</p> \\[\\Delta X \\approx \\Delta\\rho \\times l\\] <p>where \\(l\\) is the anomaly diameter. This avoids explicit ray-cylinder intersection calculations, making it much faster.</p> <p>The current DDL implementation precomputes path lengths once per detector and uses 2D prefix sums for \\(O(1)\\) rectangular range queries.</p> Property Value Speed Fast Accuracy Approximate Best for Parameter exploration, initial surveys"},{"location":"concepts/depth-resolution.html#cylinder-mode-precise","title":"Cylinder Mode (Precise)","text":"<p>Uses explicit vertical elliptic cylinder geometry and ray tracing to compute exact path lengths through the anomaly. More computationally intensive but more accurate, especially for large anomalies or oblique viewing angles.</p> Property Value Speed Slow Accuracy High Best for Final evaluation, publication-quality results <p></p>"},{"location":"concepts/depth-resolution.html#signal-amplification","title":"Signal Amplification","text":"<p>The <code>signal_noise_amplifiers</code> parameter simulates the effect of different observation resources (detector area \u00d7 exposure time) without re-running the full analysis. A signal amplifier of \\(k\\) is equivalent to multiplying \\(S_{\\text{eff}} \\times T\\) by \\(k\\):</p>"},{"location":"concepts/depth-resolution.html#diminishing-returns","title":"Diminishing Returns","text":"<p>An important finding from exclusion plot analysis is that the benefit of increasing observation resources diminishes. For example, multiplying the effective area \u00d7 time from \u00d71 to \u00d710 may substantially shift the detection boundary, but going from \u00d710 to \u00d7100 yields progressively smaller improvements. This reflects the \\(\\sqrt{N}\\) nature of Poisson statistics \u2014 doubling the muon count only improves the signal-to-noise ratio by \\(\\sqrt{2}\\).</p> <p>The three animated heatmaps below show the same Showa-shinzan det_00 exclusion sweep at signal amplifiers \u00d71, \u00d710, and \u00d7100. The detection boundary (contour lines) advances markedly from \u00d71 to \u00d710, but far less from \u00d710 to \u00d7100 \u2014 the diminishing return described above.</p> \u00d71 \u00d710 \u00d7100 Showa-shinzan det_00, S = 0.2 m\u00b2, T = 120 days, cycling \u0394\u03c1 = \u2212500 \u2026 \u22122000 kg/m\u00b3. <p>This behavior has practical implications for observation planning: users can compare whether adding detectors at different positions, increasing detector area, or extending exposure time gives the better tradeoff for the target and site constraints.</p>"},{"location":"concepts/depth-resolution.html#reference-example-showa-shinzan","title":"Reference example: Showa-shinzan","text":"<p>This reference case documents the detection-limit figures shown throughout this page, produced from a real Showa-shinzan observation campaign. It demonstrates the method on an actual volcano rather than serving as a hands-on sample; to run the workflow yourself, follow the license-free tutorial in the Quick Start.</p>"},{"location":"concepts/depth-resolution.html#configuration","title":"Configuration","text":"<p>The analysis is configured in <code>prm_reso.json5</code>:</p> <pre><code>\"DEPTH_RESOLUTION_SWEEP\": {\n  \"mode\": \"ddl\",\n  \"common\": {\n    \"output_ascii_prefix\": \"depth_res\",\n    \"x_cnt_obj\": 50481,         // Target summit x [m] (EPSG:6679)\n    \"y_cnt_obj\": -161685,       // Target summit y [m] (EPSG:6679)\n    \"base_density\": 2000.0,     // Background density [kg/m\u00b3]\n    \"obj_size_upper_limit\": 400.0,  // Max anomaly diameter [m]\n    \"obj_size_lower_limit\": 1.0,    // Min anomaly diameter [m]\n    \"elev_center_step\": 0.0025,     // Elevation step [rad]\n    \"angle_between_cut_factor\": 1.0,\n    \"sweep_range_factor\": 1.0,\n    \"vec_delta_density\": [-2000, -1500, -1000, -500],  // [kg/m\u00b3]\n    \"stat_alphas\": [0.3, 0.1],\n    \"both_side\": false,\n    \"signal_noise_amplifiers\": [\n      [1.0, 0.0], [10.0, 0.0], [100.0, 0.0], [1000.0, 0.0]\n    ]\n  },\n  \"cylinder\": {},\n  \"ddl\": {\n    \"obj_size_step\": 5.0,   // [m]\n    \"depth_step\": 5.0       // [m]\n  }\n}\n</code></pre> <p>Key parameters:</p> Parameter Description Unit <code>x_cnt_obj</code>, <code>y_cnt_obj</code> Anomaly center position m <code>base_density</code> Background rock density kg/m\u00b3 <code>vec_delta_density</code> Density contrasts to test kg/m\u00b3 <code>obj_size_upper_limit</code> / <code>lower_limit</code> Range of anomaly diameters m <code>elev_center_step</code> Elevation scan step rad <code>obj_size_step</code>, <code>depth_step</code> Parsed and validated, but unused for candidate generation in DDL v1 (real resolution comes from the angle bins via <code>elev_center_step</code>) m <code>stat_alphas</code> Significance levels for detection \u2014 <code>signal_noise_amplifiers</code> Signal amplification factors (simulate longer exposure) \u2014 <p>Supported <code>mode</code> values are:</p> <code>mode</code> Meaning <code>\"ddl\"</code> Fast Delta Density-Length approximation. The alias <code>\"baumkuchen\"</code> is also accepted. <code>\"cylinder\"</code> Explicit vertical cylinder ray-tracing mode."},{"location":"concepts/depth-resolution.html#running","title":"Running","text":"<p>The steps below reproduce the workflow on the license-free <code>tutorial</code> site, which ships with the repository. Run them from the repository root:</p> <pre><code># Generate the tutorial working directory (see the Quickstart for details)\nbash setup_station.sh tutorial\n\n# Run the depth-vs-resolution sweep\ncd work/tutorial/depth-reso/\nbash run_prg.sh true\n</code></pre> <p>This executes:</p> <ol> <li><code>depth_reso.exe</code> \u2014 Sweep over all (depth, anomaly size) combinations for each detector</li> <li><code>plot_detection_limit_heatmap.py</code> \u2014 Generate heatmap visualizations</li> </ol>"},{"location":"concepts/depth-resolution.html#output","title":"Output","text":"<p>The figures below come from a real Showa-shinzan campaign and illustrate what the sweep produces; running the tutorial command above yields the equivalent plots for the synthetic tutorial site.</p> <p>Animated heatmaps for detector 00 cycling through \\(\\Delta\\rho = -500, -1000, -1500, -2000\\) kg/m\u00b3:</p> Baseline (\u00d71) Signal amplified (\u00d710) Showa-shinzan det_00, S = 0.2 m\u00b2, T = 120 days <p>The colored region shows \\(\\log_{10}(p)\\) values; contour lines at \\(p = 0.3\\), \\(0.1\\), and \\(0.01\\) delimit the detection boundary. The diagonal line marks <code>depth = anomaly size</code> for reference. Compare the two panels to see the effect of signal amplification \u2014 and its diminishing returns.</p> <p>The sweep produces CSV files with detection statistics for each detector:</p> Column Description Unit <code>det</code> Detector index \u2014 <code>txL</code>, <code>txU</code>, <code>tyL</code>, <code>tyU</code> Angular-bin bounds detector angle unit <code>depth_anom_top</code>, <code>depth_anom_btm</code> Anomaly depth range m <code>obsize</code> Anomaly diameter m <code>ddens</code> Density difference kg/m\u00b3 <code>signal_base</code> Muon count without anomaly counts <code>signal_modi</code> Muon count with anomaly counts <code>delta_signal</code> <code>signal_modi - signal_base</code> counts <code>z_val</code> z-test statistic \u2014 <code>p_val</code> Statistical p-value \u2014 <code>alpha</code> Significance threshold \u2014 <code>signal_amp</code> Signal amplification factor \u2014 <code>is_signi</code> Detection flag (0/1) \u2014"},{"location":"concepts/depth-resolution.html#optional-base-pl-signal-distribution-dump","title":"Optional: base PL / signal distribution dump","text":"<p>Setting <code>tf_out_det_PL_signal: true</code> inside <code>DEPTH_RESOLUTION_SWEEP</code> makes Trace Path Lengths (Module 4) also emit, per detector, the base (anomaly-free) path-length and signal distributions that underlie the sweep. This is useful for diagnosing why a specific detector's detection boundary shifts or for overlaying anomaly-free vs. anomaly-present distributions in custom plots.</p>"},{"location":"concepts/depth-resolution.html#heatmap-configuration","title":"Heatmap Configuration","text":"<p>Heatmap appearance is configured in <code>heatmap_config.json5</code>:</p> <pre><code>{\n  \"colormap\": \"hot_r\",\n  \"vmin\": -3.0,      // log10(p) = -3 \u2192 p = 0.001\n  \"vmax\": 0.0,       // log10(p) = 0  \u2192 p = 1.0\n  \"contours\": [\n    {\"p_value\": 0.3,  \"color\": \"white\", \"linestyle\": \"-\"},\n    {\"p_value\": 0.1,  \"color\": \"white\", \"linestyle\": \"--\"},\n    {\"p_value\": 0.01, \"color\": \"black\", \"linestyle\": \"-.\"}\n  ],\n  \"show_diagonal\": true,  // depth = size reference line\n  \"size_max\": 300,\n  \"depth_max\": 350,\n  \"depth_axis_mode\": \"detector\"  // \"detector\"/\"top\" use depth_anom_top, \"bottom\" uses depth_anom_btm\n}\n</code></pre> <p><code>depth_axis_mode: \"detector\"</code> uses <code>depth_anom_top</code> like <code>\"top\"</code> and aligns the optional size bars by detector. Blank or gray areas in the heatmap are grid cells without valid CSV samples after interpolation.</p>"},{"location":"concepts/depth-resolution.html#related-pages","title":"Related Pages","text":"<ul> <li>Detector Model \u2014 Detector geometry and angular binning</li> <li>Ray Tracing \u2014 Path length computation through terrain</li> <li>3D Inversion \u2014 MAP-based density reconstruction</li> <li>Quick Start \u2014 First-run workflow using the primitive-shape demos and the <code>tutorial</code> station</li> <li>Parameter Reference \u2014 Complete parameter listing</li> </ul>"},{"location":"concepts/detector-angles.html","title":"From (tx, ty) to a Ray Direction","text":""},{"location":"concepts/detector-angles.html#from-tx-ty-to-a-ray-direction","title":"From (tx, ty) to a Ray Direction","text":"<p>Each detector element observes one angular bin, identified by two bin coordinates, <code>tx</code> (horizontal) and <code>ty</code> (vertical). This page explains how a <code>(tx, ty)</code> bin becomes the element's world-frame look direction, and how that direction feeds the expected signal count.</p> <p>The panel's position and the yaw convention that orient the ray (0\u00b0 = North, 90\u00b0 = East, clockwise from above) are defined in Detector Model \u2014 Detector Orientation.</p> <p></p> <p>1. Angle unit. The meaning of <code>tx</code>/<code>ty</code> depends on <code>angle_unit</code>. In the default Tangent mode they are slopes, <code>tx = tan \u03b8x</code> and <code>ty = tan \u03b8y</code>; in Degree or Radian mode they are the angles themselves. All three are reduced to tangents internally.</p> <p>2. Bin center. A bin's representative direction uses the bin center, not an edge:</p> \\[c_i = \\min + \\left(i + \\tfrac{1}{2}\\right)\\,\\Delta, \\qquad \\Delta = \\frac{\\max - \\min}{n_{\\text{bin}}}\\] <p>Because the bins are uniform in <code>angle_unit</code> space, in Tangent mode they are equal-width in slope \u2014 not in degrees.</p> <p>3. Local direction. The center <code>(tx, ty)</code> is turned into a unit vector in the detector-local frame, whose forward axis is <code>+y</code>. In Tangent mode:</p> \\[\\mathbf{v}_{\\text{local}} = \\frac{(t_x,\\; 1,\\; t_y)}{\\sqrt{1 + t_x^2 + t_y^2}}\\] <p>so the bin coordinates are simply the slopes of the direction about the forward axis:</p> \\[t_x = \\frac{v_x}{v_y}, \\qquad t_y = \\frac{v_z}{v_y}\\] <p>Degree and Radian modes use a spherical form instead, <code>v = (cos \u03b8y \u00b7 sin \u03b8x, cos \u03b8y \u00b7 cos \u03b8x, sin \u03b8y)</code>; inverting it gives</p> \\[\\tan t_x = \\frac{v_x}{v_y}, \\qquad \\tan t_y = \\frac{v_z}{\\sqrt{v_x^2 + v_y^2}}\\] <p>The horizontal coordinate obeys the same equation in both modes. The vertical one does not \u2014 the denominator is \\(v_y\\) in Tangent mode but the horizontal magnitude \\(\\sqrt{v_x^2 + v_y^2}\\) in Degree/Radian mode. The two coincide only on the panel's center column (\\(v_x = 0\\)); off the column, even properly converted values (\\(t_y \\leftrightarrow \\arctan t_y\\)) point to different directions.</p> <p>The two grids are also warped relative to each other: a row of constant \\(t_y\\) is a different surface of directions in each mode,</p> \\[ \\begin{aligned} \\text{Tangent:} \\quad &amp; v_z = t_y\\, v_y &amp;&amp; \\text{(a tilted plane)} \\\\ \\text{Degree/Radian:} \\quad &amp; v_z = \\sin t_y &amp;&amp; \\text{(a cone of constant elevation)} \\end{aligned} \\] <p>so along a Tangent-mode row the physical elevation is not constant:</p> \\[\\mathrm{elev}(t_x, t_y) = \\arctan\\frac{t_y}{\\sqrt{1 + t_x^2}}\\] <p>which drops toward the panel edges \u2014 for \\(t_y = 1\\): 45.0\u00b0 at \\(t_x = 0\\), 35.3\u00b0 at \\(t_x = 1\\). Overlaying the two grids in angle space therefore shows curved iso-elevation lines; this is by design, not an error.</p> <p>The same comparison, including the companion tool muonith-path-view; \\(u\\) denotes each raw number written in muonith-path-view's angle ranges (<code>azimuth_range</code> / <code>elevation_range</code>):</p> Tool / mode Direction from bin values Row of constant vertical value muonith-path-view, every <code>angle_unit</code> spherical, \\(v_z = \\sin\\theta_{\\mathrm{elev}}\\) (<code>tangent</code> only converts the input, \\(\\theta_{\\mathrm{elev}} = \\arctan u\\)) cone of constant elevation MUONITH <code>DetectorPanel</code>, Degree / Radian spherical, \\(v_z = \\sin t_y\\) cone of constant elevation MUONITH <code>DetectorPanel</code>, Tangent slopes, \\(t_y = v_z / v_y\\) tilted plane <p>The figure below visualizes this distortion \u2014 Tangent Grid2d vs Degree Grid2d (upper hemisphere, \\(\\theta_{\\mathrm{elev}} \\geq 0\\)):</p> <p></p> <p>Left: a Degree-mode grid (10\u00b0 steps) seen in tangent coordinates \\((t_x, t_y)\\) \u2014 rows bow upward toward the edges (\\(t_y = \\tan\\theta_{\\mathrm{elev}}/\\cos\\phi\\)). Right: a Tangent-mode grid (0.2 steps) seen in angle coordinates \\((\\phi, \\theta_{\\mathrm{elev}})\\) \u2014 rows sag toward the edges (\\(\\theta_{\\mathrm{elev}} = \\arctan(t_y/\\sqrt{1 + t_x^2})\\)). Solid lines are the mapped grid; dashed gray lines mark the undistorted reference, i.e. each row's value at the center column. Columns stay straight in both directions because the horizontal relation \\(\\tan\\phi = v_x/v_y\\) is shared by both modes.</p> <p>4. Local \u2192 world. The local vector is rotated into world coordinates by the detector's orientation:</p> \\[\\mathbf{v}_{\\text{world}} = R\\,\\mathbf{v}_{\\text{local}}, \\qquad R = R_z(-\\text{yaw})\\,R_y(\\text{pitch})\\,R_x(\\text{roll})\\] <p>using the default <code>LOCAL</code> rotation type. <code>yaw</code> is negated because it is a clockwise bearing (0\u00b0 = North), while the rotation matrix follows the right-hand convention.</p> <p>5. From ray to signal count. The world direction fixes the zenith cosine <code>cos \u03b8z</code>, which selects the penetrating-muon flux \\(F\\). The expected count of one element is then</p> \\[N = F \\cdot S_{\\text{eff}} \\cdot \\Delta\\Omega \\cdot T \\cdot \\varepsilon\\] <p>where \\(\\Delta\\Omega\\) is the exact four-corner solid angle of the bin (not <code>\u0394tx \u00b7 \u0394ty</code>), \\(S_{\\text{eff}}\\) the effective area, \\(T\\) the exposure time, and \\(\\varepsilon\\) the optional detector efficiency. These symbols match the flux notation in Muography and Depth vs. Spatial Resolution.</p>"},{"location":"concepts/detector-model.html","title":"Overview","text":""},{"location":"concepts/detector-model.html#detector-model","title":"Detector Model","text":"<p>MUONITH models muon detectors as a hierarchy of geometric elements that define the angular binning and spatial coverage of each observation.</p>"},{"location":"concepts/detector-model.html#class-hierarchy","title":"Class Hierarchy","text":"<pre><code>DetectorPanelArray\n  \u2514\u2500\u2500 DetectorPanel          \u2190 one physical detector\n        \u2514\u2500\u2500 DetectorElement  \u2190 one angular bin (path-length computation element)\n</code></pre>"},{"location":"concepts/detector-model.html#detectorelement","title":"DetectorElement","text":"<p>A DetectorElement represents the minimum angular element of the path-length computation \u2014 a single directional bin on which path lengths, fluxes, and efficiencies are evaluated.</p> <p>Bin size is a computational choice, not the instrument's angular resolution</p> <p>The angular grid (<code>nbinx</code>, <code>nbiny</code>, <code>txmin</code>\u2013<code>txmax</code>, <code>tymin</code>\u2013<code>tymax</code> in the detector parameter file) only sets how finely the computation samples the terrain. What a bin can actually resolve is limited by three independent factors:</p> <ul> <li>Count statistics \u2014 finer bins collect fewer muons each, so per-bin Poisson noise   grows. The automatic bin grouping merges bins where   the signal is sparse to recover statistical significance.</li> <li>Detector geometry \u2014 the physical channel size and the separation of the detection   planes set the hardware angular response; bins finer than this response do not add   information.</li> <li>Angular resolution effects \u2014 multiple scattering in the rock and in the detector   smears the arrival directions.</li> </ul> <p>Choose the binning fine enough to sample the terrain, and judge what is actually resolvable from the statistics and the detectability analysis (Depth vs. Spatial Resolution) \u2014 not from the bin count.</p> Property Type Description <code>unique_index</code> <code>Uqid</code> (int) Globally unique identifier <code>detid</code> <code>Detid</code> (int) Parent detector panel ID <code>id_in_this_detector</code> <code>Inthis</code> (int) Position within the parent panel <code>ray3d</code> <code>Ray3d</code> Center position and look direction <code>txmin</code>, <code>txmax</code> <code>float</code> Angular bounds (horizontal) <code>tymin</code>, <code>tymax</code> <code>float</code> Angular bounds (vertical) <code>effective_area_m2</code> <code>float</code> Detector collection area [m\u00b2] <code>solid_angle</code> <code>float</code> Angular coverage [sr] <code>exposure_time_sec</code> <code>float</code> Measurement duration [s]"},{"location":"concepts/detector-model.html#computed-quantities","title":"Computed Quantities","text":"<p>After ray tracing, each element stores:</p> Quantity Unit Description PL m Path length through rock DL kg/m\u00b2 Density length (integrated density along path) Penetrating muon flux 1/(m\u00b2 sr s) Expected muon intensity at the given DL Signal / Noise counts Expected muon counts and statistical uncertainty <code>proj_density</code> kg/m\u00b3 Projected (line-averaged) density; set to -1.0 if invalid <p>Efficiency uncertainty in the projected-density error band</p> <p>The 2D projected-density error band (its upper/lower bounds per angular bin) widens when the detector efficiency uncertainty is enabled via <code>tf_eff_cn_diag</code>. The same efficiency-uncertainty variance added to the observation covariance diagonal \\(\\mathbf{C}_N\\) (see Inversion) also feeds the grouped projected-density bounds. With the flag off, the band reflects count statistics only.</p>"},{"location":"concepts/detector-model.html#workflow","title":"Workflow","text":"<p>A typical DetectorElement lifecycle:</p> <ol> <li>Construct \u2014 Set position, angular bounds, and exposure parameters</li> <li>Ray trace \u2014 Compute PL and DL through Grid2dPillar (terrain)</li> <li>Flux lookup \u2014 Determine expected muon flux from flux table using DL and zenith angle</li> <li>Signal estimation \u2014 Calculate expected muon counts: flux \u00d7 area \u00d7 solid angle \u00d7 time</li> <li>Inversion \u2014 Use in the observation equation for density reconstruction</li> </ol>"},{"location":"concepts/detector-model.html#detectorpanel","title":"DetectorPanel","text":"<p>A DetectorPanel represents a complete physical muon detector consisting of a 2D grid of angular elements.</p> Property Description <code>name</code> Detector identifier <code>ray3d</code> Detector center position and orientation <code>v3_det_length</code> Physical dimensions [horizontal, vertical, depth] in meters <code>n_unit</code> Number of detector unit copies <code>days</code> Exposure time in days <code>angle_unit</code> Angular unit for this panel (Tangent / Degree / Radian) <p>Internally, a DetectorPanel contains a Grid2dBinGroup \u2014 a 2D grid that discretizes the detector's angular field of view into bins. Each bin corresponds to one DetectorElement.</p> <pre><code>DetectorPanel\n\u251c\u2500\u2500 Grid2dBinGroup (angular bin grid)\n\u2502     \u251c\u2500\u2500 x-axis: horizontal angle bins\n\u2502     \u2514\u2500\u2500 y-axis: vertical angle bins\n\u2514\u2500\u2500 vec_vec_DetectorElement[iy][ix]\n      \u2514\u2500\u2500 one element per angular bin\n</code></pre> <p>Where the signal is sparse, neighboring bins are adaptively merged into groups, and a bidirectional group \u2194 bin index map keeps the two layers consistent. The grouping algorithm (<code>tf_run_auto_grouping</code>), a worked example, and the index map <code>OneToManyUOBimap</code> have their own page: Bin Grouping.</p>"},{"location":"concepts/detector-model.html#detector-orientation","title":"Detector Orientation","text":"<p>The detector's viewing direction is defined by its Ray3d:</p> <ul> <li>Position: The physical location of the detector [m]</li> <li>Direction: The central look direction (unit vector)</li> <li>Yaw: 0\u00b0 = North (+y), 90\u00b0 = East (+x), measured clockwise when viewed from above</li> </ul>"},{"location":"concepts/detector-model.html#from-tx-ty-to-a-ray-direction","title":"From (tx, ty) to a ray direction","text":"<p>Each element's bin coordinates <code>(tx, ty)</code> are converted to a world-frame ray direction in three steps: the bin center is taken in <code>angle_unit</code> space, turned into a detector-local unit vector (slopes in Tangent mode, spherical angles in Degree/Radian mode), and rotated by the panel's yaw/pitch/roll. The resulting direction fixes the zenith angle for the flux lookup and thus the expected count of the element.</p> <p>The full derivation \u2014 including the Tangent-vs-Degree grid distortion and the comparison with muonith-path-view \u2014 has its own page: From (tx, ty) to a Ray Direction.</p>"},{"location":"concepts/detector-model.html#detectorpanelarray","title":"DetectorPanelArray","text":"<p>A container for multiple detector panels, providing unified access to all DetectorElement instances across all panels.</p> <p>In a multi-view observation (used for 3D density reconstruction), each panel observes the target from a different position and angle. The DetectorPanelArray maintains:</p> <ul> <li>Global detector element indexing (Uqid)</li> <li>Coordinate transformations between detector-local and global frames</li> <li>Aggregated path length and observation data</li> </ul>"},{"location":"concepts/detector-model.html#shell-path-lengths-shellpl","title":"Shell path lengths (<code>ShellPL</code>)","text":"<p>In addition to path lengths through the voxel grid, Trace Path Lengths (Module 4) computes density-independent geometric path lengths through the terrain shells that surround the grid. These are bundled in a <code>ShellPL</code> struct:</p> Field Type Description <code>upper</code> <code>Eigen::VectorXf</code> Path length through terrain above the voxel grid <code>lower</code> <code>Eigen::VectorXf</code> Path length through terrain below the voxel grid <code>lateral</code> <code>Eigen::VectorXf</code> Path length through terrain to the sides of the voxel grid <p>Each vector is laid out flat across all detector elements of a <code>DetectorPanelArray</code>; per-panel slices are obtained with <code>segment(offset, n_ele)</code>. Because the values are density-independent, they are computed once in Trace Path Lengths (Module 4) and reused across every prior-density combination in Compute Prior (Module 5) (<code>density_quad = [prior, shell_upper, shell_lower, shell_lateral]</code>).</p>"},{"location":"concepts/detector-model.html#configuration","title":"Configuration","text":"<p>Detector parameters are specified in a JSON parameter file. Key settings include:</p> Parameter Description Detector position (x, y, z) coordinates in meters Look direction Yaw and elevation angles Angular range Min/max angles for horizontal and vertical axes Angular bin size Resolution of angular discretization Physical size Detector dimensions Exposure time Observation duration Number of units Detector copies (affects effective area) <p>See Parameter Files for the complete parameter file format.</p>"},{"location":"concepts/grid-system.html","title":"Grid System","text":""},{"location":"concepts/grid-system.html#grid-system","title":"Grid System","text":"<p>MUONITH discretizes the study volume into regular grids for ray tracing and density reconstruction. The grid system is built from composable 1D, 2D, and 3D components.</p>"},{"location":"concepts/grid-system.html#grid-hierarchy","title":"Grid Hierarchy","text":"<pre><code>Grid1d          \u2190 1D uniform grid (base building block)\n  \u2502\n  \u251c\u2500\u2500 Grid1dXZ  \u2190 1D grid in XZ plane\n  \u2502\n  \u251c\u2500\u2500 Grid2d    \u2190 2D grid = x-axis \u00d7 y-axis\n  \u2502     \u2502\n  \u2502     \u251c\u2500\u2500 Grid2dPillar   \u2190 2D grid + vertical pillar per cell (terrain/DEM)\n  \u2502     \u251c\u2500\u2500 Grid2dVoxel    \u2190 2D grid + vertical voxel data\n  \u2502     \u251c\u2500\u2500 Grid2dBinGroup \u2190 2D grid for angular bins (detector angles)\n  \u2502     \u2514\u2500\u2500 Grid2dXYZ      \u2190 2D grid + scalar Z per (x,y) bin\n  \u2502\n  \u2514\u2500\u2500 Grid3d    \u2190 3D grid = x-axis \u00d7 y-axis \u00d7 z-axis\n        \u2502\n        \u2514\u2500\u2500 Grid3dVoxel    \u2190 3D grid + Voxel payload per cell\n</code></pre>"},{"location":"concepts/grid-system.html#grid1d","title":"Grid1d","text":"<p>The fundamental building block. A uniform 1D grid defined by:</p> Property Description <code>name</code> Identifier string <code>nbin</code> Number of bins <code>min</code> Lower bound (inclusive) <code>max</code> Upper bound (exclusive) <code>interval</code> Bin width: <code>(max - min) / nbin</code> <p>Key operations:</p> <ul> <li><code>get_index(value)</code> \u2014 Convert a spatial coordinate to a bin index</li> <li><code>get_lower_value(i)</code>, <code>get_upper_value(i)</code>, <code>get_center_value(i)</code> \u2014 Bin boundary and center queries</li> <li><code>get_merged(factor)</code> \u2014 Coarsen grid by merging adjacent bins</li> <li><code>get_split(factor)</code> \u2014 Refine grid by subdividing bins</li> <li><code>cut(new_min, new_max)</code> \u2014 Extract a sub-range</li> </ul>"},{"location":"concepts/grid-system.html#merge-operation","title":"Merge Operation","text":"<p>A validation check ensures <code>interval == (max - min) / nbin</code> within a tolerance of 1.0e-4.</p>"},{"location":"concepts/grid-system.html#grid1dxz","title":"Grid1dXZ","text":"<p>A specialized 1D grid storing scalar z-values on a uniformly-spaced x-axis in the XZ plane. Provides bidirectional linear interpolation:</p> Direction Function Description Forward (x \u2192 z) <code>get_linear_interpolated_z(x)</code> Returns the linearly interpolated z-value at a given x-coordinate Inverse (z \u2192 x) <code>get_linear_interpolated_x(z_target)</code> Returns a vector of x-coordinates where the profile crosses <code>z_target</code> (multiple solutions possible) <p>Use case: Terrain cross-section profiles. A Grid1dXZ can represent the elevation profile along a line, allowing queries such as \"what is the ground elevation at this x?\" (forward) or \"at what x-positions does the terrain reach this elevation?\" (inverse).</p>"},{"location":"concepts/grid-system.html#grid2d","title":"Grid2d","text":"<p>A 2D grid composed of two Grid1d axes (x and y):</p> Property Description <code>nbinx</code>, <code>nbiny</code> Number of bins per axis <code>xmin</code>, <code>xmax</code> x-axis bounds <code>ymin</code>, <code>ymax</code> y-axis bounds <code>x_interval</code>, <code>y_interval</code> Bin widths <p>Grid2d supports 2D ray traversal using the DDA (Digital Differential Analyzer) algorithm via <code>get_hit_boxes_index()</code>, which returns an ordered list of cells intersected by a ray.</p>"},{"location":"concepts/grid-system.html#grid2dpillar-terrain-representation","title":"Grid2dPillar \u2014 Terrain Representation","text":"<p>Each cell <code>(ix, iy)</code> in a Grid2dPillar stores a Pillar:</p> Pillar property Description <code>zmin</code> Bottom elevation [m] <code>zmax</code> Top elevation [m] <code>density</code> Material density [kg/m\u00b3] <p>The z-interval is half-open: \\([z_{\\min},\\; z_{\\max})\\).</p> <p>Use case: Grid2dPillar stores the Digital Elevation Model (DEM). Each column <code>(ix, iy)</code> represents the vertical extent of terrain at that horizontal position. Ray tracing through this grid yields the path length (PL) and density length (DL) for each muon trajectory.</p>"},{"location":"concepts/grid-system.html#grid2dxyz","title":"Grid2dXYZ","text":"<p>A 2D grid that stores a scalar Z value for each <code>(ix, iy)</code> bin. Uses column-major layout and supports bilinear interpolation for querying Z at arbitrary (x, y) coordinates.</p> Property Description <code>nbinx</code>, <code>nbiny</code> Number of bins per axis <code>z_ij</code> Scalar Z value at bin <code>(ix, iy)</code> <p>Interpolation: Given a query point (x, y), Grid2dXYZ identifies the enclosing cell and performs bilinear interpolation using the four surrounding Z values (\\(z_{00}\\), \\(z_{10}\\), \\(z_{01}\\), \\(z_{11}\\)).</p>"},{"location":"concepts/grid-system.html#grid3d","title":"Grid3d","text":"<p>A 3D grid composed of three Grid1d axes (x, y, z):</p> Property Description <code>Ixiyiz</code> 3D index type: <code>std::array&lt;int, 3&gt;</code> = <code>[ix, iy, iz]</code> <code>Uqiv</code> (int) Unique voxel index (linearized) AABB3d Cached bounding box for intersection tests <p>Grid3d supports 3D ray traversal via <code>get_hit_voxel_index()</code>, also using the DDA algorithm.</p>"},{"location":"concepts/grid-system.html#grid3dvoxel-3d-density-grid","title":"Grid3dVoxel \u2014 3D Density Grid","text":"<p>The primary data structure for density reconstruction. Each cell stores a Voxel:</p> Voxel property Type Description <code>tf_exist</code> <code>bool</code> <code>true</code> if material (rock) exists at this location <code>density</code> <code>double</code> Material density [kg/m\u00b3] <p>Per-cell hit information used during ray tracing is stored on <code>Grid3dVoxel</code> itself (keyed by the unique voxel index <code>Uqiv</code>), not on each <code>Voxel</code>:</p> Grid3dVoxel member Type Description <code>vec_n_hit_ele_</code> <code>vector&lt;Nhit&gt;</code> (<code>unsigned short</code>) Number of detector elements whose ray passes through each voxel <code>vec_hit_det_</code> <code>vector&lt;uint64_t&gt;</code> Bitmask of which detectors hit each voxel"},{"location":"concepts/grid-system.html#memory-layout","title":"Memory Layout","text":"<p>Voxels are stored in z-major order:</p> <pre><code>vec_vec_vec_Voxel[iz][iy][ix]\n</code></pre> <p>The outermost index is <code>iz</code> (vertical), the innermost is <code>ix</code> (easting). This layout is consistent with the z-up coordinate convention.</p>"},{"location":"concepts/grid-system.html#reconstruction-sub-volume-reconst_voxels","title":"Reconstruction sub-volume (<code>reconst_voxels</code>)","text":"<p>A subset of the full voxel grid can be selected for reconstruction via the <code>reconst_voxels</code> block in <code>GRID3D_VOXEL_PARAMETERS</code>. Dimensions are specified in meters (not voxel counts):</p> Parameter Type Description <code>tf_aabb</code> bool Enable an AABB-shaped reconstruction region <code>x_aabb_cnt</code>, <code>y_aabb_cnt</code> float Horizontal center of the AABB [m] <code>x_aabb_meters</code>, <code>y_aabb_meters</code> float Half-widths of the AABB in x / y [m] <code>aabb_zmin_mode</code> string How to derive the lower z bound (<code>auto</code> / <code>value</code> / \u2026) <code>aabb_zmin_value</code>, <code>aabb_zmax</code> float Explicit z bounds [m] (used when <code>aabb_zmin_mode = \"value\"</code>) <code>tf_cylinder</code> bool Enable a cylindrical reconstruction region <code>x_cyl_cnt</code>, <code>y_cyl_cnt</code> float Cylinder axis center [m] <code>cylinder_radius_x_meters</code>, <code>cylinder_radius_y_meters</code> float Elliptical-cylinder half-axes [m] <p>For the full parameter reference see Parameter Reference \u2014 GRID3D_VOXEL_PARAMETERS.</p>"},{"location":"concepts/grid-system.html#what-is-reconstructed-vs-what-only-attenuates","title":"What is reconstructed vs. what only attenuates","text":"<p>Only the voxels inside the reconstruction sub-volume are solved as unknowns. Every other part of the mountain a ray crosses still attenuates the muon flux, but its density is held fixed instead of being reconstructed:</p> <ul> <li>Voxels inside the grid but outside the sub-volume keep their prior density and add a fixed density length to each ray.</li> <li>Terrain surrounding the voxel grid is captured as density-independent shell path lengths \u2014 upper, lower, and lateral \u2014 computed once and reused (see Detector Model \u2014 Shell path lengths).</li> </ul> <p></p> <p>A ray from the DetectorPanel passes through the surrounding shells (upper / lateral / lower) and the central 3D reconstruction region. All of them attenuate the ray, but only the reconstruction region is solved; the shells and the out-of-sub-volume voxels contribute to the observed counts at a fixed density.</p>"},{"location":"concepts/grid-system.html#construction-from-dem","title":"Construction from DEM","text":"<p>A Grid3dVoxel is typically built from a Grid2dPillar:</p> <ol> <li>Load DEM data into Grid2dPillar (terrain elevations)</li> <li>Define vertical grid extent and resolution</li> <li>Convert via <code>convert_from_Grid2dPillar()</code> \u2014 voxels inside the terrain get <code>tf_exist = true</code></li> <li>Optionally embed density anomalies (checkerboard, ellipsoid, cylinder) for synthetic tests</li> </ol>"},{"location":"concepts/grid-system.html#resolution-control","title":"Resolution Control","text":"<ul> <li><code>merge(factor)</code> \u2014 Reduce resolution by combining adjacent voxels</li> <li>Useful for managing computational cost: fewer voxels \u2192 smaller matrices \u2192 faster inversion</li> </ul>"},{"location":"concepts/grid-system.html#dda-ray-traversal","title":"DDA Ray Traversal","text":"<p>Both Grid2d and Grid3d use the Digital Differential Analyzer (DDA) algorithm for ray traversal. This algorithm efficiently enumerates all grid cells intersected by a ray, in order from near to far:</p> <pre><code>Ray enters grid \u2192 Step through cells along ray direction \u2192 Collect hit cells\n</code></pre> <p>The DDA operates in grid-index space, stepping one cell at a time along the axis with the smallest parametric increment. This avoids the need for exhaustive intersection testing against all cells.</p> <p>Key outputs: - Ordered list of hit cell indices - Path length through each cell (computed from the ray's parametric interval within the cell)</p> <p>For Grid2d, a zero-allocation visitor pattern is also available via <code>traverse_ray_2d()</code>.</p>"},{"location":"concepts/grid-system.html#grid1d-coordinate-model","title":"Grid1d Coordinate Model","text":"<p>Grid1d internally represents bin edges. After construction, the members satisfy:</p> Member Meaning <code>min</code> Lower edge of bin 0 (inclusive) <code>max</code> Upper edge of bin <code>nbin-1</code> (exclusive) <code>interval</code> <code>(max - min) / nbin</code> Index range <code>[0, nbin)</code> <p>These values are the final coordinates used by all query functions (<code>get_index()</code>, <code>get_lower_value()</code>, etc.) and by the cached AABB. They are canonical: any half-interval shift needed to interpret raw input points is applied exactly once, at construction time, and is already baked into <code>min</code>/<code>max</code>.</p>"},{"location":"concepts/grid-system.html#half-interval-shift-tf_shift_x-tf_shift_y","title":"Half-Interval Shift (tf_shift_x / tf_shift_y)","text":"<p>The JSON5 flags <code>tf_shift_x</code> / <code>tf_shift_y</code> state whether the raw data points of an ASCII xyz file represent bin centers (<code>true</code>) or bin lower edges (<code>false</code>).  They are consumed only by the ASCII construction path; once the Grid1d is built, no shift information is retained or needed.</p> <p>Historical note: Grid1d used to carry a RAM-only <code>tf_half_shift</code> flag, and <code>save()</code>/<code>load()</code> reverse/forward transformed <code>min</code>/<code>max</code> around it.  The two authorities (RAM flag on save, JSON5 on load) could disagree \u2014 e.g. axes produced by <code>get_merged()</code> never inherited the flag \u2014 which caused a half-bin (half-interval) offset after checkpoint resume.  The flag was removed; serialization is now pass-through (see below).</p> <p>When a raw axis with data-point range <code>[raw_min, raw_max]</code> and spacing <code>interval</code> is constructed, the canonical bin edges are computed as:</p> Flag Interpretation <code>min</code> <code>max</code> <code>true</code> Data points are bin centers <code>raw_min - 0.5 * interval</code> <code>raw_max + 0.5 * interval</code> <code>false</code> Data points are bin lower edges <code>raw_min</code> <code>raw_max + interval</code> <p></p> <p>The two cases differ by half an interval on each side.  Choosing the wrong flag shifts the whole grid by half a cell without changing any z value, and no consistency check can detect it: the shifted axis still satisfies <code>max - min == nbin * interval</code>.  Only the metadata of the source data (for a GeoTIFF, <code>AREA_OR_POINT</code>) tells which case applies.</p>"},{"location":"concepts/grid-system.html#data-sources-and-grid1d-minmax","title":"Data Sources and Grid1d min/max","text":"<p>Four situations produce a Grid1d with valid <code>min</code>/<code>max</code>:</p>"},{"location":"concepts/grid-system.html#1-ascii-xyz-raw-point-data","title":"1. ASCII xyz (raw point data)","text":"<p>The file contains flat <code>(x, y, z)</code> triplets as whitespace-separated text.  During <code>set_xy_axis_from_vec_xyz()</code>, the code detects <code>raw_min</code>, <code>raw_max</code>, and <code>interval</code> from the sorted points, then applies the tf_shift adjustment (from JSON5) once to compute the canonical bin edges.</p> <pre><code>read ASCII xyz  --&gt;  sort  --&gt;  detect raw axis  --&gt;  tf_shift (JSON5, once)  --&gt;  Grid1d min/max\n</code></pre>"},{"location":"concepts/grid-system.html#2-g2zbin-compact-grid-binary-version-2","title":"2. g2zbin (compact grid binary, version 2)","text":"<p>The file header contains canonical axis ranges in Grid1d binary format (name + nbin + min + max + interval), written by <code>Grid1d::save()</code> and read back pass-through.  Version 1 files stored raw (pre-shift) ranges and are rejected with an error; regenerate them from their source data with the generating script.</p> <pre><code>read g2zbin v2 header (canonical)  --&gt;  Grid1d min/max   (no shift applied)\n</code></pre>"},{"location":"concepts/grid-system.html#3-json5-configuration","title":"3. JSON5 (configuration)","text":"<p>JSON5 supplies the <code>tf_shift_x</code> / <code>tf_shift_y</code> flags used by the ASCII xyz construction path, and determines which raw data to load.  The flags have no effect on binary (g2zbin / checkpoint) reads.</p>"},{"location":"concepts/grid-system.html#4-checkpoint-grid1dsave-load","title":"4. Checkpoint (Grid1d::save / load)","text":"<p>Checkpoint binaries store canonical coordinates and are read back pass-through, so a save/load roundtrip is bit-exact.  No shift information is exchanged; <code>exemdl::pipeline::load()</code> calls every Grid2dPillar / Grid3dVoxel / build_prior::PriorInfoAll load without tf arguments.  <code>PIPELINE_VERSION</code> 6 marks this format; v5 checkpoints (raw coordinates) are rejected at load.</p>"},{"location":"concepts/grid-system.html#save-load-pass-through-pair","title":"save() / load() Pass-Through Pair","text":"Direction Method Action Write <code>save(ofs)</code> Writes <code>name</code>, <code>nbin</code>, <code>min</code>, <code>max</code>, <code>interval</code> as-is Read <code>load(ifs, tol)</code> Reads the same fields, calls <code>assign()</code> (consistency check only, no transform)"},{"location":"concepts/grid-system.html#file-format-comparison-ascii-xyz-vs-g2zbin","title":"File Format Comparison: ASCII xyz vs g2zbin","text":"Aspect ASCII xyz g2zbin (v2) Encoding Plain text, one <code>x y z</code> row per point Binary, little-endian Header None 16 B fixed + ~200 B Grid2d header Data N rows of <code>x y z</code> text N x P B (z only; P = 8 or 4) Coordinate info Implicit in point values Explicit canonical Grid1d metadata in header tf_shift handling Applied once at construction (JSON5 flags) Not needed (edges already canonical) Read path parse text + sort + axis detection + tf_shift Direct header read (pass-through) Typical size (5M pts) Large (text encoding; depends on precision) 38 MB (f64) / 19 MB (f32)"},{"location":"concepts/inversion.html","title":"3D Inversion","text":""},{"location":"concepts/inversion.html#3d-inversion","title":"3D Inversion","text":"<p>3D density inversion is the process of estimating the internal 3D density distribution of a target volume from observed muon counts. MUONITH implements a Maximum A Posteriori (MAP) estimation approach with Bayesian regularization over a voxelized density model.</p> <p>Full equation reference</p> <p>For the linearized observation-matrix derivation, the complete notation table with input/intermediate/output roles, and the dense-covariance memory limit, see Appendix: 3D Density Reconstruction.</p>"},{"location":"concepts/inversion.html#problem-statement","title":"Problem Statement","text":"<p>Given:</p> <ul> <li>A set of muon count observations \\(\\vec{N}_{\\text{obs}}\\) from one or more detectors</li> <li>A forward model that predicts expected counts \\(\\vec{N}_0\\) based on a prior density \\(\\vec{\\rho}_0\\)</li> <li>An observation matrix \\(\\mathbf{A}\\) relating density changes to count changes (constructed in the next section)</li> </ul> <p>Estimate the density perturbation \\(\\delta\\vec{\\rho}\\) such that:</p> \\[\\vec{\\rho}' = \\vec{\\rho}_0 + \\delta\\vec{\\rho}\\] <p>best explains the observed data while remaining consistent with prior knowledge.</p> <p></p> <p>Example observation geometry at Showa-shinzan volcano. Thirteen detectors are placed around the dome, each viewing the target from a different azimuth. Numbers along the lines indicate the distance from each detector to the summit in meters. This multi-directional coverage is the basis for 3D density reconstruction.</p>"},{"location":"concepts/inversion.html#observation-matrix-construction","title":"Observation Matrix Construction","text":"<p>The observation matrix \\(\\mathbf{A}\\) is built from the geometric path-length matrix \\(\\mathbf{L}\\) computed in Ray Tracing. Each element \\(L_{ij}\\) is the path length of ray \\(i\\) through voxel \\(j\\) [m].</p> <p>Following the derivation in Nagahara et al. (2022) (Eqs. 12\u201318), the density length along ray \\(i\\) is the weighted sum of voxel densities</p> \\[X_i \\;=\\; \\sum_j L_{ij}\\,\\rho_j \\qquad [\\text{kg/m}^2]\\] <p>and the nonlinear map from density length to muon count is given by the flux table</p> \\[N_i \\;=\\; g_i(X_i)\\] <p>Linearizing around a prior density length \\(X_{0,i}\\) yields the muon count sensitivity matrix \\(\\mathbf{A}_N\\):</p> \\[A_{N,ij} \\;=\\; \\frac{\\partial N_i}{\\partial \\rho_j} \\;=\\; L_{ij}\\, \\frac{dN_i}{dX_i}\\!\\left(X_{0,i}\\right)\\] <p>which is Eq. (18) of Nagahara et al. (2022). Each element of \\(\\mathbf{A}_N\\) is the geometric path length \\(L_{ij}\\) multiplied by the derivative of the muon-count response with respect to density length, evaluated at the prior density length \\(X_{0,i}\\) of ray \\(i\\). The derivative factor \\(dN_i/dX_i\\) is computed from the muon flux table and combines:</p> <ul> <li>The derivative of muon flux with respect to density length (\\(dF/dR\\))</li> <li>The cosine of the zenith angle</li> <li>The detector's effective area, solid angle, and exposure time</li> </ul> <p>In the MAP formulas below, \\(\\mathbf{A} \\equiv \\mathbf{A}_N\\).</p> <p>Why linearize at the count level</p> <p>Building \\(\\mathbf{A}_N\\) directly \u2014 rather than treating the path-length relation \\(X_i = \\sum_j L_{ij}\\rho_j\\) as the observation equation and linearizing the flux response around it as a whole-bin operation \u2014 has two practical benefits:</p> <ol> <li>Bin merging is linear in \\(\\mathbf{A}_N\\). If angular bins \\(\\{i\\}\\) are merged into a single super-bin \\(k\\) with \\(N'_k = \\sum_i N_i\\), then \\(A'_{N,kj} = \\sum_i A_{N,ij}\\) \u2014 i.e. merging is just summation, the same as for the observed counts. This is what enables the adaptive bin grouping in Grid2dBinGroup. When a ridge line crosses a coarse bin, the sum is taken only over sub-bins below the ridge.</li> <li>Higher numerical accuracy. The muon flux varies nonlinearly with path and elevation angle, so a single linearization at the coarse-bin level degrades as bins become coarser. The \\(\\mathbf{A}_N\\) form keeps the derivative \\(dN_i/dX_i\\) evaluated at the correct \\(X_{0,i}\\) for each underlying small bin, then sums \u2014 preserving accuracy even for merged bins.</li> </ol>"},{"location":"concepts/inversion.html#map-estimation","title":"MAP Estimation","text":"<p>First, define the posterior covariance and the count residual:</p> \\[\\mathbf{C}_{\\rho'} = \\left(\\mathbf{A}^T \\mathbf{C}_N^{-1} \\mathbf{A} + \\mathbf{C}_\\rho^{-1}\\right)^{-1}\\] \\[\\delta\\vec{N} = \\vec{N}_{\\text{obs}} - \\vec{N}_0\\] <p>The reconstruction formula then becomes:</p> \\[\\delta\\vec{\\rho} = \\mathbf{C}_{\\rho'} \\, \\mathbf{A}^T \\mathbf{C}_N^{-1} \\, \\delta\\vec{N}\\] Symbol Name Dimensions Description \\(\\delta\\vec{\\rho}\\) Density perturbation \\(n_v \\times 1\\) Estimated change from prior density [kg/m\u00b3] \\(\\vec{\\rho}_0\\) Prior density \\(n_v \\times 1\\) Initial density assumption [kg/m\u00b3] \\(\\mathbf{A}\\) Observation matrix \\(n_d \\times n_v\\) \\(\\partial N / \\partial \\rho\\) (muon count sensitivity) \\(\\mathbf{C}_N\\) Observation covariance \\(n_d \\times n_d\\) Uncertainty in muon counts \\(\\mathbf{C}_\\rho\\) Prior density covariance \\(n_v \\times n_v\\) Prior uncertainty and spatial correlation \\(\\mathbf{C}_{\\rho'}\\) Posterior covariance \\(n_v \\times n_v\\) Posterior uncertainty of reconstructed density \\(\\vec{N}_{\\text{obs}}\\) Observed counts \\(n_d \\times 1\\) Measured muon counts \\(\\vec{N}_0\\) Prior expected counts \\(n_d \\times 1\\) Forward-modeled counts at \\(\\vec{\\rho}_0\\) \\(\\delta\\vec{N}\\) Count residual \\(n_d \\times 1\\) Difference between observed and prior counts <p>Where \\(n_d\\) = number of detector elements (observations) and \\(n_v\\) = number of voxels (unknowns). Here \\(n_v\\) counts only the voxels in the reconstruction sub-volume, not the whole mountain; the surrounding terrain attenuates each ray at a fixed density and is not part of the unknowns (see Grid System).</p>"},{"location":"concepts/inversion.html#computation-steps","title":"Computation Steps","text":"<p>The inversion proceeds in the following order:</p> <ol> <li>Compute \\(\\mathbf{C}_N^{-1}\\) \u2014 Inverse of observation covariance</li> <li>Compute \\(\\mathbf{C}_\\rho^{-1}\\) \u2014 Inverse of prior density covariance</li> <li>Compute posterior covariance inverse: \\(\\mathbf{C}_{\\rho'}^{-1} = \\mathbf{A}^T \\mathbf{C}_N^{-1} \\mathbf{A} + \\mathbf{C}_\\rho^{-1}\\)</li> <li>Invert to obtain posterior covariance \\(\\mathbf{C}_{\\rho'}\\) (using LAPACK LU decomposition)</li> <li>Compute count residual: \\(\\delta\\vec{N} = \\vec{N}_{\\text{obs}} - \\vec{N}_0\\)</li> <li>Compute density perturbation: \\(\\delta\\vec{\\rho} = \\mathbf{C}_{\\rho'} \\, \\mathbf{A}^T \\mathbf{C}_N^{-1} \\, \\delta\\vec{N}\\)</li> <li>Reconstruct density: \\(\\vec{\\rho}' = \\vec{\\rho}_0 + \\delta\\vec{\\rho}\\)</li> </ol>"},{"location":"concepts/inversion.html#covariance-matrices","title":"Covariance Matrices","text":""},{"location":"concepts/inversion.html#observation-covariance-mathbfc_n","title":"Observation Covariance (\\(\\mathbf{C}_N\\))","text":"<p>A diagonal matrix representing the statistical uncertainty of muon counts:</p> Variance of element \\(i\\) Description \\(N_i\\) Poisson statistics in linear space \\(N_i + \\sigma_{\\varepsilon,i}^{2}\\) When <code>tf_eff_cn_diag</code> is enabled (efficiency uncertainty added) <p>When <code>tf_eff_cn_diag</code> is on, an efficiency-uncertainty variance \\(\\sigma_{\\varepsilon,i}^{2}\\) is added to each diagonal entry, so the standard deviation of grouped observation \\(i\\) becomes</p> \\[\\sigma_i = \\sqrt{N_i + \\sigma_{\\varepsilon,i}^{2}}.\\] <p>Let \\(\\varepsilon_k\\) be the detector efficiency of angular element \\(k\\) and \\(\\delta\\varepsilon_k = \\tfrac{1}{2}\\left(\\varepsilon_{\\mathrm{upp},k} - \\varepsilon_{\\mathrm{low},k}\\right)\\) its uncertainty (the half-width of the lower/upper band in the efficiency table). With \\(b_k\\) the efficiency-free base count of element \\(k\\), the variance summed over the angular elements \\(k\\) merged into grouped bin \\(i\\) is</p> \\[ \\sigma_{\\varepsilon,i}^{2} = \\begin{cases} \\left(\\displaystyle\\sum_{k \\in i} \\delta\\varepsilon_k \\, b_k\\right)^{2} &amp; \\text{(correlated; default)} \\\\[6pt] \\displaystyle\\sum_{k \\in i} \\left(\\delta\\varepsilon_k \\, b_k\\right)^{2} &amp; \\text{(independent)} \\end{cases} \\] <p>The correlated form is the same as the grouped-relative expression \\((s_iN_i)^2\\) used in the design notes. With central element counts \\(N_k=\\varepsilon_k b_k\\), grouped count \\(N_i=\\sum_{k\\in i}N_k\\), and relative uncertainty \\(s_k=\\delta\\varepsilon_k/\\varepsilon_k\\), the grouped relative uncertainty is \\(s_i=\\left(\\sum_{k\\in i}s_kN_k\\right)/N_i\\). Therefore \\((s_iN_i)^2=\\left(\\sum_{k\\in i}\\delta\\varepsilon_k b_k\\right)^2\\).</p> <p>The independent form is selected by <code>tf_eff_cn_diag_independent</code>. The same flag also widens the 2D projected-density error band (see Detector Model). See Parameter Reference for both flags, and Appendix for the implementation mapping.</p> <p>This remains a diagonal \\(\\mathbf{C}_N\\) model because the efficiency table defines per-element uncertainty widths, but does not define correlations between angular bins, detectors, or calibration-source groups. MUONITH assumes any common efficiency component is closed inside the grouped angular bin, so that component can be folded into the grouped diagonal variance above. Cross-bin or cross-detector common calibration errors would be off-diagonal in principle, but are outside the current fast evaluation path used for layout and relative contrast comparison.</p>"},{"location":"concepts/inversion.html#prior-density-covariance-mathbfc_rho","title":"Prior Density Covariance (\\(\\mathbf{C}_\\rho\\))","text":"<p>Encodes prior knowledge about density uncertainty and spatial correlation:</p> \\[C_{\\rho,ij} = \\sigma_\\rho^2 \\exp\\left(-\\frac{d_{ij}}{l_c}\\right)\\] Parameter Symbol Unit Description <code>sigma_rho</code> \\(\\sigma_\\rho\\) kg/m\u00b3 Standard deviation of density uncertainty <code>corr_length</code> \\(l_c\\) m Spatial correlation length \\(d_{ij}\\) \u2014 m Distance between voxels \\(i\\) and \\(j\\) <ul> <li>Large \\(\\sigma_\\rho\\): Trusts the data more (less regularization)</li> <li>Small \\(\\sigma_\\rho\\): Trusts the prior more (more regularization)</li> <li>Large \\(l_c\\): Smooth reconstruction (nearby voxels correlated)</li> <li>Small \\(l_c\\): Allows sharper features (less smoothing)</li> </ul> <p>Tip</p> <p>The choice of \\(\\sigma_\\rho\\) and \\(l_c\\) significantly affects reconstruction quality. See Parameter Reference for recommended ranges.</p>"},{"location":"concepts/inversion.html#anisotropic-correlation","title":"Anisotropic Correlation","text":"<p>The isotropic form above applies a single correlation length \\(l_c\\) in every direction. MUONITH can instead use direction-dependent correlation lengths, enabled by <code>tf_aniso</code> (or automatically when <code>corr_length_xy</code> or <code>corr_length_z</code> is set to a positive value). Two anisotropic forms are available, selected by <code>aniso_cov_type</code>:</p> <p>Separable (<code>aniso_cov_type: \"separable\"</code>, the default):</p> \\[C_{\\rho,ij} = \\sigma_\\rho^2 \\exp\\left(-\\frac{d^{xy}_{ij}}{l_{xy}} - \\frac{d^{z}_{ij}}{l_{z}}\\right)\\] <p>Ellipsoidal (<code>aniso_cov_type: \"ellipsoidal\"</code>):</p> \\[C_{\\rho,ij} = \\sigma_\\rho^2 \\exp\\left(-\\sqrt{\\left(\\frac{d^{xy}_{ij}}{l_{xy}}\\right)^2 + \\left(\\frac{d^{z}_{ij}}{l_{z}}\\right)^2}\\right)\\] Parameter Symbol Unit Description <code>corr_length_xy</code> \\(l_{xy}\\) m Horizontal (xy-plane) correlation length <code>corr_length_z</code> \\(l_z\\) m Vertical (z) correlation length \\(d^{xy}_{ij}\\) \u2014 m Horizontal distance between voxels \\(i\\) and \\(j\\) \\(d^{z}_{ij}\\) \u2014 m Vertical distance between voxels \\(i\\) and \\(j\\) <p>If <code>corr_length_xy</code> or <code>corr_length_z</code> is left unset (non-positive), it falls back to the isotropic <code>corr_length</code>.</p>"},{"location":"concepts/inversion.html#numerical-implementation","title":"Numerical Implementation","text":"<p>MUONITH uses platform-specific BLAS/LAPACK implementations for matrix operations:</p> Platform BLAS/LAPACK backend Notes macOS Apple Accelerate Default; uses vecLib internally Linux OpenBLAS + LAPACKE Installed via system packages or Nix <p>On macOS, the Accelerate framework does not provide the LAPACKE C interface directly. MUONITH includes thin wrapper functions (<code>blas_backend.hpp</code>) that bridge Fortran-style <code>sgetrf_</code>/<code>sgetri_</code> calls to a LAPACKE-compatible API.</p> Function Precision Description <code>mp_reconst_density_float()</code> float Default; faster, sufficient for most cases <code>mp_reconst_density_double()</code> double More numerically stable for ill-conditioned problems <p>Matrix inversion uses LU decomposition (<code>LAPACKE_sgetrf</code> + <code>LAPACKE_sgetri</code>). All Eigen matrices are stored in column-major format for BLAS/LAPACK compatibility.</p> <p>Cross-platform numerical reproducibility</p> <p>Different BLAS implementations (Accelerate, OpenBLAS) use different internal algorithms for matrix multiplication and decomposition. This produces tiny numerical differences in the reconstruction output \u2014 typically a symmetric relative error of ~10\u207b\u2077 for density values. Both results are equally valid. If bit-exact reproducibility across platforms is required, ensure the same BLAS implementation is used on all machines.</p> <p>Warning</p> <p>The dense covariance matrix scales as \\(O(n_v^2)\\) in memory. The limit <code>MAX_VOXEL_FOR_DENSE_COV = 50000</code> corresponds to approximately 9.3 GB for a float matrix.</p>"},{"location":"concepts/inversion.html#reconstruction-output","title":"Reconstruction Output","text":"<p>The inversion produces a <code>ReconstResult</code> containing:</p> Field Description <code>vecxf_dens_rec</code> Reconstructed density \\(\\vec{\\rho}'\\) [kg/m\u00b3] <code>vecxf_diag_sqrt_cov_dens</code> Diagonal of \\(\\sqrt{\\mathbf{C}_{\\rho'}}\\) (posterior uncertainty) <code>vecxf_delta_dens_prior</code> \\(\\vec{\\rho}' - \\vec{\\rho}_0\\) (change from prior) <code>vecxf_diff_from_real</code> \\(\\vec{\\rho}' - \\vec{\\rho}_{\\text{true}}\\) (error, if true density is known) <code>is_valid</code> Whether the reconstruction succeeded"},{"location":"concepts/inversion.html#reconstruction-example","title":"Reconstruction Example","text":"Input (ground truth) Reconstruction <p>Animated horizontal cross-sections at successive elevations for the Showa-shinzan synthetic test case. Left: true density model with an embedded low-density anomaly. Right: MAP-reconstructed density from 13 detectors. The inversion successfully recovers the location and magnitude of the anomaly.</p>"},{"location":"concepts/inversion.html#pipeline-context","title":"Pipeline Context","text":"<p>In the MUONITH execution pipeline, inversion corresponds to Invert Density (Module 7):</p> <ol> <li>Build Geometry (Module 3) \u2192 Grid3dVoxel</li> <li>Trace Path Lengths (Module 4) \u2192 Density-independent path-length matrices <code>vec_spmat_PL</code> and shell path lengths <code>shell_pl</code></li> <li>Compute Prior (Module 5) \u2192 Prior density-length (<code>DL_prior</code>) and expected counts (density-dependent)</li> <li>Build Observation Matrix (Module 6) \u2192 Observation matrix \\(\\mathbf{A}\\) via the <code>calc_dNdD</code> namespace (historically under <code>pathcalc::matrix</code>, now a top-level namespace)</li> <li>Invert Density (Module 7) \u2192 Density reconstruction</li> <li>Analyze Errors and Output (Module 8) \u2192 Error analysis and output</li> </ol> <p>See Pipeline for the complete execution flow.</p>"},{"location":"concepts/inversion.html#references","title":"References","text":"<ul> <li>S. Nagahara, S. Miyamoto, et al. (2022). Bulletin of Volcanology. DOI:10.1007/s00445-022-01596-y</li> <li>M. Rosas-Carbajal, K. Jourde, J. Marteau, et al. (2017). Geophysical Research Letters. DOI:10.1002/2017GL074285</li> <li>R. Nishiyama, S. Miyamoto, N. Naganawa (2014). DOI:10.5194/gi-3-29-2014</li> <li>A. Tarantola (2005). Inverse Problem Theory and Methods for Model Parameter Estimation. SIAM. DOI:10.1137/1.9780898717921</li> <li>D. E. Groom, N. V. Mokhov, S. I. Striganov (2001). Atomic Data and Nuclear Data Tables, 78(2), 183\u2013356. DOI:10.1006/adnd.2001.0861</li> </ul>"},{"location":"concepts/muography.html","title":"Muography","text":""},{"location":"concepts/muography.html#muography-muon-radiography","title":"Muography (Muon Radiography)","text":""},{"location":"concepts/muography.html#what-are-cosmic-ray-muons","title":"What are cosmic-ray muons?","text":"<p>Cosmic rays from outer space constantly bombard the Earth's atmosphere. When high-energy protons and nuclei collide with atmospheric molecules, they produce showers of secondary particles. Among these, muons are particularly useful for imaging: they are highly penetrating, naturally abundant at ground level (~10,000 per m\u00b2 per minute on a horizontal surface), and travel in approximately straight lines through hundreds of meters of rock.</p>"},{"location":"concepts/muography.html#principle-of-muography","title":"Principle of muography","text":"<p>Muography exploits the fact that the number of muons surviving passage through matter depends on the density length (density \u00d7 path length) along their trajectory. A denser region absorbs more muons, just as bones absorb more X-rays than soft tissue.</p> <p>By placing a muon detector on one side of a geological structure and counting the muons arriving from different directions, one obtains a projected density image -- a 2D map of the average density along each line of sight.</p> \\[ X = \\int \\rho(l)\\, dl \\] \\[ N_\\text{muon}(S, \\Delta \\Omega, T) = T \\times\\int\\limits_{\\Delta\\Omega} F(X,d\\Omega)  S(d\\Omega) d\\Omega \\] <p>where:</p> <ul> <li>\\(X\\) is the density length (integrated density along the muon path)</li> <li>\\(F(X,d\\Omega)\\) is the penetrating muon flux as a function of density length</li> <li>\\(S(d\\Omega)\\) is the effective detector area</li> <li>\\(\\Delta\\Omega\\) is the solid angle of the angular bin</li> <li>\\(T\\) is the exposure time</li> </ul>"},{"location":"concepts/muography.html#from-2d-to-3d-multi-directional-muography","title":"From 2D to 3D: multi-directional muography","text":"<p>A single viewing direction provides only a projected (2D) density image. To reconstruct 3D density distributions, observations from multiple directions are required -- analogous to how CT scans combine many X-ray projections.</p> <p>Recent advances in portable nuclear-emulsion detectors have made it feasible to deploy many detectors around a target structure, enabling multi-directional muography.</p> <p>However, the number of candidate detector positions, viewing angles, and exposure-time configurations grows rapidly. MUONITH is designed to efficiently evaluate and compare these configurations before committing to fieldwork, letting researchers weigh the trade-offs up front rather than prescribing a single \"best\" setup.</p>"},{"location":"concepts/muography.html#key-physical-quantities","title":"Key physical quantities","text":"Quantity Symbol Unit Description Path length \\(l\\) m Geometric length of a muon's path through a voxel Density length \\(\\rho \\cdot l\\) kg/m\u00b2 Integrated density along a path Penetrating muon flux \\(F\\) 1/(m\u00b2 sr s) Number of muons per unit area, solid angle, and time Expected muon count \\(N_\\text{muon}\\) counts Predicted number of detected muons"},{"location":"concepts/muography.html#applications","title":"Applications","text":"<p>Muography has been applied to:</p> <ul> <li>Volcanoes -- Imaging magma conduits, hydrothermal systems, and lava   domes (e.g., Showa-shinzan, Izu-Omuroyama, La Soufri\u00e8re de Guadeloupe)</li> <li>Pyramids -- Discovering hidden chambers (ScanPyramids project)</li> <li>Underground cavities -- Detecting tunnels and voids</li> <li>Nuclear reactors -- Imaging fuel debris (Fukushima Daiichi)</li> <li>Mining and civil engineering -- Mapping density variations in the   subsurface</li> </ul>"},{"location":"concepts/muography.html#further-reading","title":"Further reading","text":"<ul> <li>Tanaka, H.K.M. (2019). \"Muography (muon radiography).\" Nature Reviews Methods Primers.</li> <li>Nagahara, S. et al. (2022). \"Three-dimensional density structure...\" Earth, Planets and Space. DOI:10.1186/s40623-022-01609-0</li> <li>Rosas-Carbajal, M. et al. (2017). \"Three-dimensional density structure of La Soufri\u00e8re de Guadeloupe...\" Journal of Geophysical Research.</li> </ul>"},{"location":"concepts/ray-tracing.html","title":"Ray Tracing","text":""},{"location":"concepts/ray-tracing.html#ray-tracing","title":"Ray Tracing","text":"<p>Ray tracing is the process of computing the path of each muon ray through the terrain and voxel grids. It determines the path length (PL) and density length (DL) for each detector element, which are essential inputs for both forward modeling and density reconstruction.</p>"},{"location":"concepts/ray-tracing.html#overview","title":"Overview","text":"<p>For each DetectorElement, a ray is cast from the detector position along the look direction. MUONITH traces this ray through:</p> <ol> <li>Grid2dPillar (terrain/DEM) \u2014 to compute the total path length and density length through rock</li> <li>Grid3dVoxel (3D voxel grid) \u2014 to determine which voxels the ray passes through and the path length within each voxel</li> </ol> <pre><code>Detector \u2500\u2500\u2500\u2500 Ray \u2500\u2500\u2500\u2500 Terrain (DEM) \u2500\u2500\u2500\u2500 Target volume\n   \u2502                      \u2502                    \u2502\n   \u2502                      \u251c\u2500 PL (path length)  \u2502\n   \u2502                      \u2514\u2500 DL (density\u00d7path) \u2502\n   \u2502                                           \u2502\n   \u2514\u2500\u2500 voxel-by-voxel path lengths \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2192 path length matrix for inversion\n</code></pre>"},{"location":"concepts/ray-tracing.html#physical-quantities","title":"Physical Quantities","text":"Quantity Symbol Unit Definition Path Length PL m Geometric length of the ray through rock Density Length DL kg/m\u00b2 \\(\\int \\rho \\, ds\\) along the ray path <p>The density length determines the muon flux attenuation: a larger DL means more material for muons to penetrate, resulting in fewer transmitted muons.</p>"},{"location":"concepts/ray-tracing.html#dda-algorithm","title":"DDA Algorithm","text":"<p>MUONITH uses the Digital Differential Analyzer (DDA) algorithm for efficient ray-grid traversal (Amanatides &amp; Woo, 1987). Rather than testing every grid cell for intersection, the DDA steps through cells sequentially along the ray direction.</p>"},{"location":"concepts/ray-tracing.html#how-it-works","title":"How it works","text":"<ol> <li>Entry point: Find where the ray enters the grid (ray-AABB intersection test)</li> <li>Initialize: Determine the starting cell and the parametric distances to the next cell boundary along each axis</li> <li>Step: Advance to the next cell boundary \u2014 always choosing the axis with the smallest parametric increment</li> <li>Record: Log the cell index and path length segment</li> <li>Repeat until the ray exits the grid</li> </ol> <p>This approach is \\(O(n)\\) in the number of traversed cells, rather than \\(O(N)\\) in the total number of grid cells.</p>"},{"location":"concepts/ray-tracing.html#2d-traversal","title":"2D Traversal","text":"<p><code>Grid2d::get_hit_boxes_index()</code> traces a ray through a 2D grid and returns the list of hit cells:</p> <p></p> <p></p> <p>A zero-allocation visitor pattern (<code>traverse_ray_2d()</code>) is also available for performance-critical paths.</p>"},{"location":"concepts/ray-tracing.html#3d-traversal","title":"3D Traversal","text":"<p><code>Grid3d::get_hit_voxel_index()</code> extends the DDA to three dimensions, stepping through voxels in the Grid3dVoxel.</p>"},{"location":"concepts/ray-tracing.html#path-length-computation","title":"Path Length Computation","text":"<p>The <code>pathcalc</code> namespace provides the core path calculation functions:</p>"},{"location":"concepts/ray-tracing.html#through-terrain-grid2dpillar","title":"Through Terrain (Grid2dPillar)","text":"<pre><code>pathcalc::g2pil::add_PLDL(element, grid2d_pillar)\n</code></pre> <p>For a single DetectorElement:</p> <ol> <li>Cast ray through Grid2dPillar using 2D DDA</li> <li>For each hit column <code>(ix, iy)</code>:</li> <li>Compute the vertical intersection of the ray with the pillar \\([z_{\\min}, z_{\\max})\\)</li> <li>Calculate the geometric path length through the material</li> <li>Accumulate PL and DL (\\(= \\rho \\times \\text{path length}\\))</li> <li>Store PL and DL in the DetectorElement</li> </ol> <p>The parallelized version <code>mp_add_PLDL()</code> processes all elements in a DetectorPanel or DetectorPanelArray using OpenMP.</p>"},{"location":"concepts/ray-tracing.html#through-voxels-grid3dvoxel","title":"Through Voxels (Grid3dVoxel)","text":"<pre><code>pathcalc::g3vox::add_PLDL(element, grid3d_voxel)\n</code></pre> <p>For the 3D voxel grid, path lengths are computed per-voxel. The resulting data forms the path length matrix \u2014 a key input for density reconstruction.</p>"},{"location":"concepts/ray-tracing.html#path-length-matrix","title":"Path Length Matrix","text":"<p>The path length matrix \\(\\mathbf{L}\\) relates detector elements to voxels:</p> \\[L_{ij} = \\text{path length of ray } i \\text{ through voxel } j \\quad [\\text{m}]\\] <p>Where:</p> <ul> <li>Row \\(i\\) = detector element index (observation)</li> <li>Column \\(j\\) = voxel index (unknown)</li> </ul> <p>This matrix is typically sparse because each ray only passes through a small fraction of the total voxels.</p> <p>Only voxels in the reconstruction sub-volume become unknown columns of \\(\\mathbf{L}\\). Path length through the terrain outside the sub-volume \u2014 the surrounding shells and any voxels held at their prior density \u2014 enters as a fixed, density-independent term rather than as columns to be solved (see Grid System).</p>"},{"location":"concepts/ray-tracing.html#dense-vs-sparse","title":"Dense vs Sparse","text":"Format Function Use case Dense <code>mp_make_mat_PL()</code> Small grids (few voxels) Sparse <code>mp_make_spmat_PL()</code> Large grids (default) <p>Sparse matrix elements below a threshold (<code>reference \u00d7 epsilon</code>) are dropped to zero to improve storage and computation efficiency.</p>"},{"location":"concepts/ray-tracing.html#shell-path-lengths-mp_calc_pl","title":"Shell path lengths (<code>mp_calc_PL</code>)","text":"<p>For density-independent path lengths through terrain outside the voxel grid (upper, lower, and lateral shells), <code>pathcalc::g2pil::mp_calc_PL()</code> is used. Unlike <code>add_PLDL</code>, which co-computes DL (and therefore needs a density), <code>mp_calc_PL</code> returns only PL and is called once in Trace Path Lengths (Module 4) \u2014 the result is cached and reused across every prior-density combination in Compute Prior (Module 5).</p> <p>The function operates on an entire <code>DetectorPanelArray</code> and returns a flat <code>Eigen::VectorXf</code> laid out by detector element index. Callers slice it per panel via <code>segment(offset, n_ele)</code>. This flat layout (introduced when <code>mp_calc_PL</code> was refactored from <code>std::vector&lt;VectorXf&gt;</code> to a single <code>VectorXf</code>) matches the flat storage used by the <code>ShellPL</code> bundle (<code>upper</code>, <code>lower</code>, <code>lateral</code>) that Trace Path Lengths (Module 4) hands off to Compute Prior (Module 5).</p>"},{"location":"concepts/ray-tracing.html#from-path-length-to-observation-equation","title":"From Path Length to Observation Equation","text":"<p>The path length matrix \\(\\mathbf{L}\\) (with elements \\(L_{ij}\\) defined above) is the geometric input to 3D density inversion. In the 3D Inversion chapter, \\(L_{ij}\\) is linearized into the muon count sensitivity matrix \\(\\mathbf{A}_N\\) (dN/d\u03c1) via</p> \\[A_{N,ij} \\;=\\; L_{ij}\\, \\frac{dN_i}{dX_i}\\!\\left(X_{0,i}\\right)\\] <p>which is Eq. (18) of Nagahara et al. (2022). See 3D Inversion \u2014 Observation Matrix Construction for the full derivation, why linearization is done at the count level, and the bin-merging property used by Grid2dBinGroup.</p>"},{"location":"concepts/ray-tracing.html#references","title":"References","text":"<ul> <li>J. Amanatides and A. Woo (1987). \"A Fast Voxel Traversal Algorithm for Ray Tracing.\" Proceedings of EuroGraphics '87, pp. 3\u201310. DOI:10.2312/egtp.19871000</li> <li>S. Nagahara, S. Miyamoto, et al. (2022). \"Three-dimensional density tomography determined from multi-directional muography of the Omuro-yama scoria cone, Higashi-Izu monogenetic volcano field, Japan.\" Bulletin of Volcanology. Eqs. (12)\u2013(18) give the derivation of the count-sensitivity matrix \\(\\mathbf{A}_N\\) used on this page. DOI:10.1007/s00445-022-01596-y</li> </ul>"},{"location":"getting-started/installation.html","title":"Installation","text":""},{"location":"getting-started/installation.html#installation","title":"Installation","text":""},{"location":"getting-started/installation.html#quick-start-just-run-these-commands","title":"Quick start \u2014 just run these commands","text":"<p>Copy-paste the commands for your platform to go from zero to a working build.</p> Ubuntu 24.04WSL2 (Ubuntu 24.04)macOS nativeDocker <pre><code># 1. System packages\nsudo apt update\nsudo apt install -y \\\n  build-essential cmake ninja-build ccache pkg-config git \\\n  libopenblas-openmp-dev liblapacke-dev \\\n  time util-linux coreutils python3-venv \\\n  imagemagick\n\n# 2. Clone and enter the repository\ngit clone git@github.com:seigomiyamoto/muonith.git   # SSH\n# git clone https://github.com/seigomiyamoto/muonith.git  # HTTPS\ncd muonith\n\n# 3. Build\nbash bdebug.sh      # Debug build\nbash brelease.sh    # Release build (optimized)\n\n# 4. Python packages for visualization (optional)\ncurl -LsSf https://astral.sh/uv/install.sh | sh\nsource ~/.local/bin/env  # or restart your shell\nuv sync                  # creates .venv from pyproject.toml + uv.lock\nsource .venv/bin/activate\n</code></pre> <pre><code># 1. Install Docker\nsudo apt update\nsudo apt install -y docker.io git\nsudo usermod -aG docker $USER\n</code></pre> <p>Log out and back in, then:</p> <pre><code># 2. Clone and build the Docker image\ngit clone git@github.com:seigomiyamoto/muonith.git   # SSH\n# git clone https://github.com/seigomiyamoto/muonith.git  # HTTPS\ncd muonith\ndocker build -f docker/Dockerfile -t muonith-test .\n\n# 3. Verify\ndocker run -it muonith-test bash\n# Inside: ls build-debug/exec/*.exe &amp;&amp; ls build-release/exec/*.exe &amp;&amp; exit\n</code></pre> <p>See Docker details for VS Code integration and cleanup.</p> nativeDocker <pre><code># 1. System packages\nsudo apt update\nsudo apt install -y \\\n  build-essential cmake ninja-build ccache pkg-config git \\\n  libopenblas-openmp-dev liblapacke-dev \\\n  time util-linux coreutils python3-venv \\\n  imagemagick\n\n# 2. Clone and enter the repository (use /home/, NOT /mnt/c/)\ncd ~\ngit clone git@github.com:seigomiyamoto/muonith.git   # SSH\n# git clone https://github.com/seigomiyamoto/muonith.git  # HTTPS\ncd muonith\n\n# 3. Build\nbash bdebug.sh      # Debug build\nbash brelease.sh    # Release build (optimized)\n\n# 4. Python packages for visualization (optional)\ncurl -LsSf https://astral.sh/uv/install.sh | sh\nsource ~/.local/bin/env  # or restart your shell\nuv sync                  # creates .venv from pyproject.toml + uv.lock\nsource .venv/bin/activate\n</code></pre> <p>Store files under <code>/home/</code>, not <code>/mnt/c/</code></p> <p>The Windows filesystem (<code>/mnt/c/</code>) is 10\u201315\u00d7 slower. Always work under the native Linux filesystem.</p> <pre><code># 1. Install Docker\nsudo apt update\nsudo apt install -y docker.io git\nsudo usermod -aG docker $USER\n</code></pre> <p>Restart WSL2 (<code>wsl --shutdown</code> in PowerShell), then reopen the terminal:</p> <pre><code># 2. Clone and build the Docker image (use /home/, NOT /mnt/c/)\ncd ~\ngit clone git@github.com:seigomiyamoto/muonith.git   # SSH\n# git clone https://github.com/seigomiyamoto/muonith.git  # HTTPS\ncd muonith\ndocker build -f docker/Dockerfile -t muonith-test .\n\n# 3. Verify\ndocker run -it muonith-test bash\n# Inside: ls build-debug/exec/*.exe &amp;&amp; ls build-release/exec/*.exe &amp;&amp; exit\n</code></pre> <p>See Docker details for VS Code integration and cleanup.</p> Nixnative <p>Install Nix first if you haven't \u2014 see Nix setup guide.</p> <pre><code># 1. Clone and enter the repository\ngit clone git@github.com:seigomiyamoto/muonith.git   # SSH\n# git clone https://github.com/seigomiyamoto/muonith.git  # HTTPS\ncd muonith\n\n# 2. Enter Nix dev shell (provides compiler, cmake, etc.)\nnix develop\n\n# 3. Build\nbash bdebug.sh      # Debug build\nbash brelease.sh    # Release build (optimized)\n\n# 4. Python packages for visualization (optional)\nuv sync                  # creates .venv from pyproject.toml + uv.lock\nsource .venv/bin/activate\n</code></pre> <pre><code># 1. Xcode CLI Tools + system packages\nxcode-select --install\nbrew install cmake ninja ccache pkg-config git gnu-time libomp gifski imagemagick\n\n# 2. Clone and enter the repository\ngit clone git@github.com:seigomiyamoto/muonith.git   # SSH\n# git clone https://github.com/seigomiyamoto/muonith.git  # HTTPS\ncd muonith\n\n# 3. Build\nbash bdebug.sh      # Debug build\nbash brelease.sh    # Release build (optimized)\n\n# 4. Python packages for visualization (optional)\ncurl -LsSf https://astral.sh/uv/install.sh | sh\nsource ~/.local/bin/env  # or restart your shell\n# Or: brew install uv\nuv sync                  # creates .venv from pyproject.toml + uv.lock\nsource .venv/bin/activate\n</code></pre> <p>For detailed explanations of each dependency, read on below.</p> <p>This guide covers building MUONITH from source on the following platforms:</p> Platform Status Linux (Ubuntu 22.04 / 24.04, including WSL2) Fully supported Linux (Docker) Fully supported macOS (Apple Clang + Accelerate) Fully supported macOS (Nix development environment) Fully supported"},{"location":"getting-started/installation.html#choose-your-build-method","title":"Choose your build method","text":"Platform Recommended Alternative Linux / WSL2 Docker (reproducible, no host modification) Install packages via <code>apt</code> (this page) macOS Nix (isolated, reproducible environment) Install via Homebrew + Xcode CLI Tools (this page) <p>macOS native build verified</p> <p>macOS native builds with Apple Clang 17 + Accelerate framework have been verified on macOS 15 Sequoia (Apple Silicon M4 Pro). All 110 build targets pass and runtime tests complete successfully. No Homebrew GCC or OpenBLAS is required.</p> <p>The sections below describe the native (alternative) installation:</p> <ul> <li>Linux / WSL2 users \u2014 Docker is recommended   for reproducibility and zero host modification. The native <code>apt</code> instructions   below are provided for users who prefer direct installation.</li> <li>macOS users \u2014 Nix development environment is recommended.   It provides all dependencies in an isolated shell without installing   Homebrew packages globally. The native instructions below are an alternative.</li> </ul>"},{"location":"getting-started/installation.html#prerequisites","title":"Prerequisites","text":"Dependency Purpose Install C++20 compiler GCC 13+ or Apple Clang 17+ See Compiler below CMake Build system 3.22.2+ Ninja Build generator \u2014 BLAS/LAPACK Linear algebra Platform-dependent (see below) OpenMP Thread parallelization (included with GCC; separate on macOS) ccache (optional) Compiler cache for faster rebuilds \u2014 Doxygen (optional) API documentation generation \u2014 <p>C++ libraries downloaded automatically</p> <p>Eigen3, fmt, spdlog, and nlohmann/json are automatically downloaded and built by CMake via FetchContent. No manual installation is required.</p>"},{"location":"getting-started/installation.html#system-packages","title":"System packages","text":"Linux / WSL2macOS <pre><code>sudo apt update\nsudo apt install -y \\\n  build-essential \\\n  cmake \\\n  ninja-build \\\n  ccache \\\n  pkg-config \\\n  git \\\n  time \\\n  python3-venv \\\n  imagemagick\n</code></pre> <p><code>time</code> provides <code>/usr/bin/time -v</code> (used by station <code>run_prg.sh</code> scripts; the bash builtin <code>time</code> is not equivalent). <code>python3-venv</code> is required by <code>setup_station.sh</code> to auto-create a <code>.venv/</code> for the visualization stack. <code>imagemagick</code> is used by <code>auto_plot.py</code> for PDF figure generation. Animated-GIF output additionally needs <code>gifski</code>, which is not packaged in <code>apt</code> \u2014 install it separately from its official prebuilt static binary (see Visualization binaries below).</p> WSL2-specific tips <p>Performance: Always store your project files under <code>/home/</code> (the native Linux filesystem), not <code>/mnt/c/</code> (the Windows filesystem). The Windows filesystem is 10\u201315\u00d7 slower for build operations.</p> <p>Memory: By default WSL2 uses 50 % of host RAM. If you run into memory issues during the build, create or edit <code>%USERPROFILE%\\.wslconfig</code> on the Windows side:</p> <pre><code>[wsl2]\nmemory=16GB\n</code></pre> <p>Then restart WSL2 with <code>wsl --shutdown</code>.</p> <p>Install Homebrew if not already present, then:</p> <pre><code>brew install cmake ninja ccache pkg-config git gnu-time gifski imagemagick\n</code></pre> <p><code>gnu-time</code> provides the <code>gtime</code> command that <code>run_prg.sh</code> uses on macOS to run the solver with time/memory measurement.</p> <p><code>gifski</code> and <code>imagemagick</code> are optional \u2014 needed only for animated-GIF and PDF figure generation in <code>auto_plot.py</code>. See Visualization binaries below.</p>"},{"location":"getting-started/installation.html#compiler","title":"Compiler","text":"<p>MUONITH requires a C++20-capable compiler. The build script <code>build.sh</code> automatically selects the compiler based on the environment:</p> Environment Compiler Selection logic macOS (native) Homebrew GCC 14 \u2192 13 \u2192 Apple Clang First available is used macOS (Nix shell) <code>cc</code> / <code>c++</code> (Nix wrapper) Apple Clang via stdenv Linux (native) <code>gcc</code> / <code>g++</code> System default Linux (Nix shell) <code>cc</code> / <code>c++</code> (Nix wrapper) GCC via stdenv <p>Apple Clang on macOS</p> <p>Apple Clang 17+ (Xcode Command Line Tools) fully supports the C++20 features used by MUONITH. If Homebrew GCC is not installed, <code>build.sh</code> falls back to Apple Clang automatically. OpenMP requires <code>libomp</code> (see OpenMP).</p> Linux / WSL2 (Ubuntu 24.04)Linux / WSL2 (Ubuntu 22.04)macOS <p>G++ 13 is included by default with Ubuntu 24.04. No additional steps needed.</p> <p>Verify:</p> <pre><code>g++ --version\n# g++ (Ubuntu 13.3.0-...) 13.3.0\n</code></pre> <pre><code>sudo add-apt-repository ppa:ubuntu-toolchain-r/test\nsudo apt update\nsudo apt install g++-13 gcc-13\n</code></pre> <p>Optionally set as default:</p> <pre><code>sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-13 60\nsudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-13 60\n</code></pre> <p>Verify:</p> <pre><code>g++ --version\n# g++ (Ubuntu 13.x.x) 13.x.x\n</code></pre> <p>Apple Clang (included with Xcode Command Line Tools) is the default compiler on macOS. Install the tools if not already present:</p> <pre><code>xcode-select --install\n</code></pre> <p>Verify:</p> <pre><code>clang++ --version\n# Apple clang version 17.0.0 (...)\n</code></pre> <p>Apple Clang 17+ fully supports the C++20 features used by MUONITH. OpenMP is not bundled with Apple Clang \u2014 install <code>libomp</code> separately (see OpenMP).</p> <p>Optional: Homebrew GCC</p> <p>If Homebrew GCC 14 or 13 is installed, <code>build.sh</code> will prefer it over Apple Clang. GCC bundles OpenMP, so no separate <code>libomp</code> is needed. To install: <code>brew install gcc@14</code>.</p>"},{"location":"getting-started/installation.html#blaslapack","title":"BLAS/LAPACK","text":"<p>MUONITH uses BLAS/LAPACK for matrix operations in the inversion step. The backend is selected automatically by CMake based on the platform:</p> Platform BLAS/LAPACK backend Notes macOS Apple Accelerate (default) Built into macOS, no installation needed macOS (optional) Homebrew OpenBLAS Requires <code>CMAKE_PREFIX_PATH</code> Linux OpenBLAS + LAPACK Manual installation required Linux / WSL2macOS <pre><code>sudo apt install -y libopenblas-openmp-dev liblapacke-dev\n</code></pre> <p>Verify:</p> <pre><code>dpkg -l | grep libopenblas\n</code></pre> <p>Apple Accelerate is built into macOS \u2014 no installation is required. CMake automatically detects and links the Accelerate framework.</p> <p>Nix users</p> <p>In the Nix shell, macOS also uses Apple Accelerate (via the system framework). No additional Nix packages are needed for BLAS/LAPACK.</p> <p>Numerical differences between backends</p> <p>Accelerate and OpenBLAS use different internal algorithms. This can produce tiny numerical differences (symmetric relative error ~10\u207b\u2077) in density reconstruction results. Both are correct \u2014 see Inversion: Numerical Implementation for details.</p>"},{"location":"getting-started/installation.html#openmp","title":"OpenMP","text":"Linux / WSL2macOS <p>OpenMP is included with GCC \u2014 no additional installation is required.</p> <p>If compiling with Apple Clang (the default), install <code>libomp</code>:</p> <pre><code>brew install libomp\n</code></pre> <p><code>build.sh</code> automatically detects Homebrew libomp and passes <code>-DOpenMP_ROOT</code> to CMake.</p> <p>If compiling with Homebrew GCC, OpenMP is included \u2014 no additional package is needed.</p>"},{"location":"getting-started/installation.html#doxygen-optional","title":"Doxygen (optional)","text":"<p>Only needed if you want to generate API reference documentation.</p> Linux / WSL2macOS <pre><code>sudo apt install doxygen graphviz\n</code></pre> <pre><code>brew install doxygen graphviz\n</code></pre>"},{"location":"getting-started/installation.html#python-packages-optional","title":"Python packages (optional)","text":"<p>Python 3 is required for visualization and post-processing scripts (<code>auto_plot.py</code>, <code>hist2d.py</code>, <code>plot_g2bg.py</code>, etc.). These are not needed to build or run MUONITH itself.</p> <p>Recommended: use <code>uv</code> for virtual environment management</p> <p><code>uv</code> is a fast Python package manager written in Rust. It creates isolated virtual environments without polluting your system Python.</p>"},{"location":"getting-started/installation.html#install-uv","title":"Install uv","text":"Linux / WSL2macOS <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre> <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre> <p>Or via Homebrew:</p> <pre><code>brew install uv\n</code></pre>"},{"location":"getting-started/installation.html#create-a-virtual-environment-and-install-packages","title":"Create a virtual environment and install packages","text":"<p>The package list is maintained in <code>pyproject.toml</code> at the repository root, and exact versions are pinned in <code>uv.lock</code>. A single command reproduces the environment:</p> <pre><code>uv sync                  # creates .venv from pyproject.toml + uv.lock\nsource .venv/bin/activate\n</code></pre> <p>macOS</p> <p>Pre-built binary packages for <code>rasterio</code> and <code>pyproj</code> require macOS 14 (Sonoma) or newer. On older macOS versions <code>uv sync</code> falls back to building them from source, which needs additional system libraries.</p> Alternative: traditional pip (not recommended) <p>If you prefer not to install <code>uv</code>, you can use the system <code>pip</code> directly. Note that installing packages globally with <code>pip</code> is deprecated in recent Python versions and may require <code>--break-system-packages</code> on Ubuntu 24.04+.</p> Linux / WSL2macOS <pre><code>sudo apt install -y python3 python3-pip python3-venv\npython3 -m venv .venv\nsource .venv/bin/activate\npip install numpy matplotlib pandas scipy requests json5 pyproj rasterio pillow\n</code></pre> <pre><code>python3 -m venv .venv\nsource .venv/bin/activate\npip install numpy matplotlib pandas scipy requests json5 pyproj rasterio pillow\n</code></pre>"},{"location":"getting-started/installation.html#documentation-development-mkdocs","title":"Documentation development (mkdocs)","text":"<p>To build or preview this documentation site locally:</p> <pre><code>uv sync --group docs     # adds the mkdocs toolchain to .venv\nsource .venv/bin/activate\nmkdocs serve             # Preview at http://127.0.0.1:8001\n</code></pre> <p>Additional packages for GIS tools</p> <p>The auxiliary tools <code>muonith-path-view</code> and <code>muonith-gsi-dem</code> require extra packages. See muonith-path-view and muonith-gsi-dem for details.</p>"},{"location":"getting-started/installation.html#visualization-binaries-optional","title":"Visualization binaries (optional)","text":"<p>The post-processing visualization driver <code>auto_plot.py</code> (invoked at the end of <code>run_prg.sh</code>) shells out to two external binaries:</p> Binary Purpose Used by <code>gifski</code> Combine PNG frames into animated GIFs of cross-section sweeps <code>scripts/plot_Grid3dVoxel.py:638-740</code> (<code>run_gifski_cross</code>) <code>convert</code> (ImageMagick) Combine PNG frames into multi-page PDFs of cross-section sweeps <code>scripts/plot_Grid3dVoxel.py:727-744</code> (<code>run_convert_pdf_cross</code>) <p>The PNG figures themselves are always generated by matplotlib and do not require these binaries. Only the animated GIF and PDF outputs require them.</p> Linux / WSL2macOS <p><code>imagemagick</code> (the <code>convert</code> command) is available via <code>apt</code>, but <code>gifski</code> is not packaged in Ubuntu's <code>apt</code>. Install <code>imagemagick</code> from <code>apt</code>, then install <code>gifski</code> from its official prebuilt static Linux binary \u2014 no Rust toolchain required:</p> <pre><code>sudo apt install -y imagemagick curl xz-utils\nmkdir -p ~/.local/bin\ncurl -sL -o /tmp/gifski.tar.xz \\\n  https://github.com/ImageOptim/gifski/releases/download/1.34.0/gifski-1.34.0.tar.xz\ntar -xf /tmp/gifski.tar.xz -C /tmp linux/gifski\ninstall -m 755 /tmp/linux/gifski ~/.local/bin/gifski\n</code></pre> <p>The archive bundles a fully static binary (<code>linux/gifski</code>) with no shared-library dependencies; it runs unchanged on Ubuntu 20.04, 22.04, and 24.04. The download URL is version-pinned \u2014 check the gifski releases page for a newer version and adjust the URL if needed. Ensure <code>~/.local/bin</code> is on your <code>PATH</code> (open a new shell, or <code>export PATH=\"$HOME/.local/bin:$PATH\"</code>).</p> <p>Verify:</p> <pre><code>which gifski &amp;&amp; gifski --version\nwhich convert &amp;&amp; convert --version | head -1\n</code></pre> <pre><code>brew install gifski imagemagick\n</code></pre> <p>Verify:</p> <pre><code>which gifski &amp;&amp; gifski --version\nwhich magick &amp;&amp; magick --version | head -1\n</code></pre> <p>ImageMagick 7 on macOS</p> <p>Homebrew installs ImageMagick 7, which provides the <code>magick</code> command. A compatibility symlink for <code>convert</code> is also created by Homebrew, so <code>plot_Grid3dVoxel.py</code>'s direct <code>convert</code> invocation still works.</p> Without these binaries <p><code>auto_plot.py</code> will still produce all PNG figures, but the final GIF/PDF combination step will fail with:</p> <pre><code>FileNotFoundError: [Errno 2] No such file or directory: 'gifski'\n</code></pre> <p>(and similarly for <code>convert</code>). To recover after installing the binaries, re-run only the visualization step from any station directory:</p> <pre><code>cd work/&lt;station_name&gt;/swp001\nbash run_prg.sh false 42   # RUN_EXE=false skips the C++ compute step\n</code></pre> Ubuntu ImageMagick PDF policy <p>On some Ubuntu installations, ImageMagick's default <code>/etc/ImageMagick-6/policy.xml</code> blocks PDF output with:</p> <pre><code>&lt;policy domain=\"coder\" rights=\"none\" pattern=\"PDF\" /&gt;\n</code></pre> <p>If PDF generation fails with a policy error, edit that line to <code>rights=\"read|write\"</code> (requires <code>sudo</code>).</p>"},{"location":"getting-started/installation.html#building-muonith","title":"Building MUONITH","text":"<p>After installing all dependencies:</p> Linux / WSL2macOS <pre><code># Debug build\nbash bdebug.sh\n\n# Release build\nbash brelease.sh\n\n# Clean debug build (removes build directory first)\nbash ccdebug.sh\n</code></pre> <p>The build scripts fully support macOS. <code>build.sh</code> detects CPU count via <code>sysctl</code> and selects the appropriate compiler automatically.</p> <pre><code># Debug build\nbash bdebug.sh\n\n# Release build\nbash brelease.sh\n</code></pre> <p>Verified on</p> <p>macOS 15 Sequoia, Apple Silicon (M4 Pro), Apple Clang 17 + Accelerate. All 110 build targets pass. See also the Nix development environment as an alternative isolated setup.</p>"},{"location":"getting-started/installation.html#alternative-build-with-docker-linux-wsl2","title":"Alternative: Build with Docker (Linux / WSL2)","text":"<p>Instead of installing all dependencies on your host, you can use Docker to build MUONITH in an isolated Ubuntu container. This is useful for:</p> <ul> <li>Quick verification \u2014 test that the build works without modifying your system</li> <li>Reproducibility \u2014 identical environment regardless of host configuration</li> </ul> <p>macOS Docker builds not supported</p> <p>Docker on macOS (Apple Silicon) requires Rosetta 2 emulation for the <code>linux/amd64</code> image, and ARM-native builds have unresolved OpenBLAS version compatibility issues. macOS users should use the native build or Nix development environment instead.</p>"},{"location":"getting-started/installation.html#install-docker","title":"Install Docker","text":"<p>Docker address pools may conflict with your LAN or VPN</p> <p>In some environments, Docker's default private address pools (for example <code>172.17.0.0/16</code>) may overlap with your LAN or VPN routes and cause connectivity issues.</p> <p>If you encounter routing failures after starting Docker, reconfigure Docker to use a non-overlapping subnet:</p> <ul> <li>Linux-native Docker \u2014 edit <code>/etc/docker/daemon.json</code> and set   <code>bip</code> (default bridge) or <code>default-address-pools</code> (all auto-created   networks).</li> <li>Docker Desktop (Windows / WSL2) \u2014 open   Settings \u2192 Docker Engine and add the same keys to the JSON config.</li> </ul> <p>Example (<code>daemon.json</code>):</p> <pre><code>{\n  \"default-address-pools\": [\n    {\"base\": \"192.168.200.0/24\", \"size\": 24}\n  ]\n}\n</code></pre> <p>Choose an address range that does not overlap with any subnet on your LAN or VPN.</p> Linux-nativeWSL2 <pre><code>sudo apt update\nsudo apt install -y docker.io\nsudo usermod -aG docker $USER\n</code></pre> <p>Log out and back in for the group change to take effect.</p> <pre><code>sudo apt update\nsudo apt install -y docker.io\nsudo usermod -aG docker $USER\n</code></pre> <p>Restart WSL2 for the group change to take effect:</p> <pre><code># In Windows PowerShell\nwsl --shutdown\n</code></pre> <p>Then reopen your WSL2 terminal.</p>"},{"location":"getting-started/installation.html#build-the-image","title":"Build the image","text":"<pre><code>cd ~/prg/muonith\ndocker build -f docker/Dockerfile -t muonith-test .\n\n# Ubuntu 22.04 variant (default is 24.04):\n# docker build -f docker/Dockerfile --build-arg UBUNTU_VERSION=22.04 -t muonith-test .\n</code></pre> <p>First build takes 20\u201330 minutes (FetchContent downloads + compilation). Subsequent builds use Docker layer cache and are much faster.</p>"},{"location":"getting-started/installation.html#verify-the-build","title":"Verify the build","text":"<pre><code># Enter the container interactively\ndocker run -it muonith-test bash\n</code></pre> <p>Inside the container:</p> <pre><code>/workspace# ls build-debug/exec/*.exe\n/workspace# ls build-release/exec/*.exe\n/workspace# exit\n</code></pre>"},{"location":"getting-started/installation.html#browse-files-with-vs-code","title":"Browse files with VS Code","text":"<p>You can attach VS Code to the running container for a full IDE experience:</p> <ol> <li>Install the Dev Containers extension (<code>ms-vscode-remote.remote-containers</code>)</li> <li>Start the container in the background:    <pre><code>docker run -it -d --name muonith muonith-test bash\n</code></pre></li> <li>In VS Code: <code>Ctrl+Shift+P</code> \u2192 Dev Containers: Attach to Running Container \u2192 select <code>muonith</code></li> <li>Open folder <code>/workspace</code> \u2014 you can browse source code and build output</li> </ol> <p>When finished:</p> <pre><code>docker stop muonith\ndocker rm muonith\n</code></pre>"},{"location":"getting-started/installation.html#clean-up","title":"Clean up","text":"<pre><code># Remove the image\ndocker rmi muonith-test\n\n# Remove stopped containers\ndocker container prune\n\n# Remove unused images\ndocker image prune\n</code></pre>"},{"location":"getting-started/installation.html#generating-api-documentation","title":"Generating API documentation","text":"<pre><code>bash rebuild-api-docs.sh\n</code></pre> <p>HTML output is generated in <code>apidocs/html/</code>.</p>"},{"location":"getting-started/nix-setup.html","title":"Nix Setup (macOS, optional)","text":""},{"location":"getting-started/nix-setup.html#nix-development-environment-macos-optional","title":"Nix Development Environment (macOS, Optional)","text":"<p>This guide covers setting up the MUONITH development environment using Nix, a purely functional package manager that provides reproducible, declarative development environments.</p> <p>Nix is optional</p> <p>MUONITH builds natively on macOS (Apple Clang + Accelerate) and Linux (GCC + OpenBLAS) without Nix. See Installation for the standard build instructions. Nix is an alternative for users who prefer an isolated environment that does not install packages globally.</p> <p>Why choose Nix?</p> <p>With Nix you get the exact same toolchain (compiler, CMake, Python packages, etc.) on every machine -- no manual dependency installation, no version mismatches, and no \"works on my machine\" surprises. A single <code>nix develop</code> command sets up everything without modifying your system.</p>"},{"location":"getting-started/nix-setup.html#prerequisites","title":"Prerequisites","text":""},{"location":"getting-started/nix-setup.html#install-nix","title":"Install Nix","text":"<p>The recommended installer is the Determinate Systems installer, which enables flakes by default and provides a clean uninstall path.</p> <pre><code>curl --proto '=https' --tlsv1.2 -sSf -L \\\n  https://install.determinate.systems/nix | sh -s -- install\n</code></pre> <p>After installation, open a new terminal (or <code>source</code> your shell profile) and verify:</p> <pre><code>nix --version\n</code></pre> <p>Flakes must be enabled</p> <p>The Determinate Systems installer enables flakes automatically. If you installed Nix via the official installer instead, you need to enable flakes manually. Add the following to <code>~/.config/nix/nix.conf</code>:</p> <pre><code>experimental-features = nix-command flakes\n</code></pre>"},{"location":"getting-started/nix-setup.html#getting-started","title":"Getting started","text":""},{"location":"getting-started/nix-setup.html#enter-the-development-shell","title":"Enter the development shell","text":"<pre><code>cd ~/prg/muonith       # Replace with the actual path to your clone\nnix develop\n</code></pre> <p>The first invocation downloads and builds all dependencies (this may take several minutes). Subsequent invocations use the Nix store cache and are near-instant.</p>"},{"location":"getting-started/nix-setup.html#verify-the-environment","title":"Verify the environment","text":"<p>Once inside the shell, confirm that the key tools are available:</p> <pre><code>cmake --version\nninja --version\nuv --version\necho $IN_NIX_SHELL   # Should print \"1\"\n</code></pre> <p>Python packages are not part of the flake; set them up once with:</p> <pre><code>uv sync                  # creates .venv from pyproject.toml + uv.lock\n.venv/bin/python -c \"import numpy; print(numpy.__version__)\"\n</code></pre>"},{"location":"getting-started/nix-setup.html#platform-differences","title":"Platform differences","text":"<p>The <code>flake.nix</code> adapts to the host platform automatically. The key differences are:</p> macOS Linux Compiler Clang (via Nix stdenv) GCC BLAS / LAPACK Apple Accelerate OpenBLAS + LAPACK OpenMP <code>llvmPackages.openmp</code> Included with GCC Nix wrappers <code>cc</code> = Clang, <code>c++</code> = Clang++ <code>cc</code> = GCC, <code>c++</code> = G++ <p>No Homebrew GCC required</p> <p>Unlike the manual installation path, the Nix environment does not require Homebrew GCC on macOS. Clang from Nix stdenv provides full C++20 support and OpenMP is supplied as a separate <code>llvmPackages.openmp</code> package.</p>"},{"location":"getting-started/nix-setup.html#building-muonith","title":"Building MUONITH","text":"<p>Inside the Nix shell, use the standard build scripts:</p> <pre><code># Debug build\nbash build.sh Debug\n\n# Or use the wrapper script\nbash bdebug.sh\n</code></pre> <pre><code># Release build\nbash build.sh Release\n\n# Or use the wrapper script\nbash brelease.sh\n</code></pre> <p>The build scripts detect the <code>IN_NIX_SHELL</code> environment variable and select the appropriate compiler automatically -- no manual <code>CMAKE_C_COMPILER</code> or <code>CMAKE_CXX_COMPILER</code> flags needed.</p>"},{"location":"getting-started/nix-setup.html#the-in_nix_shell-environment-variable","title":"The <code>IN_NIX_SHELL</code> environment variable","text":"<p>The <code>flake.nix</code> shellHook exports <code>IN_NIX_SHELL=1</code> when you enter the development shell. The <code>build.sh</code> script uses this variable to decide compiler selection:</p> Condition <code>CC</code> <code>CXX</code> <code>IN_NIX_SHELL</code> is set <code>cc</code> (Nix wrapper) <code>c++</code> (Nix wrapper) macOS without Nix <code>gcc-14</code> or <code>gcc-13</code> (Homebrew) <code>g++-14</code> or <code>g++-13</code> Linux without Nix <code>gcc</code> <code>g++</code> <p>This means the same <code>build.sh</code> works identically inside and outside of Nix, with no manual configuration.</p>"},{"location":"getting-started/nix-setup.html#what-the-flake-provides","title":"What the flake provides","text":"<p>For reference, the complete set of packages available in the Nix development shell:</p> Category Packages Build tools CMake, Ninja, ccache, pkg-config, git, bash Compiler GCC (Linux only; macOS uses Clang via stdenv) Libraries OpenBLAS + LAPACK (Linux), llvmPackages.openmp (macOS) Documentation Doxygen, Graphviz Media gifski (GIF encoding), ImageMagick (<code>convert</code>, for PDF/GIF figure output) Python Python 3 interpreter and <code>uv</code> (no packages) <p>Python packages are intentionally not provided by the flake. They are managed by <code>uv sync</code> from <code>pyproject.toml</code> + <code>uv.lock</code> at the repository root, so every platform (with or without Nix) installs the same pinned set. The shellHook sets <code>UV_PYTHON_DOWNLOADS=never</code> so that <code>uv</code> always uses the Nix-provided interpreter instead of downloading its own.</p>"},{"location":"getting-started/nix-setup.html#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/nix-setup.html#flakes-not-enabled","title":"Flakes not enabled","text":"<p>If you see an error like:</p> <pre><code>error: experimental Nix feature 'flakes' is disabled\n</code></pre> <p>Add the following to <code>~/.config/nix/nix.conf</code> (create the file if it does not exist):</p> <pre><code>experimental-features = nix-command flakes\n</code></pre> <p>Then restart your terminal.</p> <p>Tip</p> <p>If you used the Determinate Systems installer, flakes are enabled by default and you should not see this error.</p>"},{"location":"getting-started/nix-setup.html#slow-first-nix-develop","title":"Slow first <code>nix develop</code>","text":"<p>The first invocation of <code>nix develop</code> downloads and builds all dependencies from source or binary cache. This can take several minutes depending on your network speed.</p> <p>Subsequent invocations are fast because Nix caches everything in the local store (<code>/nix/store/</code>).</p> <p>Binary cache</p> <p>Nix automatically uses the official binary cache (<code>cache.nixos.org</code>). Most packages are pre-built and download as binaries. If a specific package is not cached (e.g., a pinned Python package), Nix builds it locally.</p>"},{"location":"getting-started/nix-setup.html#ccache-not-finding-the-cache","title":"<code>ccache</code> not finding the cache","text":"<p>Inside the Nix shell, <code>ccache</code> uses its default cache directory (<code>~/.cache/ccache</code> or <code>~/.ccache</code>). If you have a non-Nix ccache installation with a different cache directory, the caches are shared automatically.</p> <p>Verify ccache status:</p> <pre><code>ccache -s\n</code></pre>"},{"location":"getting-started/nix-setup.html#macos-openmp-not-found","title":"macOS: OpenMP not found","text":"<p>If CMake reports that OpenMP cannot be found on macOS, ensure you are inside the Nix shell (<code>echo $IN_NIX_SHELL</code> should print <code>1</code>). The Nix shell provides <code>llvmPackages.openmp</code> and sets the necessary paths automatically.</p> <p>If building outside Nix on macOS, see the manual installation guide instead.</p>"},{"location":"getting-started/quickstart.html","title":"Quick Start","text":""},{"location":"getting-started/quickstart.html#quick-start","title":"Quick Start","text":"<p>This guide starts with the primitive-shape demos and then shows the bundled <code>tutorial</code> station.</p> <p>Prerequisites</p> <p>Make sure you have completed the Installation steps and have a release build available (<code>bash brelease.sh</code>). Visualization scripts also require Python packages.</p>"},{"location":"getting-started/quickstart.html#primitive-shape-demos","title":"Primitive-shape demos","text":"<p>Two basic shape demos reuse the <code>tutorial</code> detector layout and generate their DEM at run time. They are useful for confirming that the pipeline works on simple known geometry before moving to a fuller synthetic volcano.</p> <p>For the flat-topped cylinder demo:</p> <pre><code>bash setup_station.sh eg-cylinder\ncd work/eg-cylinder/swp001/\nbash run_prg.sh true 42\n</code></pre> <p>For the square-pyramid demo:</p> <pre><code>bash setup_station.sh eg-square-pyramid\ncd work/eg-square-pyramid/swp001/\nbash run_prg.sh true 42\n</code></pre> <p>Both demos write the generated DEM under <code>work/&lt;station&gt;/dem/</code> and the figures under <code>work/&lt;station&gt;/swp001/figs/</code>. See Synthetic DEM Examples for the shape definitions and expected outputs.</p>"},{"location":"getting-started/quickstart.html#tutorial-station","title":"Tutorial station","text":"<p>The default runnable sample is <code>tutorial</code>, a fully synthetic station that ships without GSI (Geospatial Information Authority of Japan) survey data. Its DEM, detector layout, and parameter file are all bundled with the repository, so it is the recommended first run.</p> <p>The bundled inputs include:</p> <ul> <li>DEM (Digital Elevation Model): <code>data/tutorial/tutorial-5m.g2zbin</code></li> <li>Detector parameters: 11 detector runcards under   <code>data/tutorial/detparams/0.2m2_120days/</code></li> <li>Station config: <code>param_sites/tutorial.json5</code></li> <li>Muon flux tables: Pre-computed Daemon-Groom flux model under   <code>fluxtable/daemon_groom/</code></li> </ul> <p><code>tutorial</code> uses a local meter coordinate system, so <code>param_sites/tutorial.json5</code> sets <code>\"skip_detparams\": true</code>. This means <code>setup_station.sh</code> reuses the bundled detector runcards instead of regenerating them from KML or GeoJSON.</p>"},{"location":"getting-started/quickstart.html#generate-the-working-directory","title":"Generate the working directory","text":"<p>From the repository root, run:</p> <pre><code>bash setup_station.sh tutorial\n</code></pre> <p>This checks the local environment, prepares <code>.venv/</code> if needed, links the DEM from <code>data/tutorial/</code>, reuses the detector runcards, and creates or updates the working files under <code>work/tutorial/</code>. Existing generated files are skipped unless you pass <code>--force</code>.</p> <p>After generation, the standard reconstruction run is:</p> <pre><code>cd work/tutorial/swp001/\nbash run_prg.sh true 42\n</code></pre> <p>The arguments mean:</p> Argument Meaning <code>true</code> Run the C++ executable before plotting <code>42</code> Random seed for reproducible Poisson draws <p>To regenerate figures from existing output without rerunning the executable:</p> <pre><code>bash run_prg.sh false\n</code></pre>"},{"location":"getting-started/quickstart.html#what-the-run-does","title":"What the run does","text":"<p>The standard <code>tutorial</code> run executes the 3D reconstruction pipeline:</p> Module Step 1 Load parameters and initialize 2 Build detector array 3 Build geometry from the DEM and voxel grid 4 Compute ray-tracing path lengths 5 Compute prior density-length 6 Compute expected muon counts 7 Run linear inversion <p><code>param_sites/tutorial.json5</code> currently sets the default <code>swp001</code> run to stop after Invert Density (Module 7). Full Analyze Errors (Module 8) error analysis can be enabled through the <code>end_stage</code> and <code>module8_mode</code> settings described in the Pipeline and Output pages.</p>"},{"location":"getting-started/quickstart.html#check-the-output","title":"Check the output","text":"<p>After the command finishes, inspect:</p> Path What to check <code>logs/time_report.log</code> Runtime and peak memory report from <code>/usr/bin/time</code> <code>logs/error_auto_plot.log</code> Plotting errors, if any <code>tmp/</code> Intermediate matrices, reconstructed density, and cross-section data <code>figs/</code> Generated detector, signal, and reconstruction figures <p>For additional context about the bundled synthetic station, see Synthetic DEM Examples.</p>"},{"location":"getting-started/quickstart.html#other-bundled-stations","title":"Other bundled stations","text":"<p>Other station presets are available under <code>data/</code> and <code>param_sites/</code> and can be used as starting points for custom configurations:</p> Station Description <code>eg-cylinder</code> Primitive flat-topped cylinder demo. DEM is generated by <code>run_prg.sh</code>, JSON5 at <code>param_sites/eg-cylinder.json5</code> <code>eg-square-pyramid</code> Primitive square-pyramid demo. DEM is generated by <code>run_prg.sh</code>, JSON5 at <code>param_sites/eg-square-pyramid.json5</code> <code>tutorial</code> License-free synthetic omuro-scale volcano (no GSI data). DEM at <code>data/tutorial/tutorial-5m.g2zbin</code>, JSON5 at <code>param_sites/tutorial.json5</code> <p>To create a new station, start from:</p> <pre><code>bash setup_station.sh &lt;station_name&gt;\n</code></pre> <p>Then edit <code>param_sites/&lt;station_name&gt;.json5</code>, place DEM and detector input files under <code>data/&lt;station_name&gt;/</code>, and run:</p> <pre><code>bash setup_station.sh &lt;station_name&gt; --make-detparams\n</code></pre> <p><code>--make-detparams</code> builds the detector runcards (<code>det_XX.json5</code>) from the KML/GeoJSON detector positions; the analysis itself is started later by <code>run_prg.sh</code>.</p> <p>See Utility Scripts -- init_work_site.py and Utility Scripts for details.</p>"},{"location":"getting-started/quickstart.html#showa-case-studies","title":"Showa case studies","text":"<p>Showa-shinzan examples are shown as case studies for detection-limit plots and synthetic 3D reconstruction figures. Their terrain data is not bundled, so they are read-only references rather than runnable presets:</p> <ul> <li>Depth vs. Spatial Resolution</li> <li>Output</li> </ul>"},{"location":"getting-started/quickstart.html#whats-next","title":"What's next?","text":"<ul> <li>Parameter Files -- Detailed reference   for all JSON5 configuration sections</li> <li>Pipeline -- In-depth explanation of each   pipeline module</li> <li>Concepts -- Background on muography   (muon radiography) and the physics behind MUONITH</li> </ul>"},{"location":"reference/appendix.html","title":"Appendix","text":""},{"location":"reference/appendix.html#appendix","title":"Appendix","text":"<p>This appendix collects implementation-level formulas that are useful when checking MUONITH outputs against the code. It is intentionally narrower than the concept pages: use the concept pages for the workflow, and this appendix for the exact equations and source-code correspondence.</p>"},{"location":"reference/appendix.html#efficiency-uncertainty-in-mathbfc_n","title":"Efficiency Uncertainty in \\(\\mathbf{C}_N\\)","text":"<p>MUONITH represents the observation covariance \\(\\mathbf{C}_N\\) as a diagonal matrix. With efficiency uncertainty disabled, the diagonal element for grouped bin \\(g\\) is the Poisson variance of the grouped muon count:</p> \\[ (\\mathbf{C}_N)_{gg} = N_g . \\] <p>When <code>tf_eff_cn_diag</code> is enabled, detector-efficiency uncertainty is added to that diagonal entry:</p> \\[ (\\mathbf{C}_N)_{gg} = N_g + \\sigma_{\\varepsilon,g}^{2}. \\] <p>Let \\(k \\in g\\) be the angular elements merged into grouped bin \\(g\\). For angular element \\(k\\), the efficiency table gives lower, central, and upper efficiencies \\(\\varepsilon_{\\mathrm{low},k}\\), \\(\\varepsilon_k\\), and \\(\\varepsilon_{\\mathrm{upp},k}\\). MUONITH uses the band half-width</p> \\[ \\delta\\varepsilon_k = \\frac{1}{2}\\left(\\varepsilon_{\\mathrm{upp},k} - \\varepsilon_{\\mathrm{low},k}\\right) \\] <p>as the efficiency uncertainty. With \\(b_k\\) denoting the efficiency-free base count of element \\(k\\), the central expected count of that element is \\(N_k=\\varepsilon_k b_k\\). In the grouped-relative notation used in the design notes,</p> \\[ s_k = \\frac{\\delta\\varepsilon_k}{\\varepsilon_k}, \\qquad N_g = \\sum_{k\\in g} N_k, \\qquad s_g = \\frac{\\sum_{k\\in g}s_k N_k}{N_g}. \\] <p>Because \\(s_k N_k = \\delta\\varepsilon_k b_k\\), the default grouped efficiency variance can be written equivalently as</p> \\[ \\sigma_{\\varepsilon,g}^{2} = (s_g N_g)^2 = \\left(\\sum_{k\\in g}\\delta\\varepsilon_k b_k\\right)^2 . \\] <p>This is the correlated-in-group form: all angular elements in the grouped bin move in the same direction. If <code>tf_eff_cn_diag_independent</code> is enabled, MUONITH instead uses the independent-element form:</p> \\[ \\sigma_{\\varepsilon,g}^{2} = \\sum_{k\\in g}\\left(\\delta\\varepsilon_k b_k\\right)^2 . \\] <p>The same variance is also used when computing the grouped 2D projected-density error band:</p> \\[ \\sigma_g = \\sqrt{N_g + \\sigma_{\\varepsilon,g}^{2}} . \\]"},{"location":"reference/appendix.html#why-mathbfc_n-stays-diagonal","title":"Why \\(\\mathbf{C}_N\\) Stays Diagonal","text":"<p>The diagonal treatment is a modeling choice, not a statement that correlated efficiency errors cannot exist. The efficiency table gives each angular element's lower, central, and upper efficiencies, so it defines the width of the element-level uncertainty. It does not define correlations between angular bins, detectors, or calibration-source groups.</p> <p>MUONITH therefore treats the uncertainty consumed by <code>tf_eff_cn_diag</code> as closed within each grouped angular bin. Correlated components inside the grouped bin are folded into the diagonal variance term above. Cross-bin or cross-detector common calibration errors would require off-diagonal covariance terms, and are outside the current fast evaluation model. This keeps the implementation aligned with MUONITH's main use: comparing observation layouts and relative density contrast before data taking. Absolute-density studies that rely on detector-wide common calibration should provide a separate correlation model or evaluate that systematic outside the current diagonal \\(\\mathbf{C}_N\\) path.</p>"},{"location":"reference/appendix.html#source-code-mapping","title":"Source-Code Mapping","text":"Quantity Code location Meaning \\(\\delta\\varepsilon_k\\) <code>src/cls_DetectorPanel.cpp</code>, <code>calc_var_eff_group</code> Half-width of <code>eff_low</code> and <code>eff_upp</code> \\(b_k\\) <code>src/cls_DetectorPanel.cpp</code>, <code>calc_var_eff_group</code> Efficiency-free count from <code>DetectorElement::calc_signal()</code> \\(\\sigma_{\\varepsilon,g}^{2}\\) <code>src/cls_DetectorPanel.cpp</code>, <code>calc_var_eff_group</code> Grouped efficiency variance \\((\\mathbf{C}_N)_{gg}\\) Poisson term <code>src/cls_NagaInv.cpp</code>, <code>mp_build_mat_cov_muon</code> Diagonal Poisson variance Efficiency variance addition <code>src/cls_NagaInv.cpp</code>, <code>add_var_eff_to_cov_muon</code> Adds <code>vecxf_var_eff(g)</code> to the diagonal 2D projected-density band <code>src/cls_DetectorPanel.cpp</code>, <code>calc_set_proj_dens_grouped</code> Uses <code>sqrt(N_g + var_eff_g)</code> Main gate <code>include/cls_NagaInvParameters.hpp</code>, <code>get_tf_eff_cn_diag</code> Enables the efficiency variance term Independent-mode gate <code>include/cls_NagaInvParameters.hpp</code>, <code>get_tf_eff_cn_diag_independent</code> Switches from square-of-sum to sum-of-squares"},{"location":"reference/appendix.html#scope","title":"Scope","text":"<p>This term is an additional uncertainty on observed counts. It does not replace Poisson statistics, and it does not introduce a dense covariance matrix. Correlated errors beyond a grouped bin are outside the current diagonal implementation path described above.</p>"},{"location":"reference/appendix.html#3d-density-reconstruction","title":"3D Density Reconstruction","text":"<p>This section collects the linearized observation-matrix derivation, the MAP posterior equations, the notation list, and the dense-covariance memory limit behind MUONITH's 3D density reconstruction. The workflow and worked example are in 3D Inversion; this appendix is the equation and source-code reference. The formulation follows Nagahara et al. (2022).</p>"},{"location":"reference/appendix.html#observation-matrix","title":"Observation Matrix","text":"<p>Let \\(g\\) index the grouped angular bins (the observation rows, the same grouped bins used in the efficiency section above) and \\(j\\) index the voxels. The density length along grouped bin \\(g\\) is linear in the voxel density vector \\(\\boldsymbol{\\rho}\\) (dimension \\(n_v\\)):</p> \\[ X_g = \\sum_j L_{gj}\\, \\rho_j \\qquad [\\mathrm{kg/m^2}], \\] <p>where \\(L_{gj}\\) is the geometric path length [m] of bin \\(g\\) through voxel \\(j\\), computed by the DDA ray tracer. The expected muon count is a nonlinear function of the density length, \\(N_g = N_g(X_g)\\), obtained by folding the CSDA flux table with effective area, solid angle, and exposure time. Linearizing around the prior density length \\(X_{0,g} = \\sum_j L_{gj}\\,\\rho_{0,j}\\) gives the observation (muon-count sensitivity) matrix \\(\\mathbf{A}\\):</p> \\[ A_{gj} = \\frac{\\partial N_g}{\\partial \\rho_j} \\approx L_{gj} \\left.\\frac{dN_g}{dX_g}\\right|_{X_{0,g}}, \\] <p>which is Eq. (18) of Nagahara et al. (2022). The derivative \\(dN_g/dX_g\\) is the differential sensitivity of the penetrating-muon flux, evaluated from finite differences of the CSDA table. Because \\(\\mathbf{A}\\) is linear in \\(L\\), it is linear under the adaptive bin merging (see the efficiency section above): the observation matrix of a merged bin is the plain sum of the rows of its sub-bins, so merging does not degrade numerical accuracy.</p>"},{"location":"reference/appendix.html#map-posterior","title":"MAP Posterior","text":"<p>With a Gaussian prior [Tarantola, 2005], following the joint muon\u2013gravity formulation of Nishiyama et al. (2014b), the posterior covariance and the posterior density are</p> \\[ \\mathbf{C}_{\\rho'} = \\left(\\mathbf{A}^\\top \\mathbf{C}_N^{-1} \\mathbf{A} + \\mathbf{C}_\\rho^{-1}\\right)^{-1}, \\] \\[ \\delta\\boldsymbol{\\rho} = \\mathbf{C}_{\\rho'}\\, \\mathbf{A}^\\top \\mathbf{C}_N^{-1} \\left(\\mathbf{N}_{\\text{obs}} - \\mathbf{N}_0\\right), \\] \\[ \\boldsymbol{\\rho}' = \\boldsymbol{\\rho}_0 + \\delta\\boldsymbol{\\rho}. \\] <p>The prior density covariance uses an exponential (isotropic by default) correlation,</p> \\[ C_{\\rho,jj'} = \\sigma_\\rho^2 \\exp\\!\\left(-\\frac{d_{jj'}}{l_c}\\right), \\] <p>with density uncertainty \\(\\sigma_\\rho\\) [kg/m\u00b3] and correlation length \\(l_c\\) [m]. MUONITH also implements a separable anisotropic form with independent horizontal and vertical correlation lengths.</p>"},{"location":"reference/appendix.html#notation","title":"Notation","text":"Symbol Dimensions Meaning Role \\(\\mathbf{L}\\) \\(n_d \\times n_v\\) Path-length matrix; \\(L_{gj}\\) is the geometric path length [m] of bin \\(g\\) through voxel \\(j\\) (DDA ray tracing) Input \\(\\boldsymbol{\\rho}_0\\) \\(n_v \\times 1\\) Prior density [kg/m\u00b3] Input \\(\\sigma_\\rho\\) scalar Density uncertainty [kg/m\u00b3] Input \\(l_c\\) scalar Spatial correlation length [m] (horizontal/vertical can be set independently) Input \\(\\mathbf{N}_{\\text{obs}}\\) \\(n_d \\times 1\\) Observed muon counts Input \\(\\mathbf{C}_N\\) \\(n_d \\times n_d\\) Muon-count error covariance (diagonal; Poisson term, optionally plus efficiency variance) Input \\(\\left.dN_g/dX_g\\right\\rvert_{X_{0,g}}\\) \\(n_d \\times 1\\) Differential muon-flux sensitivity at the prior density length, precomputed from the flux table Input \\(X_g\\) scalar Density length of bin \\(g\\) [kg/m\u00b2] Intermediate \\(\\mathbf{A}\\) \\(n_d \\times n_v\\) Observation matrix; \\(A_{gj} = \\partial N_g/\\partial\\rho_j \\approx L_{gj}\\,(dN_g/dX_g)\\rvert_{X_{0,g}}\\) Intermediate \\(\\mathbf{C}_\\rho\\) \\(n_v \\times n_v\\) Prior density covariance (smoothness constraint) Intermediate \\(\\mathbf{N}_0\\) \\(n_d \\times 1\\) Expected muon counts at the prior model Intermediate \\(\\boldsymbol{\\rho}'\\) \\(n_v \\times 1\\) Reconstructed density (posterior mean) Output \\(\\mathbf{C}_{\\rho'}\\) \\(n_v \\times n_v\\) Posterior density covariance Output <p>Here \\(n_d\\) is the number of detector elements (grouped observation rows) and \\(n_v\\) is the number of voxels.</p>"},{"location":"reference/appendix.html#numerical-constraints","title":"Numerical Constraints","text":"<p>The posterior covariance inverse is solved by LU decomposition (<code>LAPACKE_sgetrf</code> + <code>LAPACKE_sgetri</code>). Single and double precision are both available; single precision is the default, with double precision recommended for ill-conditioned problems. The dense covariance matrix scales as \\(O(n_v^2)\\) in memory, so <code>MAX_VOXEL_FOR_DENSE_COV = 50000</code> (about 9.3 GB for a float matrix) is the safe limit. Beyond it, coarsen the resolution or restrict the reconstruction sub-volume.</p>"},{"location":"reference/appendix.html#source-code-mapping_1","title":"Source-Code Mapping","text":"Quantity Code location Meaning \\(\\mathbf{L}\\) <code>vec_spmat_PL</code> (Trace Path Lengths, Module 4) Density-independent path-length matrices \\(dN_g/dX_g\\), \\(\\mathbf{N}_0\\) Compute Prior (Module 5) Prior density length and expected counts \\(\\mathbf{A}\\) Build Observation Matrix (Module 6), <code>calc_dNdD</code> namespace Observation matrix assembly \\(\\boldsymbol{\\rho}'\\), \\(\\mathbf{C}_{\\rho'}\\) Invert Density (Module 7) Density reconstruction (LU via LAPACK)"},{"location":"reference/cli.html","title":"CLI","text":""},{"location":"reference/cli.html#cli-reference","title":"CLI Reference","text":"<p>This page lists the executable programs included in MUONITH and their command-line interfaces.</p>"},{"location":"reference/cli.html#building","title":"Building","text":""},{"location":"reference/cli.html#build-scripts","title":"Build Scripts","text":"Script Description <code>bash bdebug.sh</code> Debug build (<code>-O0</code>, logging enabled) <code>bash brelease.sh</code> Release build (<code>-O3</code>, NODEBUG defined) <code>bash ccdebug.sh</code> Clean debug build (clears ccache, removes <code>build-debug/</code>, rebuilds with DBGPRINT) <code>bash ccrelease.sh</code> Clean release build (clears ccache, removes <code>build-release/</code>, rebuilds) <code>bash pdebug.sh</code> Python-oriented debug build (DBGPRINT enabled) <p>All scripts call <code>build.sh</code> internally, which handles:</p> <ul> <li>Compiler selection: Nix shell \u2192 Homebrew GCC (14\u219213) \u2192 Apple Clang \u2192 system GCC</li> <li>Parallel builds: Job count = stricter of (a) core headroom (<code>logical_cpus - max(2, ncpu/8)</code>) and (b) a memory cap of ~2 GB per compile job. Override with the <code>BUILD_JOBS</code> environment variable</li> <li>ccache: Enabled by default for faster incremental builds</li> <li>OpenMP: On non-Nix macOS, detects Homebrew <code>libomp</code> and passes <code>-DOpenMP_ROOT</code> to CMake</li> </ul> <p>Compiled binaries are placed in <code>build-release/exec/</code> or <code>build-debug/exec/</code>.</p>"},{"location":"reference/cli.html#environment-variables","title":"Environment Variables","text":"Variable Description <code>OMP_NUM_THREADS</code> Number of OpenMP threads for runtime parallelization <code>BUILD_JOBS</code> Override the build parallelism (number of compile jobs). Bypasses the core-headroom and memory-cap heuristics in <code>build.sh</code> <code>IN_NIX_SHELL</code> Set to <code>1</code> by the Nix devShell; changes compiler selection in <code>build.sh</code> <code>CC</code> / <code>CXX</code> Override compiler selection (e.g., <code>CC=gcc-14 CXX=g++-14 bash build.sh</code>) <code>ENABLE_NODEBUG</code> <code>ON</code>/<code>OFF</code> \u2014 define <code>NODEBUG</code> macro (default: <code>OFF</code>) <code>ENABLE_DBGPRINT</code> <code>ON</code>/<code>OFF</code> \u2014 define <code>DBGPRINT</code> macro (default: <code>OFF</code>)"},{"location":"reference/cli.html#environment-setup","title":"Environment Setup","text":"<p>Before running, set up the thread count:</p> <pre><code>export OMP_NUM_THREADS=16    # Adjust to your CPU core count\n</code></pre>"},{"location":"reference/cli.html#main-executables","title":"Main Executables","text":""},{"location":"reference/cli.html#muonithexe","title":"<code>muonith.exe</code>","text":"<p>The primary executable for 3D density reconstruction and parameter sweep.</p> <pre><code>muonith.exe --json &lt;config.json5&gt; [--seed &lt;seed&gt;] [--resume &lt;checkpoint_dir&gt;] [--end-stage &lt;N&gt;]\n</code></pre> Option Long form Type Required Description <code>-j</code> <code>--json</code> string Yes Path to JSON5 configuration file <code>-s</code> <code>--seed</code> uint No Random seed (overrides JSON5 value) <code>-r</code> <code>--resume</code> path No Resume a previously interrupted parameter sweep from the given checkpoint directory <code>-E</code> <code>--end-stage</code> int No Stop the pipeline after completing Module N. Valid values: 3, 4, 5, 6, 7, 8. Default: 8 (all modules). Takes precedence over <code>end_stage</code> in JSON5. Incompatible with sweep mode (<code>tf_exec=true</code>) when N &lt; 7. <p>Execution modes (determined by JSON5 configuration):</p> <ul> <li>Single run: When <code>NAGAINV_PARAM_SWEEP</code> section is absent or <code>tf_exec=false</code>, runs the full pipeline once (modules 1-8).</li> <li>Parameter sweep: When <code>NAGAINV_PARAM_SWEEP.tf_exec=true</code>, runs modules 1-6 once, then sweeps module 7-8 over all parameter combinations.</li> </ul> <p>Examples:</p> <pre><code># Run full pipeline (modules 1-8, single run)\nmuonith.exe -j prm_muonith.json5\n\n# Run with a specific seed\nmuonith.exe -j prm_muonith.json5 -s 12345\n\n# Resume an interrupted parameter sweep (pass the checkpoint directory)\nmuonith.exe -j prm_muonith.json5 --resume path/to/checkpoint_dir\n</code></pre> <p>Pipeline modules (executed in order):</p> Module Name Description 1 Initialization Load JSON5, setup logging, set random seed 2 Load Parameters Parse all parameter sections 3 Build Geometry Load DEM, create Grid2dPillar and Grid3dVoxel 4 Trace Path Lengths Build detector array, compute path lengths 5 Compute Prior Compute prior density-length and expected counts 6 Build Observation Matrix Build observation matrix (dN/dD) 7 Invert Density MAP density reconstruction 8 Analyze Errors and Output Error analysis and final output <p>See Pipeline for detailed descriptions of each module.</p> <p>The JSON5 file may include a <code>NAGAINV_PARAM_SWEEP</code> section specifying the parameter values to sweep. See Parameter Files for the configuration format.</p>"},{"location":"reference/cli.html#depth_resoexe","title":"<code>depth_reso.exe</code>","text":"<p>Depth vs. resolution analysis tool. Evaluates what size and magnitude of density anomaly is detectable at a given depth for a specified detector configuration.</p> <pre><code>depth_reso.exe -j &lt;config.json5&gt; [-s &lt;seed&gt;]\n</code></pre> Option Long form Type Required Description <code>-j</code> <code>--json</code> string Yes JSON5 configuration file <code>-s</code> <code>--seed</code> uint No Random seed"},{"location":"reference/cli.html#utility-executables","title":"Utility Executables","text":""},{"location":"reference/cli.html#make_peneflux_dfdrexe","title":"<code>make_peneflux_dFdR.exe</code>","text":"<p>Generates muon flux table files (penetrating flux and dF/dR) from source data. These tables are required by the main pipeline.</p> <pre><code>make_peneflux_dFdR.exe &lt;config.json&gt; &lt;SECTION_NAME&gt;\n</code></pre> <p>See Making Penetrating Muon Flux Tables for the full walkthrough (inputs, sample config, outputs, and verification).</p>"},{"location":"reference/cli.html#rotate_vectorexe","title":"<code>rotate_vector.exe</code>","text":"<p>Rotates one or more vectors by a yaw/pitch/roll triple and prints the resulting orientation (the rotated vector, its azimuth, and its elevation). It is a hands-on tool for checking the rotation conventions and for seeing Euler-angle degeneracies such as gimbal lock.</p> <pre><code>rotate_vector.exe &lt;yaw_deg&gt; &lt;pitch_deg&gt; &lt;roll_deg&gt; &lt;LOCAL|GLOBAL&gt; [vx vy vz ...]\n</code></pre> Position Argument Description 1 <code>yaw_deg</code> Rotation about the Z axis, in degrees 2 <code>pitch_deg</code> Rotation about the Y axis, in degrees 3 <code>roll_deg</code> Rotation about the X axis, in degrees 4 <code>LOCAL</code> | <code>GLOBAL</code> Frame convention (see below) 5+ <code>vx vy vz ...</code> Zero or more vectors to rotate, given as consecutive triples <ul> <li>Angle order: ZYX Euler angles. The three numbers are read as yaw, then pitch, then roll (yaw \u2192 Z, pitch \u2192 Y, roll \u2192 X). Positive angles are counter-clockwise.</li> <li>Coordinate system: right-handed, z-up. Input angles are in degrees.</li> <li>Frame: <code>GLOBAL</code> keeps the axes fixed in space (<code>R = Rx * Ry * Rz</code>); <code>LOCAL</code> rotates the axes with the object (<code>R = Rz * Ry * Rx</code>).</li> <li>Default vectors: when no vector is given, the three body axes <code>x = (1,0,0)</code>, <code>y = (0,1,0)</code>, <code>z = (0,0,1)</code> are rotated, so a full orientation (and any gimbal lock) is visible in one run.</li> </ul> <p>Examples:</p> <pre><code># Gimbal lock: pitch = 90 deg folds yaw and roll onto the same axis.\n# With no vector given, the three body axes are rotated.\nrotate_vector.exe 0 90 45 LOCAL\n\n# Rotate a single vector (1,0,0) by yaw = 90 deg about Z (GLOBAL frame).\nrotate_vector.exe 90 0 0 GLOBAL 1 0 0\n\n# Rotate two vectors at once by roll = 90 deg about X.\nrotate_vector.exe 0 0 90 GLOBAL 1 0 0 0 1 0\n</code></pre> <p>Invalid input (an unknown frame name, a non-numeric angle, or a vector list whose length is not a multiple of three) is reported and the program exits with status 1.</p>"},{"location":"reference/cli.html#cli-precedence","title":"CLI Precedence","text":"<p>When the same setting is specified in multiple places, the following priority applies:</p> <pre><code>CLI arguments  &gt;  JSON5 file  &gt;  Default values\n</code></pre> <p>For example, <code>-s 12345</code> on the command line overrides <code>\"seed\": 42</code> in the JSON5 file.</p>"},{"location":"reference/cli.html#typical-workflow","title":"Typical Workflow","text":"<pre><code># 1. Set up environment\nexport OMP_NUM_THREADS=16\n\n# 2. Navigate to working directory\ncd work/my_site/\n\n# 3. Run full reconstruction (single run)\n../../build-release/exec/muonith.exe -j prm_muonith.json5\n\n# 4. Run parameter sweep (set tf_exec=true in NAGAINV_PARAM_SWEEP)\n../../build-release/exec/muonith.exe -j prm_muonith.json5\n</code></pre>"},{"location":"reference/faq.html","title":"FAQ","text":""},{"location":"reference/faq.html#faq","title":"FAQ","text":"<p>Frequently asked questions and troubleshooting tips.</p>"},{"location":"reference/faq.html#installation-and-building","title":"Installation and Building","text":""},{"location":"reference/faq.html#q-cmake-cannot-find-eigen3","title":"Q: CMake cannot find Eigen3","text":"<p>Eigen3 is downloaded automatically via FetchContent. If you see this error, ensure you are using the current <code>CMakeLists.txt</code> (which includes FetchContent declarations for Eigen3).</p> <p>See Installation for full dependency setup.</p>"},{"location":"reference/faq.html#q-openblas-linking-errors-linux","title":"Q: OpenBLAS linking errors (Linux)","text":"<p>Ensure OpenBLAS is installed:</p> <pre><code>sudo apt install -y libopenblas-openmp-dev liblapacke-dev\n</code></pre> <p>macOS uses Accelerate by default</p> <p>On macOS, CMake links against Apple's Accelerate framework automatically. OpenBLAS is not required. If you intentionally want to use Homebrew OpenBLAS, set <code>CMAKE_PREFIX_PATH</code>:</p> <pre><code>export CMAKE_PREFIX_PATH=\"$(brew --prefix openblas):${CMAKE_PREFIX_PATH}\"\n</code></pre>"},{"location":"reference/faq.html#q-compiler-version-too-old","title":"Q: Compiler version too old","text":"<p>MUONITH requires a C++20 compiler: GCC 13+ or Apple Clang 17+.</p> Ubuntu 22.04Ubuntu 24.04macOS <pre><code>sudo add-apt-repository ppa:ubuntu-toolchain-r/test\nsudo apt update\nsudo apt install g++-13 gcc-13\n</code></pre> <p>G++ 13 is included by default.</p> <p>Install GCC 14 (preferred) or 13 via Homebrew:</p> <pre><code>brew install gcc@14\n</code></pre> <p>Or use the Nix development environment which provides the compiler automatically.</p>"},{"location":"reference/faq.html#q-nix-shell-does-not-activate-nix-develop-fails","title":"Q: Nix shell does not activate / <code>nix develop</code> fails","text":"<p>Ensure Nix flakes are enabled. With the Determinate Systems installer, flakes are enabled by default. Otherwise, add to <code>~/.config/nix/nix.conf</code>:</p> <pre><code>experimental-features = nix-command flakes\n</code></pre> <p>See Nix Setup for full instructions.</p>"},{"location":"reference/faq.html#q-what-is-in_nix_shell","title":"Q: What is <code>IN_NIX_SHELL</code>?","text":"<p>An environment variable set to <code>1</code> by the Nix devShell. <code>build.sh</code> checks it to decide the compiler selection strategy: in a Nix shell, it uses the Nix-provided <code>cc</code>/<code>c++</code> wrappers instead of searching for Homebrew GCC.</p>"},{"location":"reference/faq.html#running","title":"Running","text":""},{"location":"reference/faq.html#q-how-do-i-set-the-number-of-threads","title":"Q: How do I set the number of threads?","text":"<p>Set the OpenMP thread count via environment variable before running:</p> <pre><code>export OMP_NUM_THREADS=16\n</code></pre> <p>MUONITH internally sets Eigen threads to <code>OMP_NUM_THREADS / 2</code> to balance parallelism between ray tracing and linear algebra.</p>"},{"location":"reference/faq.html#q-can-i-re-run-just-the-inversion-without-recomputing-path-lengths","title":"Q: Can I re-run just the inversion without recomputing path lengths?","text":"<p>Yes. Save the Trace Path Lengths (Module 4) results by setting <code>tf_save_arrdet_g3vox: true</code> in <code>PATH_LENGTH_PARAMETERS</code>. On subsequent runs, set <code>tf_load_arrdet_g3vox: true</code> so Trace Path Lengths (Module 4) reuses the saved path lengths, then let Modules 5\u20138 run.</p> <p>Parameter sweeps on inversion-side knobs (<code>sigma_rho</code>, <code>corr_length</code>, etc.) can reuse a saved <code>checkpoint_trace_path_lengths</code> via <code>--resume &lt;checkpoint_dir&gt;</code>, which skips Modules 1\u20134 entirely.</p>"},{"location":"reference/faq.html#q-how-do-i-stop-the-pipeline-early","title":"Q: How do I stop the pipeline early?","text":"<p>Use <code>--end-stage &lt;N&gt;</code> (or set <code>end_stage</code> in JSON5) to stop after Module N. Valid values are 3, 4, 5, 6, 7, 8 (default 8). For example, <code>--end-stage 4</code> stops right after the ray-tracing step, which is convenient for building a <code>checkpoint_trace_path_lengths</code> to resume from later. See CLI for the full option list.</p>"},{"location":"reference/faq.html#q-how-long-does-a-typical-run-take","title":"Q: How long does a typical run take?","text":"<p>Runtime depends heavily on grid size (number of voxels), number of detectors, and angular bin resolution. Trace Path Lengths (Module 4) (ray tracing / path length computation) is typically the bottleneck. Saving and loading binary checkpoints (<code>tf_save_arrdet_g3vox</code>, <code>tf_load_arrdet_g3vox</code>) avoids recomputation.</p> <p>Note that the dense covariance matrix <code>mat_cov_dens</code> requires <code>num_voxels\u00b2 \u00d7 4</code> bytes (float). For example, 50,000 voxels consume approximately 9.3 GB of memory for this matrix alone.</p>"},{"location":"reference/faq.html#parameters","title":"Parameters","text":""},{"location":"reference/faq.html#q-what-values-should-i-use-for-sigma_rho-and-corr_length","title":"Q: What values should I use for <code>sigma_rho</code> and <code>corr_length</code>?","text":"<p>These are the most important regularization parameters:</p> <ul> <li> <p><code>sigma_rho</code> (density prior standard deviation): Controls the trade-off between data fit and prior. Typical range: 100\u20131000 kg/m\u00b3.</p> <ul> <li>Too small \u2192 reconstruction stays close to the prior</li> <li>Too large \u2192 reconstruction overfits the data (noisy)</li> </ul> </li> <li> <p><code>corr_length</code> (spatial correlation length): Controls spatial smoothness. Should be comparable to or larger than the voxel size. Typical range: 30\u2013200 m.</p> <ul> <li>Too small \u2192 noisy, checkerboard-like artifacts</li> <li>Too large \u2192 over-smoothed, anomalies are blurred</li> </ul> </li> </ul> <p>Use <code>NAGAINV_PARAM_SWEEP</code> to systematically explore combinations.</p>"},{"location":"reference/faq.html#q-what-is-n_hit_det_min","title":"Q: What is <code>n_hit_det_min</code>?","text":"<p>The minimum number of distinct detector panels whose rays must pass through a voxel for it to be included in the inversion. Setting <code>n_hit_det_min = 3</code> means voxels seen by fewer than 3 detectors are excluded \u2014 they have insufficient angular coverage for reliable reconstruction.</p>"},{"location":"reference/faq.html#q-what-is-voxel-merging","title":"Q: What is voxel merging?","text":"<p>Merging combines adjacent voxels to reduce the grid resolution. For example, <code>merge_factor = 4</code> combines 4\u00d74\u00d74 = 64 voxels into one. This reduces the number of unknowns and the size of the covariance matrix, making the inversion faster and more stable.</p> <p>The <code>merge_center</code> parameters control the alignment of the merged grid.</p>"},{"location":"reference/faq.html#q-what-does-tf_build_g3vox-false-do","title":"Q: What does <code>tf_build_g3vox = false</code> do?","text":"<p>It skips the 3D voxel construction entirely. The pipeline stops after Build Geometry (Module 3), producing only 2D projected density maps from the DEM. Useful for quick single-view analysis without 3D reconstruction.</p>"},{"location":"reference/faq.html#input-data","title":"Input Data","text":""},{"location":"reference/faq.html#q-what-coordinate-system-should-my-dem-use","title":"Q: What coordinate system should my DEM use?","text":"<p>MUONITH uses a right-handed, z-up coordinate system in meters. Any projected coordinate system (e.g., UTM, local plane coordinates) works. Geographic coordinates (lat/lon) must be converted first.</p> <p>If your coordinates have large absolute values (common with UTM), enable <code>tf_shift_x</code> and <code>tf_shift_y</code> to shift the origin.</p>"},{"location":"reference/faq.html#q-what-dem-resolution-do-i-need","title":"Q: What DEM resolution do I need?","text":"<p>The DEM resolution determines the horizontal resolution of the terrain model. Typical choices:</p> Application DEM resolution Volcanic edifice (~2 km) 5\u201310 m Archaeological structure (~100 m) 0.5\u20132 m Underground cavity (~50 m) 1\u20135 m <p>The DEM should cover the full extent of terrain that muon rays pass through, which is often much larger than the target volume itself.</p>"},{"location":"reference/faq.html#q-what-is-the-g2zbin-format","title":"Q: What is the <code>.g2zbin</code> format?","text":"<p>A binary format for 2D grids. The file begins with an 8-byte magic (<code>G2ZBIN\\x00\\x00</code>), followed by a version byte, grid metadata (name, x-axis, y-axis), and z-values in row-major order (float32 or float64). It is the single binary DEM format read by MUONITH. Generate DEM files from GSI data or existing GeoTIFF files with the external muonith-gsi-dem utility.</p>"},{"location":"reference/faq.html#output","title":"Output","text":""},{"location":"reference/faq.html#q-how-do-i-visualize-the-cross-section-output","title":"Q: How do I visualize the cross-section output?","text":"<p>Cross-section files contain (x, y, value) data. Example with Python:</p> <pre><code>import numpy as np\nimport matplotlib.pyplot as plt\n\ndata = np.loadtxt(\"cross_section_z300.txt\")\nx, y, rho = data[:, 0], data[:, 1], data[:, 2]\n\nnx = len(np.unique(x))\nny = len(np.unique(y))\n\nplt.pcolormesh(\n    x.reshape(ny, nx),\n    y.reshape(ny, nx),\n    rho.reshape(ny, nx),\n    cmap=\"RdBu_r\"\n)\nplt.colorbar(label=\"Density [kg/m\u00b3]\")\nplt.xlabel(\"x [m]\")\nplt.ylabel(\"y [m]\")\nplt.title(\"z = 300 m\")\nplt.axis(\"equal\")\nplt.show()\n</code></pre>"},{"location":"reference/faq.html#q-what-does-a-negative-delta_prior-mean","title":"Q: What does a negative <code>delta_prior</code> mean?","text":"<p>A negative \\(\\vec{\\rho}' - \\vec{\\rho}_0\\) means the reconstructed density is lower than the prior. This could indicate a cavity, fracture zone, or low-density material at that location.</p>"},{"location":"reference/faq.html#troubleshooting","title":"Troubleshooting","text":""},{"location":"reference/faq.html#q-matrix-inversion-failed-error","title":"Q: \"Matrix inversion failed\" error","text":"<p>This typically occurs when the covariance matrix is ill-conditioned. Possible solutions:</p> <ol> <li>Increase <code>sigma_rho</code> (stronger regularization)</li> <li>Increase <code>corr_length</code> (smoother covariance)</li> <li>Reduce the number of voxels (increase merge factors)</li> <li>Use double precision: the code falls back to <code>mp_reconst_density_double()</code> for better numerical stability</li> </ol>"},{"location":"reference/faq.html#q-memory-usage-is-too-high","title":"Q: Memory usage is too high","text":"<p>The dense covariance matrix scales as \\(O(n_v^2)\\). For 50,000 voxels, this requires ~9.3 GB. Solutions:</p> <ol> <li>Increase merge factors to reduce voxel count</li> <li>Increase <code>n_hit_det_min</code> to exclude poorly-constrained voxels</li> <li>Reduce the voxel grid extent (tighter <code>zmin</code>/<code>zmax</code>)</li> </ol>"},{"location":"reference/faq.html#q-path-length-computation-is-very-slow","title":"Q: Path length computation is very slow","text":"<p>Trace Path Lengths (Module 4) (path length computation) is the most compute-intensive step. To speed it up:</p> <ol> <li>Increase <code>OMP_NUM_THREADS</code></li> <li>Save results with <code>tf_save_arrdet_g3vox: true</code> and load on subsequent runs</li> <li>Use coarser voxel resolution (larger merge factors)</li> <li>Reduce the number of angular bins per detector</li> </ol>"},{"location":"reference/glossary.html","title":"Glossary","text":""},{"location":"reference/glossary.html#glossary","title":"Glossary","text":"<p>This page is a one-stop index of the abbreviations and symbols used across the MUONITH documentation. Each entry gives a short meaning and points to the page that defines it in full. The linear-inversion matrix symbols are collected in the Appendix notation table; this page links to them rather than repeating the table.</p>"},{"location":"reference/glossary.html#abbreviations-and-quantities","title":"Abbreviations and quantities","text":"Term Meaning Unit Defined in PL Path length: geometric length of a ray through matter m Coordinate System \u2014 Units DL / density length Density integrated along the ray path kg/m\u00b2 Coordinate System \u2014 Units tx, ty Angular bin coordinates of a detector element (horizontal, vertical) tangent / degree / radian (per <code>angle_unit</code>) From (tx, ty) to a Ray Direction uqid (<code>Uqid</code>) Globally unique detector-element index \u2014 Coordinate System \u2014 Key Types uqiv (<code>Uqiv</code>) Globally unique (linearized) voxel index \u2014 Coordinate System \u2014 Key Types shell / <code>ShellPL</code> Density-independent path length through the terrain shells around the voxel grid (upper / lower / lateral) m Detector Model \u2014 Shell path lengths reconst voxel Voxel solved as an unknown in the inversion \u2014 Grid System non-rec voxel Voxel not solved as an unknown, but still contributing to ray attenuation through the prior \u2014 Grid System"},{"location":"reference/glossary.html#inversion-symbols","title":"Inversion symbols","text":"<p>The linear-inversion symbols \u2014 path-length matrix \\(\\mathbf{L}\\), observation matrix \\(\\mathbf{A}\\), observation covariance \\(\\mathbf{C}_N\\), prior covariance \\(\\mathbf{C}_\\rho\\), posterior covariance \\(\\mathbf{C}_{\\rho'}\\), prior and posterior density \\(\\boldsymbol{\\rho}_0\\) and \\(\\boldsymbol{\\rho}'\\), density uncertainty \\(\\sigma_\\rho\\), correlation length \\(l_c\\), density length \\(X\\), and muon counts \\(\\mathbf{N}_{\\text{obs}}\\) and \\(\\mathbf{N}_0\\) \u2014 are listed with their dimensions and input / intermediate / output role in the Appendix \u2014 Notation table. In code and on some pages the covariances appear with a suffix, <code>C_N</code> and <code>C_rho</code>; these denote the same \\(\\mathbf{C}_N\\) and \\(\\mathbf{C}_\\rho\\).</p>"},{"location":"reference/parameter-reference.html","title":"Parameter Reference","text":""},{"location":"reference/parameter-reference.html#parameter-reference","title":"Parameter Reference","text":"<p>Complete reference of all JSON5 configuration parameters. For usage examples and inline comments, see Parameter Files.</p>"},{"location":"reference/parameter-reference.html#global","title":"Global","text":"Key Type Default Description <code>seed</code> uint <code>42</code> Random seed for reproducibility. CLI <code>-s</code> overrides. <code>end_stage</code> int <code>8</code> Maximum module to execute. Valid values: 3, 4, 5, 6, 7, 8. CLI <code>--end-stage</code> takes precedence. When less than 8, modules after the specified number are skipped. Not compatible with sweep mode (<code>tf_exec=true</code>) when value is less than 7."},{"location":"reference/parameter-reference.html#log_file","title":"<code>LOG_FILE</code>","text":"Key Type Default Description <code>path_log_dir</code> string <code>\"logs\"</code> Output directory for log files <code>give_new_number</code> bool <code>false</code> Auto-number log files <code>log_level.stdout_level</code> string <code>\"info\"</code> Console log level <code>log_level.stderr_level</code> string <code>\"error\"</code> Error stream log level <code>log_level.file_level</code> string <code>\"trace\"</code> File log level <code>default_max_count</code> int <code>50</code> Max log files to keep <code>archive_existing</code> bool <code>false</code> Archive existing logs <p>Log levels: <code>trace</code> &gt; <code>debug</code> &gt; <code>info</code> &gt; <code>warn</code> &gt; <code>error</code> &gt; <code>critical</code>.</p>"},{"location":"reference/parameter-reference.html#run_inversion","title":"<code>RUN_INVERSION</code>","text":"Key Type Default Description <code>tf_prior_error</code> bool <code>true</code> Compute lower/upper prior bounds (3x slower). Default is <code>true</code> (omitting the key, or the whole <code>RUN_INVERSION</code> section, enables the prior-error pass); <code>omuro.json5</code> sets it <code>false</code> explicitly to skip it"},{"location":"reference/parameter-reference.html#detector_parameter_lists","title":"<code>DETECTOR_PARAMETER_LISTS</code>","text":"Key Type Default Description <code>name</code> string \u2014 Instance name <code>det_files</code> array \u2014 Paths to per-detector JSON5 files <code>tf_apply_eff</code> bool <code>false</code> Apply detector efficiency table <code>tf_out_txty_ascii</code> bool <code>true</code> Write the <code>tmp/&lt;name&gt;_txty_&lt;PL\\|dens\\|signal&gt;_det&lt;NN&gt;.tmp</code> ASCII dumps. <code>false</code> only stops these dumps: with the current swp001 settings the per-element PL / density / signal figures are plotted from the saved binary, so no figure is lost. <code>tf_out_g2bg_ascii</code> bool <code>true</code> Write the <code>tmp/g2bg_&lt;name&gt;_det&lt;NN&gt;.tmp</code> ASCII dumps. <code>false</code> only stops these dumps: with the current swp001 settings the efficiency figures are plotted from the saved binary, so no figure is lost. <code>tf_save_arrdet_prior</code> bool <code>false</code> Save the prior detector array as <code>det/arrdet_g3vox_prior&lt;suffix&gt;.bin</code>. About 160 MB per file, and three files when <code>tf_prior_error</code> is <code>true</code>. Required to plot the prior array from the binary."},{"location":"reference/parameter-reference.html#detector_parameters-per-detector-file","title":"<code>DETECTOR_PARAMETERS</code> (per-detector file)","text":"Key Type Default Unit Description <code>name</code> string \u2014 \u2014 Detector panel name <code>angle_unit</code> string \u2014 \u2014 <code>\"tangent\"</code>, <code>\"degree\"</code>, or <code>\"radian\"</code> <code>nbinx</code> int \u2014 \u2014 Number of horizontal angular bins <code>txmin</code> float \u2014 (angle_unit) Horizontal angle minimum <code>txmax</code> float \u2014 (angle_unit) Horizontal angle maximum <code>nbiny</code> int \u2014 \u2014 Number of vertical angular bins <code>tymin</code> float \u2014 (angle_unit) Vertical angle minimum <code>tymax</code> float \u2014 (angle_unit) Vertical angle maximum <code>length_hori</code> float \u2014 m Detector width <code>length_vert</code> float \u2014 m Detector height <code>length_dept</code> float \u2014 m Detector depth <code>n_unit</code> int \u2014 \u2014 Number of detector copies <code>rotation_type</code> string \u2014 \u2014 <code>\"LOCAL\"</code> or <code>\"GLOBAL\"</code> <code>yaw_deg</code> float \u2014 degrees Yaw angle (0\u00b0=East, 90\u00b0=North, clockwise) <code>roll_deg</code> float <code>0</code> degrees Roll angle (counterclockwise positive) <code>pitch_deg</code> float <code>0</code> degrees Pitch angle (counterclockwise positive) <code>x</code> float \u2014 m Detector x position <code>y</code> float \u2014 m Detector y position <code>z</code> float \u2014 m Detector z position <code>days</code> float \u2014 days Exposure time <code>tf_read_bin_list</code> bool <code>false</code> \u2014 Read angular bin list from file"},{"location":"reference/parameter-reference.html#grid2d_pillar_parameters","title":"<code>GRID2D_PILLAR_PARAMETERS</code>","text":"Key Type Default Unit Description <code>name</code> string \u2014 \u2014 Instance name <code>initial_uniform_density</code> float <code>1000</code> kg/m\u00b3 Initial uniform rock density <code>path_dem</code> string \u2014 \u2014 Path to DEM file (<code>.g2zbin</code>) <code>zmin</code> float <code>0.0</code> m Pillar base elevation <code>tf_shift_x</code> bool <code>false</code> \u2014 Shift x-origin to xmin <code>tf_shift_y</code> bool <code>false</code> \u2014 Shift y-origin to ymin <code>tolerance_ratio</code> float <code>0.001</code> \u2014 DEM grid spacing tolerance <code>vertical_dike_params</code> array <code>[]</code> \u2014 Vertical dike structures <code>vertical_cylinder_params</code> array <code>[]</code> \u2014 Vertical cylinder structures <code>vertical_checkerboard_params</code> array <code>[]</code> \u2014 Vertical checkerboard structures"},{"location":"reference/parameter-reference.html#grid3d_voxel_parameters","title":"<code>GRID3D_VOXEL_PARAMETERS</code>","text":"Key Type Default Unit Description <code>tf_build_g3vox</code> bool <code>true</code> \u2014 Build 3D grid (false = 2D only) <code>name</code> string \u2014 \u2014 Instance name <code>use_grid2d_of_g2pil</code> bool <code>true</code> \u2014 Reuse DEM x/y axes <code>zmin</code> float \u2014 m Bottom of voxel grid <code>zmax</code> float \u2014 m Top of voxel grid <code>z_pitch</code> float \u2014 m Vertical voxel spacing <code>n_hit_det_min</code> int <code>0</code> \u2014 Min detectors per voxel for inversion <code>n_hit_det_max</code> int <code>INT_MAX</code> \u2014 Max detectors per voxel <code>n_hit_ele_min</code> int <code>0</code> \u2014 Min elements per voxel <code>n_hit_ele_max</code> int <code>INT_MAX</code> \u2014 Max elements per voxel <code>tf_end_after_merged</code> bool <code>false</code> \u2014 Stop after merge"},{"location":"reference/parameter-reference.html#reconst_voxels-nested-in-grid3d_voxel_parameters-optional","title":"<code>reconst_voxels</code> (nested in <code>GRID3D_VOXEL_PARAMETERS</code>, optional)","text":"<p>Selects a sub-volume of the voxel grid that participates in reconstruction. Dimensions are expressed in meters (previously <code>_nvox</code> voxel-count fields \u2014 now removed). Either AABB or cylinder (or both) may be enabled; when neither is enabled the entire voxel grid is used.</p> Key Type Default Unit Description <code>tf_aabb</code> bool <code>false</code> \u2014 Enable an axis-aligned bounding box <code>x_aabb_cnt</code> float <code>0.0</code> m AABB center x <code>y_aabb_cnt</code> float <code>0.0</code> m AABB center y <code>x_aabb_meters</code> float <code>0.0</code> m AABB full width in x (half-extent = <code>x_aabb_meters * 0.5</code>) <code>y_aabb_meters</code> float <code>0.0</code> m AABB full width in y (half-extent = <code>y_aabb_meters * 0.5</code>) <code>aabb_zmin_mode</code> string <code>\"g3vox_zmin\"</code> \u2014 How to derive lower z: <code>\"g3vox_zmin\"</code> or <code>\"manual\"</code> <code>aabb_zmin_value</code> float <code>0.0</code> m Explicit z-min (used only when <code>aabb_zmin_mode == \"manual\"</code>) <code>aabb_zmax</code> float <code>DBL_MAX</code> m Upper z bound; terrain surface clips it when the surface is lower <code>tf_cylinder</code> bool <code>false</code> \u2014 Enable an elliptical-cylinder region <code>x_cyl_cnt</code> float <code>0.0</code> m Cylinder axis center x <code>y_cyl_cnt</code> float <code>0.0</code> m Cylinder axis center y <code>cylinder_radius_x_meters</code> float <code>0.0</code> m Elliptical-cylinder semi-axis in x (must be &gt; 0 when <code>tf_cylinder=true</code>) <code>cylinder_radius_y_meters</code> float <code>0.0</code> m Elliptical-cylinder semi-axis in y (must be &gt; 0 when <code>tf_cylinder=true</code>)"},{"location":"reference/parameter-reference.html#merge_params-nested-in-grid3d_voxel_parameters","title":"<code>merge_params</code> (nested in <code>GRID3D_VOXEL_PARAMETERS</code>)","text":"Key Type Default Unit Description <code>tf_exec</code> bool <code>false</code> \u2014 Execute voxel merge <code>name</code> string \u2014 \u2014 Merge config label <code>x_merge_factor</code> int <code>1</code> \u2014 Bins to merge in x <code>y_merge_factor</code> int <code>1</code> \u2014 Bins to merge in y <code>z_merge_factor</code> int <code>1</code> \u2014 Bins to merge in z <code>x_merge_center</code> float <code>0.0</code> m Merge alignment center x <code>y_merge_center</code> float <code>0.0</code> m Merge alignment center y <code>z_merge_center</code> float <code>0.0</code> m Merge alignment center z"},{"location":"reference/parameter-reference.html#checkerboard_3d_params-nested-array-optional","title":"<code>checkerboard_3d_params</code> (nested array, optional)","text":"Key Type Default Unit Description <code>tf_exec</code> bool \u2014 \u2014 Enable this checkerboard <code>name</code> string \u2014 \u2014 Label <code>delta_density</code> float \u2014 kg/m\u00b3 Density perturbation <code>delta_density_offset</code> float <code>0.0</code> kg/m\u00b3 Density offset <code>xlen_cells</code> int \u2014 \u2014 Total checkerboard cells in x <code>ylen_cells</code> int \u2014 \u2014 Total checkerboard cells in y <code>zlen_cells</code> int \u2014 \u2014 Total checkerboard cells in z <code>xcnt</code>, <code>ycnt</code>, <code>zcnt</code> float \u2014 m Pattern center <code>xlen_interval_mult</code> int \u2014 \u2014 Block size = x_interval \u00d7 this <code>ylen_interval_mult</code> int \u2014 \u2014 Block size = y_interval \u00d7 this <code>zlen_interval_mult</code> int \u2014 \u2014 Block size = z_interval \u00d7 this <code>tf_snap_to_grid</code> bool <code>true</code> \u2014 Snap boundaries to grid lines <code>region_type</code> string <code>\"aabb\"</code> \u2014 <code>\"aabb\"</code> or <code>\"cylinder\"</code> \u2014 shape of the region within which cells are checkerboarded <code>radius_x_meters</code> float <code>0.0</code> m Cylinder semi-axis in x (used when <code>region_type == \"cylinder\"</code>) <code>radius_y_meters</code> float <code>0.0</code> m Cylinder semi-axis in y (used when <code>region_type == \"cylinder\"</code>) <p>AABB specification: The checkerboard AABB is computed symmetrically from the (snapped) center using total cell counts. For each axis: <code>min = cnt_snapped - len_cells * 0.5 * cell_size</code>, <code>max = cnt_snapped + len_cells * 0.5 * cell_size</code>, where <code>cell_size = interval * len_interval_mult</code>. Total cells per axis = <code>len_cells</code>. Odd values are supported (e.g. <code>xlen_cells=5</code> gives a half-extent of 2.5 cells). All three <code>*len_cells</code> values are required and must be positive integers. Direct specification of xmin/xmax/ymin/ymax/zmin/zmax is deprecated and no longer supported; use <code>xlen_cells</code>/<code>ylen_cells</code>/<code>zlen_cells</code> instead.</p>"},{"location":"reference/parameter-reference.html#ellipsoid_params-nested-array-optional","title":"<code>ellipsoid_params</code> (nested array, optional)","text":"Key Type Default Unit Description <code>tf_exec</code> bool <code>false</code> \u2014 Enable this ellipsoid <code>name</code> string <code>\"none\"</code> \u2014 Label <code>delta_density</code> float <code>0.0</code> kg/m\u00b3 Density perturbation (added to interior voxels) <code>xcnt</code>, <code>ycnt</code>, <code>zcnt</code> float <code>0.0</code> m Ellipsoid center position <code>xlen</code>, <code>ylen</code>, <code>zlen</code> float <code>0.0</code> m Semi-axis lengths (before rotation) <code>theta_x_deg</code> float <code>0.0</code> degrees Rotation around x-axis <code>theta_y_deg</code> float <code>0.0</code> degrees Rotation around y-axis <code>theta_z_deg</code> float <code>0.0</code> degrees Rotation around z-axis <code>rotation_type</code> string <code>\"LOCAL\"</code> \u2014 <code>\"LOCAL\"</code> (intrinsic) or <code>\"GLOBAL\"</code> (extrinsic)"},{"location":"reference/parameter-reference.html#cylinder_params-nested-array-optional","title":"<code>cylinder_params</code> (nested array, optional)","text":"Key Type Default Unit Description <code>tf_exec</code> bool <code>false</code> \u2014 Enable this cylinder <code>name</code> string <code>\"none\"</code> \u2014 Label <code>delta_density</code> float <code>0.0</code> kg/m\u00b3 Density perturbation (added to interior voxels) <code>xcnt</code>, <code>ycnt</code>, <code>zcnt</code> float <code>0.0</code> m Cylinder center position <code>xlen</code> float \u2014 m Elliptical cross-section semi-axis in x (must be &gt; 0) <code>ylen</code> float \u2014 m Elliptical cross-section semi-axis in y (must be &gt; 0) <code>zlen</code> float \u2014 m Cylinder height (must be &gt; 0) <code>theta_x_deg</code> float <code>0.0</code> degrees Rotation around x-axis <code>theta_y_deg</code> float <code>0.0</code> degrees Rotation around y-axis <code>theta_z_deg</code> float <code>0.0</code> degrees Rotation around z-axis <code>rotation_type</code> string <code>\"LOCAL\"</code> \u2014 <code>\"LOCAL\"</code> (intrinsic) or <code>\"GLOBAL\"</code> (extrinsic)"},{"location":"reference/parameter-reference.html#flux_range_data_table_prior-flux_range_data_table_real","title":"<code>FLUX_RANGE_DATA_TABLE_PRIOR</code> / <code>FLUX_RANGE_DATA_TABLE_REAL</code>","text":"<p>Both sections have identical format.</p> Key Type Default Description <code>pathin_log_peneflux</code> string \u2014 Path to log10(flux) table (<code>.g2zbin</code>) <code>tf_xcnt_peneflux</code> bool \u2014 Use bin center for cos(\u03b8z) <code>tf_ycnt_peneflux</code> bool \u2014 Use bin center for density-length <code>pathin_dFdR_R_costhz</code> string \u2014 Path to dF/dR table (<code>.g2zbin</code>) <code>tf_xcnt_dFdR</code> bool \u2014 Use bin center for cos(\u03b8z) <code>tf_ycnt_dFdR</code> bool \u2014 Use bin center for range"},{"location":"reference/parameter-reference.html#path_length_parameters","title":"<code>PATH_LENGTH_PARAMETERS</code>","text":"Key Type Default Unit Description <code>name</code> string \u2014 \u2014 Instance name <code>tf_add_PLDL</code> bool <code>true</code> \u2014 Compute PL and DL <code>tf_incr_nhit_det</code> bool <code>true</code> \u2014 Count detector hits per voxel <code>tf_incr_nhit_ele</code> bool <code>true</code> \u2014 Count element hits per voxel <code>tf_add_shell</code> bool <code>true</code> \u2014 Include shell geometry <code>BL_max</code> float <code>5000</code> m Max beam length threshold [m]; <code>&lt;= 0</code> disables the warning <code>PL_min</code> float required m Lower bound of the path-length range <code>PL_max</code> float required m Upper bound of the path-length range <code>PL_pit</code> float required m Bin pitch along the path-length axis <code>reference_matPL_sparse</code> float <code>1.0</code> \u2014 Sparse matrix reference value <code>epsilon_matPL_sparse</code> float <code>1.0e-9</code> \u2014 Sparse matrix epsilon. Values below <code>reference_matPL_sparse * epsilon_matPL_sparse</code> are zeroed <code>tf_load_arrdet_g2pil</code> bool <code>false</code> \u2014 Load 2D detector array binary <code>tf_save_arrdet_g2pil</code> bool <code>false</code> \u2014 Save 2D detector array binary <code>path_arrdet_g2pil_bin</code> string \u2014 \u2014 File path for 2D array binary <code>tf_load_arrdet_g3vox</code> bool <code>false</code> \u2014 Load 3D detector array binary <code>tf_save_arrdet_g3vox</code> bool <code>false</code> \u2014 Save 3D detector array binary <code>path_arrdet_g3vox_bin</code> string \u2014 \u2014 File path for 3D array binary <code>path_vec_spmat_PL_bin</code> string \u2014 \u2014 File path for sparse PL matrices <code>tf_load_bin_obs_mat_dNdD</code> bool <code>false</code> \u2014 Load observation matrix binary <code>tf_save_bin_obs_mat_dNdD</code> bool <code>false</code> \u2014 Save observation matrix binary <code>path_bin_obs_mat_dNdD</code> string \u2014 \u2014 File path for dN/dD matrix"},{"location":"reference/parameter-reference.html#bin_group_parameters","title":"<code>BIN_GROUP_PARAMETERS</code>","text":"Key Type Default Unit Description <code>name</code> string \u2014 \u2014 Instance name <code>signal_init</code> float <code>0</code> \u2014 Initial signal value <code>noise_init</code> float <code>0</code> \u2014 Initial noise value <code>is_avail_init</code> bool <code>true</code> \u2014 Initial availability <code>PL_thres</code> float <code>20</code> m Min path length threshold <code>DL_thres</code> float <code>10000</code> kg/m\u00b2 Max density length threshold <code>is_avail_under_thres</code> bool <code>false</code> \u2014 Availability for sub-threshold bins <code>signal_under_thres</code> float <code>-1.0</code> \u2014 Signal for disabled bins <code>noise_under_thres</code> float <code>-1.0</code> \u2014 Noise for disabled bins <code>tf_run_1st_grouping</code> bool <code>true</code> \u2014 Run initial grouping <code>tf_run_auto_grouping</code> bool <code>true</code> \u2014 Run adaptive subdivision <code>igroup_start</code> int <code>0</code> \u2014 Starting group ID <code>nx_div_init</code> int <code>1</code> \u2014 Initial horizontal divisions <code>ny_div_init</code> int <code>1</code> \u2014 Initial vertical divisions <code>signal_noise_group_trig</code> float <code>20</code> \u2014 S/N threshold for subdivision <code>ixlen_min</code> int \u2014 \u2014 Min group width (bins) <code>iylen_min</code> int \u2014 \u2014 Min group height (bins) <code>tf_prefer_split_x</code> bool <code>false</code> \u2014 Prefer horizontal splits <code>nloop_limit</code> int <code>10000</code> \u2014 Max subdivision iterations <code>n_detector_grouping_manual</code> int <code>0</code> \u2014 Number of detectors using manual bin-group lists (<code>&gt; 0</code> enables the manual route; read only when <code>tf_run_1st_grouping</code> and <code>tf_run_auto_grouping</code> are both <code>false</code>) <code>vec_tf_read_bin_group_list</code> array of bool <code>[]</code> \u2014 Per-detector flags (size = number of detectors) to read a manual bin-group list <code>vec_file_path_bin_group_list</code> array of string <code>[]</code> \u2014 Per-detector paths to bin-group list files (size = number of detectors)"},{"location":"reference/parameter-reference.html#manual-bin-group-list-file","title":"Manual bin-group list file","text":"<p>Each file referenced by <code>vec_file_path_bin_group_list</code> is a plain-text file with one rectangle (= one group) per line, four space-separated columns:</p> <pre><code>xlow xup ylow yup\n</code></pre> <p>Values are angular coordinates in the panel's <code>angle_unit</code> (default: tangent, dimensionless). Membership uses half-open intervals (<code>min &lt;= t &lt; max</code>). The rectangles must tile the panel's field of view \u2014 overlaps between group rectangles, gaps, and uncovered bin centers are rejected with an error.</p> <p>To leave a region out of the analysis on purpose, prefix its rectangle with the keyword <code>exclude</code>:</p> <pre><code>exclude xlow xup ylow yup\n</code></pre> <p>An excluded region counts as tiled, and its bins belong to no group \u2014 they appear in no export, figure, or matrix row. A group rectangle overlapping an excluded region is dropped whole at load time (logged), so the removed area can be wider than the declared band. A gap without the keyword is still an error. Blank lines and <code>#</code> comments are skipped; any other leading token (e.g. <code>EXCLUDE</code>) stops the run with its line number. See Bin Grouping \u2014 Manual Bin Grouping.</p>"},{"location":"reference/parameter-reference.html#noise_parameters","title":"<code>NOISE_PARAMETERS</code>","text":"<p>Measurement noise added to the expected muon counts before inversion. Noise has two independent sources (flux-proportional and SOT-proportional), and each source is split into a deterministic floor part and a Poisson-fluctuated part. The total noise is <code>(flux_floor + SOT_floor) + Poisson(flux_poisson + SOT_poisson)</code>.</p> Key Type Default Unit Description <code>name</code> string <code>\"noise_tmp\"</code> \u2014 Instance name (free-form) <code>tf_exec</code> bool <code>false</code> \u2014 Apply noise modeling (false = skip the block) <code>flux_proport_ratio_floor</code> float <code>0</code> \u2014 Flux-proportional noise, deterministic floor (no fluctuation) <code>flux_proport_ratio_poisson</code> float <code>0</code> \u2014 Flux-proportional noise, Poisson-fluctuated component <code>SOT_proport_noise_ratio_floor</code> float <code>0</code> \u2014 SOT-proportional (angle-independent) noise, deterministic floor <code>SOT_proport_noise_ratio_poisson</code> float <code>0</code> \u2014 SOT-proportional (angle-independent) noise, Poisson-fluctuated component <code>user_defined_noise_flux_ratio</code> float <code>0</code> \u2014 User-defined noise flux ratio <code>pathin_user_defined_noise_distribution</code> array \u2014 \u2014 File paths to user-defined noise flux tables"},{"location":"reference/parameter-reference.html#choosing-the-poisson-noise-ratio","title":"Choosing the Poisson noise ratio","text":"<p>The Poisson-fluctuated ratios (<code>flux_proport_ratio_poisson</code>, <code>SOT_proport_noise_ratio_poisson</code>) set how strongly noise scales with the expected counts. Too large a ratio drives the deep (high-path-length) bins below a signal-to-noise ratio of one, where noise dominates the signal and the reconstruction breaks down.</p> <p>The panels below show the per-element signal-to-noise ratio (<code>sig/noi</code>) for detector <code>det_2018_A</code> of the Omuro synthetic array (green = high SNR, yellow \u2248 1, orange/red = low SNR). For where <code>det_2018_A</code> and the other detectors sit on the dome, see the Omuro detector layout. This comparison is the direct basis for preferring <code>1.0E-3</code> over <code>1.0E-2</code> in the Omuro synthetic study.</p> <p></p> <p>Noise ratio <code>1.0E-3</code> (0.1%): the deep bins (low <code>ty</code>) stay near or above SNR = 1.</p> <p></p> <p>Noise ratio <code>5.0E-3</code> (0.5%): the low-SNR region (orange) begins to spread into the deep bins.</p> <p></p> <p>Noise ratio <code>1.0E-2</code> (1%): the deep bins turn red \u2014 noise exceeds signal over a wide region.</p>"},{"location":"reference/parameter-reference.html#nagainv_parameters-array","title":"<code>NAGAINV_PARAMETERS</code> (array)","text":"Key Type Default Unit Description <code>name</code> string \u2014 \u2014 Instance name <code>tf_exec</code> bool <code>true</code> \u2014 Execute this configuration <code>tf_signal_poisson</code> bool <code>false</code> \u2014 Apply Poisson error to signal counts (noise counts fluctuate via the <code>*_poisson</code> ratios under <code>NOISE_PARAMETERS</code>) <code>nmuon_thres</code> float <code>0.0</code> \u2014 Min muon count threshold <code>nmuon_under_thres</code> float <code>0.0</code> \u2014 Replacement for sub-threshold counts <code>uniform_prior_density</code> float <code>2000</code> kg/m\u00b3 Prior density assumption <code>shell_density_upper</code> float <code>uniform_prior_density</code> kg/m\u00b3 Prior density for the upper shell (falls back to <code>uniform_prior_density</code> if omitted) <code>shell_density_lower</code> float <code>uniform_prior_density</code> kg/m\u00b3 Prior density for the lower shell (falls back to <code>uniform_prior_density</code>) <code>shell_density_lateral</code> float <code>uniform_prior_density</code> kg/m\u00b3 Prior density for the lateral shell (falls back to <code>uniform_prior_density</code>) <code>tf_logN</code> bool <code>false</code> \u2014 Use log-count formulation in the likelihood <code>corr_length</code> float \u2014 m Isotropic correlation length <code>sigma_rho</code> float \u2014 kg/m\u00b3 Density prior standard deviation <code>sigma_rho_diag</code> float \u2014 kg/m\u00b3 Diagonal covariance std dev <code>tf_aniso</code> bool <code>false</code> \u2014 Enable anisotropic covariance <code>corr_length_xy</code> float \u2014 m Horizontal correlation length <code>corr_length_z</code> float \u2014 m Vertical correlation length <code>aniso_cov_type</code> string <code>\"separable\"</code> \u2014 <code>\"separable\"</code> or <code>\"ellipsoidal\"</code> <code>tf_eff_cn_diag</code> bool <code>false</code> \u2014 Add efficiency uncertainty to the observation covariance diagonal \\(\\mathbf{C}_N\\). Also gates the central efficiency (<code>eff_cnt</code>) applied to the forward count, so the count and its error band turn on together. Widens the 2D projected-density error band (see Inversion) <code>tf_eff_cn_diag_independent</code> bool <code>false</code> \u2014 Treat the efficiency uncertainty as independent per element. Only effective when <code>tf_eff_cn_diag</code> is <code>true</code>"},{"location":"reference/parameter-reference.html#proj_dens_eval_grouped","title":"<code>PROJ_DENS_EVAL_GROUPED</code>","text":"Key Type Default Unit Description <code>tf_exec</code> bool <code>false</code> \u2014 Run projected density evaluation <code>tf_signal_poisson</code> bool <code>true</code> \u2014 Apply Poisson error to signal counts (noise counts fluctuate via the <code>*_poisson</code> ratios under <code>NOISE_PARAMETERS</code>) <code>dens_min</code> float <code>0</code> kg/m\u00b3 Min density to scan <code>dens_max</code> float <code>10000</code> kg/m\u00b3 Max density to scan <code>dens_steps</code> array \u2014 kg/m\u00b3 Multi-pass step sizes (coarse to fine) <code>range_factor</code> float <code>2.0</code> \u2014 Search range factor for refinement <code>sigma</code> float <code>1.5</code> \u03c3 Significance threshold"},{"location":"reference/parameter-reference.html#z_cross_section","title":"<code>Z_CROSS_SECTION</code>","text":"Key Type Default Unit Description <code>min</code> float \u2014 m Lowest z-elevation for output <code>max</code> float \u2014 m Highest z-elevation for output <code>zstep</code> double <code>0</code> m Direct z-step size. If 0 or omitted, z_interval is used <code>output_binary</code> bool <code>false</code> \u2014 Also emit the cross-section data in binary form"},{"location":"reference/parameter-reference.html#nagainv_param_sweep","title":"<code>NAGAINV_PARAM_SWEEP</code>","text":"Key Type Default Description <code>tf_exec</code> bool <code>false</code> Run parameter sweep <code>base_index</code> int <code>0</code> Index in <code>NAGAINV_PARAMETERS</code> to use as template <code>vec_sigma_rho</code> array \u2014 sigma_rho values to sweep [kg/m\u00b3] <code>vec_corr_length</code> array \u2014 corr_length values to sweep [m] <code>vec_uniform_prior_density</code> array \u2014 Prior density values to sweep [kg/m\u00b3]. Each element: scalar (all shells equal) or <code>[prior, upper, lower, lateral]</code>. Mixable. <code>link_diag_to_sigma</code> bool <code>true</code> Set sigma_rho_diag = sigma_rho <code>vec_sigma_rho_diag</code> array \u2014 sigma_rho_diag values to sweep [kg/m\u00b3]. Required when <code>link_diag_to_sigma=false</code> <code>module8_mode</code> string <code>\"none\"</code> <code>\"none\"</code>, <code>\"all\"</code>, <code>\"last\"</code>, <code>\"selected\"</code> <code>module8_indices</code> array <code>[]</code> Sweep indices for <code>module8_mode=\"selected\"</code> (e.g. <code>[0, 2, 5]</code>)"},{"location":"reference/scripts.html","title":"Scripts","text":""},{"location":"reference/scripts.html#utility-scripts","title":"Utility Scripts","text":"<p>The <code>scripts/</code> directory contains Python utility scripts for data conversion, visualization, parameter management, and inspection. All scripts require Python 3 and accept <code>--help</code> for full option details. Dependencies (numpy, matplotlib, pandas, scipy, pyproj, rasterio, json5, etc.) are installed with <code>uv sync</code> from <code>pyproject.toml</code> + <code>uv.lock</code> at the repository root (see Installation).</p>"},{"location":"reference/scripts.html#overview","title":"Overview","text":"Script Purpose <code>g2zbin_io.py</code> Library module for reading/writing g2zbin files <code>kml_to_csv.py</code> Extract KML placemarks to CSV/Markdown with elevation lookup <code>geojson_to_csv.py</code> Convert GeoJSON points to CSV/Markdown analysis table <code>mk_detjson5_from_template.py</code> Generate per-detector JSON5 files from a CSV and template <code>auto_plot.py</code> Auto-detect output files and run appropriate plotting scripts <code>plot_Grid3dVoxel.py</code> Render 3D voxel cross-section plots with GIF/PDF output <code>plot_detection_limit_heatmap.py</code> Plot detection limit heatmaps (p-value vs. size and depth) <code>hist2d.py</code> Unified 2D histogram / grid plotter <code>plot_dem.py</code> Visualize g2zbin DEM as a 2D heatmap <code>plot_dem_with_detectors.py</code> Plot DEM with detector positions overlay <code>plot_g2bg.py</code> Plot Grid2d background fields per detector <code>plot_det_arrdet.py</code> Plot txty/g2bg figures straight from <code>det/arrdet_*.bin</code> binary checkpoints <code>make_primitive_dem.py</code> Generate a primitive-solid DEM (cylinder/square_prism/cone/square_pyramid/gaussian) as g2zbin (in <code>param_sites/tutorial/</code>) <code>init_work_site.py</code> Scaffold a new per-site working directory from KML/CSV inputs and JSON5 templates <p>A small number of shell helpers also live at the repository root:</p> Script Purpose <code>setup_station.sh</code> Station setup wrapper. Auto-checks <code>/usr/bin/time</code>, <code>python3</code>, <code>taskset</code>; sets up <code>.venv/</code> via <code>uv sync</code> from <code>pyproject.toml</code> + <code>uv.lock</code> (falls back to venv+pip when <code>uv</code> is absent); auto-copies <code>param_sites/template.json5</code> for new stations; respects <code>data_source</code> for shared data dirs; relaxes KML requirement when <code>skip_detparams: true</code>. Pass <code>--skip-env-check</code> to bypass Phase 0 / 0-5 environment checks."},{"location":"reference/scripts.html#data-conversion","title":"Data Conversion","text":""},{"location":"reference/scripts.html#g2zbin_iopy","title":"g2zbin_io.py","text":"<p>Library Module</p> <p>This is a Python library module, not a standalone script. Import it in other scripts with <code>from g2zbin_io import read_g2zbin, write_g2zbin</code>.</p> <p>Provides <code>read_g2zbin()</code> and <code>write_g2zbin()</code> functions for the g2zbin binary format. The format stores Grid2d metadata (axis names, bin counts, min/max, interval) in a compact header followed by a flat z-value array. Used internally by <code>plot_dem.py</code>, <code>kml_to_csv.py</code>, and other grid-processing scripts.</p>"},{"location":"reference/scripts.html#geospatial-detector-setup","title":"Geospatial &amp; Detector Setup","text":""},{"location":"reference/scripts.html#kml_to_csvpy","title":"kml_to_csv.py","text":"<p>Extract point placemarks from a KML file and produce a CSV and Markdown analysis table with projected coordinates, distances, and azimuths.</p> <pre><code>python3 scripts/kml_to_csv.py --kml_in input.kml --EPSG 6676 [options]\n</code></pre> <p>Supports elevation lookup from the GSI (Geospatial Information Authority of Japan) API, GeoTIFF DEM, or g2zbin DEM. Automatically identifies a reference point (vent/summit) using configurable keywords and computes relative geometry for each detector candidate.</p> Option Description <code>--kml_in</code> Input KML file (required) <code>--EPSG</code> Output EPSG code (required) <code>--fill_elevation_from_gsi</code> Fetch elevations from the GSI API <code>--dem</code> Path to DEM file (<code>.tif</code> or <code>.g2zbin</code>); GSI API used as fallback <code>--throttle_sec</code> API call interval in seconds (default: 0.5) <pre><code>python3 scripts/kml_to_csv.py --kml_in asama.kml --EPSG 6676 --dem dem.g2zbin --verbose\n</code></pre>"},{"location":"reference/scripts.html#geojson_to_csvpy","title":"geojson_to_csv.py","text":"<p>Convert GeoJSON point features to the same CSV/Markdown analysis table format as <code>kml_to_csv.py</code>.</p> <pre><code>python3 scripts/geojson_to_csv.py --geojson_in input.geojson --EPSG 6680 [options]\n</code></pre> <p>Accepts GeoJSON FeatureCollection files exported from GSI Maps or other GIS tools. Shares the elevation-fill logic with <code>kml_to_csv.py</code>, supporting <code>--fill_elevation_from_gsi</code>, <code>--dem_tif</code>, and <code>--dem_only</code> modes.</p> <pre><code>python3 scripts/geojson_to_csv.py --geojson_in tokachi.geojson --EPSG 6680 --dem_only --dem_tif dem.tif\n</code></pre>"},{"location":"reference/scripts.html#mk_detjson5_from_templatepy","title":"mk_detjson5_from_template.py","text":"<p>Generate per-detector JSON5 parameter files from a template and a CSV table.</p> <pre><code>python3 scripts/mk_detjson5_from_template.py --template template-det.json5 --csv table.csv\n</code></pre> <p>Reads a CSV produced by <code>kml_to_csv.py</code> and fills in x, y, z, and yaw_deg values for each detector row while preserving comments in the JSON5 template. Column detection is automatic (handles various EPSG label formats) but can be overridden with <code>--xcol</code>/<code>--ycol</code>. Rows with missing numeric values are safely skipped.</p> Option Description <code>--template</code> Path to template JSON5 file (required) <code>--csv</code> Input CSV file (required) <code>--z_pos_offset</code> Add an elevation offset to all z values <code>--epsg</code> Prefer columns matching this EPSG code <pre><code>python3 scripts/mk_detjson5_from_template.py --template template-det.json5 --csv asama_table_epsg6676.csv --z_pos_offset 10.5\n</code></pre>"},{"location":"reference/scripts.html#init_work_sitepy","title":"init_work_site.py","text":"<p>Scaffold a new per-site working directory from a station JSON5 (under <code>param_sites/</code>) that points at a KML/GeoJSON for detector placement and a DEM for terrain. Produces a site directory containing <code>prm_muonith.json5</code>, per-depth/sweep subdirectories (e.g. <code>depth001/</code>, <code>swp001/</code>), and ready-to-use <code>auto_plot.json5</code> configurations.</p> <pre><code>python3 scripts/init_work_site.py param_sites/&lt;station&gt;.json5 --make-detparams\n</code></pre> <p>Typical use is to bootstrap a new station (e.g. <code>tarumae-dome</code>, <code>tarumae-mid01</code>) without hand-editing the full JSON5 tree. Reconstruction volume fields are written in meters (<code>x_aabb_meters</code>, <code>cylinder_radius_x_meters</code>, \u2026) matching the current <code>GRID3D_VOXEL_PARAMETERS.reconst_voxels</code> schema.</p> <p>Custom CRS stations (<code>skip_detparams</code>): For stations whose DEM and detector positions use a non-EPSG custom orthogonal coordinate system, set <code>\"skip_detparams\": true</code> in the station JSON5. The KML/CSV/det conversion pipeline is then bypassed, and the existing <code>work/&lt;station&gt;/detparams/&lt;label&gt;/runcard_det_*.json5</code> files are reused as-is. Required keys are relaxed (<code>epsg</code>, <code>detector_input</code>, <code>detector_spec</code> become optional). The <code>detector_spec.label</code> field can be supplied as a hint to pin which existing label directory to use when multiple are present.</p>"},{"location":"reference/scripts.html#visualization","title":"Visualization","text":""},{"location":"reference/scripts.html#auto_plotpy","title":"auto_plot.py","text":"<p>Automatically detect output files and orchestrate the appropriate plotting scripts.</p> <pre><code>python3 scripts/auto_plot.py [--config config.json5] [--dry-run] [--exclude PATTERN]\n</code></pre> <p>Scans the <code>tmp/</code> directory (and sweep subdirectories) for <code>.tmp</code> files, then dispatches hist2d, g3vox cross-section, and g2bg plotting tasks. hist2d tasks run in parallel; g3vox, g2bg, and det tasks run sequentially. Configuration is read from <code>auto_plot_default.json5</code> and can be overridden with a user config file.</p> <p>A fourth task family, det, is disabled by default: when the config sets <code>det_exec: true</code>, <code>auto_plot.py</code> also scans <code>det/arrdet_*.bin</code> and <code>checkpoint_*/det/arrdet_*.bin</code> \u2014 both directly in the working directory and under the tmp directory (the sweep driver writes its checkpoint bundles there, e.g. <code>tmp/checkpoint_m4/det/</code>) \u2014 and dispatches <code>plot_det_arrdet.py</code> for each binary. This produces the standard txty/g2bg figures directly from binary checkpoints, without needing the <code>tmp/*.tmp</code> text exports. Keep <code>det_exec</code> off when the text exports are present \u2014 the same figures would be produced twice. The swp001 template (<code>scripts/templates/swp001_auto_plot.json5.tpl</code>) sets <code>det_exec: true</code>, because the swp001 <code>prm_muonith.json5</code> it is generated with disables the text exports (<code>tf_out_txty_ascii</code> / <code>tf_out_g2bg_ascii</code> false). The <code>det</code> section's <code>out_dir</code> / <code>n_jobs</code> keys control the output directory and the drawing parallelism (see <code>plot_det_arrdet.py</code>); the swp001 template sets <code>out_dir: \"figs\"</code>, <code>n_jobs: -2</code>, and excludes <code>arrdet_g2pil_*</code>.</p> Option Description <code>--config</code>, <code>-c</code> Custom JSON5/JSON config to override defaults <code>--dry-run</code>, <code>-n</code> Print commands without executing <code>--skip-hist2d</code> Skip hist2d plots <code>--skip-g3vox</code> Skip g3vox plots <code>--skip-g2bg</code> Skip g2bg plots <code>--skip-det</code> Skip det plots (binary-direct <code>plot_det_arrdet.py</code> tasks) <code>--exclude</code>, <code>-e</code> Exclude files matching glob pattern (repeatable) <code>--delete-png</code> Delete intermediate PNG files after GIF/PDF creation <code>--tmp-dir</code> Directory containing <code>.tmp</code> files (default: <code>tmp</code>) <code>--error-log</code> File for error logs (default: <code>error_auto_plot-tmp.log</code>; empty string disables) <pre><code>python3 scripts/auto_plot.py --dry-run --exclude \"arrdet_g3vox_prior_*\"\n</code></pre>"},{"location":"reference/scripts.html#plot_grid3dvoxelpy","title":"plot_Grid3dVoxel.py","text":"<p>Render 3D voxel data as cross-section heatmap slices with animated GIF and PDF output.</p> <pre><code>python3 scripts/plot_Grid3dVoxel.py --config=config.json [--use-data-bins]\n</code></pre> <p>Reads <code>g3vox_*_zcross_all.tmp</code> (text) or <code>g3vox_*_zcross_all.tmpbin</code> (binary) files and produces per-slice PNG images along the specified cross-section axis (x, y, or z). Supports detector position markers, multiple cross-sections, and configurable color scales. Uses multiprocessing for parallel slice rendering.</p> Option Description <code>--config</code> Path to JSON configuration file <code>--use-header-bins</code> Use bin parameters from the input file header <code>--use-data-bins</code> Compute bin edges from actual data coordinates <code>--delete-png</code> Remove intermediate PNG files after GIF/PDF creation <pre><code>python3 scripts/plot_Grid3dVoxel.py --config=plot_g3vox.json --use-data-bins\n</code></pre>"},{"location":"reference/scripts.html#hist2dpy","title":"hist2d.py","text":"<p>Unified 2D histogram and grid plotter with two modes: histogram (bin scatter data) and grid (reshape pre-gridded data).</p> <pre><code>python3 scripts/hist2d.py input.tmp [--mode histogram|grid] [options]\n</code></pre> <p>Reads whitespace-separated (x, y, z) data and produces a color-mapped PNG. Supports log scale, contour lines, mask overlays, and CSV export. Typically invoked by <code>auto_plot.py</code> rather than directly.</p> Option Description <code>--mode</code> <code>histogram</code> (default) or <code>grid</code> <code>--nbinx</code>, <code>--xmin</code>, <code>--xmax</code> X-axis binning <code>--ngrad</code>, <code>--vmin</code>, <code>--vmax</code> Color scale range <code>--log</code> Use logarithmic color scale <code>--colormap</code> Matplotlib colormap name (default: jet) <code>--contour</code> Draw contour lines <code>--no-csv</code> Disable CSV output <pre><code>python3 scripts/hist2d.py tmp/hist_data.tmp --nbinx 320 --xmin -1.6 --xmax 1.6 --log\n</code></pre>"},{"location":"reference/scripts.html#plot_detection_limit_heatmappy","title":"plot_detection_limit_heatmap.py","text":"<p>Create detection limit heatmaps showing log10(p-value) as a function of anomaly size and depth for each delta-density value.</p> <pre><code>python3 scripts/plot_detection_limit_heatmap.py --main_config prm_reso.json5 [--plot_config heatmap.json5] [--outdir figs]\n</code></pre> <p>Reads significance CSV files produced by the depth-resolution sweep and generates separate heatmap figures per delta-density. Supports configurable contour lines, a secondary elevation axis, size-bar indicators, and optional animated GIF output.</p> <pre><code>python3 scripts/plot_detection_limit_heatmap.py --main_config prm_reso.json5 --plot_config heatmap_config.json5 --surface_elevation auto\n</code></pre>"},{"location":"reference/scripts.html#plot_dempy","title":"plot_dem.py","text":"<p>Visualize g2zbin DEM files as 2D heatmaps with optional contour lines.</p> <pre><code>python3 scripts/plot_dem.py --input dem.g2zbin [--contour] [--cint 10]\n</code></pre> <p>Supports color range control, contour line intervals, optional 90-degree rotation, and configurable colormaps. Saves output as PNG alongside the input file.</p> Option Description <code>--input</code> Input <code>.g2zbin</code> file (required) <code>--zmin</code>, <code>--zmax</code> Color scale range <code>--contour</code> Draw contour lines <code>--cint</code> Contour interval (default: 10) <code>--cmap</code> Matplotlib colormap (default: terrain) <pre><code>python3 scripts/plot_dem.py --input dem.g2zbin --contour --cint 50 --cmap terrain\n</code></pre>"},{"location":"reference/scripts.html#plot_dem_with_detectorspy","title":"plot_dem_with_detectors.py","text":"<p>Plot a DEM together with detector positions loaded from a CSV file or JSON5 configuration.</p> <pre><code>python3 scripts/plot_dem_with_detectors.py --config prm.json5 [--csv det_table.csv]\n</code></pre> <p>Overlays detector markers, a reference-point star, and optional distance labels on top of a DEM heatmap with contours. Accepts DEM in g2zbin or GeoTIFF format. Coordinates can be displayed in meters or kilometers.</p> Option Description <code>--config</code> JSON5 config (extracts DEM path, detectors, target coordinates) <code>--csv</code> CSV with detector coordinates (from <code>kml_to_csv.py</code>) <code>--dem</code> Path to DEM file (overrides config) <code>--show_dist</code> Show detector-to-target distance labels <code>--xyunit</code> Axis unit: <code>m</code> or <code>km</code> (default: m) <pre><code>python3 scripts/plot_dem_with_detectors.py --config prm.json5 --contour --cint 50 --xyunit km\n</code></pre>"},{"location":"reference/scripts.html#plot_g2bgpy","title":"plot_g2bg.py","text":"<p>Plot Grid2d background fields (signal, noise, density estimates, efficiency) per detector.</p> <pre><code>python3 scripts/plot_g2bg.py --jsonfile=config.json\n</code></pre> <p>Reads <code>g2bg_*_det*.tmp</code> files and generates PNG images for each configured field and detector. Supports reference-data differencing, virtual fields (e.g., S/N ratio differences), log scale, and parallel processing. Produces per-detector and per-field PDF/GIF aggregations.</p> <pre><code>python3 scripts/plot_g2bg.py --jsonfile=plot_g2bg.json\n</code></pre>"},{"location":"reference/scripts.html#plot_det_arrdetpy","title":"plot_det_arrdet.py","text":"<p>Plot per-element quantities straight from <code>det/arrdet_*.bin</code> binary checkpoints (binary-direct, no intermediate text file).</p> <pre><code>python3 scripts/plot_det_arrdet.py &lt;arrdet.bin | checkpoint_dir | det_dir&gt; \\\n    [--field PL,dens,signal] [--det 0,1,2] [--out DIR] [--jobs N] \\\n    [--plot-config auto_plot.json5] [--check-mat &lt;mat_dir&gt;] [--list-fields]\n</code></pre> <p>Reads a serialized <code>DetectorPanelArray</code> checkpoint (<code>det/arrdet_g3vox_input.bin</code> or <code>det/arrdet_g2pil_naive.bin</code>, ~160 MB each) field by field, then reproduces the standard txty (hist2d) and g2bg figures with the rendering parameters from the same <code>auto_plot.json5</code> the normal flow uses, so the output naming and style match the established figures. Useful when only binary checkpoints are available \u2014 for example a <code>--end-stage 4</code> bundle, or runs where the <code>tmp/*.tmp</code> text exports were disabled or predate the <code>mat/</code> element-table export.</p> <p>Also invoked automatically by <code>auto_plot.py</code> as the det task family when the config sets <code>det_exec: true</code> (default: false). Figures are written to the <code>det.out_dir</code> of the auto_plot config (relative paths are anchored at the config's folder; the swp001 template sets <code>\"figs\"</code>), else to <code>plot_det_arrdet/</code> next to the binary; <code>--out</code> overrides both. Drawing runs in parallel: <code>--jobs</code> (else <code>det.n_jobs</code>, default -2) sets the number of workers \u2014 positive = that many, 1 = one by one, 0 = all CPU cores, negative = leave that many cores free.</p> <p>Limitations: checkpoints older than <code>PIPELINE_VERSION</code> 7 are rejected with a clear error (v7 added the group efficiencies to the binary, so the <code>eff_*</code> figures are drawn on this path too) \u2014 regenerate them with the current code. For very large checkpoints the binary read can be slow \u2014 run the script manually if the <code>auto_plot.py</code> per-task time limit (600 s for det tasks) is exceeded.</p> Option Description <code>--field</code> Comma-separated txty fields to plot (default: <code>PL,dens,signal</code>; see <code>--list-fields</code>) <code>--det</code> Comma-separated detector IDs to keep (default: all) <code>--out</code> Output directory (default: <code>det.out_dir</code> from the auto_plot config, else <code>&lt;source_dir&gt;/plot_det_arrdet</code>) <code>--jobs</code> Figure-drawing workers (default: <code>det.n_jobs</code> from the auto_plot config, else -2 = leave 2 CPU cores free) <code>--plot-config</code> <code>auto_plot.json5</code> for rendering parameters (default: nearest one above the binary, else <code>auto_plot_default.json5</code>) <code>--check-mat</code> Validate the binary read against the <code>mat/</code> element table row for row <code>--g2bg</code> / <code>--no-g2bg</code> Force g2bg group figures on/off (default follows the config <code>g2bg_exec</code> switch) <code>--no-hist2d</code> Never draw the per-element txty (hist2d) figures <code>--list-fields</code> Print the plottable field names and exit <pre><code>python3 scripts/plot_det_arrdet.py checkpoint_trace_path_lengths --check-mat run_output/mat\n</code></pre>"},{"location":"reference/scripts.html#inspection-comparison","title":"Inspection &amp; Comparison","text":""},{"location":"reference/scripts.html#demo_recon_nnlspy","title":"demo_recon_nnls.py","text":"<p>Standalone density reconstruction driven only by the exported <code>mat/manifest.json</code>, with no dependency on the C++ code.</p> <pre><code>python3 scripts/demo_recon_nnls.py &lt;mat_dir&gt; [--out DIR] [--lams 0.01,0.1,1.0]\n</code></pre> <p>Loads <code>mat/manifest.json</code>, sanity-checks every <code>.bin</code> it lists, builds a synthetic ground truth (uniform background with one low-density block), synthesizes observed counts from the central observation matrix, and solves non-negative regularized least squares over each <code>--lams</code> weight, keeping the best by relative error. Writes a slice PNG and a z-scan GIF to <code>--out</code> (default <code>&lt;mat_dir&gt;/../demo_recon_nnls</code>). Requires NumPy, SciPy, and Matplotlib. See External Reconstruction for the <code>mat/</code> layout.</p> <pre><code>python3 scripts/demo_recon_nnls.py path/to/run_output/mat\n</code></pre>"},{"location":"reference/scripts.html#test-data-generation","title":"Test Data Generation","text":""},{"location":"reference/scripts.html#make_primitive_dempy","title":"make_primitive_dem.py","text":"<p>Generate a simple primitive-solid DEM (single-valued height field) and write it as a g2zbin binary file. Lives in <code>param_sites/tutorial/</code>. Supersedes the former <code>mk_gaussian_g2zbin.py</code> (the Gaussian surface is now the <code>gaussian</code> shape). The generated <code>.g2zbin</code> binaries are not committed to the repository; users regenerate them locally for tutorials.</p> <pre><code>python3 param_sites/tutorial/make_primitive_dem.py --shape cone \\\n    --x_min -500 --x_max 500 --y_min -500 --y_max 500 \\\n    --x_interval 5 --y_interval 5 --radius 200 --height 300 \\\n    --outbin param_sites/tutorial/eg_cone-5m.g2zbin\n</code></pre> <p>Supported shapes (<code>--shape</code>): <code>cylinder</code>, <code>square_prism</code>, <code>cone</code>, <code>square_pyramid</code>, <code>gaussian</code>. Each computes a uniform grid and writes the height field in g2zbin format.</p> Option Description <code>--shape</code> One of cylinder / square_prism / cone / square_pyramid / gaussian (required) <code>--x_min</code>, <code>--x_max</code> X-axis range <code>--y_min</code>, <code>--y_max</code> Y-axis range <code>--x_interval</code>, <code>--y_interval</code> Grid spacing <code>--xcnt</code>, <code>--ycnt</code> Shape center (default 0, 0) <code>--base</code> Base plane elevation (default 0) <code>--height</code> Peak height above base (default 300) <code>--radius</code> Radius for cylinder / cone (default 200) <code>--half_width</code> Half-width for square_prism / square_pyramid (default 200) <code>--sigma_x</code>, <code>--sigma_y</code> Gaussian widths (sigma_y defaults to sigma_x) <code>--outbin</code> Output g2zbin file path (default: <code>eg_&lt;shape&gt;-&lt;dx&gt;m.g2zbin</code>) <code>--float64</code> Store z as float64 instead of float32 (default: float32) <p>For a full worked example that generates a primitive DEM, adds an internal density anomaly, and runs the inversion end to end, see Synthetic DEM Examples.</p>"},{"location":"user-guide/external-reconstruction.html","title":"External Reconstruction","text":""},{"location":"user-guide/external-reconstruction.html#external-reconstruction","title":"External Reconstruction","text":"<p>The inversion writes its observation matrix and voxel/observation bookkeeping to a self-describing <code>mat/</code> directory. Everything an external tool needs to run its own density reconstruction \u2014 the matrix layout, the meaning of every row and column, the units \u2014 is recorded in <code>mat/manifest.json</code>. No access to the C++ code is required.</p> <p>This page documents the <code>mat/</code> export and a worked demo, <code>demo_recon_nnls.py</code>, that reconstructs density from <code>mat/</code> alone.</p>"},{"location":"user-guide/external-reconstruction.html#the-mat-export","title":"The <code>mat/</code> export","text":"<p>The <code>mat/</code> directory is written under a run's output directory (the same directory that holds the binary checkpoints \u2014 see Output). It contains the observation matrix in binary form plus a single <code>manifest.json</code> that describes it.</p>"},{"location":"user-guide/external-reconstruction.html#manifestjson-structure","title":"<code>manifest.json</code> structure","text":"<p><code>manifest.json</code> has five top-level keys:</p> Key Content <code>format_version</code> Manifest schema version (currently <code>1</code>). <code>files</code> One entry per binary file, keyed by file name (see Binary layout). <code>detectors</code> Per-detector metadata: <code>detid</code>, <code>angle_unit</code>, <code>pos_m</code> (position, meters), <code>dir</code>, <code>n_element</code>. <code>rows</code> Row contract of the matrix (see Rows and columns). <code>cols</code> Column contract of the matrix (see Rows and columns). <p>The observation matrix is exported as three prior brackets:</p> File Meaning <code>mat_dNdD_grouped_lower.bin</code> Observation matrix \\(\\mathbf{A} = \\partial N / \\partial \\rho\\) at the lower prior bracket. <code>mat_dNdD_grouped_center.bin</code> Observation matrix at the central prior. This is the matrix used by the demo. <code>mat_dNdD_grouped_upper.bin</code> Observation matrix at the upper prior bracket."},{"location":"user-guide/external-reconstruction.html#binary-layout","title":"Binary layout","text":"<p>Every matrix <code>.bin</code> file uses the same layout, also stated verbatim in each <code>files</code> entry (<code>dtype</code>, <code>order</code>, <code>header</code>, <code>rows</code>, <code>cols</code>):</p> <ol> <li>Header \u2014 two <code>uint64</code> values in native endianness: the row count, then the    column count.</li> <li>Data \u2014 <code>rows \u00d7 cols</code> <code>float32</code> values in column-major order (Eigen's default).</li> </ol> <p>To read one matrix in Python: read the 16-byte header as two <code>uint64</code>, then read the remaining <code>rows \u00d7 cols</code> <code>float32</code> values column-major. The demo's <code>load_matrix</code> helper does exactly this and is safe to import and reuse.</p>"},{"location":"user-guide/external-reconstruction.html#rows-and-columns","title":"Rows and columns","text":"<p>The matrix follows a fixed contract, so an external solver can map every row to a physical observation and every column to a voxel:</p> <ul> <li>Row \\(i\\) = one observation bin (a single detector's angle group). <code>rows.columns</code>   lists the fields <code>[detid, igroup, uqig, xmin, xmax, ymin, ymax]</code>, and <code>rows.entries</code>   gives one entry per row in matrix order.</li> <li>Column \\(j\\) = one voxel. <code>cols.columns</code> lists <code>[uqiv, ix, iy, iz]</code>, and the voxel   index satisfies <code>uqiv = uqiv_min + j</code> (<code>cols.uqiv_min</code> / <code>cols.uqiv_max</code> bound the   range). <code>cols.entries</code> gives one entry per column in matrix order.</li> </ul>"},{"location":"user-guide/external-reconstruction.html#what-the-matrix-represents","title":"What the matrix represents","text":"<p>The exported matrix \\(\\mathbf{A}\\) is the observation matrix \\(\\partial N / \\partial \\rho\\) of the standard MAP (maximum a posteriori) reconstruction. The full estimator, including the covariance terms, is described in Inversion:</p> \\[\\vec{\\rho}' = \\vec{\\rho}_0 + \\left(\\mathbf{A}^T \\mathbf{C}_N^{-1} \\mathbf{A} + \\mathbf{C}_\\rho^{-1}\\right)^{-1} \\mathbf{A}^T \\mathbf{C}_N^{-1} \\left(\\vec{N}_{\\text{obs}} - \\vec{N}_0\\right)\\] <p>Here \\(\\mathbf{A}\\) maps a voxel density vector to expected muon counts, \\(\\mathbf{C}_N\\) is the observation (count) covariance, and \\(\\mathbf{C}_\\rho\\) is the prior density covariance. An external tool is free to ignore this estimator entirely and apply any other method to the same \\(\\mathbf{A}\\) and observations.</p>"},{"location":"user-guide/external-reconstruction.html#demo-demo_recon_nnlspy","title":"Demo: <code>demo_recon_nnls.py</code>","text":"<p><code>demo_recon_nnls.py</code> is a standalone, NagaInv-free reconstruction driven only by <code>mat/manifest.json</code>. It demonstrates that the export is enough to run an alternative solver \u2014 here, non-negative regularized least squares via SciPy.</p> <pre><code>python3 scripts/demo_recon_nnls.py &lt;mat_dir&gt; [--out DIR] [--lams 0.01,0.1,1.0]\n</code></pre> <p>The script:</p> <ol> <li>Loads <code>mat/manifest.json</code> and sanity-checks every <code>.bin</code> it lists (header shape    against the manifest, file size, and a scan for <code>NaN</code>/<code>Inf</code>).</li> <li>Builds a synthetic ground truth from the column table: a uniform background density    with one low-density block placed by voxel index.</li> <li>Synthesizes observed counts \\(\\vec{b} = \\mathbf{A}\\,\\vec{x}_{\\text{true}}\\) using the    central matrix.</li> <li>Solves non-negative regularized least squares    \\(\\min_{\\vec{x} \\ge 0} \\lVert \\mathbf{A}\\vec{x} - \\vec{b} \\rVert^2 + \\lambda \\lVert \\vec{x} - \\vec{x}_{\\text{bg}} \\rVert^2\\)    for each \\(\\lambda\\) in <code>--lams</code>, and keeps the result with the smallest relative error.</li> </ol> <p>It writes a PNG of the reconstructed slice through the block and a GIF scanning all \\(z\\) slices to <code>--out</code> (default: <code>&lt;mat_dir&gt;/../demo_recon_nnls</code>). It requires NumPy, SciPy, and Matplotlib.</p> <p>Concrete example (using the <code>mat/</code> directory produced by a run):</p> <pre><code>python3 scripts/demo_recon_nnls.py path/to/run_output/mat\n</code></pre>"},{"location":"user-guide/input-data.html","title":"Input Data","text":""},{"location":"user-guide/input-data.html#input-data","title":"Input Data","text":"<p>This page describes the input data files required by MUONITH and their formats.</p>"},{"location":"user-guide/input-data.html#required-inputs","title":"Required Inputs","text":"Input Format Description DEM file <code>.g2zbin</code> (binary) Digital Elevation Model \u2014 terrain surface Flux tables <code>.g2zbin</code> (binary) Muon flux and flux derivative tables Detector parameter files <code>.json5</code> One per detector panel Main configuration <code>.json5</code> Pipeline parameters"},{"location":"user-guide/input-data.html#dem-digital-elevation-model","title":"DEM (Digital Elevation Model)","text":"<p>The DEM defines the terrain surface that muons pass through.</p>"},{"location":"user-guide/input-data.html#format","title":"Format","text":"<p>Binary file (<code>.g2zbin</code>) \u2014 a compact, little-endian uniform-grid format. A fixed header (magic <code>\"G2ZBIN\\0\\0\"</code>, version, <code>z</code> precision, storage order) is followed by a <code>Grid2d</code> header that defines the x and y axes (name, bin count, min, max, interval), then a flat row-major array of <code>z</code> (elevation) values (float32 by default, or float64). Each point's x/y is reconstructed from the axes, so only <code>z</code> is stored. Conceptually it represents a regular grid of 3D points:</p> Quantity Stored? Type Unit Description x reconstructed from x-axis float/double m Easting coordinate y reconstructed from y-axis float/double m Northing coordinate z stored (row-major array) float32/float64 m Elevation (m a.s.l.) <p>The grid structure (axis ranges and interval) comes directly from the <code>.g2zbin</code> header. <code>tolerance_ratio</code> validates that the grid spacing stays uniform within the given relative tolerance.</p>"},{"location":"user-guide/input-data.html#configuration","title":"Configuration","text":"<pre><code>\"GRID2D_PILLAR_PARAMETERS\": {\n  \"path_dem\": \"dem/my_terrain.g2zbin\",\n  \"initial_uniform_density\": 2000,     // kg/m\u00b3\n  \"zmin\": 0.0,                          // Base elevation [m]\n  \"tf_shift_x\": true,                   // Shift to local coordinates\n  \"tf_shift_y\": true,\n  \"tolerance_ratio\": 0.01               // Grid spacing tolerance\n}\n</code></pre>"},{"location":"user-guide/input-data.html#coordinate-shift","title":"Coordinate Shift","text":"<p>When <code>tf_shift_x</code> and <code>tf_shift_y</code> are <code>true</code>, the grid origin is shifted so that the minimum x and y values become the grid origin. This is useful when DEM coordinates are in a geographic system with large absolute values (e.g., UTM coordinates).</p>"},{"location":"user-guide/input-data.html#preparing-a-dem","title":"Preparing a DEM","text":"<p>DEM data can be obtained from:</p> <ul> <li>National geographic surveys (GSI in Japan, USGS in the US)</li> <li>Satellite-based DEMs (SRTM, ALOS)</li> <li>Airborne LiDAR surveys</li> </ul> <p>The data must be converted to the <code>.g2zbin</code> binary format. See muonith-gsi-dem for a tool that converts GSI (Japan) DEM ZIP archives to <code>.g2zbin</code> format. The grid spacing determines the horizontal resolution of the terrain model.</p>"},{"location":"user-guide/input-data.html#muon-flux-tables","title":"Muon Flux Tables","text":"<p>MUONITH requires pre-computed muon flux tables for two purposes:</p> <ol> <li>Forward modeling: Predicting expected muon counts for a given density distribution</li> <li>Sensitivity matrix: Computing how muon counts change with density perturbations</li> </ol>"},{"location":"user-guide/input-data.html#required-tables","title":"Required Tables","text":"Table Content Axes Penetrating muon flux \\(\\log_{10} F\\) [1/(m\u00b2 sr s)] cos(\u03b8_zenith) \u00d7 density-length [kg/m\u00b2] Flux derivative dF/dR \\(dF/dR\\) density-length \u00d7 cos(\u03b8_zenith)"},{"location":"user-guide/input-data.html#configuration_1","title":"Configuration","text":"<p>Two flux table sections are required \u2014 one for the prior model and one for the observed (real) data. They often point to the same files:</p> <pre><code>\"FLUX_RANGE_DATA_TABLE_PRIOR\": {\n  \"pathin_log_peneflux\": \"fluxtable/costhz-kgm2-log10peneflux-allinone.g2zbin\",\n  \"pathin_dFdR_R_costhz\": \"fluxtable/g2_dFdR_R_costhz.g2zbin\",\n  \"tf_xcnt_peneflux\": true,\n  \"tf_ycnt_peneflux\": false,\n  \"tf_xcnt_dFdR\": true,\n  \"tf_ycnt_dFdR\": false\n},\n\n\"FLUX_RANGE_DATA_TABLE_REAL\": {\n  // Same format as PRIOR \u2014 can point to the same or different files\n}\n</code></pre> <p>The <code>tf_xcnt_*</code> and <code>tf_ycnt_*</code> flags control whether the lookup uses bin centers or bin edges for interpolation.</p>"},{"location":"user-guide/input-data.html#flux-models","title":"Flux Models","text":"<p>MUONITH ships with flux tables derived from the daemonflux model (combined with the Groom range-energy table), located in <code>fluxtable/daemon_groom/</code>. This is the default model for generated station configs. Tables based on the Honda model are kept in <code>fluxtable/honda_groom/</code> for comparison, but have no confirmed redistribution permission.</p> <p>The bundled <code>.g2zbin</code> files are not raw daemonflux samples; they are pre-computed tables already interpolated from the original data with a 2D cubic spline.</p> <p>To regenerate these tables from the source data, or to introduce another flux model or absorber material, see Making Penetrating Muon Flux Tables.</p>"},{"location":"user-guide/input-data.html#detector-parameter-files","title":"Detector Parameter Files","text":"<p>Each detector panel is configured in its own JSON5 file. See Parameter Files for the complete format.</p>"},{"location":"user-guide/input-data.html#key-parameters","title":"Key Parameters","text":"Parameter Unit Description <code>x</code>, <code>y</code>, <code>z</code> m Detector position <code>yaw_deg</code> degrees Viewing direction (0\u00b0 = East, 90\u00b0 = North) <code>nbinx</code>, <code>nbiny</code> \u2014 Angular binning (path-length computation elements \u2014 not the instrument's angular resolution, see Detector Model) <code>txmin</code>\u2013<code>txmax</code>, <code>tymin</code>\u2013<code>tymax</code> (unit depends on <code>angle_unit</code>) Angular range <code>length_hori</code>, <code>length_vert</code>, <code>length_dept</code> m Physical detector dimensions <code>n_unit</code> \u2014 Number of detector copies <code>days</code> days Exposure time"},{"location":"user-guide/input-data.html#detector-efficiency","title":"Detector Efficiency","text":"<p>When detector efficiency is modeled (<code>tf_eff_cn_diag</code>), each detector element carries, per angular bin <code>(tx, ty)</code>, a central efficiency <code>eff_cnt</code> and a band <code>[eff_low, eff_upp]</code>. Two input routes exist per detector: an analytic efficiency model (<code>path_eff_model</code>, JSON5, preferred) and a per-bin text table (<code>path_eff_table</code>, for measured or otherwise non-analytic efficiency). See Detector Efficiency for the file formats, the evaluation formulas, and the precedence rule.</p>"},{"location":"user-guide/input-data.html#detector-placement","title":"Detector Placement","text":"<p>Detectors should be placed where they have a clear line of sight to the target volume. Key considerations:</p> <ul> <li>Elevation: Detectors below the target observe more rock, but with lower muon flux</li> <li>Distance: Closer detectors have finer angular resolution but narrower coverage</li> <li>Multi-view: For 3D reconstruction, place detectors at different azimuthal angles around the target</li> </ul>"},{"location":"user-guide/input-data.html#observed-muon-count-data","title":"Observed Muon Count Data","text":"<p>For real-data analysis, observed muon counts are provided through the detector parameter framework. In synthetic tests, muon counts are generated internally by the forward model using the \"real\" flux table and the input density distribution.</p>"},{"location":"user-guide/input-data.html#directory-structure-example","title":"Directory Structure Example","text":"<p>A typical working directory:</p> <pre><code>work/my_site/\n\u251c\u2500\u2500 prm_rec.json5              # Main configuration\n\u251c\u2500\u2500 dem/\n\u2502   \u2514\u2500\u2500 my_terrain.g2zbin      # DEM data\n\u251c\u2500\u2500 detparams/\n\u2502   \u251c\u2500\u2500 det_00.json5           # Detector 0\n\u2502   \u251c\u2500\u2500 det_01.json5           # Detector 1\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 fluxtable/ \u2192 ../fluxtable  # Symlink to shared flux tables\n\u2514\u2500\u2500 logs/                      # Log output (auto-created)\n</code></pre>"},{"location":"user-guide/output.html","title":"Output","text":""},{"location":"user-guide/output.html#output","title":"Output","text":"<p>MUONITH produces several types of output files: cross-section density maps, log files, and optional binary checkpoints.</p>"},{"location":"user-guide/output.html#cross-section-files","title":"Cross-Section Files","text":"<p>The primary output consists of 2D density maps at specified z-elevations. These represent horizontal slices through the reconstructed 3D density volume.</p>"},{"location":"user-guide/output.html#z-range-configuration","title":"Z-Range Configuration","text":"<p>The output z-range is set in the <code>Z_CROSS_SECTION</code> section:</p> <pre><code>\"Z_CROSS_SECTION\": {\n  \"min\": 160.0,           // Lowest z-elevation [m]\n  \"max\": 400.0,           // Highest z-elevation [m]\n  \"zstep\": 40,            // Z-step in meters (0 or omitted = use z_interval)\n  \"output_binary\": false  // false = ASCII text (.tmp), true = binary (.tmpbin)\n}\n</code></pre> <p>Each slice is output at an elevation within this range, stepped by <code>zstep</code> meters. If <code>zstep</code> is 0 or omitted, the voxel grid's z_interval is used as the step size. All slices for one field are written into a single file. <code>output_binary</code> selects the content format and the extension: <code>false</code> (default) writes ASCII text as <code>&lt;field&gt;_zcross_all.tmp</code>, <code>true</code> writes binary as <code>&lt;field&gt;_zcross_all.tmpbin</code>. Exactly one file is written per field.</p>"},{"location":"user-guide/output.html#auto_plotjson5-generated-by-init_work_sitepy","title":"<code>auto_plot.json5</code> (generated by <code>init_work_site.py</code>)","text":"<p><code>scripts/init_work_site.py</code> writes an <code>auto_plot.json5</code> file in each site's working directory (e.g. <code>depth001/auto_plot.json5</code>, <code>swp001/auto_plot.json5</code>). The file drives <code>scripts/auto_plot.py</code> and controls which families of plots are produced and with what binning. Typical usage:</p> <pre><code>python3 ../../scripts/auto_plot.py --config auto_plot.json5\n</code></pre> <p>Hand-editing <code>auto_plot.json5</code> is how you change fixed-bin settings, exclude specific outputs, or disable entire plot families (hist2d / g3vox / g2bg) for a site without touching the reconstruction JSON5. See <code>auto_plot.py</code> for the full option list.</p> <p>Setting <code>det_exec: true</code> in <code>auto_plot.json5</code> enables a fourth, binary-direct plot family: <code>auto_plot.py</code> scans <code>det/arrdet_*.bin</code> and <code>checkpoint_*/det/arrdet_*.bin</code> (in the working directory and under <code>tmp/</code>, where the sweep driver writes its checkpoint bundles) and runs <code>plot_det_arrdet.py</code> on each, reproducing the standard txty/g2bg figures without the <code>tmp/*.tmp</code> text exports. The built-in default is <code>false</code>: when the text exports are present, enabling it would produce the same figures twice. The swp001 <code>auto_plot.json5</code> generated by <code>init_work_site.py</code> sets <code>det_exec: true</code>, because the generated swp001 <code>prm_muonith.json5</code> disables the text exports (<code>tf_out_txty_ascii</code> / <code>tf_out_g2bg_ascii</code> false). Since PIPELINE_VERSION 7 the checkpoints also carry the group efficiencies, so the <code>eff_*</code> figures are produced on this path as well (checkpoints older than v7 cannot be read \u2014 regenerate them with the current code). The <code>det</code> section of <code>auto_plot.json5</code> controls where the figures go and how they are drawn: <code>out_dir</code> (relative paths are anchored at the config's folder; the swp001 template sets <code>\"figs\"</code>, so the figures land in the work dir's <code>figs/</code>; unset = <code>plot_det_arrdet/</code> next to the binary) and <code>n_jobs</code> (drawing workers inside <code>plot_det_arrdet.py</code>: positive = that many, 1 = one by one, 0 = all CPU cores, negative = leave that many cores free; default -2).</p>"},{"location":"user-guide/output.html#output-types","title":"Output Types","text":"<p>For each inversion configuration, the following cross-section sets are generated:</p> Output Content Unit Reconstruction density Inverted density field \\(\\vec{\\rho}'\\) kg/m\u00b3 Delta prior \\(\\vec{\\rho}' - \\vec{\\rho}_0\\) (change from prior) kg/m\u00b3 Difference from input \\(\\vec{\\rho}' - \\vec{\\rho}_{\\text{true}}\\) (error, synthetic tests only) kg/m\u00b3 Density uncertainty \\(\\sqrt{\\text{diag}(\\mathbf{C}_{\\rho'})}\\) (posterior standard deviation) kg/m\u00b3"},{"location":"user-guide/output.html#file-format","title":"File Format","text":"<p>Cross-section files contain (x, y, value) data on the voxel grid:</p> <pre><code># Header information (grid dimensions, z-elevation, etc.)\nx1  y1  density1\nx2  y1  density2\n...\n</code></pre> <p>ASCII cross-section files use the <code>.tmp</code> extension (e.g. <code>g3vox_rec_center_00_zcross_all.tmp</code>); binary ones use <code>.tmpbin</code> (e.g. <code>g3vox_rec_center_00_zcross_all.tmpbin</code>). The example above shows the ASCII layout (<code>output_binary: false</code>).</p>"},{"location":"user-guide/output.html#analyze-errors-and-output-module-8","title":"Analyze Errors and Output (Module 8)","text":"<p>When Analyze Errors (Module 8) runs, additional outputs are generated:</p>"},{"location":"user-guide/output.html#leave-one-out-results","title":"Leave-One-Out Results","text":"<p>For each detector in the array, Analyze Errors (Module 8) re-runs the inversion with that detector removed (leave-one-out). Each output file is named <code>{name}_disable_NN_zcross_all.tmp</code>, where <code>{name}</code> is the inversion run name and <code>NN</code> is the iteration index (<code>00</code>, <code>01</code>, ...; iteration order, not necessarily detector ID order):</p> Output Description <code>{name}_disable_NN</code> Reconstruction with detector NN removed <code>{name}_disable_NN_delta_prior</code> Delta-prior for that leave-one-out configuration <code>{name}_disable_NN_diff_from_all</code> Difference from the all-detector reconstruction (written only when a reference reconstruction is provided)"},{"location":"user-guide/output.html#error-components","title":"Error Components","text":"Component Description Statistical error From the posterior covariance diagonal Prior error Difference between lower/upper prior reconstructions Leave-one-out sensitivity Maximum deviation when any single detector is removed Combined error Root-sum-square of all components <p>Each error component is written as a cross-section file:</p> File Error component <code>g3vox_stat_error_zcross_all.tmp</code> Statistical <code>g3vox_prior_error_zcross_all.tmp</code> Prior <code>g3vox_disabled_error_zcross_all.tmp</code> Leave-one-out <code>g3vox_mixed_error_zcross_all.tmp</code> Combined <p>The figure below shows three of these error maps for a synthetic 11-detector example. Columns are the error type \u2014 statistical (left), leave-one-out (center), and combined (right); rows are five elevation slices. The color scale is density error in kg/m\u00b3. Error is smallest where viewing directions cross densely (center of the array) and grows toward the sparsely-viewed edges.</p> <p></p>"},{"location":"user-guide/output.html#log-files","title":"Log Files","text":"<p>Log files are written to the directory specified by <code>LOG_FILE.path_log_dir</code> (default: <code>logs/</code>).</p> Level Content <code>trace</code> Detailed execution flow, matrix sizes, timing <code>debug</code> Parameter values, intermediate results <code>info</code> Progress messages, module start/completion <code>warn</code> Non-critical issues (e.g., low muon counts) <code>error</code> Fatal errors <p>Log levels are independently configurable for stdout, stderr, and file output.</p>"},{"location":"user-guide/output.html#binary-checkpoints","title":"Binary Checkpoints","text":"<p>Intermediate results can be saved for reuse via <code>tf_save_*</code> flags in <code>PATH_LENGTH_PARAMETERS</code>:</p> Checkpoint Flag Description Detector array (2D) <code>tf_save_arrdet_g2pil</code> DetectorPanelArray after 2D ray tracing Detector array (3D) <code>tf_save_arrdet_g3vox</code> DetectorPanelArray after 3D ray tracing Path-length matrices <code>path_vec_spmat_PL_bin</code> Sparse matrices per detector Observation matrix <code>tf_save_bin_obs_mat_dNdD</code> dN/dD sensitivity matrix <p>On subsequent runs, set the corresponding <code>tf_load_*</code> flag to <code>true</code> to skip the computation and load from the saved file.</p> <p>Tip</p> <p>When sweeping inversion parameters (<code>sigma_rho</code>, <code>corr_length</code>), save the Trace Path Lengths (Module 4) outputs and load them for each sweep iteration. Only Invert Density (Module 7) needs to be re-executed.</p> <p>The observation matrix is additionally exported to a self-describing <code>mat/</code> directory (matrix binaries plus <code>manifest.json</code>) that an external tool can consume without the C++ code. See External Reconstruction.</p>"},{"location":"user-guide/output.html#interpreting-results","title":"Interpreting Results","text":""},{"location":"user-guide/output.html#reconstruction-quality-indicators","title":"Reconstruction Quality Indicators","text":"<ul> <li>Delta prior near zero: The reconstruction is close to the prior \u2014 this may indicate insufficient data or too-strong regularization</li> <li>Large uncertainty: The density at that voxel is poorly constrained \u2014 consider adding more detectors or increasing exposure time</li> <li>Leave-one-out sensitivity: Voxels sensitive to a single detector suggest that more viewing angles are needed for robust reconstruction</li> </ul>"},{"location":"user-guide/output.html#density-values","title":"Density Values","text":"<ul> <li>Internal computations use kg/m\u00b3</li> <li>Typical rock density: 2000\u20132700 kg/m\u00b3</li> <li>Low-density anomalies (cavities, fractures): below 2000 kg/m\u00b3</li> <li>High-density anomalies (mineralization, magma): above 2700 kg/m\u00b3</li> </ul>"},{"location":"user-guide/output.html#visualization","title":"Visualization","text":"<p>Cross-section files can be visualized with standard tools:</p> <ul> <li>Python: matplotlib (<code>pcolormesh</code>, <code>contourf</code>)</li> <li>GMT (Generic Mapping Tools): <code>gmt surface</code>, <code>gmt grdimage</code></li> <li>ParaView: For 3D volume rendering from stacked cross-sections</li> </ul>"},{"location":"user-guide/output.html#showa-shinzan-reference-reconstruction","title":"Showa-shinzan reference reconstruction","text":"<p>The animated GIFs below show horizontal cross-sections sweeping through elevations for a Showa-shinzan reference case (<code>work/showa/rec001/</code>). Star markers indicate the 13 detector positions surrounding the lava dome.</p> Input (ground truth) Reconstruction (MAP) <p>The synthetic low-density anomaly embedded at the summit is recovered by the inversion. This is an advanced reference case, not the default first-run sample; the default runnable sample is <code>tutorial</code> in the Quick Start.</p>"},{"location":"user-guide/parameter-files.html","title":"Parameter Files","text":""},{"location":"user-guide/parameter-files.html#parameter-files","title":"Parameter Files","text":"<p>MUONITH configuration files use the <code>.json5</code> extension and are parsed by nlohmann/json with comment stripping enabled (<code>ignore_comments</code>). The parser accepts standard JSON plus <code>//</code> line comments and <code>/* */</code> block comments. Trailing commas and unquoted keys are not supported: every key must be double-quoted, and no comma may follow the last element of an array or object.</p>"},{"location":"user-guide/parameter-files.html#file-format","title":"File Format","text":"<pre><code>// This is a comment\n{\n  \"section_name\": {\n    \"key\": \"value\",     // inline comment\n    \"number\": 42,\n    \"flag\": true        // no comma after the last element\n  }\n}\n</code></pre> <ul> <li>Extension: <code>.json5</code></li> <li>Comments: <code>//</code> line comments and <code>/* */</code> block comments</li> <li>Trailing commas: Not supported (standard JSON syntax)</li> <li>Keys: Must be double-quoted (unquoted keys are not supported)</li> </ul>"},{"location":"user-guide/parameter-files.html#configuration-structure","title":"Configuration Structure","text":"<p>A MUONITH configuration file contains multiple sections. Here is a minimal example:</p> <pre><code>{\n  \"seed\": 42,                          // Random seed for reproducibility (CLI -s overrides)\n\n  \"LOG_FILE\": {\n    \"path_log_dir\": \"logs\",            // Directory for log file output\n    \"log_level\": {\n      \"stdout_level\": \"info\",          // Console output: trace/debug/info/warn/error\n      \"stderr_level\": \"error\",         // Error stream level\n      \"file_level\": \"trace\"            // Log file level (most verbose)\n    }\n  },\n\n  \"DETECTOR_PARAMETER_LISTS\": {\n    \"name\": \"my_detectors\",            // Instance name (arbitrary label)\n    \"det_files\": [                     // Per-detector JSON5 files (relative paths)\n      \"detparams/det_00.json5\",\n      \"detparams/det_01.json5\"\n    ]\n  },\n\n  \"GRID2D_PILLAR_PARAMETERS\": {\n    \"name\": \"terrain\",                 // Instance name\n    \"initial_uniform_density\": 2000,   // Uniform rock density [kg/m\u00b3]\n    \"path_dem\": \"dem/my_terrain.g2zbin\" // Path to DEM binary file\n  },\n\n  \"GRID3D_VOXEL_PARAMETERS\": {\n    \"tf_build_g3vox\": true,            // true = build 3D grid; false = 2D analysis only\n    \"name\": \"voxel_grid\",              // Instance name\n    \"use_grid2d_of_g2pil\": true,       // Reuse DEM's x/y axes for voxel grid\n    \"zmin\": 100,                       // Bottom of voxel grid [m a.s.l.]\n    \"zmax\": 500,                       // Top of voxel grid [m a.s.l.]\n    \"z_pitch\": 10                      // Vertical voxel spacing [m]\n  },\n\n  \"FLUX_RANGE_DATA_TABLE_PRIOR\": { ... },  // Muon flux table for prior model\n  \"FLUX_RANGE_DATA_TABLE_REAL\": { ... },   // Muon flux table for observed data\n\n  \"NAGAINV_PARAMETERS\": [ { ... } ],  // Array of inversion configs (sigma_rho, corr_length, etc.)\n\n  \"Z_CROSS_SECTION\": {\n    \"min\": 100.0,                      // Lowest z-elevation for output cross-sections [m]\n    \"max\": 500.0                       // Highest z-elevation for output cross-sections [m]\n  }\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#sections-reference","title":"Sections Reference","text":""},{"location":"user-guide/parameter-files.html#seed-optional","title":"<code>seed</code> (optional)","text":"<p>Random seed for reproducibility. CLI argument (<code>-s</code>) overrides this value.</p> <pre><code>\"seed\": 42  // Reproducibility seed; CLI -s overrides this value\n</code></pre>"},{"location":"user-guide/parameter-files.html#log_file","title":"<code>LOG_FILE</code>","text":"<p>Controls logging output.</p> <pre><code>\"LOG_FILE\": {\n  \"path_log_dir\": \"logs\",         // Output directory for log files\n  \"give_new_number\": false,       // true = auto-number logs (log_00, log_01, ...)\n  \"log_level\": {\n    \"stdout_level\": \"debug\",      // Console verbosity: trace/debug/info/warn/error\n    \"stderr_level\": \"error\",      // Error stream verbosity\n    \"file_level\": \"trace\"         // File verbosity (trace = most verbose)\n  },\n  \"default_max_count\": 50,        // Max number of log files to keep\n  \"archive_existing\": false       // true = archive old logs before overwriting\n}\n</code></pre> <p>Log levels (from most to least verbose): <code>trace</code>, <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>.</p>"},{"location":"user-guide/parameter-files.html#end_stage-optional-top-level","title":"<code>end_stage</code> (optional, top-level)","text":"<p>Controls how far the pipeline executes.</p> <pre><code>// Valid values: 3, 4, 5, 6, 7, 8 (default: 8 = all modules)\n// CLI --end-stage takes precedence over this value.\n\"end_stage\": 7\n</code></pre>"},{"location":"user-guide/parameter-files.html#detector_parameter_lists","title":"<code>DETECTOR_PARAMETER_LISTS</code>","text":"<p>Specifies the detector array configuration.</p> <pre><code>\"DETECTOR_PARAMETER_LISTS\": {\n  \"name\": \"arrdet_02msq120days\",   // Instance name (arbitrary label)\n  \"det_files\": [                   // List of per-detector JSON5 files (relative paths)\n    \"../detparams/det_00.json5\",\n    \"../detparams/det_01.json5\",\n    \"../detparams/det_02.json5\"\n  ],\n  \"tf_apply_eff\": false,           // true = apply detector efficiency table\n  // false stops the txty ASCII dumps under tmp/. With the current swp001 settings the\n  // per-element PL / density / signal figures are plotted from the saved binary,\n  // so no figure is lost.\n  \"tf_out_txty_ascii\": true,\n  // false stops the g2bg ASCII dumps under tmp/. With the current swp001 settings the\n  // efficiency figures are plotted from the saved binary, so no figure is lost.\n  \"tf_out_g2bg_ascii\": true,\n  // true saves det/arrdet_g3vox_prior&lt;suffix&gt;.bin (about 160 MB each, three files\n  // when tf_prior_error is true). Needed to plot the prior array from the binary.\n  \"tf_save_arrdet_prior\": false\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#grid2d_pillar_parameters","title":"<code>GRID2D_PILLAR_PARAMETERS</code>","text":"<p>Defines the terrain (DEM) grid.</p> <pre><code>\"GRID2D_PILLAR_PARAMETERS\": {\n  \"name\": \"showa01_2x2km_2000kgm3\", // Instance name\n  \"initial_uniform_density\": 2000,   // Uniform initial rock density [kg/m\u00b3]\n  \"path_dem\": \"../dem/showa01_2x2km.g2zbin\", // Path to DEM binary file\n  \"zmin\": 0.0,                       // Pillar base elevation [m a.s.l.]\n  \"tf_shift_x\": true,                // true = shift x origin to xmin (useful for UTM coords)\n  \"tf_shift_y\": true,                // true = shift y origin to ymin\n  \"tolerance_ratio\": 0.01,           // Allowed relative error in DEM grid spacing\n  \"vertical_dike_params\": [],        // Optional: vertical dike density structures\n  \"vertical_cylinder_params\": [],    // Optional: vertical cylinder density structures\n  \"vertical_checkerboard_params\": [] // Optional: vertical checkerboard density structures\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#grid3d_voxel_parameters","title":"<code>GRID3D_VOXEL_PARAMETERS</code>","text":"<p>Defines the 3D voxel grid for inversion.</p> <p></p> <pre><code>\"GRID3D_VOXEL_PARAMETERS\": {\n  \"tf_build_g3vox\": true,         // true = build 3D grid; false = 2D analysis only\n  \"name\": \"g3vox_5m_2000kgm3\",   // Instance name\n  \"use_grid2d_of_g2pil\": true,    // true = reuse DEM's x/y grid axes\n  \"zmin\": 160,                    // Bottom of voxel grid [m a.s.l.]\n  \"zmax\": 400,                    // Top of voxel grid [m a.s.l.]\n  \"z_pitch\": 5,                   // Vertical voxel size [m]\n  \"n_hit_det_min\": 3,             // Min detectors hitting a voxel to include in inversion\n  \"n_hit_det_max\": 2147483647,    // Max detectors hitting a voxel (default: INT_MAX)\n  \"n_hit_ele_min\": 0,             // Min detector elements hitting a voxel\n  \"n_hit_ele_max\": 2147483647,    // Max detector elements hitting a voxel (default: INT_MAX)\n\n  // Optional: density anomalies for synthetic tests\n  \"checkerboard_3d_params\": [\n    // Extent is specified via total cell counts (xlen_cells, ylen_cells, zlen_cells).\n    // Direct xmin/xmax/ymin/ymax/zmin/zmax specification is deprecated.\n    // The AABB is computed symmetrically from the (snapped) center:\n    //   min = cnt_snapped \u2212 len_cells \u00d7 0.5 \u00d7 cell_size\n    //   max = cnt_snapped + len_cells \u00d7 0.5 \u00d7 cell_size\n    //   where cell_size = interval \u00d7 len_interval_mult\n    //   Total cells per axis = len_cells\n    {\n      \"tf_exec\": true,            // Enable this checkerboard\n      \"name\": \"cb_3d_500\",        // Checkerboard label\n      \"delta_density_offset\": 0.0, // Density offset [kg/m\u00b3] (applied before delta_density)\n      \"delta_density\": 500,       // Density perturbation [kg/m\u00b3]\n      \"xlen_cells\": 6,            // Total number of checkerboard cells in x\n      \"ylen_cells\": 6,            // Total number of checkerboard cells in y\n      \"zlen_cells\": 4,            // Total number of checkerboard cells in z\n      \"xcnt\": -71565.3, \"ycnt\": -145112.5, \"zcnt\": 990, // Pattern center [m]\n      \"xlen_interval_mult\": 5,    // Block size = voxel_x_interval \u00d7 5\n      \"ylen_interval_mult\": 5,\n      \"zlen_interval_mult\": 5,\n      \"tf_snap_to_grid\": true     // Snap block boundaries to grid lines\n    }\n  ],\n\n  // Optional: ellipsoidal density anomaly for synthetic tests\n  \"ellipsoid_params\": [\n    {\n      \"tf_exec\": true,            // Enable this ellipsoid\n      \"name\": \"ell_low_density\",  // Ellipsoid label\n      \"delta_density\": -300,      // Density perturbation [kg/m\u00b3]\n      \"xcnt\": 50500, \"ycnt\": -161500, \"zcnt\": 280, // Center position [m]\n      \"xlen\": 100, \"ylen\": 80, \"zlen\": 60, // Semi-axis lengths [m]\n      \"theta_x_deg\": 0.0,        // Rotation around x-axis [degrees]\n      \"theta_y_deg\": 0.0,        // Rotation around y-axis [degrees]\n      \"theta_z_deg\": 30.0,       // Rotation around z-axis [degrees]\n      \"rotation_type\": \"LOCAL\"    // \"LOCAL\" (intrinsic) or \"GLOBAL\" (extrinsic)\n    }\n  ],\n\n  // Optional: cylindrical density anomaly for synthetic tests\n  \"cylinder_params\": [\n    {\n      \"tf_exec\": true,            // Enable this cylinder\n      \"name\": \"cyl_conduit\",     // Cylinder label\n      \"delta_density\": -500,      // Density perturbation [kg/m\u00b3]\n      \"xcnt\": 50400, \"ycnt\": -161600, \"zcnt\": 300, // Center position [m]\n      \"xlen\": 50, \"ylen\": 50, \"zlen\": 200, // x/y = cross-section semi-axes, z = height [m]\n      \"theta_x_deg\": 0.0,        // Rotation around x-axis [degrees]\n      \"theta_y_deg\": 0.0,        // Rotation around y-axis [degrees]\n      \"theta_z_deg\": 0.0,        // Rotation around z-axis [degrees]\n      \"rotation_type\": \"LOCAL\"    // \"LOCAL\" (intrinsic) or \"GLOBAL\" (extrinsic)\n    }\n  ],\n\n  // Voxel merging: reduce resolution by grouping adjacent voxels\n  \"merge_params\": {\n    \"tf_exec\": true,              // true = perform merge\n    \"name\": \"merge_01\",           // Merge config label\n    \"x_merge_center\": 50500,      // Merge alignment center x [m]\n    \"y_merge_center\": -161720,    // Merge alignment center y [m]\n    \"z_merge_center\": 300.0,      // Merge alignment center z [m]\n    \"x_merge_factor\": 4,          // Merge 4 bins \u2192 1 in x direction\n    \"y_merge_factor\": 4,          // Merge 4 bins \u2192 1 in y direction\n    \"z_merge_factor\": 4           // Merge 4 bins \u2192 1 in z direction\n  },\n\n  // Reconstruction sub-volume (sizes in METERS, not voxel counts).\n  // AABB and cylinder can be combined; when neither is enabled the whole voxel grid is used.\n  \"reconst_voxels\": {\n    \"tf_aabb\": true,              // true = enable AABB region\n    \"x_aabb_cnt\": 50500,          // AABB center x [m]\n    \"y_aabb_cnt\": -161720,        // AABB center y [m]\n    \"x_aabb_meters\": 800,         // Full AABB width in x [m] (half-extent = x_aabb_meters * 0.5)\n    \"y_aabb_meters\": 800,         // Full AABB width in y [m]\n    \"aabb_zmin_mode\": \"g3vox_zmin\", // \"g3vox_zmin\" (default) | \"manual\"\n    \"aabb_zmin_value\": 160,       // Used only when aabb_zmin_mode = \"manual\"\n    \"aabb_zmax\": 400,             // Terrain surface clips this when the surface is lower\n\n    \"tf_cylinder\": false,         // true = enable elliptical-cylinder region\n    \"x_cyl_cnt\": 50500,\n    \"y_cyl_cnt\": -161720,\n    \"cylinder_radius_x_meters\": 400, // Semi-axis in x [m]; must be &gt; 0 when tf_cylinder=true\n    \"cylinder_radius_y_meters\": 400  // Semi-axis in y [m]\n  }\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#flux_range_data_table_prior-flux_range_data_table_real","title":"<code>FLUX_RANGE_DATA_TABLE_PRIOR</code> / <code>FLUX_RANGE_DATA_TABLE_REAL</code>","text":"<p>Muon flux lookup tables. Both sections have the same format. They often point to the same files.</p> <pre><code>\"FLUX_RANGE_DATA_TABLE_PRIOR\": {\n  \"pathin_log_peneflux\": \"../fluxtable/costhz-kgm2-log10peneflux-allinone.g2zbin\",\n                                   // log10(penetrating muon flux) table [1/(m\u00b2 sr s)]\n  \"tf_xcnt_peneflux\": true,        // true = use bin center for cos(\u03b8z); false = use bin edge\n  \"tf_ycnt_peneflux\": false,       // true = use bin center for DL; false = use bin edge\n  \"pathin_dFdR_R_costhz\": \"../fluxtable/g2_dFdR_R_costhz.g2zbin\",\n                                   // dF/dR flux derivative table\n  \"tf_xcnt_dFdR\": true,            // true = use bin center for cos(\u03b8z)\n  \"tf_ycnt_dFdR\": false            // true = use bin center for range\n},\n\n\"FLUX_RANGE_DATA_TABLE_REAL\": {\n  // Same format as PRIOR \u2014 can point to the same or different files\n  \"pathin_log_peneflux\": \"../fluxtable/costhz-kgm2-log10peneflux-allinone.g2zbin\",\n  \"tf_xcnt_peneflux\": true,\n  \"tf_ycnt_peneflux\": false,\n  \"pathin_dFdR_R_costhz\": \"../fluxtable/g2_dFdR_R_costhz.g2zbin\",\n  \"tf_xcnt_dFdR\": true,\n  \"tf_ycnt_dFdR\": false\n}\n</code></pre> <p>Selecting a flux model with <code>flux_groom</code></p> <p>Station configs generated by <code>scripts/init_work_site.py</code> fill these two paths from the <code>flux_groom</code> key in <code>param_sites/&lt;site&gt;.json5</code> (default: <code>daemon_groom</code>), pointing to <code>fluxtable/&lt;flux_groom&gt;/costhz-kgm2-log10peneflux-allinone.g2zbin</code> and <code>fluxtable/&lt;flux_groom&gt;/g2_dFdR_R_costhz.g2zbin</code>. To switch flux models, place the two tables under <code>fluxtable/&lt;name&gt;/</code> with those canonical names and set <code>\"flux_groom\": \"&lt;name&gt;\"</code>. See Making Penetrating Muon Flux Tables.</p>"},{"location":"user-guide/parameter-files.html#path_length_parameters","title":"<code>PATH_LENGTH_PARAMETERS</code>","text":"<p>Controls ray tracing and path length computation.</p> <pre><code>\"PATH_LENGTH_PARAMETERS\": {\n  \"name\": \"range2000_pitch1\",      // Instance name\n  \"tf_add_PLDL\": true,            // true = compute path length (PL) and density length (DL)\n  \"tf_incr_nhit_det\": true,       // true = count how many detectors hit each voxel\n  \"tf_incr_nhit_ele\": true,       // true = count how many elements hit each voxel\n  \"tf_add_shell\": true,           // true = include upper/lower shell in path calculation\n  \"BL_max\": 1500,                 // Max beam length warning threshold [kg/m\u00b2]\n  \"reference_matPL_sparse\": 1,    // Reference value for sparse matrix zero-threshold\n  \"epsilon_matPL_sparse\": 0.0001, // Elements &lt; reference \u00d7 epsilon are set to zero\n\n  // Binary I/O: save/load intermediate results to avoid recomputation\n  \"tf_load_arrdet_g2pil\": false,   // true = load 2D detector array from binary\n  \"tf_save_arrdet_g2pil\": false,   // true = save 2D detector array to binary\n  \"path_arrdet_g2pil_bin\": \"arrdet_g2pil.tmp.bin\", // File path for 2D array binary\n\n  \"tf_load_arrdet_g3vox\": false,   // true = load 3D detector array from binary\n  \"tf_save_arrdet_g3vox\": false,   // true = save 3D detector array to binary\n  \"path_arrdet_g3vox_bin\": \"arrdet_g3vox.tmp.bin\", // File path for 3D array binary\n  \"path_vec_spmat_PL_bin\": \"vec_spmat_PL.tmp.bin\", // File path for sparse PL matrices\n\n  \"tf_load_bin_obs_mat_dNdD\": false, // true = load observation matrix from binary\n  \"tf_save_bin_obs_mat_dNdD\": false, // true = save observation matrix to binary\n  \"path_bin_obs_mat_dNdD\": \"obs_mat_dNdD.tmp.bin\"  // File path for dN/dD matrix\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#bin_group_parameters","title":"<code>BIN_GROUP_PARAMETERS</code>","text":"<p>Controls angular bin grouping for signal-to-noise optimization. Bins with insufficient statistics are merged into groups.</p> <pre><code>\"BIN_GROUP_PARAMETERS\": {\n  \"name\": \"prm_bingroup_g2pil\",   // Instance name\n  \"signal_init\": 0,               // Initial signal value for bins\n  \"noise_init\": 0,                // Initial noise value for bins\n  \"is_avail_init\": true,          // Initial availability flag for bins\n  \"PL_thres\": 0.0,                // Min path length [m]; bins below this are disabled\n  \"DL_thres\": 10000,              // Max density length [kg/m\u00b2]; bins above this are disabled\n  \"is_avail_under_thres\": false,  // Availability for bins below PL_thres\n  \"signal_under_thres\": -1.0,     // Signal value for disabled bins (-1 = marker)\n  \"noise_under_thres\": -1.0,      // Noise value for disabled bins\n  \"tf_run_1st_grouping\": true,    // true = run initial grouping pass\n  \"tf_run_auto_grouping\": true,   // true = run adaptive subdivision loop\n  \"igroup_start\": 0,              // Starting group ID\n  \"nx_div_init\": 4,               // Initial horizontal divisions\n  \"ny_div_init\": 2,               // Initial vertical divisions\n  \"signal_noise_group_trig\": 100, // S/N threshold; groups above this are subdivided\n  \"ixlen_min\": 5,                 // Min group width in bins (stops subdivision)\n  \"iylen_min\": 5,                 // Min group height in bins (stops subdivision)\n  \"tf_prefer_split_x\": false,     // true = prefer horizontal splits over vertical\n  \"nloop_limit\": 10000            // Max subdivision iterations\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#manual-bin-grouping","title":"Manual bin grouping","text":"<p>To specify groups yourself instead of the automatic subdivision, disable both grouping passes and provide one rectangle-list file per detector:</p> <pre><code>\"BIN_GROUP_PARAMETERS\": {\n  // ... same keys as above ...\n  \"tf_run_1st_grouping\": false,   // both passes must be false\n  \"tf_run_auto_grouping\": false,  //   to enable the manual route\n  \"n_detector_grouping_manual\": 2,                  // number of detectors with a manual list\n  \"vec_tf_read_bin_group_list\": [true, true],       // one flag per detector\n  \"vec_file_path_bin_group_list\": [\"bin_group_list.txt\", \"bin_group_list.txt\"] // one path per detector\n}\n</code></pre> <p>Each list file has one rectangle (= one group) per line, <code>xlow xup ylow yup</code>, in the panel's <code>angle_unit</code> (default: tangent). For example, four quadrants covering tx in [-1.6, 1.6] and ty in [0, 1.6]:</p> <pre><code>-1.6 0.0 0.0 0.8\n0.0 1.6 0.0 0.8\n-1.6 0.0 0.8 1.6\n0.0 1.6 0.8 1.6\n</code></pre> <p>The rectangles must tile the panel's field of view without gaps, and two group rectangles must not overlap; every bin center must fall inside a rectangle. Violations stop the run with an error naming the offending rectangles or bin.</p> <p>To leave part of the field of view out of the analysis on purpose, declare it with the <code>exclude</code> keyword instead of simply omitting it:</p> <pre><code>exclude -1.6 1.6 0.0 0.05\n-1.6 0.0 0.05 0.8\n0.0 1.6 0.05 0.8\n-1.6 0.0 0.8 1.6\n0.0 1.6 0.8 1.6\n</code></pre> <p>A declared region counts as tiled, so it does not trigger the gap check. The bins inside it belong to no group at all, so they appear in no group export or figure and contribute to no row of the inversion matrix. Omitting the same band without the keyword still stops the run \u2014 that is what separates a deliberate exclusion from a rectangle you forgot to write.</p> <p>A group rectangle overlapping a declared region is not an error: the rectangle is dropped whole at load time and its bins count as excluded too. Because the whole rectangle disappears, the removed area can be wider than the declared band \u2014 in the example above you could keep the two lower rectangles at <code>ylow</code> = 0.0, but both would then be dropped and the whole lower half would vanish. With a fine grid the drop removes just the touching row, so a plain full-grid list plus one <code>exclude</code> line is the easiest way to cut a band off the bottom of the field of view. Every drop is reported in the log with the number of dropped rectangles. Overlaps between two group rectangles still stop the run.</p> <p>Blank lines and lines starting with <code>#</code> are skipped. Any other leading token, <code>EXCLUDE</code> included, stops the run and names the offending line number, so a malformed file is never read only part-way.</p> <p>To verify a configuration, run the pipeline through detector construction only, e.g. <code>bash run_prg.sh true 42 4</code>, and check the log for <code>Loaded RectAngularBinGroup from &lt;file&gt; (nbin=N, nbin_exclude=M, nbin_dropped=K)</code> lines \u2014 <code>N</code> is the number of surviving group rectangles, <code>M</code> the number of <code>exclude</code> lines, and <code>K</code> the number of rectangles dropped for overlapping an excluded region (each drop is also announced as <code>dropped K group rectangle(s) overlapping M excluded region(s) ...</code>). Excluded bins are reported once per detector as <code>... bins excluded on purpose by M exclude region(s) ...</code>. See Parameter Reference \u2014 BIN_GROUP_PARAMETERS for the key list and file format.</p> <p>A ready-made example ships with the tutorial station. Adding <code>manual_bin_group_file</code> to a <code>swp001_runs</code> entry in <code>param_sites/&lt;site&gt;.json5</code> makes <code>setup_station.sh</code> generate that run with the manual route enabled and copy the named list file from <code>scripts/templates/</code> into the run directory:</p> <pre><code>bash setup_station.sh tutorial\ncd work/tutorial/manual-bin-merge-rec3d\nbash run_prg.sh true 42\n</code></pre> <p>This run uses <code>bin_group_list_16x8.txt</code> (128 rectangles: tx in [-1.6, 1.6], ty in [0, 1.6], 0.2 pitch) and runs through 3-D reconstruction. Check the log for <code>Loaded RectAngularBinGroup from bin_group_list_16x8.txt (nbin=128)</code> \u2014 one line per detector.</p>"},{"location":"user-guide/parameter-files.html#noise_parameters","title":"<code>NOISE_PARAMETERS</code>","text":"<p>Models measurement noise added to the expected muon counts before inversion. Noise comes from two independent sources, and each source is split into a deterministic floor part (added as-is) and a Poisson part (Poisson-fluctuated):</p> <ul> <li>Flux-proportional noise scales with each bin's own signal.</li> <li>SOT-proportional noise is an angle-independent background: it takes a fixed fraction of the total signal and redistributes it across bins by their exposure share.</li> </ul> <p>The noise is computed per detector bin (<code>DetectorPanel::mp_set_noise</code>). Bins whose density length <code>DL</code> is below <code>DL_thres</code> are skipped: these empty bins (too little density length) contribute nothing and are excluded from the totals below. For a bin with <code>signal</code> (<code>get_signal()</code>) and exposure <code>SOT</code> (<code>get_SOT()</code>, the factor converting flux to counts), <code>total_signal_DL_thres</code> and <code>total_SOT_DL_thres</code> are the <code>signal</code> and <code>SOT</code> summed over the kept bins only (<code>DL &gt;= DL_thres</code>; the empty bins are not counted \u2014 <code>mp_calc_total_signal_DLthres</code> / <code>mp_calc_total_SOT_DLthres</code>):</p> <pre><code>Flux-proportional (uses the bin's own signal):\n  flux_floor   = signal * flux_proport_ratio_floor\n  flux_poisson = signal * flux_proport_ratio_poisson\n\nSOT-proportional (angle-independent; a share of total_signal_DL_thres):\n  SOT_floor    = (SOT / total_SOT_DL_thres) * SOT_proport_noise_ratio_floor   * total_signal_DL_thres\n  SOT_poisson  = (SOT / total_SOT_DL_thres) * SOT_proport_noise_ratio_poisson * total_signal_DL_thres\n</code></pre> <p>Each bin stores two accumulators \u2014 <code>noise_det = flux_floor + SOT_floor</code> (deterministic, passed through as-is) and <code>noise_poi = flux_poisson + SOT_poisson</code> (the mean of a Poisson-fluctuated bucket). The <code>*_floor</code> parts stay deterministic; the <code>*_poisson</code> parts are drawn from a Poisson distribution at bin-grouping time, so a group's observed noise is <code>noise_det + Poisson(noise_poi)</code>. In short: flux-proportional noise is large where the bin's own signal is large, while SOT-proportional noise ignores the bin's own signal and spreads a fraction of the total signal by exposure.</p> <pre><code>\"NOISE_PARAMETERS\": {\n  \"name\": \"noise_tmp\",                        // Free-form instance name\n  \"tf_exec\": true,                            // true = apply noise modeling (false = skip)\n  \"flux_proport_ratio_floor\": 0.0,            // Flux-proportional, deterministic floor\n  \"flux_proport_ratio_poisson\": 1.0E-3,       // Flux-proportional, Poisson-fluctuated\n  \"SOT_proport_noise_ratio_floor\": 0.0,       // SOT-proportional, deterministic floor\n  \"SOT_proport_noise_ratio_poisson\": 1.0E-3   // SOT-proportional, Poisson-fluctuated\n}\n</code></pre> <p>All four ratios are dimensionless and default to <code>0.0</code> (no contribution); <code>tf_exec</code> defaults to <code>false</code>. When <code>tf_exec</code> is <code>false</code> the block is skipped regardless of the ratio values.</p>"},{"location":"user-guide/parameter-files.html#nagainv_parameters","title":"<code>NAGAINV_PARAMETERS</code>","text":"<p>An array of inversion configurations. Multiple configurations can be defined and selectively executed.</p> <pre><code>\"NAGAINV_PARAMETERS\": [\n  {\n    \"name\": \"nagainv72_0\",          // Instance name\n    \"tf_exec\": true,               // true = execute this inversion config\n    \"tf_signal_poisson\": false,    // true = apply Poisson error to signal counts\n    \"nmuon_thres\": 1.0e-10,       // Min muon count; elements below are excluded\n    \"nmuon_under_thres\": 1.0e-10, // Replacement value for excluded elements\n    \"uniform_prior_density\": 2000, // Prior density assumption [kg/m\u00b3]\n\n    // Per-shell density override (optional, non-sweep only)\n    // If omitted, each shell uses uniform_prior_density as its density.\n    // These keys are used in non-sweep execution (tf_exec=false in sweep config).\n    // In sweep mode, shell densities come from vec_uniform_prior_density instead.\n    \"shell_density_upper\": 2100,   // Upper shell density [kg/m\u00b3] (optional)\n    \"shell_density_lower\": 2300,   // Lower shell density [kg/m\u00b3] (optional)\n    \"shell_density_lateral\": 2200, // Lateral shell density [kg/m\u00b3] (optional)\n\n    // Covariance matrix parameters \u2014 see Concepts &gt; Inversion\n    \"corr_length\": 70,             // Isotropic spatial correlation length [m]\n    \"sigma_rho\": 300,              // Density prior std dev [kg/m\u00b3] (larger = trust data more)\n    \"sigma_rho_diag\": 300,         // Diagonal covariance std dev [kg/m\u00b3]\n\n    // Anisotropic covariance (optional)\n    \"tf_aniso\": false,             // true = use direction-dependent correlation\n    \"corr_length_xy\": 70,          // Horizontal correlation length [m] (if tf_aniso=true)\n    \"corr_length_z\": 70,           // Vertical correlation length [m] (if tf_aniso=true)\n    \"aniso_cov_type\": \"separable\"  // \"separable\" or \"ellipsoidal\"\n  }\n]\n</code></pre> <p>See 3D Inversion for the mathematical background of these parameters.</p>"},{"location":"user-guide/parameter-files.html#proj_dens_eval_grouped-required-from-trace-path-lengths-module-4-onward","title":"<code>PROJ_DENS_EVAL_GROUPED</code> (required from Trace Path Lengths, Module 4, onward)","text":"<p>Controls projected density evaluation for grouped bins. This section must be present whenever Trace Path Lengths (Module 4) or later runs \u2014 the pipeline reads it with <code>.at(...)</code>, which fails if the section is missing. Set <code>tf_exec=false</code> to skip the projected-density computation while keeping the section in place.</p> <pre><code>\"PROJ_DENS_EVAL_GROUPED\": {\n  \"tf_exec\": true,                // true = run projected density evaluation\n  \"tf_signal_poisson\": true,      // true = apply Poisson error to signal counts\n  \"dens_min\": 0,                  // Min density to scan [kg/m\u00b3]\n  \"dens_max\": 10000,              // Max density to scan [kg/m\u00b3]\n  \"dens_steps\": [200, 50, 10],    // Multi-pass search step sizes [kg/m\u00b3] (coarse \u2192 fine)\n  \"range_factor\": 2.0,            // Search range factor for refinement passes\n  \"sigma\": 1.5                    // Significance threshold [\u03c3]\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#z_cross_section","title":"<code>Z_CROSS_SECTION</code>","text":"<p>Defines the z-range for cross-section output.</p> <pre><code>\"Z_CROSS_SECTION\": {\n  \"min\": 160.0,                    // Lowest z-elevation for output [m a.s.l.]\n  \"max\": 400.0,                    // Highest z-elevation for output [m a.s.l.]\n  // \"zstep\": 40                   // Optional: z-step in meters (default: 0 = use z_interval)\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#run_inversion-optional","title":"<code>RUN_INVERSION</code> (optional)","text":"<p>Controls inversion behavior.</p> <pre><code>\"RUN_INVERSION\": {\n  \"tf_prior_error\": false          // true = compute lower/upper prior bounds (3\u00d7 slower)\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#nagainv_param_sweep-optional","title":"<code>NAGAINV_PARAM_SWEEP</code> (optional)","text":"<p>Systematic parameter sweep over inversion parameters. Total combinations = <code>len(vec_sigma_rho) \u00d7 len(vec_corr_length) \u00d7 len(vec_uniform_prior_density)</code>.</p> <pre><code>\"NAGAINV_PARAM_SWEEP\": {\n  \"tf_exec\": true,                // true = run parameter sweep\n  \"base_index\": 0,                // Index in NAGAINV_PARAMETERS to use as template\n  \"vec_sigma_rho\": [200, 300, 400],           // Values to sweep [kg/m\u00b3]\n  \"vec_corr_length\": [50, 70, 100],           // Values to sweep [m]\n  // Density sweep values [kg/m\u00b3]. Each element can be:\n  //   Scalar: applies to voxels and all shells equally (expanded to [v, v, v, v])\n  //   Quad:   [prior_density, shell_upper, shell_lower, shell_lateral]\n  // Scalar and Quad can be mixed in the same array.\n  \"vec_uniform_prior_density\": [\n    1500, 2000,                           // scalar: all shells equal\n    [2000, 500, 2500, 1800]              // quad: per-shell override\n  ],\n  \"link_diag_to_sigma\": true,     // true = set sigma_rho_diag = sigma_rho for each run\n  // \"vec_sigma_rho_diag\": [200, 300], // Required when link_diag_to_sigma=false [kg/m\u00b3]\n  \"module8_mode\": \"none\",         // \"none\"/\"all\"/\"last\"/\"selected\" \u2014 run Analyze Errors (Module 8) for which combos\n  \"module8_indices\": []           // Sweep indices for module8_mode=\"selected\" (e.g. [0, 2, 5])\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#detector-parameter-files","title":"Detector Parameter Files","text":"<p>Each detector is configured in a separate JSON5 file referenced from <code>DETECTOR_PARAMETER_LISTS.det_files</code>.</p> <pre><code>{\n  \"DETECTOR_PARAMETERS\": {\n    \"name\": \"det_00\",\n    \"angle_unit\": \"tangent\",     // \"tangent\", \"degree\", or \"radian\"\n\n    // Angular bins: path-length computation elements, not the\n    // hardware angular resolution (see Concepts -- Detector Model).\n    \"nbinx\": 320,               // Number of horizontal bins\n    \"txmin\": -1.6,               // Horizontal angle min\n    \"txmax\": 1.6,                // Horizontal angle max\n    \"nbiny\": 160,                // Number of vertical bins\n    \"tymin\": 0.0,                // Vertical angle min\n    \"tymax\": 1.6,                // Vertical angle max\n\n    // Physical dimensions [m]\n    \"length_hori\": 0.33,\n    \"length_vert\": 0.33,\n    \"length_dept\": 0.03,\n\n    \"n_unit\": 2,                 // Number of detector copies\n\n    // Position [m]\n    \"x\": 50168.1,\n    \"y\": -161872.5,\n    \"z\": 208.4,\n\n    // Orientation [degrees]\n    \"rotation_type\": \"LOCAL\",    // \"LOCAL\" or \"GLOBAL\"\n    \"yaw_deg\": 59.09,\n    \"roll_deg\": 0.0,\n    \"pitch_deg\": 0.0,\n\n    \"days\": 120.0,               // Exposure time [days]\n\n    // Efficiency input (see the Detector Efficiency page).\n    // path_eff_model takes precedence unless set to \"none\".\n    \"path_eff_model\": \"../detparams/eff_model/small_uncertainty.json5\",\n    \"path_eff_table\": \"../detparams/eff_table/eff_table_sample00.txt\"\n  }\n}\n</code></pre>"},{"location":"user-guide/parameter-files.html#efficiency-input","title":"Efficiency input","text":"Parameter Default Description <code>path_eff_model</code> <code>\"none\"</code> Analytic efficiency model (JSON5). Preferred; used when not <code>\"none\"</code>. <code>path_eff_table</code> \u2014 Legacy per-bin text table. Used only when <code>path_eff_model</code> is <code>\"none\"</code>. <p>See Detector Efficiency for the model file format and the evaluation formulas.</p>"},{"location":"user-guide/parameter-files.html#orientation-parameters","title":"Orientation parameters","text":"Parameter Unit Convention <code>yaw_deg</code> degrees 0\u00b0 = East, 90\u00b0 = North; clockwise positive <code>roll_deg</code> degrees Counterclockwise positive <code>pitch_deg</code> degrees Counterclockwise positive <code>rotation_type</code> \u2014 <code>LOCAL</code> = detector-local axes; <code>GLOBAL</code> = terrain-fixed axes"},{"location":"user-guide/parameter-files.html#file-path-resolution","title":"File Path Resolution","text":"<p>All relative file paths in the JSON5 configuration are resolved relative to the current working directory at run time, not relative to the JSON5 file's location. The station scripts run the executable from inside the run directory \u2014 for example <code>cd work/&lt;site&gt;/swp001</code> before <code>bash run_prg.sh ...</code> \u2014 so a path such as <code>../dem/terrain.g2zbin</code> is interpreted from that directory. Always run the executable from the directory that the relative paths assume.</p>"},{"location":"user-guide/pipeline.html","title":"Pipeline","text":""},{"location":"user-guide/pipeline.html#pipeline","title":"Pipeline","text":"<p>MUONITH processes muon tomography data through an 8-module pipeline. Each module performs a distinct step, and the pipeline supports checkpoint/resume so that expensive computations are not repeated unnecessarily.</p>"},{"location":"user-guide/pipeline.html#overview","title":"Overview","text":"<pre><code>Module 1  \u2192  Module 2  \u2192  Module 3  \u2192  Module 4  \u2192  Module 5  \u2192  Module 6  \u2192  Module 7  \u2192  Module 8\n  Init        Params      Geometry     Path Len     Prior        Matrix      Inversion     Error\n              Loading     Build        Trace        (DL/Count)   Build       (Density      Analysis\n                          (DEM\u2192Voxel)  (Ray Trace)              (dN/dD)      Reconst.)    &amp; Output\n</code></pre>"},{"location":"user-guide/pipeline.html#module-io-summary","title":"Module I/O Summary","text":"Module Name Key Outputs Checkpoint 1 Initialization Logger, OpenMP config, random seed \u2014 2 Load Parameters <code>app_params</code> (detector, grid, flux, inversion settings) \u2014 3 Build Geometry <code>g2pil_naive</code>, <code>g3vox_input</code>, <code>g3vox_merged_input</code>, shell grids \u2014 4 Trace Path Lengths <code>arrdet_g2pil_naive</code>, <code>arrdet_g3vox_input</code>, <code>vec_spmat_PL</code>, <code>shell_pl</code> <code>tf_save/load_arrdet_g2pil</code>, <code>tf_save/load_arrdet_g3vox</code>, <code>checkpoint_trace_path_lengths</code> 5 Compute Prior <code>prior_info_all</code> (<code>DL_prior</code>, expected counts per density scenario) \u2014 6 Build Observation Matrix <code>mat_dNdD_grouped</code> (lower/center/upper) <code>tf_save/load_bin_obs_mat_dNdD</code> 7 Invert Density Reconstructed density, posterior covariance, cross-sections \u2014 8 Analyze Errors Statistical / prior / leave-one-out / combined error cross-sections \u2014"},{"location":"user-guide/pipeline.html#module-descriptions","title":"Module Descriptions","text":""},{"location":"user-guide/pipeline.html#initialization-module-1","title":"Initialization (Module 1)","text":"<ul> <li>Load and validate JSON5 configuration file</li> <li>Setup logging (spdlog: stdout, stderr, file)</li> <li>Initialize random seed (priority: CLI argument &gt; JSON5 &gt; default 42)</li> <li>Configure OpenMP thread count</li> </ul>"},{"location":"user-guide/pipeline.html#load-parameters-module-2","title":"Load Parameters (Module 2)","text":"<p>Parse JSON5 into structured parameter objects:</p> <ul> <li>Detector panel configurations</li> <li>Grid2dPillar parameters (DEM settings)</li> <li>Grid3dVoxel parameters (3D grid extent)</li> <li>Path length calculation settings</li> <li>Flux table paths</li> <li>Inversion parameters</li> </ul>"},{"location":"user-guide/pipeline.html#build-geometry-module-3","title":"Build Geometry (Module 3)","text":"<p>Construct the spatial grids from terrain data.</p> <pre><code>DEM file (.g2zbin)\n    \u2193\nGrid2dPillar (2D terrain grid)\n    \u2193\nGrid3dVoxel (3D voxel grid)\n    \u2193\nMerge voxels (optional resolution reduction)\n    \u2193\nShell regions (terrain above/below voxel grid)\n</code></pre> <p></p> <p></p> <p>Inputs: DEM file path, grid parameters</p> <p>Outputs:</p> Object Description <code>g2pil_naive</code> 2D terrain grid from DEM <code>g3vox_input</code> 3D voxel grid with initial density <code>g3vox_merged_input</code> Merged voxel grid (lower resolution) <code>g2pil_shell_upper</code> Terrain above the voxel grid <code>g2pil_shell_lower</code> Terrain below the voxel grid <p>If <code>tf_build_g3vox = false</code>, the pipeline stops here and outputs 2D cross-sections only.</p>"},{"location":"user-guide/pipeline.html#trace-path-lengths-module-4","title":"Trace Path Lengths (Module 4)","text":"<p>Construct the detector array and compute path lengths through the geometry.</p> <pre><code>DetectorPanelArray (from detector JSON5 files)\n    \u2193\nRay trace through Grid2dPillar \u2192 PL, DL per element\n    \u2193\nRay trace through Grid3dVoxel \u2192 sparse path-length matrices\n    \u2193\nCompute signal/noise statistics from flux tables\n</code></pre> <p></p> <p></p> <p>Inputs: Detector parameters, geometry (Build Geometry, Module 3), flux tables</p> <p>Outputs:</p> Object Description <code>arrdet_g2pil_naive</code> Detector array linked to 2D terrain <code>arrdet_g3vox_input</code> Detector array linked to 3D voxels <code>vec_spmat_PL</code> Sparse path-length matrices (one per detector) <code>shell_pl</code> Density-independent shell path lengths (<code>upper</code>, <code>lower</code>, <code>lateral</code>), flat <code>VectorXf</code> per component <p>Trace Path Lengths (Module 4) is purely density-independent \u2014 it computes geometric path lengths only. Density-dependent prior quantities (<code>DL_prior</code>, expected counts, shell contributions) are built in Compute Prior (Module 5).</p> <p>This module is typically the most time-consuming step due to ray tracing through the 3D voxel grid. Several save/load knobs can avoid recomputation:</p> <ul> <li><code>tf_save_arrdet_g3vox</code> / <code>tf_load_arrdet_g3vox</code> \u2014 per-detector path-length artifacts</li> <li><code>--end-stage 4</code> followed by <code>--resume &lt;checkpoint_dir&gt;</code> \u2014 stops after Trace Path Lengths (Module 4) and writes a <code>checkpoint_trace_path_lengths</code> bundle that later runs reuse to skip Modules 1\u20134 entirely (raytrace is detected as already completed and skipped)</li> </ul>"},{"location":"user-guide/pipeline.html#compute-prior-module-5","title":"Compute Prior (Module 5)","text":"<p>Combine the density-independent Trace Path Lengths (Module 4) outputs with prior density assumptions to produce the prior muon-count prediction.</p> <p>Inputs:</p> <ul> <li>Sparse path-length matrices <code>vec_spmat_PL</code></li> <li>Shell path lengths <code>shell_pl</code> (upper / lower / lateral)</li> <li>Flux tables, detector array</li> <li><code>density_quad = [prior, shell_upper, shell_lower, shell_lateral]</code> \u2014 per-shell prior densities from <code>NAGAINV_PARAMETERS</code> (falls back to <code>uniform_prior_density</code> when shell-specific values are omitted)</li> </ul> <p>Outputs: Prior density-length (<code>DL_prior</code>), prior expected counts per density scenario (<code>prior_info_all</code>).</p>"},{"location":"user-guide/pipeline.html#build-observation-matrix-module-6","title":"Build Observation Matrix (Module 6)","text":"<p>Convert path-length matrices into muon count sensitivity matrices by linearizing the nonlinear flux response \\(N_i = g_i(X_i)\\) around the prior density length \\(X_{0,i}\\) \u2014 i.e. Eq. (18) of Nagahara et al. (2022):</p> \\[A_{ij} \\;=\\; \\frac{\\partial N_i}{\\partial \\rho_j} \\;=\\; L_{ij}\\, \\frac{dN_i}{dX_i}\\!\\left(X_{0,i}\\right)\\] <p>where \\(L_{ij}\\) is the geometric path length of ray \\(i\\) through voxel \\(j\\) (from Trace Path Lengths, Module 4) and \\(dN_i/dX_i\\) is the muon-count derivative with respect to density length, evaluated at \\(X_{0,i}\\) via the flux table (weighted by \\(dF/dR\\), \\(\\cos\\theta_z\\), detector area, solid angle, and exposure time). See Ray Tracing \u2014 From Path Length to Observation Equation for the full derivation.</p> <p>Inputs: Sparse path-length matrices, flux tables, detector array</p> <p>Outputs: Observation matrices <code>mat_dNdD_grouped</code> (one each for lower/center/upper prior)</p>"},{"location":"user-guide/pipeline.html#invert-density-module-7","title":"Invert Density (Module 7)","text":"<p>Perform MAP-based density reconstruction.</p> \\[\\mathbf{C}_{\\rho'} = \\left(\\mathbf{A}^T \\mathbf{C}_N^{-1} \\mathbf{A} + \\mathbf{C}_\\rho^{-1}\\right)^{-1}\\] \\[\\delta\\vec{\\rho} = \\mathbf{C}_{\\rho'} \\, \\mathbf{A}^T \\mathbf{C}_N^{-1} \\left(\\vec{N}_{\\text{obs}} - \\vec{N}_0\\right)\\] <p>Inputs: Observation matrix, observed/prior muon counts, regularization parameters</p> <p>Outputs:</p> Field Description Reconstructed density \\(\\vec{\\rho}' = \\vec{\\rho}_0 + \\delta\\vec{\\rho}\\) Posterior uncertainty \\(\\sqrt{\\text{diag}(\\mathbf{C}_{\\rho'})}\\) Delta prior \\(\\vec{\\rho}' - \\vec{\\rho}_0\\) Cross-section files 2D density maps at specified z-elevations <p>Multiple inversion configurations (different <code>sigma_rho</code>, <code>corr_length</code>, etc.) can be defined in the <code>NAGAINV_PARAMETERS</code> array and executed sequentially.</p> <p>See 3D Inversion for the mathematical details.</p>"},{"location":"user-guide/pipeline.html#analyze-errors-and-output-module-8","title":"Analyze Errors and Output (Module 8)","text":"<p>Perform systematic error analysis and generate final outputs.</p> <ol> <li>Leave-one-out analysis: Re-run inversion with each detector systematically removed to assess detector contribution sensitivity</li> <li>Error components: Statistical error, prior error (lower/center/upper bounds), leave-one-out sensitivity</li> <li>Combined error: Root-sum-square of all error sources</li> <li>Output: Cross-section files for all error components</li> </ol>"},{"location":"user-guide/pipeline.html#execution-control","title":"Execution Control","text":""},{"location":"user-guide/pipeline.html#running-the-pipeline","title":"Running the Pipeline","text":"<pre><code># Run all modules (1 through 8, single run)\nmuonith.exe -j prm_muonith.json5\n\n# Run parameter sweep (set tf_exec=true in NAGAINV_PARAM_SWEEP section)\nmuonith.exe -j prm_muonith.json5\n</code></pre> <p>To stop the pipeline at a specific module, use <code>end_stage</code> in the JSON5 config or the <code>--end-stage</code> CLI flag (CLI takes precedence):</p> <pre><code>// In the top-level section of prm_muonith.json5\n// Valid values: 3, 4, 5, 6, 7, 8 (default: 8 = all modules)\n\"end_stage\": 6\n</code></pre> <pre><code># Or via CLI flag\nmuonith.exe -j prm_muonith.json5 --end-stage 6\n</code></pre>"},{"location":"user-guide/pipeline.html#checkpoint-and-resume","title":"Checkpoint and Resume","text":"<p>Intermediate results (detector arrays, path-length matrices, observation matrices) can be saved to binary files via <code>tf_save_*</code> flags in <code>PATH_LENGTH_PARAMETERS</code>. On subsequent runs, these can be loaded via <code>tf_load_*</code> flags to skip expensive computations.</p> <p>A coarser checkpoint is also available at the Trace Path Lengths (Module 4) boundary: running with <code>--end-stage 4</code> writes a <code>checkpoint_trace_path_lengths</code> bundle (detector arrays, <code>vec_spmat_PL</code>, <code>shell_pl</code>, and a copy of the JSON5 configuration as <code>app_params.json</code>). A later run can start from that bundle with:</p> <pre><code>muonith.exe -j prm_muonith.json5 --resume path/to/checkpoint_trace_path_lengths\n</code></pre> <p>When a valid <code>checkpoint_trace_path_lengths</code> is passed, the sweep driver detects \"raytrace already completed\" and skips Modules 1\u20134 entirely, resuming from Compute Prior (Module 5) onward.</p> <p>During a parameter sweep the driver also writes finer-grained checkpoints automatically: <code>checkpoint_build_geometry</code> (saved after Modules 1\u20133) and per-sweep-point <code>checkpoint_build_observation_matrix_p{prior}_u{upper}_l{lower}_lat{lateral}</code> bundles, named by the four shell-density indices. These let later sweep points reuse the Module 5/6 prior when they share the same shell-density combination.</p> <p>This is particularly useful when:</p> <ul> <li>Changing only inversion parameters (Invert Density, Module 7) \u2014 geometry and path lengths don't need recomputation</li> <li>Running parameter sweeps across <code>sigma_rho</code> and <code>corr_length</code> values</li> <li>Switching between different <code>uniform_prior_density</code> / shell-density combinations (Compute Prior, Module 5, onward is cheap)</li> </ul>"},{"location":"user-guide/pipeline.html#parameter-sweep","title":"Parameter Sweep","text":"<p>For systematic exploration of inversion parameters, use the <code>NAGAINV_PARAM_SWEEP</code> section:</p> <pre><code>\"NAGAINV_PARAM_SWEEP\": {\n  \"tf_exec\": true,\n  \"base_index\": 0,\n  \"vec_sigma_rho\": [200, 300, 400],\n  \"vec_corr_length\": [50, 70, 100],\n  // Each element: scalar (all shells equal) or [prior, upper, lower, lateral].\n  // Scalar and Quad can be mixed in the same array.\n  \"vec_uniform_prior_density\": [1500, 2000, [2000, 500, 2500, 1800]],\n  \"link_diag_to_sigma\": true,             // true = sigma_rho_diag follows sigma_rho\n  // \"vec_sigma_rho_diag\": [200, 300],    // Required when link_diag_to_sigma=false\n  \"module8_mode\": \"none\",                 // \"none\" | \"all\" | \"last\" | \"selected\"\n  \"module8_indices\": []                   // Sweep indices for module8_mode=\"selected\"\n}\n</code></pre> <p>This generates all combinations of the specified parameter values and runs Invert Density (Module 7) for each combination. Analyze Errors (Module 8) execution is controlled by <code>module8_mode</code>.</p>"},{"location":"user-guide/pipeline.html#dependency-graph","title":"Dependency Graph","text":"<p>The following table shows which modules must be re-executed when a parameter changes:</p> Parameter changed Re-run from Detector position/orientation Trace Path Lengths (Module 4) DEM file / grid extent Build Geometry (Module 3) Voxel resolution (merge) Build Geometry (Module 3) <code>sigma_rho</code>, <code>corr_length</code> Invert Density (Module 7) only <code>uniform_prior_density</code> Compute Prior (Module 5) Flux table Compute Prior (Module 5)"},{"location":"user-guide/placing-detectors.html","title":"Placing Detectors from a Map","text":""},{"location":"user-guide/placing-detectors.html#placing-detectors-from-a-map","title":"Placing Detectors from a Map","text":"<p>This guide shows how to pick detector positions on a map with a GUI tool, export them as KML or GeoJSON, and turn them into the detector input that <code>setup_station.sh</code> consumes.</p> <p>The conversion scripts (<code>kml_to_csv.py</code>, <code>geojson_to_csv.py</code>, <code>mk_detjson5_from_template.py</code>) are already documented in Scripts. This page covers the upstream GUI step: how to create the KML/GeoJSON file in the first place.</p> <p>Where this fits</p> <p>The full station workflow is: place points on a map \u2192 export KML/GeoJSON \u2192 convert to CSV \u2192 fill the detector JSON5 \u2192 run <code>setup_station.sh</code>. Skip this page if your site uses a custom orthogonal coordinate system with <code>\"skip_detparams\": true</code> (see Scripts), since tracked runcards are reused instead.</p>"},{"location":"user-guide/placing-detectors.html#two-routes","title":"Two routes","text":"Tool Export format Use when Google Earth Pro KML (<code>.kml</code>) 3D terrain view, satellite imagery GSI Maps (\u5730\u7406\u9662\u5730\u56f3) Vector GeoJSON (<code>.geojson</code>) Japanese base maps, no Google Earth, contour view <p>Both produce a list of named points. Name each point consistently (<code>center</code>, <code>det_00</code>, <code>det_01</code>, \u2026); the name is carried into the detector table, so keeping it identical across tools lets you reuse the same downstream commands.</p>"},{"location":"user-guide/placing-detectors.html#route-a-google-earth-pro-kml","title":"Route A \u2014 Google Earth Pro (KML)","text":""},{"location":"user-guide/placing-detectors.html#1-place-the-detector-candidates","title":"1. Place the detector candidates","text":"<p>Add a folder (here <code>tokachi02</code>) and drop a placemark for each detector position, plus one for the target center. Name the placemarks <code>center</code>, <code>det_00</code>, <code>det_01</code>, \u2026</p> <p></p> <p>Detector candidates placed as placemarks on the terrain. The left panel holds the <code>tokachi02</code> folder with <code>center</code> and <code>det_00</code>\u2013<code>det_10</code>.</p>"},{"location":"user-guide/placing-detectors.html#2-export-the-folder-as-kml","title":"2. Export the folder as KML","text":"<p>Right-click the folder in the Places panel \u2192 Save Place As\u2026 (\u540d\u524d\u3092\u4ed8\u3051\u3066\u5834\u6240\u3092\u4fdd\u5b58). In the save dialog, choose KML (<code>.kml</code>) as the format \u2014 not KMZ, which is a zipped variant.</p> <p></p> <p>Choose <code>Kml (*.kml)</code> so the file can be read directly by <code>kml_to_csv.py</code>.</p> <p>KMZ instead of KML?</p> <p>KMZ is a compressed KML archive. If you only have a <code>.kmz</code>, unzip it and use the <code>doc.kml</code> inside, or re-export as <code>.kml</code>.</p>"},{"location":"user-guide/placing-detectors.html#3-convert-kml-csv","title":"3. Convert KML \u2192 CSV","text":"<pre><code>python3 scripts/kml_to_csv.py --kml_in tokachi02.kml --EPSG &lt;EPSG&gt;\n</code></pre> <p>Pick the projected <code>&lt;EPSG&gt;</code> for your site (see Coordinate System). KML placemarks carry longitude/latitude; elevation can be filled from the GSI elevation API or a GeoTIFF (<code>--fill_elevation_from_gsi</code>, <code>--dem</code>). Full options are in Scripts.</p>"},{"location":"user-guide/placing-detectors.html#route-b-gsi-maps-vector-geojson","title":"Route B \u2014 GSI Maps (\u5730\u7406\u9662\u5730\u56f3) Vector (GeoJSON)","text":""},{"location":"user-guide/placing-detectors.html#1-open-the-drawing-panel","title":"1. Open the drawing panel","text":"<p>On GSI Maps Vector, open \u4f5c\u56f3\u30fb\u30d5\u30a1\u30a4\u30eb (Drawing / File) from the top-right toolbar.</p> <p></p> <p>The drawing panel lets you add points, lines, and polygons on the GSI base map.</p>"},{"location":"user-guide/placing-detectors.html#2-place-and-name-each-marker","title":"2. Place and name each marker","text":"<p>Select the marker (point) tool and click on the map to drop a marker. Enter the name (<code>center</code>, <code>det_00</code>, \u2026) in the marker dialog; this becomes the <code>properties.name</code> field in the exported GeoJSON.</p> <p></p> <p>Each marker name is written to <code>properties.name</code>, which the converter reads.</p> <p>Repeat for every detector position.</p> <p></p> <p>All detector markers placed, with labels showing the <code>name</code> attribute.</p>"},{"location":"user-guide/placing-detectors.html#3-save-as-geojson","title":"3. Save as GeoJSON","text":"<p>Click the save (disk) icon in the panel, select GeoJSON as the format, then save.</p> <p></p> <p>Select GeoJSON, then save. The output is a <code>FeatureCollection</code> of <code>Point</code> features following the GSI geojson-with-style spec.</p> <p>GeoJSON coordinate order and elevation</p> <p>GeoJSON stores coordinates as <code>[longitude, latitude]</code> (same order as KML). Markers carry no elevation, so <code>geojson_to_csv.py</code> fills it from the GSI elevation API or a GeoTIFF.</p>"},{"location":"user-guide/placing-detectors.html#4-convert-geojson-csv","title":"4. Convert GeoJSON \u2192 CSV","text":"<pre><code>python3 scripts/geojson_to_csv.py --geojson_in tokachi02.geojson --EPSG &lt;EPSG&gt;\n</code></pre> <p>Shares the elevation-fill logic with <code>kml_to_csv.py</code> (<code>--fill_elevation_from_gsi</code>, <code>--dem_tif</code>, <code>--dem_only</code>). See Scripts.</p>"},{"location":"user-guide/placing-detectors.html#from-csv-to-a-runnable-station","title":"From CSV to a runnable station","text":"<p>Once you have the CSV (from either route), the remaining steps are the same:</p> <ol> <li> <p>Fill the detector JSON5 from a template:</p> <pre><code>python3 scripts/mk_detjson5_from_template.py --template template-det.json5 --csv tokachi02_table.csv\n</code></pre> </li> <li> <p>Place the exported file and the DEM under <code>data/&lt;station&gt;/</code>, and point    <code>param_sites/&lt;station&gt;.json5</code> at them (<code>detector_input</code>, DEM path).</p> </li> <li> <p>Run the station setup:</p> <pre><code>bash setup_station.sh &lt;station&gt; --make-detparams\n</code></pre> </li> </ol> <p>See Scripts and Input Data for the detector parameter format, and Quick Start for an end-to-end run.</p>"},{"location":"user-guide/synthetic-dem-examples.html","title":"Synthetic DEM Examples","text":""},{"location":"user-guide/synthetic-dem-examples.html#synthetic-dem-examples","title":"Synthetic DEM Examples","text":"<p>This page walks through two ready-to-run demo stations built on primitive-solid DEMs (Digital Elevation Models). Each station is a synthetic mountain of a simple shape that carries a single rectangular density anomaly inside it. Running the station produces a synthetic observed muon signal from that \"true\" density and then inverts it, so you can see the whole forward-then-inverse pipeline end to end without any real survey data or licensing constraints.</p> <p>Two demo stations ship with the repository:</p> Station Shape Config <code>eg-cylinder</code> flat-topped cylinder <code>param_sites/eg-cylinder.json5</code> <code>eg-square-pyramid</code> square pyramid <code>param_sites/eg-square-pyramid.json5</code> <p>Both reuse the tutorial detector layout, so no detector data has to be generated.</p> <p>The DEM binary is never committed</p> <p>The <code>.g2zbin</code> DEM is generated at run time by <code>run_prg.sh</code> (via the <code>dem_generator</code> block in the station config) and written into <code>work/&lt;station&gt;/dem/</code>. It is not stored in the repository. Every run regenerates it, so the example is fully reproducible from the config alone.</p>"},{"location":"user-guide/synthetic-dem-examples.html#background-what-a-primitive-dem-is","title":"Background: what a primitive DEM is","text":"<p>A DEM here is a single-valued height field <code>z(x, y)</code> \u2014 one surface elevation per horizontal grid point \u2014 stored in the repository's <code>g2zbin</code> binary format. The generator <code>param_sites/tutorial/make_primitive_dem.py</code> can produce five shapes:</p> <code>--shape</code> Height field <code>cylinder</code> flat top at <code>base + height</code> within a radius, else <code>base</code> <code>square_prism</code> flat top within a half-width (Chebyshev square), else <code>base</code> <code>cone</code> <code>base + height * (1 - r / radius)</code> inside the radius <code>square_pyramid</code> <code>base + height * (1 - c / half_width)</code> inside the half-width <code>gaussian</code> <code>base + height * exp(-0.5 * ((dx/sigma_x)^2 + (dy/sigma_y)^2))</code> <p>A single unified width knob keeps shapes comparable: <code>--width W</code> sets the cylinder/cone diameter to <code>W</code>, the square side to <code>W</code>, and the Gaussian to <code>4*sigma</code> (<code>\u00b12 sigma</code>) across <code>W</code>. Concretely <code>radius = half_width = W/2</code> and <code>sigma = W/4</code>. Explicit per-shape flags (<code>--radius</code>, <code>--half_width</code>, <code>--sigma_x</code>, <code>--sigma_y</code>) override <code>--width</code> when both are given. See the <code>make_primitive_dem.py</code> reference for the full option table.</p> <p>The demo stations do not call the generator by hand \u2014 the <code>dem_generator</code> block in each station config records the shape and size, and <code>run_prg.sh</code> invokes the generator for you before every run.</p>"},{"location":"user-guide/synthetic-dem-examples.html#example-1-eg-cylinder","title":"Example 1: <code>eg-cylinder</code>","text":"<p>A flat-topped cylinder with a unified footprint width <code>W = 500 m</code> (radius 250 m), <code>base = 40 m</code>, <code>height = 360 m</code> (summit \u2248 400 m), centered on the tutorial detector ring.</p>"},{"location":"user-guide/synthetic-dem-examples.html#run-it","title":"Run it","text":"<pre><code># 1. Scaffold the work directory (detectors come from the tutorial station).\nbash setup_station.sh eg-cylinder\n\n# 2. Regenerate the DEM and run the inversion.\ncd work/eg-cylinder/swp001 &amp;&amp; bash run_prg.sh true 42\n</code></pre> <p><code>run_prg.sh</code> takes <code>&lt;run_exe: true|false&gt; &lt;seed&gt;</code>: <code>true</code> runs the executable and <code>42</code> is the random seed. How far the pipeline runs is taken from the station config (<code>prm_muonith.json5</code>), so you do not have to specify it on the command line.</p>"},{"location":"user-guide/synthetic-dem-examples.html#what-happens","title":"What happens","text":"<p>First the DEM is regenerated (the binary is not in the repo):</p> <pre><code>Wrote 90601 cells to ../dem/eg_cylinder-5m.g2zbin (301x301 grid, z range [40.000, 400.000] m)\n</code></pre> <p>Then the internal density anomaly is loaded and applied to the 3D voxel grid:</p> <pre><code>read cuboid parameters...num of cuboid = 1\nadding cuboid density structure: cuboid_demo\n</code></pre> <p>The processing pipeline then runs to completion:</p> <pre><code>Sweep completed: 1 configurations executed\n</code></pre>"},{"location":"user-guide/synthetic-dem-examples.html#outputs","title":"Outputs","text":"<p>Plots land in <code>work/eg-cylinder/swp001/figs/</code> (density cross-sections, per-detector path-length and signal maps, and the reconstructed density). Cross-section binaries and CSVs are written under the run directory. See Output for the full list and naming scheme.</p>"},{"location":"user-guide/synthetic-dem-examples.html#example-2-eg-square-pyramid","title":"Example 2: <code>eg-square-pyramid</code>","text":"<p>Same footprint width (<code>W = 500 m</code>, so a 500 m base side), <code>base</code> and <code>height</code> as <code>eg-cylinder</code>, but the surface slopes linearly to the summit instead of being flat on top \u2014 this changes how the terrain thickness varies across each detector's field of view.</p> <pre><code>bash setup_station.sh eg-square-pyramid\ncd work/eg-square-pyramid/swp001 &amp;&amp; bash run_prg.sh true 42\n</code></pre> <p>The run proceeds exactly as above; only the terrain shape (and therefore the DEM <code>eg_square_pyramid-5m.g2zbin</code> and the forward signal) differs.</p>"},{"location":"user-guide/synthetic-dem-examples.html#the-internal-density-anomaly","title":"The internal density anomaly","text":"<p>Each demo station places one rectangular density anomaly inside the mountain via the <code>cuboid_params</code> block. This is what the inversion tries to recover. In both stations it is a single dense box near the center:</p> <pre><code>\"cuboid_params\": [\n  {\n    \"tf_exec\": true,\n    \"name\": \"cuboid_demo\",\n    \"delta_density\": 600,        // +600 kg/m^3 relative to the surrounding rock\n    \"xcnt\": 130.0, \"ycnt\": 150.0, \"zcnt\": 200.0,   // center (m)\n    \"xlen\": 200.0, \"ylen\": 200.0, \"zlen\": 120.0,   // side lengths (m)\n    \"theta_x_deg\": 0.0, \"theta_y_deg\": 0.0, \"theta_z_deg\": 0.0,  // no rotation\n    \"rotation_type\": \"LOCAL\",\n  },\n]\n</code></pre> <p><code>delta_density</code> is added to whatever background density the voxel already has, only inside the box. The box can be rotated with <code>theta_x_deg</code> / <code>theta_y_deg</code> / <code>theta_z_deg</code> (yaw/pitch/roll); <code>rotation_type</code> selects <code>LOCAL</code> or <code>GLOBAL</code> axes, matching the existing <code>CylinderParameters</code> convention. On the C++ side this is read by <code>Grid3dVoxel::CuboidParameters</code> and applied by <code>add_density_structure</code>, so the forward model computes the synthetic observed signal from a density field that contains the box.</p>"},{"location":"user-guide/synthetic-dem-examples.html#trying-other-shapes","title":"Trying other shapes","text":"<p><code>make_primitive_dem.py</code> also supports <code>cone</code>, <code>square_prism</code>, and <code>gaussian</code>. There is no pre-built station for these, but you can add one by copying <code>param_sites/eg-cylinder.json5</code> to <code>param_sites/eg-&lt;shape&gt;.json5</code> and changing <code>dem_generator.shape</code> (and the size fields if desired). <code>setup_station.sh</code> and <code>run_prg.sh</code> then work the same way \u2014 no other changes are needed.</p>"},{"location":"user-guide/detector-efficiency/index.html","title":"Overview","text":""},{"location":"user-guide/detector-efficiency/index.html#detector-efficiency","title":"Detector Efficiency","text":"<p>Detector efficiency is the probability (0 to 1) that a muon crossing the detector is actually counted. A real detector never counts every muon \u2014 scintillator response, electronics dead time, and readout thresholds all lose some events, and the loss usually depends on the arrival direction. If this loss is not modeled, the reconstruction interprets \"fewer counts\" as \"more rock\", biasing the density result.</p> <p>MUONITH therefore lets each detector carry an efficiency value per angular bin. Every bin <code>(tx, ty)</code> \u2014 the same angular grid defined by <code>nbinx</code>/<code>txmin</code>/<code>txmax</code> and <code>nbiny</code>/<code>tymin</code>/<code>tymax</code> in the detector parameter file \u2014 holds three numbers. (These bins are the computational elements of the path-length calculation, not the instrument's angular resolution; see the note in Detector Model.)</p> Value Meaning <code>eff_cnt</code> Central (best-estimate) efficiency of the bin <code>eff_low</code>, <code>eff_upp</code> Lower / upper bound of the efficiency uncertainty band <p>The half-width of the band feeds the observation covariance and the 2D projected-density error band when efficiency modeling is enabled (<code>tf_eff_cn_diag</code>; see Parameter Reference and the Appendix for the exact formulas).</p>"},{"location":"user-guide/detector-efficiency/index.html#two-ways-to-provide-the-efficiency","title":"Two Ways to Provide the Efficiency","text":"<p>The detector parameter file selects the input route with two keys:</p> Key Input Status <code>path_eff_model</code> Analytic efficiency model (one small JSON5 file) Preferred <code>path_eff_table</code> Text table with one row per angular bin For measured or otherwise non-analytic efficiency <p>Rules:</p> <ul> <li>If <code>path_eff_model</code> is set to anything other than <code>\"none\"</code>, the model is used and <code>path_eff_table</code> is ignored.</li> <li>If <code>path_eff_model</code> is <code>\"none\"</code> (or absent), the text table at <code>path_eff_table</code> is read.</li> <li>If neither is provided, the run stops with a file-open error \u2014 one of the two routes is required.</li> </ul>"},{"location":"user-guide/detector-efficiency/index.html#which-route-to-use","title":"Which Route to Use","text":"<ul> <li>Efficiency Model (JSON5) \u2014 a ~20-line file of coefficients, evaluated at the center of every bin of the detector's own grid. It cannot go out of alignment with that grid, and one file serves detectors with different binnings. Use it whenever the efficiency can be written as a formula.</li> <li>Efficiency Table (text) \u2014 one row per angular bin. The model is a separable product of two axis functions, so an arbitrary per-bin map \u2014 a measured one, for instance \u2014 generally cannot be expressed as a model. The table is the only input that accepts one.</li> </ul>"},{"location":"user-guide/detector-efficiency/index.html#quick-start-efficiency-model","title":"Quick Start (Efficiency Model)","text":"<ol> <li> <p>Write a model file, e.g. <code>data/&lt;site&gt;/detparams/eff_model/my_model.json5</code>. The smallest useful model is a constant efficiency with a constant uncertainty:</p> <pre><code>{\n  \"base_eff\": 0.95,\n  \"sigma\": { \"base\": 0.05 }\n}\n</code></pre> <p>This gives every bin <code>eff_cnt</code> = 0.95 and a band of \u00b10.05.</p> </li> <li> <p>Point the detector parameter file at it:</p> <pre><code>\"path_eff_model\": \"../detparams/eff_model/my_model.json5\"\n</code></pre> </li> <li> <p>Run as usual. <code>setup_station.sh</code> links <code>data/&lt;site&gt;/detparams/eff_model/</code> into <code>work/&lt;site&gt;/detparams/eff_model/</code>, so the relative path above resolves from the run directory (see File Path Resolution). The log confirms the route with <code>EfficiencyModel::load: path=...</code> and <code>Set efficiency for N elements from the efficiency model</code>.</p> </li> </ol>"},{"location":"user-guide/detector-efficiency/efficiency-model.html","title":"Efficiency Model (JSON5)","text":""},{"location":"user-guide/detector-efficiency/efficiency-model.html#efficiency-model-json5","title":"Efficiency Model (JSON5)","text":"<p>The efficiency model is the preferred route (see Overview). It is selected by the <code>path_eff_model</code> key of the detector parameter file.</p>"},{"location":"user-guide/detector-efficiency/efficiency-model.html#how-the-model-is-structured","title":"How the Model Is Structured","text":"<p>The model treats the efficiency as a product of three factors: an overall scale <code>base_eff</code>, a shape along <code>tx</code>, and a shape along <code>ty</code>. The uncertainty is described the same way: a scale <code>sigma.base</code> and one shape per axis. Each shape is one of six simple functions (flat, polynomial, Gaussian, ...) chosen by name.</p> <p>Because the model is a formula rather than a grid of numbers, MUONITH evaluates it directly at the center of every angular bin of the detector's own grid. The model is therefore independent of the binning: one file serves detectors with different <code>nbinx</code>/<code>nbiny</code>/angular ranges.</p>"},{"location":"user-guide/detector-efficiency/efficiency-model.html#full-example","title":"Full Example","text":"<p><code>data/tutorial/detparams/eff_model/small_uncertainty.json5</code> (used by the tutorial):</p> <pre><code>{\n  \"base_eff\": 0.975,                    // overall scale\n  \"tx\": {                               // efficiency shape along tx\n    \"fn\": \"poly\",\n    \"params\": { \"coef\": [1.0, 0.0, -0.0195] }   // 1 - 0.0195*tx^2\n  },\n  \"ty\": {                               // efficiency shape along ty\n    \"fn\": \"poly\",\n    \"params\": { \"coef\": [1.0, 0.0, -0.0195] }\n  },\n  \"sigma\": {                            // uncertainty block\n    \"base\": 0.025,                      // uncertainty scale\n    \"tx\": { \"fn\": \"flat\" },             // uncertainty shape along tx (constant)\n    \"ty\": { \"fn\": \"flat\" }              // uncertainty shape along ty (constant)\n  }\n}\n</code></pre> <p>Every key is optional. Omitted <code>tx</code>/<code>ty</code>/<code>sigma.tx</code>/<code>sigma.ty</code> blocks default to <code>flat</code> (factor 1); an omitted <code>base_eff</code> defaults to 1.0; an omitted <code>sigma</code> block gives a zero-width band (<code>sigma.base</code> = 0).</p>"},{"location":"user-guide/detector-efficiency/efficiency-model.html#evaluation-formulas","title":"Evaluation Formulas","text":"<p>Writing the <code>tx</code>/<code>ty</code> shapes as <code>g_tx</code>, <code>g_ty</code> and the <code>sigma.tx</code>/<code>sigma.ty</code> shapes as <code>h_tx</code>, <code>h_ty</code>, each bin center <code>(tx, ty)</code> is evaluated as:</p> <pre><code>eff_cnt   = clip( base_eff * g_tx(tx) * g_ty(ty), 0, 1 )\nsigma_eff = max( 0, sigma.base * ( h_tx(tx) + h_ty(ty) ) / 2 )\nhalf      = min( sigma_eff, eff_cnt, 1 - eff_cnt )    // band stays inside [0, 1]\neff_low   = eff_cnt - half\neff_upp   = eff_cnt + half\n</code></pre> <p>In words: the central efficiency is the product of the scale and the two axis shapes, clipped to [0, 1]; the uncertainty is the scale times the average of its two axis shapes; and the band is shrunk if needed so it never leaves [0, 1].</p> <p><code>eff_cnt</code> scales the expected muon count of the bin; the half-width <code>half</code> is what propagates into the observation covariance \\(\\mathbf{C}_N\\) when <code>tf_eff_cn_diag</code> is enabled. See Efficiency Uncertainty in \\(\\mathbf{C}_N\\) for that derivation, and Why \\(\\mathbf{C}_N\\) Stays Diagonal for why the bins stay uncorrelated. A model with <code>sigma.base</code> = 0 gives a zero-width band and therefore adds nothing to \\(\\mathbf{C}_N\\).</p>"},{"location":"user-guide/detector-efficiency/efficiency-model.html#fit-functions","title":"Fit Functions","text":"<p>Each shape block is <code>{ \"fn\": &lt;name&gt;, \"params\": { ... } }</code> with one of six functions (<code>t</code> stands for <code>tx</code> or <code>ty</code>):</p> <code>fn</code> <code>params</code> Formula <code>flat</code> \u2014 <code>1</code> <code>poly</code> <code>coef</code> (array, low order first) <code>coef[0] + coef[1]*t + coef[2]*t^2 + ...</code> <code>gauss</code> <code>mu</code>, <code>sigma</code> (non-zero) <code>exp( -(t - mu)^2 / (2 * sigma^2) )</code> <code>cos_power</code> <code>n</code>, optional <code>cutoff_deg</code> <code>cos(theta)^n</code> with <code>theta = atan(abs(t))</code>; <code>0</code> beyond <code>cutoff_deg</code> <code>sigmoid</code> <code>x0</code>, <code>w</code> (non-zero) <code>1 / (1 + exp( (abs(t) - x0) / w ))</code> <code>step</code> <code>x0</code>, <code>w</code> (non-zero), <code>inner</code>, <code>outer</code> <code>outer + (inner - outer) / (1 + exp( (abs(t) - x0) / w ))</code> <p>The same formulas are implemented in <code>scripts/make_eff_table.py</code> (Python) and <code>src/cls_EfficiencyModel.cpp</code> (C++), so a table generated by the Python script from a given model matches what the C++ side assigns from the same model.</p> <p>Because <code>eff_cnt</code> is a product of one function of <code>tx</code> and one function of <code>ty</code>, the model can only express separable shapes. An efficiency map that is not such a product needs the Efficiency Table.</p>"},{"location":"user-guide/detector-efficiency/efficiency-model.html#constraints","title":"Constraints","text":"<ul> <li>JSON dialect: the C++ parser accepts <code>//</code> comments but requires quoted keys and no trailing commas. Files under <code>eff_table_lab/configs/</code> use a looser dialect (unquoted keys) that Python's <code>json5</code> module reads but C++ does not; shared model files must use quoted keys.</li> <li><code>randomize</code>: ignored with a warning. The C++ side is deterministic by design; the band comes from the <code>sigma</code> block only.</li> <li><code>layers</code> (k-of-n coincidence model): not supported on the C++ side; loading such a file raises an error.</li> </ul>"},{"location":"user-guide/detector-efficiency/efficiency-model.html#example-model-shapes","title":"Example Model Shapes","text":"<p>Each panel below shows the <code>tx</code> slice <code>eff(tx)</code> (top-left), the <code>ty</code> slice <code>eff(ty)</code> (top-right), the central efficiency <code>eff_cnt(tx, ty)</code> (bottom-left), and the uncertainty <code>sigma_eff</code> (bottom-right).</p> <p></p> <p>Small uncertainty (\u00b12.5%): a nearly flat central efficiency <code>eff_cnt</code> with a narrow <code>sigma_eff</code> band.</p> <p></p> <p>Large uncertainty (\u00b115%): the same central efficiency as the small case but a much wider <code>sigma_eff</code> band.</p> <p></p> <p>Angle-dependent uncertainty: <code>sigma_eff</code> grows from zero at the origin to \u00b115% toward the corners.</p>"},{"location":"user-guide/detector-efficiency/efficiency-table.html","title":"Efficiency Table (text)","text":""},{"location":"user-guide/detector-efficiency/efficiency-table.html#efficiency-table-text","title":"Efficiency Table (text)","text":"<p>The table route is selected by the <code>path_eff_table</code> key of the detector parameter file, and only when <code>path_eff_model</code> is <code>\"none\"</code> or absent (see Overview).</p> <p>Use it when the efficiency cannot be written as a formula. The efficiency model is a separable product <code>base_eff * g_tx(tx) * g_ty(ty)</code>, so an arbitrary per-bin efficiency map \u2014 a measured one, for instance \u2014 generally cannot be expressed as a model. The table is the only input that accepts one.</p>"},{"location":"user-guide/detector-efficiency/efficiency-table.html#format","title":"Format","text":"<p>A whitespace-separated text file with one row per angular bin and seven columns:</p> <pre><code>tx_min tx_max ty_min ty_max eff_low eff_cnt eff_upp\n</code></pre> <p>Every bin of the detector grid gets its own row. The rows must match that grid exactly: a row whose bin center falls outside the detector's angular range is dropped with an <code>out of range</code> warning (the first five are logged), and a table generated for one binning cannot be reused for another. The model, by contrast, is evaluated on the detector's own grid and cannot go out of alignment.</p> <p>The last three columns are the band of the bin. <code>eff_cnt</code> scales the expected muon count; the half-width implied by <code>eff_low</code> and <code>eff_upp</code> propagates into the observation covariance \\(\\mathbf{C}_N\\) when <code>tf_eff_cn_diag</code> is enabled. See Efficiency Uncertainty in \\(\\mathbf{C}_N\\) for that derivation. Writing <code>eff_low</code> = <code>eff_cnt</code> = <code>eff_upp</code> gives a zero-width band, which adds nothing to \\(\\mathbf{C}_N\\).</p>"},{"location":"user-guide/detector-efficiency/efficiency-table.html#using-a-user-defined-table","title":"Using a User-Defined Table","text":"<ol> <li> <p>Read the angular grid off the detector parameter file you intend to use: <code>nbinx</code>, <code>txmin</code>, <code>txmax</code>, <code>nbiny</code>, <code>tymin</code>, <code>tymax</code>. Your table must have exactly <code>nbinx * nbiny</code> rows covering that rectangle with uniform bins.</p> </li> <li> <p>Write the table to <code>data/_shared/eff_table/&lt;your_table&gt;.txt</code>. One row per bin, seven numeric columns, whitespace-separated. Blank lines are skipped; every other line must parse as seven numbers, so do not add <code>#</code> comment lines or a header row \u2014 the reader stops with a format error on the first line it cannot parse.</p> <pre><code>-1.60 -1.59 0.00 0.01 0.93 0.95 0.97\n-1.59 -1.58 0.00 0.01 0.93 0.95 0.97\n...\n</code></pre> <p>Column order within a row is fixed; row order is free, because each row is placed by its own bin center.</p> </li> <li> <p>Point the detector parameter file at the table and disable the model, which otherwise takes precedence:</p> <pre><code>, \"path_eff_table\" : \"../detparams/eff_table/&lt;your_table&gt;.txt\"\n, \"path_eff_model\" : \"none\"\n</code></pre> <p>Omitting the second line is the most common mistake: a runcard that still names a model file reads the model and never opens your table.</p> </li> <li> <p>Run <code>setup_station.sh &lt;site&gt;</code>. It links <code>data/_shared/eff_table/</code> into <code>work/&lt;site&gt;/detparams/eff_table/</code>, so the relative path above resolves from the run directory, and it then checks your table's grid against every detector parameter file. A mismatch in <code>nbinx</code>, <code>nbiny</code>, <code>txmin</code>, <code>txmax</code>, <code>tymin</code>, or <code>tymax</code> stops the setup with <code>Detector angle grid and eff_table grid differ</code>, before any physics runs.</p> </li> <li> <p>Run as usual. The log confirms the route with <code>Set efficiency for N elements from &lt;path&gt;</code>, where <code>N</code> equals <code>nbinx * nbiny</code> when the grids agree.</p> </li> </ol> <p><code>scripts/make_eff_table.py</code> writes tables in this format from a model file; use it as a worked example of the layout, or as a starting point for a script that emits your measured values.</p>"},{"location":"user-guide/flux-tables/index.html","title":"Overview","text":""},{"location":"user-guide/flux-tables/index.html#making-penetrating-muon-flux-tables","title":"Making Penetrating Muon Flux Tables","text":"<p>MUONITH ships pre-computed muon flux tables (see Input Data for how the pipeline consumes them). This section describes how those tables are made, how to reproduce the shipped tables, and how to introduce a different flux model or a different absorber material.</p> <p>Standard-rock assumption</p> <p>The shipped tables and this entire walkthrough assume standard rock (\u27e8Z/A\u27e9 = 0.5, density 2.65 g/cm\u00b3, mean excitation energy I = 136.4 eV) as the absorber composition. Density itself is not assumed: the pipeline works in density-length X = density \u00d7 path length [kg/m\u00b2], so local density enters through the voxel densities. Only the composition per unit mass is fixed. For media whose composition differs substantially from standard rock (water, ice, iron, ...), regenerate the range table for that material (see Range Table) and consult Lechmann et al. (2018), The effect of rock composition on muon tomography measurements, Solid Earth 9, 1517, doi:10.5194/se-9-1517-2018.</p>"},{"location":"user-guide/flux-tables/index.html#what-is-produced","title":"What is produced","text":"<p>Two tables are produced, both as <code>.g2zbin</code> binary grids (plus <code>.txt</code> dumps):</p> Table Content Axes Scale Penetrating flux F \\(\\log_{10} F\\) [1/(m\u00b2 sr s)] cos(\u03b8_zenith) \u00d7 density-length X [kg/m\u00b2] log10 Flux derivative dF/dR \\(dF/dR\\) [1/(m\u00b2 sr s)/(kg/m\u00b2)] density-length X \u00d7 cos(\u03b8_zenith) linear (all values \u2264 0)"},{"location":"user-guide/flux-tables/index.html#build-pipeline","title":"Build pipeline","text":"<p>Two independent input branches are prepared and then joined by a single builder:</p> <p></p> <ul> <li>Range branch (CSDA) \u2014 the muon range-energy table derived from the PDG   (Groom-Mokhov-Striganov) CSDA range data. Model-independent: every flux model   reuses the same range table. See Range Table (CSDA).</li> <li>Flux-model branch \u2014 the sea-level differential flux dF/dE on a uniform grid.   The shipped tables use daemonflux; you can bring your own model.   See Flux Model (dF/dE).</li> <li>Build &amp; install \u2014 <code>make_peneflux_dFdR.exe</code> integrates the two inputs into the   F and dF/dR tables and they are installed under <code>fluxtable/&lt;flux_groom&gt;/</code>.   See Build and Install.</li> </ul>"},{"location":"user-guide/flux-tables/index.html#when-you-need-this","title":"When you need this","text":"<ul> <li>You want to reproduce the shipped <code>fluxtable/daemon_groom/</code> tables from source data.</li> <li>You want a different binning (range axis, cos\u03b8 axis) of the same model.</li> <li>You want to introduce another flux model (your own dF/dE table).</li> <li>You want a different absorber material (another PDG <code>muE_&lt;material&gt;.txt</code>).</li> </ul>"},{"location":"user-guide/flux-tables/build-and-install.html","title":"Build and Install","text":""},{"location":"user-guide/flux-tables/build-and-install.html#build-and-install","title":"Build and Install","text":"<p>This page joins the two prepared inputs (Range Table and Flux Model) into the final F and dF/dR tables and installs them.</p>"},{"location":"user-guide/flux-tables/build-and-install.html#prerequisites","title":"Prerequisites","text":"<ul> <li>A Release build: <code>bash brelease.sh</code> produces <code>build-release/exec/make_peneflux_dFdR.exe</code>.</li> <li>Python 3 with <code>numpy</code>, <code>scipy</code>, and <code>matplotlib</code> (used only by the pre-processing steps).</li> </ul>"},{"location":"user-guide/flux-tables/build-and-install.html#build","title":"Build","text":"<p>Run the builder with the sample config committed in <code>fluxtable_build/daemon_groom/</code>:</p> <pre><code>cd fluxtable_build/daemon_groom\n../../build-release/exec/make_peneflux_dFdR.exe make_peneflux_daemon.json5 FLUX_BUILDER\n</code></pre> <p>The second argument selects the config section to read. The config must also contain a <code>LOG_FILE</code> section (see <code>make_peneflux_daemon.json5</code> for a working example).</p>"},{"location":"user-guide/flux-tables/build-and-install.html#config-keys-flux_builder-section","title":"Config Keys (<code>FLUX_BUILDER</code> section)","text":"Key Meaning <code>pathin_logR</code> Range table (column 1: log10 KE [GeV], column 2: log10 range [kg/m\u00b2]; the builder inverts it internally to obtain E_cut(R)) <code>tf_xcnt_logR</code> If true, interpret the range-table x values as bin centers <code>pathin_log_dFdE</code> dF/dE table (ASCII x y z or <code>.g2zbin</code>) <code>tf_xcnt_dFdE</code>, <code>tf_ycnt_dFdE</code> Bin-center flags for the dF/dE grid axes <code>pathout_log_penef_R_costhz</code> Output path for the log10 F table (<code>.g2zbin</code>; a <code>.txt</code> dump is written alongside) <code>pathout_dFdR_R_costhz</code> Output path for the dF/dR table <code>dlogE</code> Integration step in log10 E <code>Rmin</code>, <code>Rmax</code>, <code>dR</code> Density-length axis [kg/m\u00b2]. <code>Rmin</code> may be omitted: the builder then derives it from the dF/dE table's lowest energy (R at E_min via the range table) and snaps it up onto the <code>Rmax</code>/<code>dR</code> lattice, logging the adopted value. The shipped configs set <code>Rmin: 5.0E+3</code> explicitly <code>tf_costhz_cnt</code>, <code>tf_range_cnt</code> Bin-center flags for the output axes <code>tf_check_dFdR_divergence</code> Enable the post-build dF/dR sign check (a healthy table is finite and all-negative) <code>dFdR_divergence_threshold</code> Maximum tolerated sign-flip ratio per cos\u03b8 slice <code>tf_dFdR_divergence_fatal</code> If true, abort the build on divergence; if false, warn only"},{"location":"user-guide/flux-tables/build-and-install.html#default-rmin-when-omitted","title":"Default <code>Rmin</code> (when omitted)","text":"<p>Without an explicit <code>Rmin</code>, the builder converts the dF/dE table's lowest energy to a range (363.6 kg/m\u00b2 for the shipped daemonflux input) and snaps it up onto the <code>Rmax</code>/<code>dR</code> lattice (1000 kg/m\u00b2 here), because the axis constructor rejects a non-integer bin count instead of rounding. The log prints both the derived and the adopted value:</p> <pre><code>[FluxTableBuilder::FluxTableBuilder] FluxTableBuilder: Rmin not given in JSON; derived R(Emin)=3.6360E+02 kg/m^2, snapped up to Rmin=1.0000E+03 on the Rmax/dR lattice.\n</code></pre> <p>Multiple Coulomb scattering below ~1 GeV</p> <p>A derived <code>Rmin</code> can fall well below 5\u00d710\u00b3 kg/m\u00b2 (the range of a ~1 GeV muon in standard rock). For muons of about 1 GeV and below, multiple Coulomb scattering grows rapidly, so their trajectories deviate substantially from the straight lines the reconstruction assumes. Treat the thin-absorber rows of the table (R below the ~1 GeV range) with caution.</p>"},{"location":"user-guide/flux-tables/build-and-install.html#expected-log-milestones","title":"Expected Log Milestones","text":"<p>A successful run (about half a minute on a multi-core machine; uses OpenMP) prints:</p> <pre><code>[FluxTableBuilder::build_log10Ecut_table] Completed build_log10Ecut_table.\n[FluxTableBuilder::build_log_peneflux] Completed build_log_peneflux.\n[check_dFdR_divergence] ... dF/dR divergence check passed. pos_ratio=0.000e+00, ...\n[FluxTableBuilder::build_dFdR_from_peneflux] Completed build_dFdR_from_peneflux.\n[FluxTableBuilder::out_built_tables] All tables written.\n</code></pre> <p>Outputs (paths from the sample config):</p> <ul> <li><code>peneflux/daemon-costhz-kgm2-log10peneflux-allinone.g2zbin</code> + <code>.txt</code> \u2014 log10 F</li> <li><code>peneflux/daemon-g2_dFdR_R_costhz.g2zbin</code> + <code>.txt</code> \u2014 dF/dR</li> </ul>"},{"location":"user-guide/flux-tables/build-and-install.html#install","title":"Install","text":"<p>The pipeline reads flux tables from <code>fluxtable/&lt;flux_groom&gt;/</code> under canonical names (without the model prefix). <code>daemon_groom</code> is the default <code>flux_groom</code> in generated station configs (<code>scripts/init_work_site.py</code>). Copy and rename:</p> Generated file Install as <code>peneflux/daemon-costhz-kgm2-log10peneflux-allinone.g2zbin</code> <code>fluxtable/daemon_groom/costhz-kgm2-log10peneflux-allinone.g2zbin</code> <code>peneflux/daemon-g2_dFdR_R_costhz.g2zbin</code> <code>fluxtable/daemon_groom/g2_dFdR_R_costhz.g2zbin</code> <p>For your own model, install under <code>fluxtable/&lt;your_model_name&gt;/</code> with the same canonical names and set <code>\"flux_groom\": \"&lt;your_model_name&gt;\"</code> in <code>param_sites/&lt;site&gt;.json5</code> (see Parameter Files).</p> <p>At run time, density-length lookups into the F table are clamped to the table's own R axis by default (a <code>PARAM_CONSTANTS</code> section with <code>DL_min</code>/<code>DL_max</code> in the run config overrides this). Each run states the adopted bounds and their origin in one startup log line:</p> <pre><code>[FluxTable::load_tables] FluxTable::load_tables: DL clamp bounds [5.0000E+03, 5.0000E+06] kg/m^2 (min: table, max: table)\n</code></pre>"},{"location":"user-guide/flux-tables/build-and-install.html#verifying-the-result","title":"Verifying the Result","text":"<ul> <li>The run exits with code 0 and logs <code>All tables written</code>; the divergence check reports   <code>passed</code> with <code>pos_ratio=0.000e+00</code> (dF/dR must be negative everywhere).</li> <li>The <code>.txt</code> dumps contain no <code>nan</code>/<code>inf</code>; values cover the full grid   (with the shipped config: cos\u03b8 0.000\u20131.000 in 1001 columns, R = 5\u00d710\u00b3 to 5\u00d710\u2076 kg/m\u00b2   in 10\u00b3 steps \u2014 these bounds come from <code>Rmin</code>/<code>Rmax</code>/<code>dR</code> in the config, not from the   code).</li> </ul>"},{"location":"user-guide/flux-tables/build-and-install.html#if-the-divergence-check-fails","title":"If the divergence check fails","text":"<p>The check aborts with <code>dF/dR table looks divergent. pos_ratio=... worst_sign_flip_ratio=...</code> (at build time, and again whenever the pipeline loads the table). A healthy dF/dR table is negative everywhere \u2014 F can only decrease with more absorber \u2014 so any significant fraction of positive values or of sign flips along R means the table is numerical noise, not physics.</p> <p>The known cause is a grid axis misread of the dF/dE input: if <code>tf_xcnt_dFdE</code> / <code>tf_ycnt_dFdE</code> declare bin edges while the table's axis values are bin centers (or vice versa), every lookup is shifted by half a bin. The resulting small error in F changes sign from point to point, and differentiating along R turns it into a sign-oscillating dF/dR. This exact mistake (<code>tf_ycnt_dFdE: false</code> for the bin-centered daemonflux grid) produced a divergent table during development, and the reconstruction downstream degraded into salt-and-pepper noise before the check existed.</p> <p>Remedy:</p> <ol> <li>Set <code>tf_xcnt_dFdE</code> / <code>tf_ycnt_dFdE</code> to match your dF/dE grid \u2014 <code>true</code> (centers)    for tables built as described in Flux Model, including the    shipped daemonflux input.</li> <li>Rebuild and confirm the log prints <code>dF/dR divergence check passed</code> with    <code>pos_ratio=0.000e+00</code>.</li> </ol> <p><code>tf_dFdR_divergence_fatal: false</code> downgrades the abort to a warning; use it only while tuning <code>dFdR_divergence_threshold</code>, never to push a divergent table through.</p>"},{"location":"user-guide/flux-tables/build-and-install.html#units-and-conventions","title":"Units and Conventions","text":"<ul> <li>Flux F is stored as log10 of particles/(m\u00b2 sr s); dF/dR is linear.</li> <li>Density-length R (density \u00d7 path length) is in kg/m\u00b2.</li> <li>The zenith axis is cos(\u03b8_zenith): 1.0 = vertical, 0.0 = horizontal.</li> </ul>"},{"location":"user-guide/flux-tables/build-and-install.html#licensing-notes","title":"Licensing Notes","text":"<ul> <li>daemonflux data and code are BSD-3-Clause: redistribution (including the modified,   unit-corrected tables) is permitted provided the copyright notice, license terms, and   disclaimer are retained. See <code>LICENSES/README.md</code>. Please cite   Y\u00e1\u00f1ez &amp; Fedynitch, Phys. Rev. D 107, 123037 (2023), arXiv:2303.00022.</li> <li>Honda-model tables (<code>fluxtable/honda_groom/</code>) have no confirmed redistribution   permission and must not be redistributed; they remain for internal comparison only.</li> <li>The Groom range-energy table derives from Groom et al. / PDG data: attribute the   source when redistributing (see <code>LICENSES/README.md</code> and   Range Table).</li> </ul>"},{"location":"user-guide/flux-tables/flux-model.html","title":"Flux Model (dF/dE)","text":""},{"location":"user-guide/flux-tables/flux-model.html#flux-model-dfde","title":"Flux Model (dF/dE)","text":"<p>The flux-model branch of the build pipeline provides the sea-level differential muon flux dF/dE. The shipped tables use daemonflux; this page defines the input contract so that you can also bring your own model.</p> <p></p> <p>Shipped daemonflux dF/dE. Colour is log10(dF/dE) in 1/(m\u00b2 sr s GeV) over cos(zenith angle) and log10(kinetic energy). The flux peaks near the vertical (cos\u03b8 = 1) at low energy and falls steeply with both increasing energy and increasing inclination.</p>"},{"location":"user-guide/flux-tables/flux-model.html#input-contract-for-make_peneflux_dfdrexe","title":"Input contract for <code>make_peneflux_dFdR.exe</code>","text":"<p>The builder reads the dF/dE table through the <code>pathin_log_dFdE</code> config key (ASCII <code>x y z</code> rows, or a <code>.g2zbin</code> grid). The shipped daemonflux input uses:</p> Property Requirement Format 3-column ASCII: cos\u03b8_zenith, log10(KE [GeV]), log10(dF/dE) Grid Uniform in both axes. Shipped grid: cos\u03b8 = 0.000..1.000 step 0.001 (1001 values) \u00d7 log10 KE = \u22121.00..7.00 step 0.01 (801 values) Units dF/dE in 1/(m\u00b2 sr s GeV) \u2014 per m\u00b2, not per cm\u00b2 Energy range Must cover the E_cut values implied by your R axis (for the shipped R axis, up to well above 1 TeV) <p>Uniform grid required</p> <p>The grid reader validates that both axes are uniformly spaced. A non-uniform grid aborts the run with an <code>is_check_variables</code> error rather than producing a table.</p> <p>Sanity anchor: at the vertical (cos\u03b8 = 1.0) and 1 GeV, the value must be log10(dF/dE) \u2248 +1.37 in per-m\u00b2 units (the Honda model gives \u2248 +1.36 at the same point). If your table reads \u2248 \u22122.6 there, it is in per-cm\u00b2 units \u2014 convert by adding log10(10\u2074) = 4. Unit errors do not crash the build; this anchor is the cheap way to catch them.</p>"},{"location":"user-guide/flux-tables/flux-model.html#worked-example-daemonflux","title":"Worked example: daemonflux","text":"<p>All commands run from <code>fluxtable_build/daemon_groom/</code>:</p> <pre><code>bash process.sh\n</code></pre> <p><code>process.sh</code> extracts <code>daemonflux.txt</code> from the committed <code>org/daemonflux.txt.zip</code> and calls <code>dFdE_interp.py</code>, which converts the momentum spectrum dF/dP to a kinetic-energy spectrum dF/dE (KE = \u221a(P\u00b2 + m\u00b2) \u2212 m, m = 0.105658 GeV), applies the cm\u207b\u00b2 \u2192 m\u207b\u00b2 correction, and re-grids by 2D spline interpolation onto the uniform grid above. Output: <code>daemonflux_ke_m2.tmp</code> plus cross-section figures under <code>figs/</code>.</p> <p><code>make_figures.sh</code> wraps this step and additionally draws the 2D overview shown at the top of this page:</p> <pre><code>bash make_figures.sh\n</code></pre> <p>Source unit mislabel</p> <p>The daemonflux source header labels the flux as <code>(m^2 sr s GeV/c)^-1</code>, but the values are actually per cm\u00b2 (the vertical ~1 GeV/c value 2.5\u00d710\u207b\u00b3 matches the PDG per-cm\u00b2 spectrum). <code>dFdE_interp.py</code> applies a \u00d710\u2074 correction (<code>CM2_TO_M2</code>). When you bring another model, check your source's units against the sanity anchor above rather than trusting its header.</p>"},{"location":"user-guide/flux-tables/flux-model.html#bringing-your-own-model","title":"Bringing your own model","text":"<p>The walkthrough below uses <code>mymodel</code> as the model name \u2014 replace it with yours.</p>"},{"location":"user-guide/flux-tables/flux-model.html#step-1-write-your-flux-as-a-3-column-text-file","title":"Step 1 \u2014 write your flux as a 3-column text file","text":"<p>Produce <code>mymodel_dFdE.txt</code> with one row per grid point:</p> <pre><code># cos(theta_zenith)   log10(KE [GeV])   log10(dF/dE [1/(m^2 sr s GeV)])\n0.000  -1.00  &lt;value&gt;\n0.000  -0.99  &lt;value&gt;\n...\n1.000   7.00  &lt;value&gt;\n</code></pre> <p>Both axes must be uniformly spaced (see the input contract above). If your model comes on a different grid or in different units, <code>fluxtable_build/daemon_groom/dFdE_interp.py</code> shows working code for the re-gridding and the unit conversion.</p> <p>Check before moving on: the row <code>1.000 0.00</code> (vertical, 1 GeV) must read \u2248 +1.37. If it reads \u2248 \u22122.6, your values are per cm\u00b2 \u2014 add 4.</p>"},{"location":"user-guide/flux-tables/flux-model.html#step-2-build-the-f-and-dfdr-tables","title":"Step 2 \u2014 build the F and dF/dR tables","text":"<p>Copy <code>fluxtable_build/daemon_groom/make_peneflux_daemon.json5</code>, point <code>pathin_log_dFdE</code> at <code>mymodel_dFdE.txt</code>, and run the builder as described in Build and Install. Keep <code>pathin_logR</code> as it is: the range table does not depend on the flux model. (Change it only when you change the absorber material \u2014 see Range Table.)</p>"},{"location":"user-guide/flux-tables/flux-model.html#step-3-install-under-a-new-model-directory","title":"Step 3 \u2014 install under a new model directory","text":"<p>The pipeline looks the tables up as <code>fluxtable/&lt;flux_groom&gt;/&lt;fixed file name&gt;</code>, so the directory name is your model name and the file names must be exactly:</p> <pre><code>fluxtable/mymodel/costhz-kgm2-log10peneflux-allinone.g2zbin   # log10 F\nfluxtable/mymodel/g2_dFdR_R_costhz.g2zbin                     # dF/dR\n</code></pre> <p>Copy the two builder outputs to these paths (dropping the model prefix the sample config puts on the output file names).</p>"},{"location":"user-guide/flux-tables/flux-model.html#step-4-select-the-model-per-site-and-validate","title":"Step 4 \u2014 select the model per site and validate","text":"<p>In <code>param_sites/&lt;site&gt;.json5</code> set <code>\"flux_groom\": \"mymodel\"</code>; the generated station configs then resolve to <code>fluxtable/mymodel/</code> (see Parameter Files).</p> <p>Two checks:</p> <ul> <li>the builder's divergence check reported <code>passed</code> (dF/dR \u2264 0 everywhere) \u2014 if it   fails, see If the divergence check fails;</li> <li>pick a few (cos\u03b8, R) points in the <code>.txt</code> dump of your F table and compare with   the shipped <code>fluxtable/daemon_groom/</code> one: flux models disagree by tens of   percent at most, so a difference of orders of magnitude means a unit or grid   error, not a model difference.</li> </ul>"},{"location":"user-guide/flux-tables/flux-model.html#model-validity-note","title":"Model validity note","text":"<p>daemonflux is calibrated against surface muon spectrometer data; its constraints weaken above ~1 TeV muon energy. For standard muography depths this is not a practical limitation, but keep it in mind for very long density-length studies.</p>"},{"location":"user-guide/flux-tables/range-table.html","title":"Range Table (CSDA)","text":""},{"location":"user-guide/flux-tables/range-table.html#range-table-csda","title":"Range Table (CSDA)","text":"<p>The range branch of the build pipeline provides the muon range-energy relation: how much matter (in kg/m\u00b2) a muon of a given kinetic energy can penetrate. The builder inverts this relation to obtain the cutoff energy E_cut(R) during the flux integration.</p>"},{"location":"user-guide/flux-tables/range-table.html#the-shipped-table","title":"The shipped table","text":"<p><code>fluxtable/daemon_groom/logRangeKgm2_logGeV_hokan_70001.txt</code>:</p> Property Value Column 1 log10(kinetic energy [GeV]), \u22122.0000 to 5.0000, step 10\u207b\u2074 (70001 rows) Column 2 log10(CSDA range [kg/m\u00b2]) Material standard rock (\u27e8Z/A\u27e9 = 0.5, \u03c1 = 2.65 g/cm\u00b3, I = 136.4 eV) Source PDG muon table <code>muE_standard_rock.txt</code> (Groom-Mokhov-Striganov) <p>CSDA (Continuous Slowing Down Approximation) range is the deterministic path length obtained by integrating the mean energy loss; at TeV energies the actual penetration depth fluctuates because radiative losses are stochastic, and the CSDA value is used here as the deterministic representative.</p> <p>The lower bound of 10 MeV (log10 KE = \u22122) follows the PDG file itself, whose header warns that results below 10 MeV are not dependable.</p>"},{"location":"user-guide/flux-tables/range-table.html#source-data","title":"Source data","text":"<p>The Particle Data Group publishes muon energy-loss tables for about 350 materials in a common ASCII format:</p> <pre><code>https://pdg.lbl.gov/2024/AtomicNuclearProperties/MUE/muE_&lt;material&gt;.txt\n</code></pre> <p>Column 1 is the kinetic energy T [MeV] and column 9 is the CSDA range [g/cm\u00b2] (non-numeric header/comment lines interleave the data and must be skipped). The files carry no explicit license; cite Groom, Mokhov, Striganov, Atomic Data and Nuclear Data Tables 78, 183 (2001) and the PDG page when redistributing derived tables. The raw PDG file itself is therefore not committed to this repository; fetch it with the script below.</p>"},{"location":"user-guide/flux-tables/range-table.html#regenerating-the-table","title":"Regenerating the table","text":"<p>Both scripts live in <code>fluxtable_build/csda_range/</code>:</p> <pre><code>cd fluxtable_build/csda_range\nbash fetch_pdg_muE.sh                 # downloads muE_standard_rock.txt (md5-checked)\npython3 make_range_table.py muE_standard_rock.txt out_logRangeKgm2_logGeV_70001.txt\n</code></pre> <p><code>make_range_table.py</code> interpolates log10(CSDA range) versus log10(KE) with a node-exact cubic spline, samples log10 KE = \u22122..5 in 10\u207b\u2074 steps (70001 rows), and converts g/cm\u00b2 to kg/m\u00b2 (+1 in log10). The output format is identical to the shipped table.</p> <p>To compare a regenerated table against the shipped one:</p> <pre><code>python3 make_range_table.py muE_standard_rock.txt out.txt \\\n  --compare ../../fluxtable/daemon_groom/logRangeKgm2_logGeV_hokan_70001.txt\n</code></pre> <p>Expected agreement with the shipped table (which was produced from an earlier PDG release with the same method): |\u0394| &lt; 10\u207b\u00b3 dex above 1 GeV, growing to about 6\u00d710\u207b\u00b3 dex at the 10 MeV end.</p>"},{"location":"user-guide/flux-tables/range-table.html#other-materials","title":"Other materials","text":"<p>Pass any other PDG file (e.g. <code>muE_water_liquid.txt</code>, <code>muE_iron_Fe.txt</code>) to <code>make_range_table.py</code> to build a range table for that material, then follow Build and Install with the new table. For the physics impact of composition differences on muography, see Lechmann et al. (2018), The effect of rock composition on muon tomography measurements, Solid Earth 9, 1517, doi:10.5194/se-9-1517-2018.</p>"}]}